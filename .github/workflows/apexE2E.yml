name: Apex End to End Tests

on:
  workflow_dispatch:
    inputs:
      # createOasDoc:
      #   description: 'Create OAS Doc'
      #   required: false
      #   default: true
      #   type: boolean
      # debugApexTests: migrated to Playwright in apexReplayDebuggerE2E.yml (debugApexTests.desktop.spec.ts) — W-22968398
      vscodeVersion:
        description: 'VSCode Version'
        required: false
        default: 'latest'
        type: string
      runId:
        description: 'Run ID of the workflow run that created the vsixes'
        required: true
        type: string

  workflow_call:
    inputs:
      # createOasDoc:
      #   description: 'Create OAS Doc'
      #   required: false
      #   default: true
      #   type: boolean
      # debugApexTests: migrated to Playwright in apexReplayDebuggerE2E.yml (debugApexTests.desktop.spec.ts) — W-22968398
      vscodeVersion:
        description: 'VSCode Version'
        required: false
        default: 'latest'
        type: string
      runId:
        description: 'Run ID of the workflow run that created the vsixes'
        required: true
        type: string

jobs:
  # createOasDoc:
  #   if: ${{ inputs.createOasDoc }}
  #   uses: ./.github/workflows/runE2ETest.yml
  #   secrets: inherit
  #   with:
  #     testToRun: 'createOasDoc.e2e.js'
  #     vscodeVersion: ${{ inputs.vscodeVersion }}
  #     runId: ${{ inputs.runId }}
  #     os: '["ubuntu-latest"]'

  # debugApexTests: migrated to Playwright in apexReplayDebuggerE2E.yml (debugApexTests.desktop.spec.ts) — W-22968398

  # runApexTests: migrated to Playwright in apexTestingE2E.yml (runApexTestsCommandPalette/CodeLens/FailAndFix.headless.spec.ts)

  # All Apex redhat e2e suites above are migrated to Playwright. This no-op job keeps the reusable workflow
  # valid (jobs must be a non-empty object) and its callers in e2e.yml (Apex, Apex_min_vscode_version) wired.
  no-redhat-apex-e2e:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'All Apex redhat e2e migrated to Playwright (apexLspE2E / apexTestingE2E / apexReplayDebuggerE2E).'
