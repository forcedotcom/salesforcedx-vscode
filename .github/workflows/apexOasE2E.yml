name: Apex OAS E2E (Playwright)

on:
  workflow_dispatch:
    inputs:
      vscode_version:
        description: 'VS Code version for E2E (e.g. 1.116.0). Empty uses repo variable
          PLAYWRIGHT_VSCODE_VERSION.'
        required: false
        type: string
        default: ''
  workflow_call:
    inputs:
      vscode_version:
        description: 'VS Code version for E2E (e.g. 1.116.0). Empty uses repo variable
          PLAYWRIGHT_VSCODE_VERSION.'
        required: false
        type: string
        default: ''
  push:
    branches-ignore: [ main, develop ]
    paths-ignore:
      - '.claude/**'
      - '.cursor/**'
      - 'contributing/**'
      - 'CONTRIBUTING.md'
      - 'docs/**'
      - '**/README*'
      - 'SECURITY*'
      - 'CODE_OF_CONDUCT*'
      - 'CHANGELOG*'
      - '**/CHANGELOG*'

concurrency:
  group: ci-${{ github.ref }}-apexOasE2E
  cancel-in-progress: true

jobs:
  e2e-desktop:
    # OAS broken by A4V v4 marketplace release; re-enable when fixed (tracked in W-22997620).
    if: false
    runs-on: ${{ matrix.os }}
    timeout-minutes: 60
    strategy:
      matrix:
        os: [ macos-latest, ubuntu-latest ]
        include:
          - os: ubuntu-latest
            test_runner_prefix: 'xvfb-run -a '
      fail-fast: false

    env:
      MINIMAL_ORG_ALIAS: minimalTestOrg
      SFDX_AUTH_URL: ${{ secrets.SFDX_AUTH_URL_E2E }}
      VSCODE_DESKTOP: 1
      E2E_FROM_VSIX: 1
      PLAYWRIGHT_VSCODE_VERSION: ${{ github.event_name != 'push' &&
        inputs.vscode_version != '' && inputs.vscode_version ||
        vars.PLAYWRIGHT_VSCODE_VERSION }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Log desktop VS Code version
        shell: bash
        run: echo "::notice::VS Code version=${PLAYWRIGHT_VSCODE_VERSION}"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.22.3'
          cache: 'npm'

      - uses: google/wireit@setup-github-actions-caching/v2

      - name: Install dependencies
        uses: salesforcecli/github-workflows/.github/actions/npmInstallWithRetries@main

      # Try E2E tests first. When wireit cache hits, it skips the command and exits 0—we never need CLI, orgs, or Playwright.
      # When cache misses, tests fail on missing org. E2E_NO_RETRIES=1 disables Playwright retries in config
      # so failures are fast (env var doesn't affect wireit cache key since it's not part of the npm command).
      - name: Try E2E tests
        id: try-run
        continue-on-error: true
        timeout-minutes: 5
        env:
          CI: 1
          VSCODE_DESKTOP: 1
          MINIMAL_ORG_ALIAS: minimalTestOrg
          E2E_NO_RETRIES: 1
        run: ${{ matrix.test_runner_prefix }}npm run test:desktop -w salesforcedx-vscode-apex-oas -- --reporter=html

      # Only run expensive setup when try-run failed (cache miss). On cache hit we never reach these steps.
      - name: Install Salesforce CLI
        if: steps.try-run.outcome == 'failure'
        uses: salesforcecli/github-workflows/.github/actions/retry@main
        with:
          max_attempts: 3
          command: npm i -g @salesforce/cli@${{ vars.SF_CLI_VERSION || 'latest' }}
          retry_wait_seconds: 30

      - name: Auth to dev hub (global)
        if: steps.try-run.outcome == 'failure'
        uses: salesforcecli/github-workflows/.github/actions/retry@main
        with:
          max_attempts: 3
          command: bash -c 'echo "$SFDX_AUTH_URL" | sf org login sfdx-url
            --set-default-dev-hub --alias hub --sfdx-url-stdin'
          retry_wait_seconds: 30

      - name: Install Playwright browsers and OS deps
        if: steps.try-run.outcome == 'failure'
        uses: salesforcecli/github-workflows/.github/actions/retry@main
        with:
          max_attempts: 3
          command: npx playwright install chromium --with-deps
          retry_wait_seconds: 60

      - name: Generate minimal project
        if: steps.try-run.outcome == 'failure'
        shell: bash
        run: sf project generate -n minimal-project

      - name: Create scratch org
        if: steps.try-run.outcome == 'failure'
        shell: bash
        run: |
          sf org create scratch -y 1 -d -f config/project-scratch-def.json -a "$MINIMAL_ORG_ALIAS" --json --wait 30
        working-directory: minimal-project

      # OAS specs are serialized via workers:1 in playwright.config.desktop.ts (single shared scratch org).
      - name: Run Apex OAS E2E tests
        if: steps.try-run.outcome == 'failure'
        id: serial-run
        continue-on-error: true
        env:
          CI: 1
          VSCODE_DESKTOP: 1
          MINIMAL_ORG_ALIAS: minimalTestOrg
        run: ${{ matrix.test_runner_prefix }}npm run test:desktop -w salesforcedx-vscode-apex-oas -- --reporter=html

      - name: Recreate scratch org before retry
        if: steps.try-run.outcome == 'failure' && steps.serial-run.outcome ==
          'failure'
        continue-on-error: true
        shell: bash
        run: |
          sf org delete scratch -o "$MINIMAL_ORG_ALIAS" --no-prompt 2>/dev/null || true
          sf org create scratch -y 1 -d -f config/project-scratch-def.json -a "$MINIMAL_ORG_ALIAS" --json --wait 30
        working-directory: minimal-project

      - name: Retry failed tests
        if: steps.try-run.outcome == 'failure' && steps.serial-run.outcome ==
          'failure'
        env:
          CI: 1
          VSCODE_DESKTOP: 1
          MINIMAL_ORG_ALIAS: minimalTestOrg
        run: ${{ matrix.test_runner_prefix }}npm run test:desktop -w salesforcedx-vscode-apex-oas -- --last-failed --reporter=html

      - name: Copy span files to test-results
        if: always()
        shell: bash
        run: |
          mkdir -p packages/salesforcedx-vscode-apex-oas/test-results/spans
          cp -r ~/.sf/vscode-spans/. packages/salesforcedx-vscode-apex-oas/test-results/spans/ 2>/dev/null || true

      - name: Upload Playwright HTML report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-apex-oas-desktop-${{ matrix.os }}
          path: packages/salesforcedx-vscode-apex-oas/playwright-report
          if-no-files-found: ignore

      - name: Upload Playwright Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-results-apex-oas-desktop-${{ matrix.os }}
          path: packages/salesforcedx-vscode-apex-oas/test-results
          if-no-files-found: ignore

      - name: Cleanup scratch org
        if: always() && steps.try-run.outcome == 'failure'
        continue-on-error: true
        shell: bash
        run: |
          sf org delete scratch -o "$MINIMAL_ORG_ALIAS" --no-prompt || true
