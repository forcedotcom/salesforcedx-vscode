name: Nightly Build Develop

on:
  workflow_dispatch:
  schedule: # 10 20 * * *
    - cron: 24 16 * * *

jobs:
  nightly-build-develop:
    uses: ./.github/workflows/buildAll.yml
    with:
      branch: develop