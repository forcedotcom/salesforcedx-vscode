name: Deploy and Retrieve End to End Tests

on:
  workflow_dispatch:
    inputs:
      orgBrowser:
        description: 'Org Browser'
        required: false
        default: true
        type: boolean
      vscodeVersion:
        description: 'VSCode Version'
        required: false
        default: 'latest'
        type: string
      runId:
        description: 'Run ID of the workflow run that created the vsixes'
        required: true
        type: string

  workflow_call:
    inputs:
      orgBrowser:
        description: 'Org Browser'
        required: false
        default: true
        type: boolean
      vscodeVersion:
        description: 'VSCode Version'
        required: false
        default: 'latest'
        type: string
      runId:
        description: 'Run ID of the workflow run that created the vsixes'
        required: false
        type: string

jobs:
  # orgBrowser.e2e.ts removed; new org browser has Playwright tests in org-browser package
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy org browser e2e removed"
