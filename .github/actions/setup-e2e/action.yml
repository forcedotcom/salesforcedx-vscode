name: Setup E2E
description: Set up Node.js, pnpm, Wireit caching, and dependencies
runs:
  using: composite
  steps:
    - name: Setup pnpm
      uses: ./.github/actions/setup-pnpm
      with:
        node-version: '22.22.3'
    - uses: google/wireit@setup-github-actions-caching/v2
