# W-23231085 — Remove duplicate command labels from i18n.ts

## Context

- Pkg: `packages/salesforcedx-vscode-org`
- 2 NLS channels (per `docs/ApexTestingMessageKeys.md`):
  - **Manifest**: `package.json` `%key%` titles → VS Code resolves from `package.nls.json`
  - **Runtime**: `nls.localize('key')` → reads `src/messages/i18n.ts` ONLY (`createNls` in `packages/salesforcedx-vscode-i18n/src/nlsFactory.ts`)
- 14 `_text` command labels live in BOTH `i18n.ts` and `package.nls.json` (dupes).
- Goal: Delete 6 manifest-only keys from `i18n.ts`; keep 8 runtime-used keys in BOTH files (legitimately in both since manifest `%key%` and runtime `nls.localize` both read them). Runtime callers stay on `nls.localize` (preserves Japanese localization via `i18n.ja.ts`).

### 14 dupe keys → 2 groups

Runtime-used (8) — must migrate callers:
- `org_login_web_authorize_org_text`, `org_login_web_authorize_dev_hub_text`, `org_create_default_scratch_org_text`, `org_login_access_token_text`, `org_list_clean_text` → `src/orgPicker/orgList.ts` ACTION_ITEMS
- `org_select_text` → `orgPicker/orgList.ts:195`, `parameterGatherers/selectOrgForDisplay.ts:24`
- `org_delete_default_text`, `org_delete_username_text` → `commands/orgDelete.ts:41,119`

Manifest-only (6) — no runtime caller, just delete from i18n.ts:
- `config_set_org_text`, `org_display_default_text`, `org_display_username_text`, `org_logout_all_text`, `org_logout_default_text`, `org_open_default_scratch_org_text`
- Verified: all 6 have zero `nls.localize(...)` matches under `packages/salesforcedx-vscode-org/src`; titles resolve only via manifest `%key%`.
- Rebuttal (adversary: `org_logout_default_text` is a runtime constructor arg): FALSE — based on pre-migration code. `sf.org.logout.default` is now an Effect command (`orgLogoutDefaultCommand`, `src/commands/auth/orgLogout.ts`, migrated in 4014eafc5), registered via `registerCommand` (`src/index.ts:84`). No `LibraryCommandletExecutor`/`executionName`/`progress_notification_text` path exists in the pkg (grep: zero `LibraryCommandletExecutor` in src). orgLogout.ts:150 is `promptService.confirmOrThrow`, not `super(nls.localize(...))`. Title comes only from manifest `%org_logout_default_text%` (`package.json:286`); e2e already covers it at `test/playwright/specs/orgPickers.desktop.spec.ts:185` (`packageNls.org_logout_default_text`). Correctly stays manifest-only; NOT added to Phase 1.

### WI-detail corrections
- No `configSet.ts` exists; `orgCreate.ts`/`orgLogout.ts`/`orgLoginWeb.ts` do NOT reference the 14 keys. Real callers: `orgPicker/orgList.ts`, `parameterGatherers/selectOrgForDisplay.ts`, `commands/orgDelete.ts`.
- Keep (NOT dupes, leave in i18n.ts): `org_login_web_show_output_button_text`, `org_open_container_mode_message_text`.
- Don't touch `package.nls.json` / `package.nls.ja.json` (manifest JA parity).
- Dep gate: 4.x command-migration WIs (4.1–4.11) closed — confirm before merge (`work-item-sequencing`).

## Phases

### Phase 1 — Attempted migration to package.nls.json (REVERTED)

- Commit 1d64ad278 attempted to migrate runtime callers to direct JSON import.
- Revert: commit d570500b4 restored runtime callers to `nls.localize` to preserve Japanese localization via `i18n.ja.ts`.
- Lesson: VS Code only resolves manifest `%key%` from locale-specific NLS files (`package.nls.ja.json`); direct JSON imports lose locale context.
- The 8 runtime-used keys now legitimately live in BOTH `i18n.ts` and `package.nls.json` (no re-migration needed).

### Phase 2 — delete 6 manifest-only dupe keys from i18n.ts (COMPLETED)

- Commit e1533b600 removed all 14 `_text` keys from `i18n.ts`; the Phase 1 revert (d570500b4) restored the 8 runtime-used keys, leaving 6 net removed.
- Net result — `src/messages/i18n.ts`: 6 `_text` keys removed (config_set_org, org_display_*, org_logout_*, org_open_default_scratch_org); 8 runtime-used keys remain (both i18n.ts and package.nls.json).
- Compiles; `nls.localize` type no longer offers removed keys.
- commit: `refactor(org): drop 6 manifest-only duplicate command labels from i18n.ts - W-23231085`

### Phase 3 — keep 8 runtime-used keys in both places (COMPLETED)

- Commit d570500b4 restored the 8 keys to i18n.ts, confirmed via runtime callers (orgList, selectOrgForDisplay, orgDelete all use `nls.localize`).
- Keys stay in both i18n.ts and package.nls.json (correct design: manifest title + runtime localized label).
- commit: `fix(org): keep runtime command labels localized via nls.localize - W-23231085`

## Skills
- `i18n-messages` — 2-channel model, per-surface style
- `typescript` — orgList.ts, selectOrgForDisplay.ts, orgDelete.ts, i18n.ts edits; tsc/compile check
- `work-item-sequencing` — 4.x dep gate
- `verification`
- `concise`

## Verification
- `grep _text src/messages/i18n.ts` → only show_output_button_text, container_mode_message_text remain
- `grep -rn "nls.localize('.*_text')" src` for the 14 → zero
- compile/tsc pass (proves no dangling typed `nls.localize` refs)
- `npm run lint` — `local/no-unused-i18n-messages`, `package-json-i18n-descriptions` pass
- jest `test/jest/orgPicker/orgList.test.ts` pass
- `package.nls.json` unchanged; manifest `%key%` titles still resolve
- **e2e-covered** (playwright desktop specs, no manual check): command-palette titles + picker action-item labels — `orgCommands`, `orgPicker`, `orgPickers`, `orgListClean`, `orgLoginAccessToken`, `orgDelete*`, `orgDisplay`, `orgOpen`, `telemetryIdentitySeeding` (all read `packageNls` from package.nls.json)
