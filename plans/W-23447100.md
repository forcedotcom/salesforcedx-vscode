# W-23447100 — bump azure exporter + otel suite (GHSA-8988-4f7v-96qf)

## Context

- Advisory GHSA-8988-4f7v-96qf / CVE-2026-54285, medium: `@opentelemetry/core` `<2.8.0`.
- Nested `@opentelemetry/core@2.0.0` dragged in by `@azure/monitor-opentelemetry-exporter@1.0.0-beta.32` (pins `core ^2.0.0`).
- Lockfile now: nested core `2.0.0` (line 371), root core `2.7.1` (line 7634).
- Fix: beta.32 -> beta.43 (pins `core ^2.8.0`) + otel suite bump; add jest coverage for divert path.
- Vetted safe (per WI): beta.33-43 breaking changes miss our API surface (`AzureMonitorLogExporter`/`AzureMonitorTraceExporter` w/ `{connectionString, storageDirectory}` only). Private-field overrides present in beta.43. CJS require intact.
- 2 pkgs: `packages/salesforcedx-vscode-services/package.json`, `packages/salesforcedx-vscode-services-types/package.json`. Types pkg lacks `@opentelemetry/resources`; services has it.

## Phases

### Phase 1 — bump manifests + regen lockfile

Both package.json:
- `@azure/monitor-opentelemetry-exporter` `^1.0.0-beta.32` -> `^1.0.0-beta.43`
- `@opentelemetry/exporter-trace-otlp-http` `0.218.0` -> `0.219.0`
- `@opentelemetry/sdk-logs` `0.218.0` -> `0.219.0`
- `@opentelemetry/sdk-metrics`, `sdk-trace-base`, `sdk-trace-node` `2.7.1` -> `2.8.0`
- services only: `@opentelemetry/resources` `2.7.1` -> `2.8.0`
- `@opentelemetry/core` stays `2.8.0`

- `npm install`
- `npm update @opentelemetry/core` — flush stale nested copies
- Commit lockfile

Files: both package.json, package-lock.json

Commit: `fix(services): bump azure exporter beta.43 + otel 2.8.0 for GHSA-8988-4f7v-96qf`

### Phase 2 — pin the Azure divert path against the bumped SDK

Risk: private-field access — `sender`/`shouldCreateResourceMetric` (spansNode.ts:35,41), `_sender` (azureMonitorLogExporterWrapper.ts:31). Rename in beta.43 → `@ts-expect-error` overrides no-op, telemetry silently stops diverting. Jest unit coverage guards both paths.

Unit (fast, deterministic, real SDK):
- `spansNode.test.ts` already constructs the real `FilteredAzureMonitorTraceExporter`, injects a `LocalEnvelopeSender`, and asserts Breeze envelopes emit with `_OTELRESOURCE_` suppressed — direct guard on `sender`/`shouldCreateResourceMetric`. Re-run as-is (no edit); it now runs against beta.43.
- Gap: `AzureMonitorLogExporterWrapper._sender` untested. Add `packages/salesforcedx-vscode-services/test/jest/observability/azureMonitorLogExporterWrapper.test.ts` mirroring spansNode pattern — wrapper w/ `localIngestionEndpoint`, assert injected sender's `exportEnvelopes` gets `EventData`/`MessageData` envelope on `export()`; catches beta.43 rename of `_sender`.

Files: azureMonitorLogExporterWrapper.test.ts (new)

Commit: `test(services): cover azure exporter divert path (log _sender) for beta.43`

## Skills to apply

- packageJson — dep-bump conventions
- typescript — package edits, lockfile regen
- dependabot-alerts / gha-dependabot — advisory-driven bump context
- merge-conflicts — if lockfile/package.json conflict on rebase
- verification — lockfile assertions below
- concise — this plan

## Verification

- `git grep -n "beta.43\|0.219.0\|2.8.0" packages/salesforcedx-vscode-services{,-types}/package.json` — all target versions present, no lingering `beta.32`/`0.218.0`/`2.7.1`.
- Lockfile: every `@opentelemetry/core` copy `>=2.8.0`.
- Lockfile: NO `node_modules/@azure/monitor-opentelemetry-exporter/node_modules/@opentelemetry/core` at `2.0.0`.
- Lockfile: azure exporter resolves `1.0.0-beta.43`.
- `node --check` n/a (no JS touched); build passes via CI.
- Trace divert (unit, real beta.43 SDK): in `packages/salesforcedx-vscode-services` run `npx jest observability/spansNode` (pkg `test` script is wireit→jest) — private `sender`/`shouldCreateResourceMetric` still land envelopes with `_OTELRESOURCE_` suppressed.
- Log divert (unit, new): `npx jest observability/azureMonitorLogExporterWrapper` — private `_sender` override routes LogRecord envelopes to the local sender.
