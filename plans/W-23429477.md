# W-23429477 — 14 Fix + enforce returnEffectInGen

## Context

- Epic "Effect LS diagnostics: enforce in CI". WI #1 (W-23429440) shipped the ratchet: `scripts/effect-diagnostics.mjs` fails only on findings whose rule is listed in `config/effect-diagnostics.json` `enforcedRules` (any severity). 12 enforced today (latest sibling: `unnecessaryPipeChain`, W-23429475). "Promote to error" = append the rule name there; `tsconfig.common.json` sets no `diagnosticSeverity`.
- Rule `returnEffectInGen` (LS 0.86.2, code 11, severity `message`, group antipattern): a generator `return`s an Effect-able expression without `yield*` → `Effect<Effect<…>>`. Fires only when the return expression's type is an Effect and the enclosing generator is an `Effect.gen`/`Effect.fn`/`fnUntraced` body; `return yield* …` is skipped by the rule itself (`node_modules/@effect/language-service/transform.js:11594-11660`).
- **The WI's 1 finding is already fixed.** WI cites `packages/salesforcedx-vscode-org-browser/src/commands/retrieveMetadata.ts:23` (`return Effect.void;`). Commit `169366aa3a` (#7854, W-23450775 org-browser composition refactor) rewrote it to `return yield* Effect.void;` — now line 22. Verified: measured repo-wide on a compiled tree (`node node_modules/@effect/language-service/cli.js diagnostics --project <pkg>/tsconfig.json --format json` over all 33 `packages/*/tsconfig.json`) → **0 `returnEffectInGen`**; the only repo findings are the 2 pre-existing `unsupportedServiceAccessors` in services (`fsService.ts:121`, `settingsService.ts:41`), which no enforced rule covers.
- Negative control already run: reverting that one line to `return Effect.void;` makes the org-browser project report exactly 1 `returnEffectInGen` at `retrieveMetadata.ts:22:5`; restoring clears it. Confirms the rule name string, that the WI's site is the only one, and that the gate catches a regression.
- So no source edit remains. Work = append the rule to `enforcedRules` (regression guard) + dedupe the doc overlap.
- Overlap (WI's OVERLAP CHECK): `references/composition-style.md` carries the `return yield*` guidance as prose judgment in 2 places — quick-reference row `Return the run's value` (line 271) and the tail sentence of §"Keep side effects in the pipe with tap" (line 105). Only the *missing-`yield*`* half is machine-enforced. The other half ("don't bind to a local just to `return` it") stays judgment: `const r = yield* eff; return r;` has a non-Effect return expression, so the rule never fires. Mark the enforced half, keep the judgment half. Annotation form precedent: the `unnecessaryEffectGen` / `unnecessaryPipeChain` rows in the same table.
- No `SKILL.md` edit: its diagnostics bullet already delegates per-rule fixes to `references/diagnostics-findings.md` and the gate list to `config/effect-diagnostics.json`; neither enumerates rules inline.

## Phases

### Phase 1 — enforce returnEffectInGen, dedupe the skill guidance

- `config/effect-diagnostics.json`: append `"returnEffectInGen"` to `enforcedRules` (13 total). No source change — the sole site is already `return yield*` (see Context); don't "re-fix" it, don't touch the unrelated `unsupportedServiceAccessors` sites.
- `.claude/skills/effect-best-practices/references/diagnostics-findings.md`: add the `returnEffectInGen` row — finding: generator `return`s an Effect without `yield*` → `Effect<Effect<…>>`; fix: `return yield* X`. Keep the table's existing shape/order convention; no enforced-ness annotation (that list lives only in the config).
- `.claude/skills/effect-best-practices/references/composition-style.md`: annotate the `Return the run's value` quick-reference row as config-enforced by `returnEffectInGen` for the raw-`return effect` shape, and state the bind-to-a-local variant stays judgment (no rule fires). Strip the §"Keep side effects…" tail prose that only restates it; leave that section's success-value/failure-channel and `Effect.sync`-in-tap content intact. Apply `concise`; no new examples.
- commit: `chore(effect): enforce returnEffectInGen + dedupe skill guidance - W-23429477`

## Skills to apply

- effect-best-practices — rule semantics, LS CLI invocation, the docs being deduped
- concise — skill-doc edits
- work-item-sequencing — 1 rule per sibling WI; touch only this rule's config entry
- verification — run the checks below before handoff

## Verification

- `npm run compile` first (the CLI reads cross-package `out/*.d.ts`; missing decls spawn phantom findings; wireit `dependencies: ["compile"]` enforces it in CI), then `npm run check:effect-diagnostics` → exit 0, reports 13 rules enforced, 0 failed, 0 enforced violation(s).
- `node node_modules/@effect/language-service/cli.js diagnostics --project packages/salesforcedx-vscode-org-browser/tsconfig.json --format json` → `diagnostics: []`.
- Negative control, rerun after the config edit (the guard is the whole deliverable): change `retrieveMetadata.ts:22` to `return Effect.void;` → `npm run check:effect-diagnostics` exits 1 naming `retrieveMetadata.ts:22` `returnEffectInGen`; restore the line, `git status` clean for that file, gate exit 0 again.
- `npx prettier --check` on the 3 edited files. No `tsc`/`eslint`/jest delta expected: no `.ts` file changes.
- Existing-test coverage of the already-fixed site: no jest test references `retrieveEffect` (`test/jest/` = `index.test.ts`, `metadataTypeTreeProvider.test.ts`); e2e-covered by `packages/salesforcedx-vscode-org-browser/test/playwright/specs/orgBrowser.customObject.headless.spec.ts` ("retrieve Broker__c") plus `orgBrowser.customTab.headless.spec.ts` and `orgBrowser.folderedReport.headless.spec.ts`, which drive the retrieve command through this generator. No new test: no source change, and the rule is now the regression guard.
- `npm run precommit` green.
