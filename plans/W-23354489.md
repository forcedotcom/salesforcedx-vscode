# W-23354489 — stricter prefer-arrow for apex-testing

## Context

- WI 8.8: tighten `prefer-arrow/prefer-arrow-functions` to strict opts for apex-testing src, matching Effect glob.
- Target opts: `{ disallowPrototype: true, singleReturnOnly: false, classPropertiesAllowed: false }` (same as Effect-services block, `eslint.config.mjs:734`).
- Current apex-testing src block (`eslint.config.mjs:817-822`) sets `no-explicit-any` + `prefer-property-signatures` only; inherits base `['error', {}]` for prefer-arrow.
- Scan result: apex-testing src has 0 `.prototype` uses; all `function` keywords are `function*` generators inside `Effect.fn`/`Effect.gen` (rule ignores generators). Strict flip = 0 new violations.
- Follows sibling WIs 5/6/7 (no-explicit-any W-23354483, prefer-property-signatures W-23354485, no-successive-annotate W-23354487) — same pattern: flip rule in scoped override, no src edits.

## Phases

### Phase 1 — flip prefer-arrow to strict for apex-testing src

- Add strict `prefer-arrow/prefer-arrow-functions` to apex-testing src override (`eslint.config.mjs`, block at `files: ['packages/salesforcedx-vscode-apex-testing/src/**/*.ts']`):
  ```
  'prefer-arrow/prefer-arrow-functions': [
    'error',
    { disallowPrototype: true, singleReturnOnly: false, classPropertiesAllowed: false }
  ]
  ```
- No src changes expected (0 violations).
- Commit: `chore(apex-testing): stricter prefer-arrow opts - W-23354489`

## Skills to apply

- concise (plan doc)
- typescript (eslint.config.mjs TS rule config, TS package)
- effect-best-practices (if any unexpected violation needs fn->arrow refactor)
- verification

## Verification

- Build local rules first: `npm run compile --workspace=packages/eslint-local-rules` (eslint.config imports `packages/eslint-local-rules/out/index.js`).
- `npx eslint 'packages/salesforcedx-vscode-apex-testing/src/**/*.ts'` — no `prefer-arrow/*` errors (pre-existing 2 prefer-nullish errors at testResultProcessor.ts:143 are unrelated, out of scope).
- Confirm strict opts reject fn-keyword/prototype: no such uses exist post-flip.
- Not e2e-covered: pure lint-config change, no runtime surface.
