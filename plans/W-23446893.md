# W-23446893 — bump esbuild to ^0.28.1 (GHSA-g7r4-m6w7-qqqr)

## Context
- Advisory GHSA-g7r4-m6w7-qqqr: esbuild `>=0.27.3 <0.28.1`, patched `0.28.1`. Root devDep (build tool).
- Root `package.json`: `esbuild ^0.27.7`, `esbuild-plugins-node-modules-polyfill 1.8.1`.
- Polyfill 1.8.1 peer = `>=0.14.0 <=0.27.x` (lock L18011) — blocks 0.28; 1.8.2 widens to `<=0.28.x`, so bump companion.
- Vetted SAFE: 0.28.0/0.28.1 breaking changes = install-path integrity check + dev-server backslash-disallow. No `.serve`/`context`/`servedir` in any `esbuild.config.mjs` (grep clean). Other esbuild plugins (copy, polyfill-node, transform) tolerate 0.28.1. tsx bundles own esbuild `~0.28.0` (lock L42008).

## Phases

### Phase 1 — bump versions + relock
- `package.json`: `esbuild` `^0.27.7` -> `^0.28.1`; `esbuild-plugins-node-modules-polyfill` `1.8.1` -> `1.8.2`.
- `npm install` to regen `package-lock.json`.
- commit: `fix(deps): bump esbuild to ^0.28.1 for GHSA-g7r4-m6w7-qqqr`
- files: `package.json`, `package-lock.json`

## Skills to apply
- packageJson — dependency edit conventions
- dependabot-alerts — advisory-driven bump context
- merge-conflicts — if lockfile/package.json conflict on rebase
- typescript — type-check impact of esbuild bump
- verification — build check

## Verification
- `package-lock.json`: single `node_modules/esbuild` = `0.28.1`; no copy in `>=0.27.3 <0.28.1`.
- tsx nested esbuild dedupes/resolves to 0.28.1.
- polyfill entry = 1.8.2, peer `<=0.28.x`.
- `npm run build` (or bundle task) succeeds across packages — e2e-covered on branch CI.
- extension bundles produced — e2e-covered (packaging/smoke CI).
