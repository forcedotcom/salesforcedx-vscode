# W-23233936 — Add OrgInfoService to services; drop @salesforce/core from util/orgDisplay

## Context

- Goal: remove direct `@salesforce/core` import from `packages/salesforcedx-vscode-org/src/util/orgDisplay.ts` (currently `import { AuthFields, AuthInfo, Connection, Org } from '@salesforce/core'`, line 8).
- Reland branch is STALE — do NOT cherry-pick/diff wholesale. `origin/sm/W-23233936-reland-from-7687` was cut from `e738a71034` (an older/divergent point, NOT the actual merged #7687 `50b83a46ee`). `git diff 50b83a46ee origin/sm/W-23233936-reland-from-7687 --stat` shows ~59 unrelated files (observability o11ySpanExporter/spansNode/spansWeb, gatedSpanExporter deletion, servicesRuntime.ts, terminal/fsService/settingsService, CONTEXT.md, esbuild). Use it ONLY as a text reference for the exact `orgInfoService.ts` / `schemas/orgInfo.ts` bodies. Hand-port those two files; hand-write the tiny `connectionService.ts` web-guard change; then re-diff working tree against develop tip to confirm no incidental churn rides along.
- **CURRENT develop connectionService state (verified `connectionService.ts:296-336`) — foundation the plan builds on:**
  - `getConnection(username?: string)` ALREADY EXISTS (added by #7738/W-23354940 after the reland source was authored). It: (1) desktop branch resolves `usernameOrAlias = username ?? config TARGET_ORG` via inlined `getConfigAggregator().pipe(...getPropertyValue(TARGET_ORG))` (NOT `getTargetOrg()` — that helper exists at configService.ts:95 and is used only by `promptReauth` at line 230); (2) alias-resolves + hits `connectionCache.get`; (3) `validateAccessTokenOrPromptReauth`; (4) forks `maybeUpdateDefaultOrgRef` ONLY `if (username === undefined)` (line 327) — so it already never corrupts the default-org ref when inspecting a non-default org.
  - Therefore the reland's separate `resolveAndConnect` + `getConnectionForUsername(username)` pair is REDUNDANT — it was built against the pre-#7738 param-less `getConnection()`. Do NOT reintroduce a second near-duplicate accessor.
  - reauthCache is USERNAME-keyed (lines 256-264); `promptReauth(conn, username)` signature; NO `ChannelService` dependency. The reland's Connection-keyed reauth rekey + `promptReauth(conn, username, error)` + `ChannelService` all came from #7687 and are ALREADY on develop's shape as username-keyed. 936 MUST NOT touch `promptReauth` signature, reauthCache keying, or ConnectionService.dependencies — doing so reintroduces the race class the prior review hardened.
- **Web gap (the ONLY connectionService change 936 needs):** `getConnection`'s web branch (lines 297-305) reads settings instanceUrl/accessToken/apiVersion and ignores `username` entirely. So `getConnection(username)` on web silently returns the DEFAULT web org's Connection for a requested non-default username — NOT a typed error. OrgInfoService's degraded-builder mapping depends on a typed `UsernameConnectionNotSupportedOnWebError`. Add that guard directly to `getConnection` dispatch.
- NOT yet on develop: `UsernameConnectionNotSupportedOnWebError`, `orgInfoService.ts`, `schemas/orgInfo.ts`. (`OrgInfoService` accessor + wiring absent.)
- `OrgInfo` type today: `org/src/types/orgInfo.ts`. Consumers: `commands/orgDisplay.ts` (`formatOrgInfoAsTable`), `util/orgDisplay.ts`.
- Org pkg reaches services only via `ExtensionProviderService.getServicesApi` → `api.services.*` (no direct impl import).
- `shared.getOrgFromConnection(conn)` exists → reuse instead of `Org.create`.
- `getConnectionStatusFromError`/`shouldRemoveOrg` live in `org/util/orgUtil.ts`; service can't import org pkg → clone into services w/ jscpd markers (clears `check:dupes`).

## Phases

### Phase 1 — OrgInfo schema in services
- new `services/src/core/schemas/orgInfo.ts`: `OrgInfoStruct = Schema.Struct({...})` mirroring `types/orgInfo.ts` (`password`/`namespace` optional); `export type OrgInfo = typeof OrgInfoStruct.Type`.
- `index.ts`: `export { OrgInfoStruct, type OrgInfo } from './core/schemas/orgInfo'`.
- commit: `feat(services): add OrgInfo schema`

### Phase 2 — Web guard on existing getConnection(username) — NO new accessor
- Do NOT add `getConnectionForUsername`/`resolveAndConnect`. develop's `getConnection(username?)` already provides alias-resolve + cache + reauth + the `if (username === undefined)` ref-mutation guard.
- `connectionService.ts`: add `UsernameConnectionNotSupportedOnWebError` (Schema.TaggedError, `@ExportTaggedError`, ts4023 rules) + export.
- In `getConnection`'s web branch, guard FIRST: if `process.env.ESBUILD_PLATFORM === 'web'` AND `username !== undefined` → `Effect.fail(UsernameConnectionNotSupportedOnWebError)` (before reading settings/connectionCache). Web with NO username (default org) path unchanged.
- Update `getConnection`'s return-type union to include `UsernameConnectionNotSupportedOnWebError`. Verify existing default-org callers (`username` omitted) are unaffected — the error is reachable only when a username is explicitly passed on web.
- Do NOT touch `promptReauth` signature, reauthCache keying, or `ConnectionService.dependencies`.
- commit: `feat(services): fail typed when getConnection(username) called on web`

### Phase 3 — OrgInfoService
- new `services/src/core/orgInfoService.ts` (deps: ConnectionService, AliasService, ConfigService), hand-ported from reland text but wired to `getConnection(username)` (NOT `getConnectionForUsername`):
  - `getOrgInfoForUsername(username?)`: `resolvedUsername = arg ?? ConfigService.getTargetOrg()` else `NoUsernameError`; → `connectionService.getConnection(resolvedUsername)` → derive `OrgInfo`. `Effect.catchTags`: `UsernameConnectionNotSupportedOnWebError` → degraded `OrgInfo` builder; connection/query failures (`FailedToCreateConnectionError`/`FailedToResolveUsernameError`/etc. + `OrgInfoConnectionError`) → SAME degraded builder.
  - `getOrgInfoFromConnection(conn)`: derive `OrgInfo` from established `Connection` (default path; carries its own username). Degrades identically.
  - port helpers from `util/orgDisplay.ts`: `createOrgInfo`, connection→OrgInfo core, `queryScratchOrg` (via `shared.getOrgFromConnection`, no `Org.create`), connection-status (`conn.identity()`), degraded builder.
  - aliases: reuse `AliasService.getAliasesFromUsername` (has `catchAll`) — NOT hand-rolled filter/map (avoids uncaught `FsServiceError` on success path).
  - clone `getConnectionStatusFromError`/`shouldRemoveOrg` (jscpd markers).
  - tagged errors `NoUsernameError`, `OrgInfoConnectionError` (`@ExportTaggedError`, ts4023).
- `index.ts`: add `OrgInfoService` to prebuilt-deps union, API accessor field, `globalLayers` mergeAll, return obj, `export type { OrgInfoService, NoUsernameError, OrgInfoConnectionError }` + `UsernameConnectionNotSupportedOnWebError`.
- commit: `feat(services): add OrgInfoService for org metadata by username`

### Phase 4 — Delegate org pkg; drop @salesforce/core; deletes
- `commands/orgDisplay.ts`: default path → `api.services.OrgInfoService.getOrgInfoFromConnection(conn)`; username path → `getOrgInfoForUsername(username)`; import `OrgInfo` from `@salesforce/vscode-services` (drop `../types/orgInfo`, `../util/orgDisplay`).
- delete `util/orgDisplay.ts` + `types/orgInfo.ts` (verify no other importers).
- knip: after deleting util/orgDisplay.ts, un-export `resolveUsernameFromAliasEffect` + `readAliasesByUsernameFromDiskEffect` in `orgUtil.ts`.
- commit: `refactor(org): delegate orgDisplay to OrgInfoService, drop @salesforce/core`

### Phase 5 — Tests
- new `services/test/jest/core/orgInfoService.test.ts`: port cases from org `util/orgDisplay.test.ts` (username resolution, scratch vs non-scratch, degraded/connection-error, alias supplement).
  - REQUIRED regression: `defaultOrgRef` (SubscriptionRef) UNCHANGED after `getConnection(nonDefaultUsername)` / `getOrgInfoForUsername(nonDefaultUsername)` — snapshot ref before/after, assert equal. Contrast a `getConnection()` (no arg) test that DOES update ref. (Guards the `if (username === undefined)` fork at connectionService.ts:327.)
  - REQUIRED web case: `ESBUILD_PLATFORM='web'` → `getConnection(username)` fails `UsernameConnectionNotSupportedOnWebError`; `getOrgInfoForUsername(username)` degrades gracefully (no `fromKey` mis-parse of a bare username). NOTE: jest mocks ConnectionService, so this exercises the DEGRADE MAPPING in OrgInfoService, not the real web bundle dispatch — see Verification for the e2e-coverage justification.
- update org `orgDisplay.test.ts` (default path uses service via mocked ExtensionProviderService); remove obsolete `util/orgDisplay.test.ts`.
- commit: `test(services): OrgInfoService; update org orgDisplay tests`

## Skills to apply
- effect-best-practices (Effect.Service, Schema.TaggedError, catchTags, layer wiring)
- ts4023-effect-errors (tagged-error exports w/o TS4023/knip)
- services-extension-consumption (org pkg consuming api.services.OrgInfoService)
- typescript (camelCase filenames, direct imports, no barrels)
- unit-test-critic (test quality)
- verification (post-change checks)
- concise (plan/docs style)

## Verification
- Re-diff working tree vs develop tip BEFORE committing each phase — confirm ONLY `orgInfoService.ts`, `schemas/orgInfo.ts`, the `getConnection` web-guard + error export in `connectionService.ts`, `index.ts` wiring, `commands/orgDisplay.ts`, deletes, and tests changed. NO observability/servicesRuntime/settingsService/esbuild churn from the stale reland branch.
- tsc typecheck services + org; grep confirms NO `@salesforce/core` in `util/orgDisplay.ts` (file deleted) + no dangling `types/orgInfo` importers.
- lint + knip (no unused / TS4023 on new tagged-error exports; orgUtil un-exports resolved).
- `check:dupes` (jscpd markers on cloned status helpers).
- jest: new `orgInfoService.test.ts` (incl. defaultOrgRef regression + web degrade mapping); updated org orgDisplay unit tests.
- e2e (e2e-covered, NOT re-run in unit verify) — run BOTH desktop specs post-refactor (distinct code paths):
  - `org/test/playwright/specs/orgDisplay.desktop.spec.ts` — `sf.org.display.default` (`org_display_default_text`, line 47) → default path via `getOrgInfoFromConnection`.
  - `org/test/playwright/specs/orgPickers.desktop.spec.ts` — `sf.org.display.username` (`org_display_username_text`, DISPLAY test.step, lines 77-101) → USERNAME path via `getOrgInfoForUsername` + `getConnection(username)`, incl. alias-reuse, username-resolution, sensitive-info modal, cancel handling. This is the core of Phase 3; the `.username` text does NOT appear in orgDisplay.desktop.spec.ts.
- Web degraded-path e2e: NO web/headless playwright spec exists in the repo (all org specs are `*.desktop.spec.ts`; `find packages -path '*playwright/specs*' -name '*.web.spec.ts'` = empty). The org-display command is not exercised by any web e2e harness. The web guard (`UsernameConnectionNotSupportedOnWebError` → degraded OrgInfo) is therefore covered by: (1) jest degrade-mapping test in Phase 5, and (2) the typed-fail unit test on `getConnection(username)` under `ESBUILD_PLATFORM='web'`. Real web-bundle dispatch coverage is out of scope — no web e2e infrastructure to add one to. Flag this gap explicitly to the reviewer rather than claiming e2e coverage.
