# W-23348768 — Migrate Apex telemetry to Effect spans/logs; drop core TelemetryService

## Context

- Implements ADR-0002 (`packages/salesforcedx-vscode-apex/docs/adr/0002-telemetry-to-effect-spans.md`), which implements repo ADR-0012 (spans-only).
- Retire legacy core event API in `salesforcedx-vscode-apex`: `sendEventData`, `sendException`, `sendExtensionDeactivationEvent`.
- Delete holder `src/telemetry/telemetry.ts` (`TelemetryServiceInterface` + `get/setTelemetryService`) and the `vscodeCoreExtension.exports.services.TelemetryService.getInstance` fetch at `src/index.ts:62-67`.
- Holder consumers (all must migrate before delete):
  - `src/languageServer.ts` — `apexLSPSettings` (L105), `apexLSPError`/LSP_ERR (L134), `apexLSPLog` (L156)
  - `src/index.ts` — deactivation event (L121)
  - `src/languageUtils/languageClientManager.ts` — `apexLSPStartup` (L325), `apexLSPRestart` (L149)
  - `src/languageUtils/index.ts` — `apexTestDiscoveryStart`/`apexTestDiscoveryEnd` (L20/L23)
- Export rules (services observability README): only top-level + command spans reach App Insights/O11y. Nested-fiber data → `annotateRootSpan` (`@salesforce/effect-ext-utils`). `Effect.logDebug/logError` = local-debug only, not event replacement.
- `apexLSPLog` = high-volume, allowlist-gated at `client.onTelemetry`. ADR: do NOT make each event a top-level span (reinstates suppressed volume). Write attrs onto 1 long-lived language-client root span (held Span reference); keep allowlist gate at boundary.
- CRITICAL — client span must be genuine trace ROOT (`root: true`), not parented to activation:
  - `annotateRootSpan` walks `Span.parent` to trace's ultimate root (`annotateRootSpan.ts:13-18`) — `withParentSpan(clientSpan)` on a non-root clientSpan writes attrs onto the activation span, not clientSpan.
  - Activation span closes when `activateEffect` returns (`index.ts:52-101`, `Effect.fn`); Jorje emits `apexLSPLog` for the rest of the session — writes to an ended span are silent no-ops (`@opentelemetry/sdk-trace-base` `Span.setAttribute` returns early once ended).
  - Fix: scoped root span, attrs written to the held `Span` reference directly (Phase 1 below).
- Runtime: `getRuntime()` (`src/services/runtime.ts`) → `ManagedRuntime`. Emit standalone one-shot spans via `getRuntime().runFork(...)` with `root: true` (matches `registerCommand.ts:36-37` — root ensures top-level export, not an orphaned child of activation). Precedent: `languageServerOrphanHandler.ts` (annotateRootSpan, withSpan, Effect.fn).
- Allowlist (`apexLspTelemetryAllowlist.ts`) + its test stay.

## Phases

### Phase 1 — languageServer.ts events → spans/logs
- `apexLSPSettings` (L105): standalone low-volume top-level span via `getRuntime().runFork` — `Effect.withSpan('apex.lsp.settings', { attributes: { maxHeapSize }, root: true })`.
- `apexLSPError`/LSP_ERR (L134): span carrying error (`root: true`, one-shot) + `Effect.logError`; keep `showErrorMessage`.
- `apexLSPLog` (L154-158) — 1 long-lived client span, attrs written to the held Span reference (NOT annotateRootSpan, NOT withParentSpan) — see Context CRITICAL note for why:
  - In `createLanguageServer`, before `client.onTelemetry`, create scoped ROOT span, keep its `Span` handle:
    `const clientSpan = getRuntime().runSync(Effect.makeSpanScoped('apex.lsp.client', { root: true }).pipe(Scope.extend(getExtensionScope())))`
    - `makeSpanScoped` returns the `Tracer.Span`.
    - `root: true` → `findRoot`/`onStart` treat it as top-level → exports.
    - extending extension scope keeps it unended (so `setAttribute` keeps working) until `closeExtensionScope()` on deactivate/restart — then it ends and the file/App Insights exporter ships it.
  - In `onTelemetry`, keep `isApexLspTelemetryAllowed(data.properties)` gate at boundary, then write directly: `Object.entries({ ...data.properties, ...data.measures }).forEach(([k, v]) => clientSpan.attribute(k, v))`.
    - No fork, no annotateRootSpan — `Span.attribute` mutates held span in place.
    - 1 span, not N (attrs last-write-wins, per ADR).
- Confirm `Scope.extend` + `makeSpanScoped` shape against `effect-best-practices`; if `Scope.extend` piping proves awkward, equivalently `getRuntime().runFork(getExtensionScope().pipe(Effect.flatMap(scope => Effect.makeSpan('apex.lsp.client', { root: true }) ... )))` — the invariant is: span is `root: true`, held by reference, ended only at scope close.
- Drop `getTelemetryService` import + both `const telemetryService = getTelemetryService()` (L76, L145).
- Holder still present (index.ts consumers remain) → compiles.
- Commit: `refactor(apex): emit languageServer telemetry as Effect spans - W-23348768`

### Phase 2 — remaining holder consumers → spans
- `languageClientManager.ts`: `apexLSPStartup` (L325, activationTime) + `apexLSPRestart` (L149, restart attrs) → `getRuntime().runFork` spans with `root: true` (top-level export); drop `getTelemetryService` import + L148/L301 calls.
- `languageUtils/index.ts`: `apexTestDiscoveryStart`/`End` (L20/L23) → span `apex.test.discovery` wrapping fetch (`root: true`), attrs `source`/`durationMs`/`numClasses`/`numMethods`; drop `getTelemetryService` import + L18.
- Holder now referenced only by index.ts → compiles.
- Commit: `refactor(apex): migrate LSP startup/restart/test-discovery telemetry to spans - W-23348768`

### Phase 3 — index.ts deactivation + delete holder + tests
- `index.ts`: replace L121 `getTelemetryService().sendExtensionDeactivationEvent()` with deactivation span in `deactivate()` (mirror apex-log `deactivation` Effect.fn, `root: true`); remove `getInstance`/`initializeService`/`setTelemetryService` block (L62-67); drop telemetry.ts imports.
- Dead-code cleanup (else `noUnusedLocals` in `tsconfig.common.json:22` fails `npm run compile`): once the `TelemetryService.getInstance(pjson.name)` call is deleted, its only consumer `pjson` is dead — also remove the `Schema.decodeUnknown(ExtensionPackageJsonSchema)` block (L58-61) and the `ExtensionPackageJsonSchema` + `type ExtensionPackageJson` imports (L11, L13). Deleting `TelemetryUnavailableError` (L37-39) then removes the last `Schema` consumer → drop the `import * as Schema from 'effect/Schema'` (L17) entirely. Verify no other `Schema.`/`ExtensionPackageJson`/`pjson` refs remain in index.ts before compile.
- Delete `src/telemetry/telemetry.ts`.
- Tests: update `test/jest/index.test.ts` (mock at L29-31, deactivation asserts L279-283 → assert span/no-throw), `test/jest/telemetry/mockTelemetryService.ts` (delete if unreferenced), `languageServerOrphanHandler.test.ts` / `languageUtils/index.test.ts` / `languageUtils/languageClientManager.test.ts` (drop `setTelemetryService` setup).
- Add e2e spec `test/playwright/specs/apexTelemetrySpans.desktop.spec.ts` (see Verification for asserts).
- Commit: `refactor(apex): drop core TelemetryService holder + deactivation event - W-23348768`
- Commit (e2e): `test(apex): assert LSP telemetry spans via span-file playwright spec - W-23348768`

## Skills to apply

- effect-best-practices — span/fn/runFork/withParentSpan idioms
- span-file-export — inspect emitted spans locally
- core-extension-api / services-extension-consumption — confirm dropping `exports.services.TelemetryService` allowed
- concise — plan/docs style
- typescript — types over interfaces, functional
- unit-test-critic — test rewrites

## Verification

- Typecheck/build: `npm run compile -w salesforcedx-vscode-apex` (must pass after each phase — holder present until Phase 3).
- Jest (per phase): `npm test -w salesforcedx-vscode-apex` — targeted: `index.test.ts`, `languageUtils/*.test.ts`, `apexLspTelemetryAllowlist.test.ts`.
- Grep clean: no `sendEventData|sendException|sendExtensionDeactivationEvent|getTelemetryService|telemetry/telemetry` in `src/`. Also no residual `Schema.`/`ExtensionPackageJson`/`pjson` in `index.ts`.
- E2e (committed, not manual) — new spec `packages/salesforcedx-vscode-apex/test/playwright/specs/apexTelemetrySpans.desktop.spec.ts`, reusing the `~/.sf/vscode-spans/*.jsonl` read pattern from `packages/salesforcedx-vscode-org/test/playwright/specs/telemetryIdentitySeeding.desktop.spec.ts` (poll newest jsonl, JSON.parse lines, assert on `span.name`/`span.attributes`; no App Insights dependency). Use the apex `test` fixture (`test/playwright/fixtures/index.ts`) and `triggerLspRestart`/`waitForApexLspReady` (`test/playwright/utils/apexLspUtils.ts`). Assert:
  1. `apex.lsp.settings` span emitted on activation with `maxHeapSize` attribute.
  2. `apexLSPStartup` (activationTime) and `apexLSPRestart` (restart attrs) spans emitted — drive restart via `triggerLspRestart`.
  3. ADR's central decision: exactly ONE `apex.lsp.client` span exists (count `span.name === 'apex.lsp.client'` rows == 1, not N) carrying `apexLSPLog` attrs after multiple Jorje feature events (open .cls, hover, etc.).
     - Client span ends only at scope close (deactivate/restart) — not flushed to jsonl before then.
     - Assertion must run after restart or extension teardown.
     - Alternative: assert the single-span invariant post-restart.
- Manual span check (supplementary, App Insights routing not e2e-observable): span file server per observability README — confirm the same spans reach App Insights/O11y export path.
