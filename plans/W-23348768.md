# W-23348768 — Migrate Apex telemetry to Effect spans/logs; drop core TelemetryService

## Context

- Implements ADR-0002 (`packages/salesforcedx-vscode-apex/docs/adr/0002-telemetry-to-effect-spans.md`), which implements repo ADR-0012 (spans-only).
- Retire legacy core event API in `salesforcedx-vscode-apex`: `sendEventData`, `sendException`, `sendExtensionDeactivationEvent`.
- Delete holder `src/telemetry/telemetry.ts` (`TelemetryServiceInterface` + `get/setTelemetryService`) and the `vscodeCoreExtension.exports.services.TelemetryService.getInstance` fetch at `src/index.ts:62-67`.
- Holder consumers (all must migrate before delete):
  - `src/languageServer.ts` — `apexLSPSettings` (L105), `apexLSPError`/LSP_ERR (L134), `apexLSPLog` (L156)
  - `src/index.ts` — deactivation event (L121)
  - `src/languageUtils/languageClientManager.ts` — `apexLSPStartup` (L325), `apexLSPRestart` (L149)
  - `src/languageUtils/index.ts` — `apexTestDiscoveryStart`/`apexTestDiscoveryEnd` (L20/L23)
- Export rules (services observability README): only top-level + command spans reach App Insights/O11y. Nested-fiber data → `annotateRootSpan` (`@salesforce/effect-ext-utils`). `Effect.logDebug/logError` = local-debug only, not event replacement.
- `apexLSPLog` = high-volume, allowlist-gated at `client.onTelemetry`. ADR: do NOT make each event a top-level span (reinstates suppressed volume). Write attrs onto 1 long-lived language-client root span (held Span reference); keep allowlist gate at boundary.
- **Invariant (canonical, ADR)**: exactly ONE live `apex.lsp.client` span per client lifetime — not per-`apexLSPLog`-event, not accumulated across restarts. (Referenced elsewhere below as "the 1-per-lifetime invariant".)
  - Why at risk: `createLanguageServer` — runs on first activation AND every LSP restart (`languageClientManager.ts:260-268` restartTimeout → `createLanguageClient` → `createLanguageServer`). `closeExtensionScope()` — fires ONLY in `deactivate()` (`index.ts:102`, sole call site). Extending the memoized module-level `extensionScope` singleton directly (effect-ext-utils/src/extensionScope.ts:13-17) → each restart's span left unended → N unended root spans flush together at deactivate → invariant violated.
  - Fix: per-client-lifetime child scope (`Scope.fork(extensionScope, …)`); `Scope.close` prior child before forking next — prior span ends/flushes at restart, exactly one live at a time.
- CRITICAL — client span must be genuine trace ROOT (`root: true`), not parented to activation:
  - `annotateRootSpan` — walks `Span.parent` to trace's ultimate root (`annotateRootSpan.ts:13-18`); `withParentSpan(clientSpan)` on a non-root clientSpan writes attrs onto the activation span, not clientSpan.
  - Activation span — closes when `activateEffect` returns (`index.ts:52-101`, `Effect.fn`); Jorje emits `apexLSPLog` for rest of session; writes to an ended span = silent no-op (`@opentelemetry/sdk-trace-base` `Span.setAttribute` returns early once ended).
  - Fix: scoped root span, attrs written to the held `Span` reference directly (Phase 1 below).
- Runtime: `getRuntime()` (`src/services/runtime.ts`) → `ManagedRuntime`. Standalone attribute-only one-shot spans go through the shared `fireSpan` helper (Phase 1) which wraps `getRuntime().runFork(...)` + `Effect.withSpan(...,{root:true})` ONCE (matches `registerCommand.ts:36-37` — root ensures top-level export, not an orphaned child of activation). Precedent: `languageServerOrphanHandler.ts` (annotateRootSpan, withSpan, Effect.fn).
- Allowlist (`apexLspTelemetryAllowlist.ts`) + its test stay.

## Phases

### Phase 1 — shared one-shot-span helper + languageServer.ts events → spans/logs
- FIRST: add `src/services/fireSpan.ts` — a single shared helper for the fire-and-forget attribute-only one-shot-span idiom, so the `runFork`+`withSpan(root:true)` boilerplate is written ONCE, not repeated at every call site:
  ```
  export const fireSpan = (name: string, attributes: Record<string, string | number>): void => {
    getRuntime().runFork(Effect.void.pipe(Effect.withSpan(name, { attributes, root: true })));
  };
  ```
  - The 3 attribute-only one-shot sites use `fireSpan`: `apexLSPSettings` (Phase 1), `apexLSPStartup` + `apexLSPRestart` (Phase 2). Do NOT re-spell `getRuntime().runFork(...withSpan(...root:true))` inline at those sites.
  - `apex.lsp.client` (long-lived scoped span) and `LSP_ERR` (error-status fail span) and `apex.test.discovery` (`Effect.fn` wrapper) are deliberately NOT fireSpan: each needs a distinct span shape (held-scoped, `Effect.fail` for ERROR status, generator span). Keep those bespoke.
- `apexLSPSettings` (L105): `fireSpan('apex.lsp.settings', { maxHeapSize })`.
- `apexLSPError`/LSP_ERR (L134): NOT fireSpan — must end with ERROR status so AppInsights classifies it as exception (severity 17), not INFO (9). Emit `getRuntime().runFork(Effect.annotateCurrentSpan('error', String(err)).pipe(Effect.zipRight(Effect.fail(err)), Effect.withSpan(LSP_ERR, { root: true })))`; keep `showErrorMessage`.
- `apexLSPLog` (L154-158) — 1 long-lived client span, attrs written to the held Span reference (NOT annotateRootSpan, NOT withParentSpan) — see Context CRITICAL note for why:
  - Resolve the extension scope to a `Scope` VALUE first, then fork a per-client child scope, then extend the scoped span into that child. `getExtensionScope()` is an `Effect.fn` returning `Effect<Scope.CloseableScope>` (extensionScope.ts:15), and `Scope.extend`/`Scope.fork` take a resolved `Scope`, NOT an `Effect<Scope>` (Scope.d.ts:124-140, 153) — so `Scope.extend(getExtensionScope())` does NOT compile. Resolve via `runSync` like every existing entrypoint (`apex-log/src/index.ts:47`, `org/src/index.ts:56` both call `Effect.runSync(getExtensionScope())` before `Scope.extend`):
    - Module-level held handle so restart can end the prior span: `let clientScope: Scope.CloseableScope | undefined;` (single `functional/no-let` disable, mirroring extensionScope.ts:12-13).
    - In `createLanguageServer`, before `client.onTelemetry`:
      ```
      const extScope = getRuntime().runSync(getExtensionScope());
      if (clientScope) getRuntime().runSync(Scope.close(clientScope, Exit.void)); // end/flush prior client span on restart
      clientScope = getRuntime().runSync(Scope.fork(extScope, ExecutionStrategy.sequential));
      const clientSpan = getRuntime().runSync(Effect.makeSpanScoped('apex.lsp.client', { root: true }).pipe(Scope.extend(clientScope)));
      ```
    - `makeSpanScoped` returns the `Tracer.Span`.
    - `root: true` → `findRoot`/`onStart` treat it as top-level → exports.
    - Child `clientScope` closed either on next restart (above) or transitively when `closeExtensionScope()` closes parent on deactivate — either way span ends, exporter ships it. Satisfies 1-per-lifetime invariant (Context).
  - In `onTelemetry`, keep `isApexLspTelemetryAllowed(data.properties)` gate at boundary, then write directly: `Object.entries({ ...data.properties, ...data.measures }).forEach(([k, v]) => clientSpan.attribute(k, v))`.
    - No fork-per-event, no annotateRootSpan — `Span.attribute` mutates held span in place.
    - 1 span, not N (attrs last-write-wins, per ADR).
  - Confirm `Scope.fork`/`Scope.extend`/`makeSpanScoped` shape against `effect-best-practices`. Span: `root: true`, held by reference, ended at restart or scope close — 1-per-lifetime invariant (Context).
- Add imports for the fork/close snippet: `import * as Scope from 'effect/Scope'; import * as Exit from 'effect/Exit'; import * as ExecutionStrategy from 'effect/ExecutionStrategy';` (Scope likely already imported).
- Drop `getTelemetryService` import + both `const telemetryService = getTelemetryService()` (L76, L145).
- Holder still present (index.ts consumers remain) → compiles.
- Commit: `refactor(apex): emit languageServer telemetry as Effect spans - W-23348768`

### Phase 2 — remaining holder consumers → spans
- `languageClientManager.ts`: `apexLSPStartup` (L325, activationTime) + `apexLSPRestart` (L149, restart attrs) → `fireSpan('apex.lsp.startup', { activationTime })` / `fireSpan('apex.lsp.restart', { restartBehavior, selectedOption, source, defaultOption })` (Phase-1 helper, no inline runFork); drop `getTelemetryService` import + L148/L301 calls. Import `fireSpan` from `../services/fireSpan`; the `Effect`/`getRuntime` imports become unused here — drop them.
- `createLanguageServer` re-runs on restart (via `createLanguageClient` in restartTimeout callback, `languageClientManager.ts:260-268`) — Phase 1's `clientScope` close-before-fork logic upholds the 1-per-lifetime invariant (Context) here. No extra work beyond migration — confirm nothing short-circuits `createLanguageServer` on restart.
- `languageUtils/index.ts`: `apexTestDiscoveryStart`/`End` (L20/L23) → span `apex.test.discovery` wrapping fetch (`root: true`), attrs `source`/`durationMs`/`numClasses`/`numMethods`; drop `getTelemetryService` import + L18.
- Holder now referenced only by index.ts → compiles.
- Commit: `refactor(apex): migrate LSP startup/restart/test-discovery telemetry to spans - W-23348768`

### Phase 3 — index.ts deactivation + delete holder + tests
- `index.ts`: replace L121 `getTelemetryService().sendExtensionDeactivationEvent()` with deactivation span in `deactivate()` (mirror apex-log `deactivation` Effect.fn, `root: true`); remove `getInstance`/`initializeService`/`setTelemetryService` block (L62-67); drop telemetry.ts imports.
- Dead-code cleanup (else `noUnusedLocals` in `tsconfig.common.json:22` fails `npm run compile`): `TelemetryService.getInstance(pjson.name)` deleted → `pjson` dead (only consumer) → also drop `Schema.decodeUnknown(ExtensionPackageJsonSchema)` block (L58-61) + `ExtensionPackageJsonSchema`/`type ExtensionPackageJson` imports (L11, L13). `TelemetryUnavailableError` deleted (L37-39) → last `Schema` consumer gone → drop `import * as Schema from 'effect/Schema'` (L17). Verify no `Schema.`/`ExtensionPackageJson`/`pjson` refs remain in index.ts before compile.
- Delete `src/telemetry/telemetry.ts`.
- Tests: update `test/jest/index.test.ts` (mock at L29-31, deactivation asserts L279-283 → assert span/no-throw), `test/jest/telemetry/mockTelemetryService.ts` (delete if unreferenced), `languageServerOrphanHandler.test.ts` / `languageUtils/index.test.ts` / `languageUtils/languageClientManager.test.ts`.
- CRITICAL — do NOT merely drop `setTelemetryService` setup: the existing `sendEventData` spy assertions (e.g. `languageClientManager.test.ts:326-356,399-404` assert exact `apexLSPRestart` attrs `restartBehavior`/`selectedOption`/`source`/`defaultOption`; and `apexLSPStartup`/`apexTestDiscoveryStart`/`apexTestDiscoveryEnd` attr sets elsewhere) are regression coverage for span attribute KEYS. Deleting the spy setup would silently drop that coverage. Instead, re-assert the same attribute sets against the new span mechanism via a span-capture harness:
  - Mock `../../../src/services/runtime` so `getRuntime().runFork` runs the effect synchronously against a recording `Tracer` (`Tracer.make` → `Layer.setTracer`) that pushes `{ name, attributes }` into a module-level `mockRecordedSpans` array. Provide a `spanAttributes(name)` helper (`Object.fromEntries` of the captured attribute Map).
  - Replace each `expect(mockTelemetryService.sendEventData).toHaveBeenCalledWith(...)` with `expect(spanAttributes('apex.lsp.restart')).toEqual({ restartBehavior, selectedOption, source, defaultOption })` (and equivalently for `apex.lsp.startup`, `apex.test.discovery`). Same attribute keys, asserted on the span instead of the event — no coverage lost.
- Add e2e spec `test/playwright/specs/apexTelemetrySpans.desktop.spec.ts` (see Verification for asserts).
- Commit: `refactor(apex): drop core TelemetryService holder + deactivation event - W-23348768`
- Commit (e2e): `test(apex): assert LSP telemetry spans via span-file playwright spec - W-23348768`

## Skills to apply

- effect-best-practices — span/fn/runFork/withParentSpan idioms
- span-file-export — inspect emitted spans locally
- core-extension-api / services-extension-consumption — confirm dropping `exports.services.TelemetryService` allowed
- concise — plan/docs style
- typescript — types over interfaces, functional
- unit-test-critic — test rewrites

## Verification

- Typecheck/build: `npm run compile -w salesforcedx-vscode-apex` (must pass after each phase — holder present until Phase 3).
- Jest (per phase): `npm test -w salesforcedx-vscode-apex` — targeted: `index.test.ts`, `languageUtils/*.test.ts`, `apexLspTelemetryAllowlist.test.ts`.
- Grep clean: no `sendEventData|sendException|sendExtensionDeactivationEvent|getTelemetryService|telemetry/telemetry` in `src/`. Also no residual `Schema.`/`ExtensionPackageJson`/`pjson` in `index.ts`.
- No-duplication guard (finding 1): the attribute-only one-shot idiom lives only in `fireSpan.ts`. Outside `fireSpan.ts`, `LSP_ERR` (error-status), and `apex.lsp.client` (scoped), no `.withSpan(` call in `src/` should pair with a fresh `runFork` for a plain attribute payload — those go through `fireSpan`.
- E2e (committed, not manual) — new spec `packages/salesforcedx-vscode-apex/test/playwright/specs/apexTelemetrySpans.desktop.spec.ts`, reusing the `~/.sf/vscode-spans/*.jsonl` read pattern from `packages/salesforcedx-vscode-org/test/playwright/specs/telemetryIdentitySeeding.desktop.spec.ts` (poll newest jsonl, JSON.parse lines, assert on `span.name`/`span.attributes`; no App Insights dependency). Use the apex `test` fixture (`test/playwright/fixtures/index.ts`) and `triggerLspRestart`/`waitForApexLspReady` (`test/playwright/utils/apexLspUtils.ts`). Assert:
  1. `apex.lsp.settings` span emitted on activation with `maxHeapSize` attribute.
  2. `apexLSPStartup` (activationTime) and `apexLSPRestart` (restart attrs) spans emitted — drive restart via `triggerLspRestart`.
  3. ADR's central decision — the 1-per-lifetime invariant (Context): after a client lifetime carrying `apexLSPLog` attrs (open .cls, hover, etc.), assert exactly one `apex.lsp.client` span flushes. Span ends only at scope close (restart closes prior child scope; deactivate closes parent) — not flushed to jsonl before then.
     - MUST cover the restart path, not just clean first-activation teardown. The current spec has two independent tests (clean activation+reload asserts client==1; separate restart test asserts only the `apex.lsp.restart` span) — neither exercises restart-then-count, so the N-not-1 restart regression would ship green. Fix the coverage gap:
       - PINNED expected count (do not leave open): each client lifetime flushes exactly ONE `apex.lsp.client` span, so N restarts in one session ⇒ N+1 lifetimes ⇒ N+1 spans in that session's jsonl. Both lifetimes flush to the same session file (restart closes the prior child scope → prior span flushes; deactivate closes parent → final span flushes).
       - Restart test drives exactly ONE `triggerLspRestart` ⇒ 2 lifetimes ⇒ assert `byName(flushed, 'apex.lsp.client').length === 2` (poll for `>= 2` to await flush, then assert `=== 2`). This is the load-bearing invariant assertion — the N-not-1 regression (shared scope, never-ended spans) fails it. Steps: open .cls → `waitForApexLspReady` → `triggerLspRestart` (drives second `createLanguageServer`) → generate more Jorje events → `reloadWindow` (deactivate flush).
       - Clean-activation baseline (no restart) asserts `byName(flushed, 'apex.lsp.client').length === 1`.
- Manual span check (supplementary, App Insights routing not e2e-observable): span file server per observability README — confirm the same spans reach App Insights/O11y export path.
