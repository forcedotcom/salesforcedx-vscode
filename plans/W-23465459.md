# W-23465459 — Migrate ARD hasRootWorkspace to WorkspaceService, fileExtensionsMatch to Utils.extname

## Context

Pkg `salesforcedx-vscode-apex-replay-debugger`. Drop `salesforcedx-utils-vscode` workspace/path helpers:

- `hasRootWorkspace()` -> `api.services.WorkspaceService.getWorkspaceInfo()` + check `!isEmpty` (exact boolean).
  - API: `ExtensionProviderService` -> `getServicesApi` -> `api.services.WorkspaceService.getWorkspaceInfo()` returns `{ isEmpty, ... }` (see `salesforcedx-vscode-services/src/vscode/workspaceService.ts:68`). Consumption pattern: `workspaceOrgShape.ts:16`.
- `fileExtensionsMatch(uri, ext)` -> `Utils.extname(uri).toLowerCase() === `.${ext}`` (vscode-uri; keep case-insensitive, add leading dot). Ref usage `services/.../fileSystemProvider.ts:43`.

3 hasRootWorkspace sites, all must be Effect-reachable:
- `commands/quickLaunch.ts:52` — already in `Effect.gen`/`Effect.fn`; `api` already bound.
- `commands/anonApexDebug.ts:61` `getAnonApexContext` — caller `executeAnonApexDebug` is `Effect.fn`; convert to Effect.
- `activation/getDialogStartingPath.ts:14` — convert fn to Effect; run via `getRuntime().runPromise` at `index.ts:61`.

Out of scope: fs helpers (`fileOrFolderExists`/`readFile`/`writeFile`/`createDirectory`) — WI 2.

## Phases

### Phase 1 — fileExtensionsMatch -> Utils.extname
`commands/launchApexReplayDebuggerWithCurrentFile.ts`
- drop `fileExtensionsMatch` import from utils-vscode
- `Utils` already? no — add `import { Utils } from 'vscode-uri'` (URI already imported)
- `isLogFile`: `Utils.extname(sourceUri).toLowerCase() === '.log'`
- `isAnonymousApexFile`: `=== '.apex'`
- commit: `refactor(apex-replay-debugger): fileExtensionsMatch -> Utils.extname`

### Phase 2 — quickLaunch hasRootWorkspace
`commands/quickLaunch.ts:52`
- drop `workspaceUtils` from utils-vscode import (keep `projectPaths`)
- replace `if (workspaceUtils.hasRootWorkspace())` -> `const { isEmpty } = yield* api.services.WorkspaceService.getWorkspaceInfo(); if (!isEmpty) { ... }` (`api` bound line 29)
- commit: `refactor(apex-replay-debugger): quickLaunch hasRootWorkspace -> WorkspaceService`

### Phase 3 — anonApexDebug getAnonApexContext -> Effect
`commands/anonApexDebug.ts`
- drop `hasRootWorkspace` import
- convert `getAnonApexContext` to `Effect.fn` (or inline into `executeAnonApexDebug`): `const { isEmpty } = yield* api.services.WorkspaceService.getWorkspaceInfo(); if (isEmpty) return undefined`
- `api` currently bound at line 85 (after ctx use) — move `api` bind above ctx, or bind inside context Effect
- `executeAnonApexDebug`: `yield*` the ctx Effect
- commit: `refactor(apex-replay-debugger): anonApexDebug hasRootWorkspace -> WorkspaceService`

### Phase 4 — getDialogStartingPath -> Effect + index wiring
`activation/getDialogStartingPath.ts`, `index.ts`
- convert `getDialogStartingPath` to `Effect.fn`; bind `api = yield* (yield* ExtensionProviderService).getServicesApi`
- replace `workspaceUtils.hasRootWorkspace()` -> `const { isEmpty } = yield* api.services.WorkspaceService.getWorkspaceInfo(); if (isEmpty) return undefined` (else branch); keep `fileOrFolderExists`/`projectPaths` (WI 2)
- fs stat awaits -> `Effect.promise` wraps
- drop `workspaceUtils` import
- `index.ts:61`: `await getDialogStartingPath(...)` -> `await getRuntime().runPromise(getDialogStartingPath(...))` (`getRuntime` imported line 47)
- update `test/jest/activation/getDialogStartingPath.test.ts`: replace `hasRootWorkspace` spy w/ WorkspaceService mock (mock `getWorkspaceInfo` via services api / ExtensionProviderService); run through runtime
- commit: `refactor(apex-replay-debugger): getDialogStartingPath hasRootWorkspace -> WorkspaceService`

## Skills to apply
- services-extension-consumption (WorkspaceService via `api.services`, ExtensionProviderService)
- effect-best-practices (Effect.fn conversions, Effect.promise wrapping)
- typescript
- paths (Utils.extname)
- unit-test-critic (Phase 4 test rewrite)
- concise

## Verification
- `node --check` n/a; `npm run compile` / tsc on pkg — no type errors, no leftover `hasRootWorkspace`/`fileExtensionsMatch`/`workspaceUtils` imports
- `grep -rn "hasRootWorkspace\|fileExtensionsMatch\|workspaceUtils" packages/salesforcedx-vscode-apex-replay-debugger/src` -> empty
- jest: `getDialogStartingPath.test.ts` (rewritten), `anonApexDebug.test.ts` (unaffected exports still pass)
- lint pkg
- e2e (branch playwright) — run locally before PR (`packages/salesforcedx-vscode-apex-replay-debugger`):
  - `test/playwright/specs/apexReplayDebugger.desktop.spec.ts` — Phase 1 `isLogFile`/`isAnonymousApexFile` (`.log`/`.apex` active-tab switches); Phase 2 quickLaunch `WorkspaceService` path.
  - `test/playwright/specs/debugAnonymousApex.desktop.spec.ts` — Phase 3 `anonApexDebug`/`getAnonApexContext` `WorkspaceService` path.
- COVERAGE GAP — Phase 4 `getDialogStartingPath` dialog-default-path behavior (`showOpenDialog` `defaultUri`) has no e2e spec; jest `getDialogStartingPath.test.ts` is the only coverage.
