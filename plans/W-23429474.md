# W-23429474 — 12 Fix + enforce unnecessaryEffectGen

## Context

- Epic "Effect LS diagnostics: enforce in CI". WI #1 (W-23429440) shipped the ratchet: `scripts/effect-diagnostics.mjs` fails only on rules listed in `config/effect-diagnostics.json` `enforcedRules`. 10 rules enforced today (latest sibling: `effectFnIife`, W-23429473). "Promote to error" = append the rule name there; `tsconfig.common.json` sets no `diagnosticSeverity`.
- Rule `unnecessaryEffectGen`: `Effect.gen` whose body is a single `return yield* X` collapses to `X`.
- Measured repo-wide (per-package `node node_modules/@effect/language-service/cli.js diagnostics --project <pkg>/tsconfig.json --format json`, all pkgs with a tsconfig): exactly 1 finding — `packages/salesforcedx-vscode-services/src/vscode/prompts/promptService.ts:150` (message severity), inside `promptForOutputDir`'s `folderName` branch.
- Trial-applied the collapse and re-measured: rule clears, no new findings (services then reports only its 2 pre-existing `unsupportedServiceAccessors`), `tsc -p` clean at the site, prettier clean. So the fix is the mechanical de-indent + drop of the wrapper; no restructuring.
- Span unaffected (ADR 0012): inner gen carried no span; outer `Effect.fn('PromptService.promptForOutputDir')` stays.
- Overlap: `effect-best-practices/references/composition-style.md` §"Linear body → point-free pipe, not a generator" restates the single-yield case as prose judgment. Dedupe = mark that shape machine-enforced, keep the section's remaining judgment content (multi-yield linear bodies, point-free seed choice, throw-lifting) which no LS rule covers. Precedent: W-23429451, W-23429473.

## Phases

### Phase 1 — collapse the single-yield gen, enforce the rule, dedupe the skill

- `packages/salesforcedx-vscode-services/src/vscode/prompts/promptService.ts:150-160`: drop the `Effect.gen(function* () { return yield* … })` wrapper; the ternary's truthy arm becomes `yield* projectService.getSfProject().pipe(…)` with the existing pipe steps re-indented unchanged (`params.folderName!` assertion stays). No other edit in the file.
- `config/effect-diagnostics.json`: append `"unnecessaryEffectGen"` to `enforcedRules` (11 total).
- `.claude/skills/effect-best-practices/SKILL.md`: extend the "Address warnings AND messages" common-findings bullet with `unnecessaryEffectGen` (config-enforced; single-`yield*` gen → the inner effect), matching the sibling entries' form.
- `.claude/skills/effect-best-practices/references/composition-style.md` §"Linear body → point-free pipe, not a generator" (and its summary-table `Linear Effect.fn body` row): note the single-`return yield*` shape is config-enforced by `unnecessaryEffectGen`; strip prose that only restates it. Apply the `concise` skill; no new examples.
- commit: `fix(effect): collapse single-yield Effect.gen + enforce unnecessaryEffectGen - W-23429474`

## Skills to apply

- effect-best-practices — rule semantics, LS CLI invocation, the doc files being deduped
- concise — skill-doc edits
- typescript — services package conventions
- work-item-sequencing — one rule per sibling WI; touch only this rule's config entry
- verification — run the checks below before handoff

## Verification

- `npm run compile` (required: runner/CLI read cross-package `out/*.d.ts`), then `npm run check:effect-diagnostics` → exit 0, reports 11 rules enforced.
- `node node_modules/@effect/language-service/cli.js diagnostics --project packages/salesforcedx-vscode-services/tsconfig.json --format json` → 0 `unnecessaryEffectGen`, no rule that was absent before.
- Negative control: revert the promptService edit only → `npm run check:effect-diagnostics` exits 1 naming `promptService.ts:150`; restore the edit.
- `npx tsc --noEmit -p packages/salesforcedx-vscode-services/tsconfig.json`, `npx eslint`, `npx prettier --check` on the edited file.
- `npm run test -w salesforcedx-vscode-services` (wireit → `jest`; no `test:jest` script exists) — existing suite only; `promptService.test.ts` covers `confirmOrThrow`/`withCancellableProgressReporting`, not `promptForOutputDir`. No new unit test: wrapper removal, identical types, no new branch.
- e2e-covered: `packages/salesforcedx-vscode-lwc/test/playwright/specs/lwcGenerateComponent.headless.spec.ts` step 3 accepts the output-directory quick pick from the `folderName: 'lwc'` caller, exercising the edited branch.
- `npm run precommit` green.
