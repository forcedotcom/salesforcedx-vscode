# W-23313214 — shrink services bundle via esbuild ESM resolution of effect

## Context

- Apply node-build-only effect ESM conditions override to `packages/salesforcedx-vscode-services/esbuild.config.mjs`
- Pattern proven repo-wide: W-23293744 (org-browser POC), siblings W-23313212 metadata / W-23313207 apex / etc
- Mechanism: spread `effectEsmConditions` (`scripts/bundling/effect.mjs`) into `nodeBuild` after `nodeConfig` → esbuild resolves effect `dist/esm/*` (`sideEffects:[]`) → tree-shakes unused submodules (fast-check via Schema). Output stays CJS.
- Desktop/node bundle ONLY; `browserBuild` untouched (per ADR 0021, W-23293744 scope)
- effect dep present: `^3.21.2`

### Target file shape

- `esbuild.config.mjs` builds `nodeBuild` (outdir `./dist`, entry `out/src/index.js`) + `browserBuild` (outfile `dist/web/index.js`), both `metafile: true`, writes `dist/node-metafile.json` + `dist/browser-metafile.json`
- add import `effectEsmConditions` from `../../scripts/bundling/effect.mjs`
- spread into `nodeBuild` object after `...nodeConfig`

## Phases

### Phase 1 — record baseline

- compile: `npm run compile -w salesforcedx-vscode-services`
- rebuild: `npm run vscode:bundle -w salesforcedx-vscode-services`
- record from `dist/node-metafile.json` + `dist`: node `dist/index.js` bytes, effect input count (cjs vs esm), web `dist/web/index.js` bytes
- baseline numbers fold into Phase 2 commit body — no metafile committed (siblings keep it out of git)
- no file change → no standalone commit

### Phase 2 — apply override + measure

- edit `packages/salesforcedx-vscode-services/esbuild.config.mjs`:
  - add `import { effectEsmConditions } from '../../scripts/bundling/effect.mjs';`
  - add `...effectEsmConditions,` after `...nodeConfig,` in `nodeBuild`
- rebuild `vscode:bundle`, re-measure node bytes + effect esm/cjs input counts + web bytes (must be unchanged)
- verify CJS output: no `import.meta`, no top-level ESM syntax in `dist/index.js`
- files: `packages/salesforcedx-vscode-services/esbuild.config.mjs`
- commit msg: `perf(services): resolve effect ESM to shrink bundle - W-23313214`
- body: before/after node bytes + % reduction, effect esm/cjs counts, web bytes unchanged, CJS verified (match W-23293744 PR format)

### Phase 3 — wireit cache invalidation

- add `"../../scripts/bundling/effect.mjs"` (after `node.mjs`) to `files[]` of BOTH `vscode:bundle` and `vscode:bundle:local` in `packages/salesforcedx-vscode-services/package.json`
- prevents stale bundle when helper changes (sibling review finding)
- files: `packages/salesforcedx-vscode-services/package.json`
- commit msg: `fix(services): add effect.mjs to wireit bundle files - W-23313214`

## Skills to apply

- concise — plan + commit bodies
- typescript — .mjs/.json edits stay type-safe with existing config
- effect-best-practices — context on effect ESM/Schema
- services-extension-consumption — services consumed by other exts; desktop runtime risk
- packageJson / wireit — Phase 3 edits
- verification — pre-PR checks
- playwright-e2e — consuming-ext `test:desktop` runtime gate (metadata/org)
- pr-draft — PR body must report before/after + % (matches W-23293744)

## Verification

- `node --check packages/salesforcedx-vscode-services/esbuild.config.mjs`
- node `dist/index.js` size drops vs baseline; % recorded in PR
- effect inputs: esm > 0, cjs = 0 (from `dist/node-metafile.json`)
- web `dist/web/index.js` bytes unchanged
- CJS output intact: grep `dist/index.js` for `import.meta` / top-level `import`/`export` → none
- wireit: edit `scripts/bundling/effect.mjs` → `vscode:bundle` + `:local` cache invalidates (no stale dist)

### Desktop runtime (cjs→esm resolution swap: runtime exercise = gate)

- services has NO package-level `test:desktop`; `test:web`/`test:e2e` (playwright web) exercise the UNTOUCHED browser bundle → NOT a substitute for node-bundle validation. Bare `node -e "require('dist/index.js')"` INVALID: `src/index.ts:12` imports `vscode`, esbuild keeps it external (`scripts/bundling/node.mjs` `external:['vscode']`) → throws `Cannot find module 'vscode'` before any effect/Schema code runs.

Required local gates before PR (mirrors sibling W-23313212/W-23313213/W-23313202 pattern):

- `npm run test -w salesforcedx-vscode-services` (jest) — REQUIRED. `test/jest/index.test.ts:26` imports `{ activate, deactivate }` from `src/index.ts`; `:245`/`:259` call `activate(context)`/`deactivate()` with `vscode` mocked via `config/jest.base.config.js` moduleNameMapper (`^vscode$` → `scripts/setup-jest.ts`). Exercises the same effect/Schema decode paths the ESM swap re-resolves — sibling W-23313202 (apex-debugger) jest-as-backstop pattern.
- Consuming-extension desktop Playwright suite whose `test:desktop` wireit-depends on `../salesforcedx-vscode-services:vscode:package` (verified in each package.json) — REQUIRED. Run at least one; both preferred since they cover disjoint Schema call sites:
  - `npm run test:desktop -w salesforcedx-vscode-metadata` — at-risk specs: `packageInstall.headless.spec.ts` (`Schema.TaggedError`), `sourceDiff.headless.spec.ts` / `sourceDiffMultiple.headless.spec.ts` (`diffTypes.ts` `Schema.declare`/`Schema.Struct`) — all decode against types sourced from the services bundle.
  - `npm run test:desktop -w salesforcedx-vscode-org` — at-risk specs: `orgDisplay.desktop.spec.ts` (`cliJson.ts` `Schema.parseJson` + `Schema.decodeUnknown`), `orgPicker.desktop.spec.ts` / `orgPickers.desktop.spec.ts` (`orgList.ts` effect pipelines), `telemetryIdentitySeeding.desktop.spec.ts` (`activateEffect` at activation).
- Branch CI e2e = backstop across the remaining consumers (apex, apex-log, apex-oas, apex-replay-debugger, apex-testing, core, lightning, lwc — all `extensionDependencies`/wireit-depend on services `vscode:package`). Backstop, not substitute for the local runs above.
