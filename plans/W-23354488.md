# W-23354488 — enable local/no-effect-service-accessor-calls (apex-testing)

## Context

- WI: 8.7 flip `local/no-effect-service-accessor-calls` on for `packages/salesforcedx-vscode-apex-testing`
- Rule src: `packages/eslint-local-rules/src/noEffectServiceAccessorCalls.ts`
- Rule purpose: when a service-with-accessors is a dependency, yield it first; call accessors only inside `Effect.fn` callbacks (top-level accessor calls run at layer-build time)
- Currently `error` only in Effect-services block (`eslint.config.mjs` ~L689-753); apex-testing NOT in that block
- apex-testing sits in class-methods block (`eslint.config.mjs` ~L768) — sibling WI W-23354481 enabled `local/no-explicit-effect-return-type` there same way (see commit 412786bcf6)
- Effect.Service defs in src (5, WI said 4): apexTestDiscoveryService, codeCoverageService, apexTestTreeService, apexTestRunCacheService, apexTestExecutionService
- Only `ApexTestDiscoveryService` has non-empty `dependencies`; already yields service top-level + uses inside `Effect.fn` (compliant)
- Audit result: running rule vs apex-testing src = 0 violations. Pure config flip, no code fixes needed
- DoD: rule = `error` for apex-testing; lint passes

## Phases

### Phase 1 — enable rule + verify clean

- Build rules pkg if needed: `packages/eslint-local-rules` compiles to `out/` (untracked; needed for eslint to load `local/*`)
- Add `'local/no-effect-service-accessor-calls': 'error'` to class-methods block in `eslint.config.mjs` (the block already listing apex-testing, ~L768-778), mirroring W-23354481
- Scope note: that shared files-array block also covers `salesforcedx-vscode-soql`, `soql-common`, `soql-model`, so the rule newly applies to those 3 packages too (2 real Effect.Service defs: soql toolingSDK.ts, toolingModelService.ts). Audited clean: 0 violations across all 4 packages
- Confirm 0 violations across apex-testing src + test
- No source changes expected (audit clean); if any surface, yield service before accessor use inside `Effect.fn`
- Files: `eslint.config.mjs`
- Commit: `chore(apex-testing): enable local/no-effect-service-accessor-calls - W-23354488`

## Skills

- concise (plan/docs)
- effect-best-practices (if any accessor fix surfaces)
- typescript
- verification

## Verification

- `npx eslint "packages/salesforcedx-vscode-apex-testing/src/**/*.ts"` — no `no-effect-service-accessor-calls` errors (ran during planning: 0)
- rule unit tests: `packages/eslint-local-rules/test/no-effect-service-accessor-calls.test.ts` still green (rule unchanged; sanity only)
- full package lint clean
- not e2e-covered — config/lint change, no runtime surface. Playwright exemption: verification skill lists apex-testing unconditionally, but this diff touches only `eslint.config.mjs` (no files under any covered package), so there is no runtime behavior to exercise (config-only diff carve-out)
