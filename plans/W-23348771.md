# W-23348771 — Migrate langServer org-namespace read to services ConnectionService

## Context

- File: `packages/salesforcedx-vscode-apex/src/languageServer.ts`
- `provideCodeLenses` (line 234) reads org namespace via `vscodeCoreExtension.exports.getAuthFields().namespacePrefix` (line 242).
- Migrate to services `ConnectionService.getConnection().getAuthInfoFields().namespacePrefix` via `getServicesApi` — mirror sibling `getNamespaceFromProject` (line 228, PR #7677).
- Behavior-identical: `getAuthInfoFields()` reads auth-file fields, no live org query. Core `getAuthFields` already delegates to same `ConnectionService.getConnection` (`orgAuthInfoExtensions.ts:38-40`) — no behavior change, incl. no-target-org: `getConnection` rejects with `NoTargetOrgConfiguredError` on old + new paths, so the `Promise.all` at `provideCodeLenses` (line 240) already rejects; no new catch/failure mode.
- `getVscodeCoreExtension` used ONLY at line 239/242 in this file → drop import (line 23) after migration.

## Phases

### Phase 1 — Migrate org-namespace read to ConnectionService

- Add `getNamespaceFromOrg` Effect helper beside `getNamespaceFromProject`:
  ```ts
  const getNamespaceFromOrg = Effect.fn('apex.provideCodeLenses.getNamespaceFromOrg')(function* () {
    const api = yield* (yield* ExtensionProviderService).getServicesApi;
    const connection = yield* api.services.ConnectionService.getConnection();
    return connection.getAuthInfoFields().namespacePrefix ?? undefined;
  });
  ```
- In `provideCodeLenses`: replace `vscodeCoreExtension.exports.getAuthFields()...` with `getRuntime().runPromise(getNamespaceFromOrg())`.
- Drop `const vscodeCoreExtension = await getVscodeCoreExtension();` (line 239) + import (line 23) — no other uses in file.

commit: `refactor(apex): migrate langServer org namespace read to ConnectionService - W-23348771`

## Skills to apply

- core-extension-api — rescan consumers after dropping `getAuthFields` core-api use
- services-extension-consumption — `getServicesApi` / `api.services.ConnectionService` pattern
- typescript, concise, verification

## Verification

- `node_modules/.bin/tsc` typecheck apex pkg — compiles, no `getVscodeCoreExtension` unused
- lint apex pkg — no unused import
- jest apex pkg (`namespaceLensRewriter.test.ts`) — green (unit-covers `rewriteNamespaceLens`; does NOT touch `provideCodeLenses` / the org read)
- build apex pkg — bundles
- e2e: `runApexTestsCodeLens.headless.spec.ts` (desktop-only via `isDesktop()`) — loads apex ext (fixtures `additionalExtensionDirs`), auths real org (`setupNonTrackingOrgAndAuth`), `clickCodeLens('Run All Tests')` / `clickCodeLens('Run Test')`. Lenses flow: apex `provideCodeLenses` middleware → `getNamespaceFromOrg()` (changed `ConnectionService.getConnection().getAuthInfoFields().namespacePrefix` read) → `rewriteNamespaceLens` → click runs test. Green = migrated read works end-to-end against live authed org.
  - Coverage bound: non-tracking org has no namespace, so `nsFromOrg` is `undefined` on old + new paths — covers the click flow, NOT the namespaced-org branch (`nsFromOrg` defined). That branch stays unit-only (`namespaceLensRewriter.test.ts`); no namespaced-scratch-org e2e fixture, out of scope for behavior-identical migration.
- confirm core-extension-api consumer scan: `getAuthFields` no longer referenced from apex
