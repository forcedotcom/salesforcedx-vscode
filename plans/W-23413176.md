# W-23413176 — Remove dead LSP-based Apex test discovery

## Context

- Remove dead LSP test-discovery path from `salesforcedx-vscode-apex`.
- Superseded by API discovery (Tooling API) in `salesforcedx-vscode-apex-testing` (own `lspConverter.ts` + `ApexTestMethod` — untouched).
- `apex.test.discovery` span fires only via exported `getApexTests()`; zero consumers (in-repo + gh search org:forcedotcom/salesforcecli).
- Leftover from removed api-vs-ls toggle (PR #6653). Depends on W-23348768 (introduced span, now Closed).

### Verified

- `getApexTests` / `ApexVSCodeApi.getApexTests`: no external consumer. Other pkgs (replay-debugger, apex-debugger, apex-oas) consume `ApexVSCodeApi` but only `.languageClientManager` / breakpoint members.
- apex-oas: only `.languageClientManager` (`getClientInstance`/`getStatus`).
- `src/views/lspConverter.ts`: sole consumers are dead path (`languageUtils/index.ts`, `languageClientManager.getApexTests`).
- Imports `Clock`/`Data`/`Effect`/`getRuntime` in `languageUtils/index.ts` used only by dead path → drop.

## Phases

### Phase 1 — remove dead LSP test-discovery (1 commit)

Delete:

- `src/languageUtils/index.ts`: `ApexTestDiscoveryError`, `discoverFromLs`, `fetchFromLs`, `getApexTests`, `buildMeasuresFromTests`; drop now-unused imports `Clock`, `Data`, `Effect`, `getRuntime`, `ApexTestMethod`.
- `src/languageUtils/languageClientManager.ts`: `getApexTests()` method (+ `test/getTestMethods` request); drop unused import `{ ApexLSPConverter, ApexTestMethod, LSPApexTestMethod }`.
- `src/views/lspConverter.ts`: whole file (`ApexTestMethod`, `LSPApexTestMethod`, `LSPLocation`, `LSPPosition`, `ApexLSPConverter`).
- `src/index.ts`: `getApexTests` from imports, returned exports obj, `ApexVSCodeApi` type.
- `test/jest/languageUtils/index.test.ts`: whole file (only fetchFromLs/getApexTests suites; nothing else there).

Verify no other refs to removed symbols before delete.

Commit: `refactor: remove dead LSP-based Apex test discovery`

## Skills

- concise — plan/doc style
- span-file-export — confirm `apex.test.discovery` span gone from export
- typescript — dangling-import cleanup, tsc
- verification — run build + jest
- external-consumers — already verified no external `getApexTests` consumer

## Verification

- `grep -rn "getApexTests\|ApexTestMethod\|LSPApexTestMethod\|ApexLSPConverter\|apex.test.discovery\|lspConverter" packages/salesforcedx-vscode-apex` → 0 hits.
- `node --check` / tsc build green for apex + dependent pkgs (replay-debugger, apex-debugger, apex-oas compile against `ApexVSCodeApi`).
- jest green: `salesforcedx-vscode-apex` (deleted suite gone; LCM test unaffected — no getApexTests refs).
- apex-testing `apex-test-discovery` span (API path) untouched — not e2e-verified here; covered by existing apex-testing jest (`apexTestTreeService.test.ts`).
- Test Explorer refresh firing API discovery — **e2e-covered** on branch; skip manual.
