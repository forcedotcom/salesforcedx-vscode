# W-23489769 — Bump actions/setup-node to v7 across workflows

## Context

- 54 `actions/setup-node` uses in `.github/workflows/`: 53 `@v6` across 33 files + 1 `@v4` in `apexConsumerNuts.yml` (L37). No uses in `.github/actions/*/action.yml`. No `.yaml` workflows.
- v7.0.0 (2026-07-14): cache-primary-key/cache-matched-key outputs added, ESM internals, `@actions/cache` → 6.1.0 (ESM-only major since 6.0.0; cache-key format and `hashFiles` digest unchanged, `ACTIONS_CACHE_MODE` unset repo-wide → inert here), dummy `NODE_AUTH_TOKEN` export removed, `mirrorToken` only used in `getManifest` when provided. No input removed/renamed; no output removed. Runtime `node24` in both v6.0.0 and v7.0.0 → no runner-image requirement change.
- Repo's inputs all still valid in v7: `node-version` (`'22.22.3'` x28, `${{ vars.NODE_VERSION || 'lts/*' }}` x16, `lts/*` x10), `cache: npm` x40, `cache-dependency-path: '**/package-lock.json'` x1. No `always-auth`, `registry-url`, `scope`, `mirror`, `mirror-token` anywhere → the two v7 bug fixes are no-ops here.
- No workflow step reads a setup-node output (`steps.*.outputs.cache-hit` etc. → 0 hits) → new outputs additive only.
- `.github/` is prettier-ignored (`.prettierignore:7`) → no formatter constraint. No dependabot `github-actions` ecosystem → this bump is manual by design.
- Precedent: `.claude/plans/W-23201856.md` did the same v4→v6 sweep as one mechanical pin-bump commit.
- No ADR governs GHA pins.
- `apexConsumerNuts.yml` still on v4 is unintentional drift (missed by the v4→v6 sweep); goes straight to v7 in the same commit.

## Phases

### Phase 1 — bump every setup-node pin to `@v7`

- Replace `actions/setup-node@v6` → `actions/setup-node@v7` (53 uses, 33 files) and `actions/setup-node@v4` → `actions/setup-node@v7` (`apexConsumerNuts.yml:37`).
- Pin line only. No `with:` key added, removed, or reordered. No other action bumped (`actions/checkout@v4` etc. untouched).

commitMessage: `chore(deps): bump actions/setup-node to v7 across workflows - W-23489769`

## Skills to apply

- `.claude/skills/verification/SKILL.md` — workflow changes require `npm run check:actions`.
- `.claude/skills/concise/SKILL.md` — plan/PR wording.

## Verification

No automated tests cover workflow YAML pins (none exist on branch); `check:actions` is the only tooling gate.

1. `grep -rc 'setup-node@v7' .github/workflows/ | grep -v ':0'` → 34 files, 54 total uses; `grep -rn 'setup-node@v[0-6]' .github/` → empty.
2. Pin-only diff: `git diff -U0 -- .github | grep '^[-+][^-+]'` → every line contains `uses:` + `actions/setup-node`; 54 removals, 54 additions; `git diff --stat` = 34 files.
3. `npm run check:actions` passes (schema-validates all `.github/workflows/*.yml`).
4. Runtime: push branch; `validatePR.yml` + `unitTestsLinux.yml` + `unitTestsWindows.yml` green — proves v7 resolves, installs each `node-version` form, npm cache restore/save works on ubuntu and windows runners.
5. `apexConsumerNuts.yml` (v4→v7, `lts/*`, no `vars.NODE_VERSION`) exercised: dispatch that workflow once, setup-node step succeeds.
