# W-23348778 — Drop core extensionDependency + SalesforceVSCodeCoreApi + coreExtensionUtils (apex)

## Context

- Final WI (5) of series; WIs 2-4 removed all `core.exports` usage in apex.
- Scope: `packages/salesforcedx-vscode-apex` only.
- Remaining core coupling in apex:
  - `src/coreExtensionUtils.ts` — `getVscodeCoreExtension()`, imports `SalesforceVSCodeCoreApi` from `salesforcedx-vscode-core`.
  - `src/index.ts:17` import + `:44` `yield* Effect.promise(() => getVscodeCoreExtension())` — only activates core; no exports consumed.
  - `package.json` `extensionDependencies[]` includes `salesforce.salesforcedx-vscode-core`.
- Only `getVscodeCoreExtension` caller = apex `index.ts` (no test refs). `grep -rn getVscodeCoreExtension packages/salesforcedx-vscode-apex/{src,test}` = 2 hits, both `index.ts` (:17 import, :44 call); definition in `coreExtensionUtils.ts`.
- Namespace CodeLens (`getNamespaces` :261) already reads org namespace via services `ConnectionService` (:262-269), not core — migrated in W-23348771 (`d15c763241`). Unaffected by this WI; `provideCodeLenses` rewriting depends only on services; no dangling import in `languageServer.ts`. Namespaced-org CodeLens e2e coverage owned by W-23348771, out of scope here.
- Out of scope: `apex-debugger` has own `src/utils/coreExtensionUtils.ts` + own core dependency — not this WI's package.
- Keep `salesforce.salesforcedx-vscode-services` dependency.
- `SalesforceVSCodeCoreApi` type still defined+exported in core `index.ts` — leave (other consumers may exist; WI only drops apex's import).

## Phases

### Phase 1 — Drop core activation call + coreExtensionUtils
- `src/index.ts`: remove line 17 import + line 44 `getVscodeCoreExtension()` call.
- Delete `src/coreExtensionUtils.ts`.
- Verify no dangling refs.
- Commit: `refactor(apex): drop core extension activation + coreExtensionUtils - W-23348778`

### Phase 2 — Drop core extensionDependency
- `package.json`: remove `"salesforce.salesforcedx-vscode-core"` from `extensionDependencies`; keep services entry.
- Commit: `refactor(apex): drop core extensionDependency - W-23348778`

## Skills to apply
- typescript
- core-extension-api (context on core API surface / consumers)
- services-extension-consumption (confirm services stays)
- packageJson (extensionDependencies edit)
- verification
- concise (plan/doc edits)

## Verification
- `grep -rn getVscodeCoreExtension packages/salesforcedx-vscode-apex` → 0 hits (whole package src+test, confirms no dangling import in `languageServer.ts` or elsewhere, not just `index.ts`).
- `grep -rn SalesforceVSCodeCoreApi packages/salesforcedx-vscode-apex` → 0 hits.
- `grep -rn "from 'salesforcedx-vscode-core'" packages/salesforcedx-vscode-apex` → 0 hits.
- `find packages/salesforcedx-vscode-apex -name coreExtensionUtils.ts` → none.
- apex `package.json` extensionDependencies = services only.
- Type-check/compile apex package (tsc/wireit) — passes, no missing import.
- apex unit tests pass (incl. `test/jest/languageServer.test.ts` — unchanged; already uses services runtime mock, no core stub).
- Namespace CodeLens path unchanged (see Context; migrated in W-23348771).
- e2e-covered: extension activation w/o core dependency (activation flow) — leave to branch e2e.
