# W-23451860 — Move SfCommandlet from utils-vscode into apex-debugger

## Context

- `SfCommandlet<T>` (`packages/salesforcedx-utils-vscode/src/commands/sfCommandlet.ts`) exported from utils-vscode barrel (`src/index.ts:13`) AND re-exported via core API (`SalesforceVSCodeCoreApi.SfCommandlet`).
- Sole consumer: `salesforcedx-vscode-apex-debugger` — `debuggerStop.ts` (2 uses, L114/L130) + `isvdebugging/bootstrapCmd.ts` (1 use, L489), all via `getSfCommandlet()` in `src/utils/coreExtensionUtils.ts` (core API path, not barrel). Verified repo-wide + /external-consumers.
- **apex-replay-debugger is NOT a consumer** (re-verified 2026-07): commit `09f51babf5` (W-23366429) migrated ARD off SfCommandlet to Effect (`sfProjectPreconditionChecker` from `@salesforce/effect-ext-utils` + direct `anonApexDebug()`). `grep -rn "SfCommandlet" packages/salesforcedx-vscode-apex-replay-debugger` → 0 hits. Prior advocate finding citing `anonApexDebug.ts:137` / `launchApexReplayDebuggerWithCurrentFile.ts:72,86` referenced pre-migration code (those lines no longer exist; files are 111/73 lines). No live barrel consumer remains outside apex-debugger's core-API path.
- Goal: local-own the class in apex-debugger; drop cross-ext fetch; shrink core frozen API (ADR 0006 — safe: move removes last consumer same change).
- Ownership temporary; follow-up WI converts to Effect services pattern (ADR 0005) + deletes class.
- Gotchas:
  - Barrel (`utils-vscode/src/index.ts`) exports `SfCommandlet` (L13) but NOT `EmptyPostChecker` nor `CommandletExecutor` type. Moved file must inline both (WI names CommandletExecutor; EmptyPostChecker same problem). Import `notificationService`, `ParametersGatherer`, `PreconditionChecker`, `PostconditionChecker`, `ContinueResponse` from barrel (all exported).
  - Keep `getSfCommandletExecutorClass()` + core `SfCommandletExecutor` (still used; follow-up WI).
  - `apex-debugger/src/utils/` dir does not exist yet (coreExtensionUtils sits in `src/utils/` — it does exist). Confirm: `src/utils/` exists (holds coreExtensionUtils.ts).

## Phases

### Phase 1 — add local SfCommandlet, rewire consumers, drop getSfCommandlet()
- New `packages/salesforcedx-vscode-apex-debugger/src/utils/sfCommandlet.ts`:
  - class `SfCommandlet<T>` copied from utils-vscode
  - inline `type CommandletExecutor<T>` + `class EmptyPostChecker`
  - import `notificationService`, types `ParametersGatherer`/`PreconditionChecker`/`PostconditionChecker`/`ContinueResponse`/`CancelResponse` from `@salesforce/salesforcedx-utils-vscode`; `vscode` for `Event`
- `debuggerStop.ts`: import `SfCommandlet` from `../utils/sfCommandlet`; drop `getSfCommandlet` from coreExtensionUtils import; replace 2 `await getSfCommandlet()` w/ direct `new SfCommandlet(...)`
- `bootstrapCmd.ts`: same — import local; drop `getSfCommandlet`; replace 1 use
- `coreExtensionUtils.ts`: remove `getSfCommandlet` export
- `test/jest/commands/isvdebugging/bootstrapCmd.test.ts`: drop `SfCommandlet` from mocked `exports` (no longer fetched)
- commit: `refactor(apex-debugger): own SfCommandlet locally, drop getSfCommandlet`

### Phase 2 — remove now-dead exports/source
- **GATE (blocking): must not proceed until grep guard is run and clean.** Run repo-wide (all packages incl. apex-replay-debugger, apex, org):
  - `grep -rn "SfCommandlet\b" packages --include="*.ts"` → only survivors permitted: core `SfCommandletExecutor` chain, the new local `apex-debugger/src/utils/sfCommandlet.ts`, and comment-only mentions (`effect-ext-utils/src/preconditionCheckers.ts`, `org/*` comments).
  - `grep -rn "from '@salesforce/salesforcedx-utils-vscode'" packages --include="*.ts"` filtered for `SfCommandlet` → must be 0. If ANY package still imports `SfCommandlet` from the utils-vscode barrel, STOP: either fold that package into this migration or leave utils-vscode source/export intact. Do not delete on the strength of the Phase-1 inventory alone.
- `salesforcedx-vscode-core/src/index.ts`: remove `SfCommandlet` — import (L11), api obj (L58), `SalesforceVSCodeCoreApi` type (L209)
- `salesforcedx-utils-vscode/src/index.ts`: remove barrel export (L13)
- delete `salesforcedx-utils-vscode/src/commands/sfCommandlet.ts`
- commit: `refactor(core): remove frozen-API SfCommandlet export (last consumer gone)`

## Skills to apply
- concise (this plan)
- core-extension-api (ADR 0006 frozen-API removal reasoning)
- typescript / ts1261-filename-casing (new file naming)
- i18n-messages — n/a (no message changes)

## Verification
- `node`-level compile: build apex-debugger, core, utils-vscode (wireit `compile`) — catches broken imports/exports
- jest: `bootstrapCmd.test.ts` (mock change), `activation.test.ts` (still mocks coreExtensionUtils; getSfCommandlet removal must not break requireActual spread)
- lint (eslint) new + edited files
- grep guard (Phase-2 blocking gate — see Phase 2): repo-wide, no `getSfCommandlet(` anywhere; no `SfCommandlet` imported from utils-vscode barrel or core API by ANY package (explicitly re-checked against apex-replay-debugger, apex, org — not just apex-debugger). Only permitted survivors: new local `apex-debugger/src/utils/sfCommandlet.ts` and core `SfCommandletExecutor` chain (untouched). This gate overrides the Phase-1 consumer inventory: if grep is not clean, the inventory was wrong — revise scope, do not delete.
- e2e: no playwright/vscode-integration test exercises debuggerStop or isvDebugBootstrap (verified: apex-debugger test dir holds only jest unit tests — `activation.test.ts`, `bootstrapCmd.test.ts`; no `test/integration` or `test/vscode-integration` dirs). Runtime behavior unchanged (same class, same constructor args, only import source moves), so unit + compile is the appropriate coverage. Confirm build+jest green across all three packages before Phase 2 delete.
