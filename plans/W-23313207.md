# W-23313207 — Shrink apex bundle via esbuild ESM resolution of effect

## Context

- Apply effect ESM conditions override to `salesforcedx-vscode-apex` node (desktop) esbuild build.
- Pattern from W-23293744 (org-browser POC, #7675) + siblings W-23313202/03/04 (apex-debugger/log/oas).
- Shared infra already on develop — do NOT recreate:
  - `scripts/bundling/effect.mjs` exports `effectEsmConditions = { conditions: ['import','module','default'] }`.
  - `docs/adr/0021-effect-esm-condition-override-scope.md`.
  - apex `.vscodeignore:16` already excludes `dist/*-metafile.json`.
- apex deps `effect` (package.json:36); already writes `dist/node-metafile.json`.
- Target: `packages/salesforcedx-vscode-apex/esbuild.config.mjs` — node-only config (NO browser build present). WI = node build only anyway.
- Output stays CJS (from `nodeConfig`). Main shrink lever: effect submodule resolution `dist/cjs/*` -> `dist/esm/*` tree-shakes unused (e.g. fast-check via Schema).
- BUT `conditions` re-resolves EVERY dep with a dual export map, not just effect (ADR 0021 blast-radius warning). Verified apex dep export maps: `vscode-uri` has a genuine `import`(`lib/esm/index.mjs`)/`require`(`lib/umd/index.js`) split — a DIFFERENT module, re-resolves under the override. Others (`@salesforce/apex-node`, `@salesforce/vscode-i18n`, `@salesforce/effect-ext-utils`, `@salesforce/salesforcedx-utils`, `@salesforce/salesforcedx-utils-vscode`, `vscode-languageclient`, `find-java-home`) are `default`-only / `main`-only / no `exports` → resolve identically with or without the override. So blast radius = effect + vscode-uri only. vscode-uri feeds `URI.parse`/`URI.file` on the hot LSP path (`languageServer.ts:139` `protocol2CodeConverter`, lspConverter), so its re-resolve MUST be verified, not assumed inert.

## Phases

### Phase 1 — resolve effect ESM in apex node build

- Import `effectEsmConditions` from `../../scripts/bundling/effect.mjs`.
- Spread `...effectEsmConditions` into `nodeBuild` config (after `...nodeConfig`).
- Build; capture before/after `dist/index.js` bytes + % from `dist/node-metafile.json`.
- Blast-radius diff (before/after metafiles): confirm effect resolves `dist/esm/*` (was `dist/cjs/*`); confirm `vscode-uri` re-resolves to `lib/esm/index.mjs` (was `lib/umd/index.js`); record its `bytesInOutput` delta. Confirm no other dep's resolved input path changed (matches export-map audit in Context).
- If pattern `SG.\w` hits minified output: add `effectSecretScannerWorkaroundPlugin` (see apex-log) to node `plugins`. Else: no change.
- commit: `perf(apex): resolve effect ESM to shrink bundle - W-23313207`
- files: `packages/salesforcedx-vscode-apex/esbuild.config.mjs`

## Skills

- concise — plan + any md
- typescript — build tooling
- wireit — build/package via `vscode:bundle` / `vscode:package`
- pr-draft — PR body w/ before/after node `dist` size + % (match W-23293744 format)
- packageJson — only if package.json touched (not expected)
- playwright-e2e — apex `test:desktop` suite

## Verification

- Effect ESM swap: runtime resolution change, not just byte count (ADR 0021 — `conditions` alters resolution for every dep with a dual export map, here effect + vscode-uri).
- Broken ESM entry surfaces only at activation/runtime; size checks miss it.
- apex uses Effect/Schema/Schedule/ManagedRuntime/Context across LSP activation: src/index.ts, languageServer.ts, languageServerOrphanHandler.ts, services/runtime.ts.

### Build/bundle (non-e2e)

- `npm run vscode:bundle -w salesforcedx-vscode-apex` passes. [non-e2e]
- Before/after `dist/index.js` bytes + % from `dist/node-metafile.json` — record for PR. [non-e2e]
- Metafile: effect resolves `dist/esm/*` not `dist/cjs/*`. [non-e2e]
- Per-dep blast-radius (required before claiming shrink): from before/after metafile `inputs`, `vscode-uri` re-resolves `lib/umd/index.js` -> `lib/esm/index.mjs` (record `bytesInOutput` delta — may grow; net still a win via fast-check drop); confirm no error (`require-resolve-not-external`, broken ESM entry) and no other apex dep's resolved input path changed vs the Context export-map audit. [non-e2e]
- Output format still CJS (grep bundle/metafile). [non-e2e]
- `npm run vscode:package -w salesforcedx-vscode-apex` succeeds — no Sendgrid secret-scanner false positive on node output. [non-e2e]

### e2e (required before PR)

- `npm run test:desktop -w salesforcedx-vscode-apex` — required. Exercises re-resolved effect submodules via LSP activation + orphan handler (Schedule/Schema) + ManagedRuntime. [e2e]
- Also confirms re-resolved `vscode-uri` (`URI.parse`/`URI.file` on the LSP path): `apexLsp.desktop.spec.ts` (go-to-definition) + `apexLspHover.desktop.spec.ts` (hover) drive `protocol2CodeConverter` (`languageServer.ts:139`). Existing specs — cheap coverage of the URI re-resolve, no new authoring. [e2e]
- No web/browser build in apex config — no `test:web` (unlike apex-log). No new specs to author.
