# W-24250190

## Context

Remove the Wednesday scheduled trigger from `.github/workflows/build-github-release.yml`. Keep manual `workflow_dispatch` execution available. Update release documentation and the dependent pre-release workflow comments for the manual stable build. Keep release jobs and inputs unchanged.

## Phases

1. Remove the `schedule` block and its schedule-specific comments from `.github/workflows/build-github-release.yml`.
2. Update `.github/workflows/promote-to-prerelease.yml`, `docs/release-workflow-architecture.md`, `.claude/skills/release/SKILL.md`, and `.claude/skills/changelog/SKILL.md` to describe the manual stable build after Wednesday's pre-release promotion. Require operators to select the previous Wednesday's validated nightly with `prereleaseTag`.
   - Commit: `fix: remove Wednesday GitHub release cron`

## Skills to apply

- `concise`: keep the workflow edit and commit focused.
- `verification`: verify YAML formatting, trigger configuration, and diff scope.

## Verification

- Run `npx prettier --check .github/workflows/build-github-release.yml .github/workflows/promote-to-prerelease.yml`.
- Run `WIREIT_CACHE=none npm run check:actions`.
- Confirm `.github/workflows/build-github-release.yml` has `workflow_dispatch` and no `schedule` or `0 7 * * 3` trigger.
- Run `git diff --check`.
- Confirm the implementation diff changes only `.github/workflows/build-github-release.yml`, `.github/workflows/promote-to-prerelease.yml`, `docs/release-workflow-architecture.md`, `.claude/skills/release/SKILL.md`, `.claude/skills/changelog/SKILL.md`, and `.claude/plans/W-24250190.md`.
