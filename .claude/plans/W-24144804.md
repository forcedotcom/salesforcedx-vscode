# W-24144804

## Context

Standardize GitHub Actions Node.js versions on `${{ vars.NODE_VERSION || 'lts/*' }}`. Workflow steps pass that value to composite actions because composite action metadata cannot read `vars` directly.

Files to change:

- `.github/actions/setup-e2e/action.yml`
- `.github/workflows/*E2E.yml` files that invoke `setup-e2e`
- `.github/workflows/apexConsumerNuts.yml`
- `.github/workflows/closeStaleIssues.yml`
- `.github/workflows/codeBuilderE2E.yml`
- `.github/workflows/copyrightYearBump.yml`
- `.github/workflows/generateXsdForMetadataTypes.yml`
- `.github/workflows/validateNewIssues.yml`
- `.github/workflows/validateUpdatedIssues.yml`

## Phases

1. Replace each hard-coded workflow `node-version` value with `${{ vars.NODE_VERSION || 'lts/*' }}`.
2. Add a `node-version` input to `setup-e2e`; pass the standardized value from each calling workflow.
   - Commit: `chore: standardize Actions Node.js version`

## Skills to apply

- `concise`: keep the YAML-only change minimal.
- `verification`: validate changed GitHub Actions definitions.

## Verification

1. Confirm every workflow `node-version` is `${{ vars.NODE_VERSION || 'lts/*' }}` and every `setup-e2e` call passes it.
2. Run `npm run check:actions` from the repository root.
