# W-23251304: Add auto-terminate setting for orphaned Apex LS processes

## Context

- Target: `packages/salesforcedx-vscode-apex/src/languageServerOrphanHandler.ts`
- New boolean setting `salesforcedx-vscode-apex.autoTerminateOrphanedProcesses` (default `false`)
- If `true`: kill orphans silently on activation, no notification
- If `false`: show existing toast + new "Always Auto-Terminate" button
- "Always Auto-Terminate" → modal: (1) enables setting, (2) kills now, (3) can disable later; Confirm = enable + kill; Cancel = no-op

## Phases

### Phase 1: Register setting in package.json + NLS

- Add `salesforcedx-vscode-apex.autoTerminateOrphanedProcesses` boolean config (default `false`) to `package.json` contributes.configuration
- Add NLS key `auto_terminate_orphaned_processes_description` to `package.nls.json` + `package.nls.ja.json`
- Use `%key%` placeholder in package.json per eslint rule

Files:
- `packages/salesforcedx-vscode-apex/package.json`
- `packages/salesforcedx-vscode-apex/package.nls.json`
- `packages/salesforcedx-vscode-apex/package.nls.ja.json`

Commit: `feat(apex): register autoTerminateOrphanedProcesses setting - W-23251304`

### Phase 2: Add i18n messages

- Add messages to `i18n.ts` + `i18n.ja.ts`:
  - `always_auto_terminate` (button label)
  - `auto_terminate_confirm_modal` (modal body: enables setting, kills now, disable later via Settings)
  - `confirm` (button for modal; reuse `yes` if suitable -- check first)

Note: no new `settings.ts` accessors; read/write inline via `api.services.SettingsService` — already provides `getValue<T>` and `setValue<T>` with typed `SettingsError` and `ConfigurationTarget.Global`. Avoids re-implementing existing service.

Files:
- `packages/salesforcedx-vscode-apex/src/messages/i18n.ts`
- `packages/salesforcedx-vscode-apex/src/messages/i18n.ja.ts`

Commit: `feat(apex): add i18n messages for auto-terminate - W-23251304`

### Phase 3: Implement auto-terminate logic in orphan handler

- In `checkAndResolveOrphanedLanguageServers`:
  - Read setting via `(yield* api.services.SettingsService).getValue(...)`
  - If `true`, skip prompt, kill all orphans directly, return
  - On `SettingsError`: treat as `false`, fall through
- In `promptOnce`:
  - Add 3rd button `nls.localize('always_auto_terminate')`
  - Handle new button choice: show modal via `showWarningMessage` with `{ modal: true, detail: nls.localize('auto_terminate_confirm_modal') }` explaining consequences; on Confirm -> persist setting via `(yield* api.services.SettingsService).setValue('salesforcedx-vscode-apex', 'autoTerminateOrphanedProcesses', true)` + return `true` (kill); on Cancel -> return `false`
  - Catch `SettingsError` from `setValue` on the span (annotateRootSpan) so failures are observable, not silently swallowed
- No new exports; setting access inline via Effect pipeline

Files:
- `packages/salesforcedx-vscode-apex/src/languageServerOrphanHandler.ts`

Commit: `feat(apex): implement auto-terminate and Always Auto-Terminate button - W-23251304`

### Phase 4: Unit tests

- Add third discriminator queue (`autoTerminateConfirm`) to `setWarningChoices` + `Choices` type (keying on modal-unique `detail` substring; avoids response-queue corruption)
  - Routing: `'Terminate them?' → confirmQueue` / `detail includes auto_terminate_confirm_modal → autoTerminateConfirmQueue` / else `queue`
- Add `SettingsService` stub to `makeApi` returning a mock that tracks `getValue` / `setValue` calls
- Test: setting `true` + orphans found -> kills without prompt
- Test: setting `false` + user clicks "Always Auto-Terminate" + confirms modal -> `setValue` called with `true` + kills
- Test: setting `false` + user clicks "Always Auto-Terminate" + cancels modal -> no kill, `setValue` not called
- Test: setting `false` + existing flows unchanged (terminate, show, dismiss)
- Test: `SettingsService.setValue` failure -> span annotation recorded, kill still proceeds (graceful degradation)

Files:
- `packages/salesforcedx-vscode-apex/test/jest/languageServerOrphanHandler.test.ts`

Commit: `test(apex): add unit tests for auto-terminate orphan setting - W-23251304`

## Skills

- i18n-messages
- vscode-window-messages
- packageJson
- effect-best-practices
- typescript
- services-extension-consumption

## Verification

- `npm run lint` in `packages/salesforcedx-vscode-apex` (catches `no-vscode-message-literals`, `package-json-i18n-descriptions`)
- `npm run compile -w packages/salesforcedx-vscode-apex` (catches TS errors in new Effect pipelines and MissingSettingsError catchTag)
- `npm run test` in `packages/salesforcedx-vscode-apex` (wireit-backed jest tests pass)
- `npm run check:dupes` (jscpd report for new auto-terminate block)
- Manual: confirm setting appears in Settings UI under "Salesforce Apex Configuration"
- e2e-covered: none (orphan handler is background + prompt-based; no playwright spec exists)
