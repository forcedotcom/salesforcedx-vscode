# W-23711830

## Context

Refactor the trace-flags CRUD E2E confirmation in `packages/salesforcedx-vscode-apex-log/test/playwright/specs/traceFlagsCrud.headless.spec.ts` to select the intended quick-input option by accessible name. This replaces position-dependent `selectFirstQuickInputOption` usage with the existing `selectQuickInputOption` helper.

## Phases

1. Update the trace-flags CRUD spec import and confirmation call to use `selectQuickInputOption` with the expected option label.
   Commit: `test: select trace flag confirmation by name`

## Skills to apply

- `playwright-e2e`: follow shared quick-input helper and locator conventions.
- `typescript`: keep imports and helper usage type-safe and consistent.
- `verification`: run focused checks for the changed E2E spec.
- `concise`: keep the change and documentation minimal.

## Verification

- Run the trace-flags CRUD spec on web with retries disabled: `WIREIT_CACHE=none npm run test:web -w salesforcedx-vscode-apex-log -- --retries 0 test/playwright/specs/traceFlagsCrud.headless.spec.ts`.
- Run the trace-flags CRUD spec on desktop with retries disabled: `WIREIT_CACHE=none npm run test:desktop -w salesforcedx-vscode-apex-log -- --retries 0 test/playwright/specs/traceFlagsCrud.headless.spec.ts`.
- Confirm repository stop-hook compile, lint, test, bundle, and knip checks pass.
