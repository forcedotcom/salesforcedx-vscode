# W-23506236

## Context

Replace hand-written guard branches that return or yield `Effect.fail` in `salesforcedx-vscode-services` with `Effect.filterOrFail`. Preserve each predicate, success value, and existing error. This makes guard-and-fail behavior use Effect's dedicated combinator without changing service behavior.

## Phases

### 1. Convert guard-then-fail sites

- Update guards in:
  - `packages/salesforcedx-vscode-services/src/core/connectionService.ts`
  - `packages/salesforcedx-vscode-services/src/orgCatalog/orgCatalogDocuments.ts`
  - `packages/salesforcedx-vscode-services/src/orgCatalog/orgCatalogRemoteSource.ts`
  - `packages/salesforcedx-vscode-services/src/orgCatalog/orgCatalogTreeProjection.ts`
  - `packages/salesforcedx-vscode-services/src/orgCatalog/orgMetadataCatalog.ts`
  - `packages/salesforcedx-vscode-services/src/orgCatalog/orgMetadataDocumentProvider.ts`
  - `packages/salesforcedx-vscode-services/src/virtualFsProvider/fileSystemProvider.ts`
  - `packages/salesforcedx-vscode-services/src/vscode/extensionContext.ts`
  - `packages/salesforcedx-vscode-services/src/vscode/prompts/promptService.ts`
  - `packages/salesforcedx-vscode-services/src/vscode/settingsService.ts`
- Keep current error types, messages, and predicates.
- Use predicate narrowing from `effect/Predicate` where the guarded type includes `undefined`.
- Commit: `refactor: use Effect.filterOrFail for service guards`

## Skills to apply

- `effect-best-practices`: Effect combinators, predicates, typed errors, diagnostics
- `typescript`: imports, inference, narrowing, formatting
- `verification`: package checks and end-to-end verification

## Verification

- Review the diff: only equivalent guard composition and required imports changed.
- Run the repository stop-hook checks: compile, lint, Effect LS diagnostics, tests, bundle, and knip.
- Run `npm run test:web -w salesforcedx-vscode-services -- --retries 0`.
- Run `npm run test:desktop -w salesforcedx-vscode-services -- --retries 0`.
- Run `npm run check:dupes`; inspect `jscpd-report` for changed-code findings.
