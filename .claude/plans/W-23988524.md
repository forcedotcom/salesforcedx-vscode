# W-23988524

## Context

Delete the deprecated release-branch workflow chain after the replacement release process has produced 2 successful stable-mode builds and the mid-September wait has elapsed. Removing all 4 entrypoints and callers prevents use of the old release-branch flow.

## Phases

1. Record qualifying replacement workflow runs and release tags in `docs/release-workflow-architecture.md`.
2. Delete the full legacy chain:
   - `.github/workflows/createReleaseBranch.yml`
   - `.github/workflows/prerelease.yml`
   - `.github/workflows/testBuildAndRelease.yml`
   - `.github/workflows/mergeReleaseBranch.yml`
3. Remove active operator instructions for the deleted path while retaining the labeled historical timeline.
   - Commit: `chore: delete deprecated release branch workflows`

## Skills to apply

- `concise`: keep the change and documentation minimal.
- `verification`: validate the GitHub Actions change.

## Verification

1. Confirm all 4 deleted workflow files no longer exist.
2. Search `.github/workflows`, `contributing/publishing.md`, and `.claude/skills/release/SKILL.md` for deleted workflow names; expect no matches.
3. Run `npm run check:actions` from the repository root.
4. Run `git diff --check`.
