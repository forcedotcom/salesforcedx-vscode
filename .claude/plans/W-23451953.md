# W-23451953: Delete WorkspaceContextUtil

## Context

- GUS subject: `7 Delete WorkspaceContextUtil [ai-auto] [repo:forcedotcom/salesforcedx-vscode]`.
- `packages/salesforcedx-utils-vscode/src/context/workspaceContextUtil.ts` still owns the obsolete singleton, target-org file watcher, connection facade, cached org fields, and `OrgUserInfo`/`OrgShape` types.
- Current package source has no runtime `WorkspaceContextUtil` consumer. Remaining references are its public barrel export, its Jest suite, 2 imports of `OrgShape`, and a Playwright allowlist comment.
- Delete the class and its test without a compatibility facade. Preserve the independently used `OrgUserInfo` and `OrgShape` type exports by moving them to files matching their remaining responsibilities.

## Phases

### Phase 1: Delete WorkspaceContextUtil

- Add `packages/salesforcedx-utils-vscode/src/context/orgUserInfo.ts` containing the existing `OrgUserInfo` type.
- Move `OrgShape` into `packages/salesforcedx-utils-vscode/src/context/workspaceOrgShape.ts`; update `packages/salesforcedx-utils-vscode/src/telemetry/reporters/telemetryReporterConfig.ts` to import it there.
- Update `packages/salesforcedx-utils-vscode/src/index.ts` to retain `OrgUserInfo`, `OrgShape`, `OrgShapeInfo`, and `shapeFrom` exports while removing `WorkspaceContextUtil`.
- Delete `packages/salesforcedx-utils-vscode/src/context/workspaceContextUtil.ts` and `packages/salesforcedx-utils-vscode/test/jest/context/workspaceContextUtil.test.ts`.
- Update the stale `WorkspaceContextUtil.getConnection` attribution in `packages/playwright-vscode-ext/src/utils/nonCriticalErrorPatterns.ts`; retain the allowlisted text because other package messages still contain it.
- Commit: `refactor(utils): delete WorkspaceContextUtil - W-23451953`

## Skills to apply

- `concise`: compact tracked plan.
- `typescript`: type-only imports, file naming, and public type exports.
- `external-consumers`: explicit removal of the exported class without accidentally removing the still-used exported types.
- `verification`: repository checks after deletion.

## Verification

- Confirm `WorkspaceContextUtil`, `workspaceContextUtil`, and `WORKSPACE_CONTEXT_ORG_ID_ERROR` are absent from `packages/*/src` and `packages/*/test`; confirm no source import targets the deleted file.
- Run `npm run test -w @salesforce/salesforcedx-utils-vscode`.
- Run the repository stop-hook checks; require compile, lint, Effect diagnostics, tests, bundle, and knip to pass.
- Run `npm run check:dupes`; inspect `jscpd-report` for changed-code duplication.
