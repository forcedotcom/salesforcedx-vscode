# W-24183843 — Thin LanguageClientManager; enable class-methods-use-this on vscode-apex

## Context

- `packages/salesforcedx-vscode-apex/src/languageUtils/languageClientManager.ts` — `LanguageClientManager` still holds instance methods that do not use instance state. Module helpers `promptForRestartOption` and `removeApexDbEffect` already sit above the class.
- `class-methods-use-this` is `error` on Effect packages (`eslint.config.mjs` ~781) and on soql / soql-common / soql-model (~887). `packages/salesforcedx-vscode-apex/**/*.ts` is in neither block.
- Turn the rule on for vscode-apex by moving those stateless methods to module functions. Do not add vscode-apex to the soql block (that block also sets `local/no-explicit-effect-return-type` and related rules).

## Phases

### 1. Thin LanguageClientManager

**Commit:** `refactor: thin LanguageClientManager stateless methods`

- In `languageClientManager.ts`, move instance methods that do not read or write instance fields to module-level functions in that file. Match the existing helper style (`Effect.fn` where the body is already Effect).
- Leave the singleton, client / status / status-bar fields, and methods that use them on the class.
- Point in-file and in-package call sites at the functions. No new re-exports from `languageUtils/index.ts`.
- Test edits only when an import or call name changes (`test/jest/languageUtils/languageClientManager.test.ts`).

### 2. Enable the rule on vscode-apex

**Commit:** `chore: enable class-methods-use-this on vscode-apex`

- `eslint.config.mjs`: new override, files `packages/salesforcedx-vscode-apex/**/*.ts`, rules only `'class-methods-use-this': 'error'`.
- Any other hit in that package: same move to a module function in the file that owns the method.

## Skills to apply

- `typescript` — module functions, no new barrels, preserve comments
- `effect-best-practices` — existing `Effect.fn` helpers in `languageClientManager.ts`
- `verification` — package lint must pass with the new override

## Verification

- `npm run compile -w salesforcedx-vscode-apex`
- `npm run lint -w salesforcedx-vscode-apex` — `class-methods-use-this` clean for `packages/salesforcedx-vscode-apex/**/*.ts`
- `npm run test -w salesforcedx-vscode-apex`
- `npm run vscode:bundle -w salesforcedx-vscode-apex`
- effect language service diagnostics for `packages/salesforcedx-vscode-apex/tsconfig.json`
- `npm run check:dupes`
- repo knip (stop hook)
