# W-23094908 — Enable unicorn/no-loop-iterable-mutation

## Context
- WI: enable `unicorn/no-loop-iterable-mutation` (forbids mutating iterable while `for...of` consumes it)
- Dep W-23094906 merged (commit eeeef4bc6, HEAD)
- Config: `eslint.config.mjs`; unicorn rules alphabetized, lines 167-226
- unicorn plugin v68.0.0 — rule present (`node_modules/eslint-plugin-unicorn/rules/no-loop-iterable-mutation.js`)
- Probe result: rule enabled → 0 violations repo-wide. Baseline 226 errors/8468 problems unchanged with rule on. Pure config flip, no code fixes.
- Requires `packages/eslint-local-rules` compiled before lint runs (else config import fails)

## Phases

### Phase 1 — enable rule
- add `'unicorn/no-loop-iterable-mutation': 'error',` after line 185 (`no-invalid-character-comparison`), before `no-single-promise-in-promise-methods`
- file: `eslint.config.mjs`
- commit: `chore(eslint): enable unicorn/no-loop-iterable-mutation - W-23094908`

## Skills
- packageJson (if any dep touch — none expected)
- typescript
- verification

## Verification
- `cd packages/eslint-local-rules && npm run compile` (prereq)
- `NODE_OPTIONS=--max-old-space-size=8192 npx eslint .` → no `no-loop-iterable-mutation` lines; error count stays 226 (pre-existing, unrelated)
- not e2e-covered (lint config change)
