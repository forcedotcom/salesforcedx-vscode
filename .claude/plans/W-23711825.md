# W-23711825: Select the default Apex class template by name

## Approach

In the Apex Log create-class Playwright spec, replace first-row waiting plus keyboard Enter with the shared `selectQuickInputOption(page, 'DefaultApexClass')`. Keep the class-name input, output-directory selection, and generated-file assertions unchanged.

## Evidence

- `packages/salesforcedx-vscode-apex-log/test/playwright/specs/apexGenerateClass.headless.spec.ts:9-23,50-53` imports and calls `selectQuickInputOption(page, 'DefaultApexClass')` in the template-selection step.
- `packages/salesforcedx-vscode-apex-log/src/commands/createApexClass.ts:16,28-48` defines `DefaultApexClass` as one named QuickPick option among 3 templates.
- `packages/playwright-vscode-ext/src/utils/helpers.ts:199-233` defines `selectQuickInputOption`, which waits for the active QuickPick and clicks an option found by accessible name.
- `packages/playwright-vscode-ext/src/index.ts:7-26` exports the shared helper for package Playwright specs.
- `packages/salesforcedx-vscode-apex-log/test/playwright/specs/apexGenerateClass.headless.spec.ts:55-83` retains the separate class-name, output-directory, and generated-file flows.

## Phases

### Phase 1: Select `DefaultApexClass` explicitly

- Update the `@salesforce/playwright-vscode-ext` imports in `packages/salesforcedx-vscode-apex-log/test/playwright/specs/apexGenerateClass.headless.spec.ts`: add `selectQuickInputOption`; retain `waitForQuickInputFirstOption` for output-directory selection.
- In the template-selection step, replace `waitForQuickInputFirstOption(page)` and `page.keyboard.press('Enter')` with `selectQuickInputOption(page, 'DefaultApexClass')`.
- Preserve the screenshot, class-name InputBox flow, first output-directory choice, and generated-file assertions exactly.

### Phase 2: Verify the focused regression and repository readiness

- Run the focused cross-platform spec in Apex Log web and desktop modes with Wireit caching disabled and Playwright retries disabled.
- Run `npm run check:dupes` and inspect findings involving the changed spec.
- Inspect the final scoped diff to confirm only template selection changed.
- Run the repository readiness hook; require compile, lint, Effect diagnostics, unit tests, VS Code bundle, and knip to pass before reporting implementation complete.

## Applicable Skills

- `concise`: focused tracked plan.
- `playwright-e2e`: shared accessible-name selector and focused web/desktop execution.
- `typescript`: import and async call correctness.
- `verification`: focused Playwright, duplicate detection, diff review, and readiness gates.

## Verification

- `WIREIT_CACHE=none npm run test:web -w salesforcedx-vscode-apex-log -- --retries 0 test/playwright/specs/apexGenerateClass.headless.spec.ts`
- `WIREIT_CACHE=none npm run test:desktop -w salesforcedx-vscode-apex-log -- --retries 0 test/playwright/specs/apexGenerateClass.headless.spec.ts`
- `npm run check:dupes`
- Repository readiness hook: compile, lint, Effect diagnostics, tests, `vscode:bundle`, and knip
- Final scoped diff and `git diff --check -- .claude/plans/W-23711825.md`

<!-- opencode-plan-evidence:{"planPath":".claude/plans/W-23711825.md","observedRevision":"2026-08-04T17:46:55.000+0000","summary":{"phases":["Replace template-order and Enter-based selection in `packages/salesforcedx-vscode-apex-log/test/playwright/specs/apexGenerateClass.headless.spec.ts` with `selectQuickInputOption(page, 'DefaultApexClass')`, retaining class-name input, output-directory selection, screenshots, and generated-file assertions.","Run focused Apex Log web and desktop Playwright tests with Wireit caching and retries disabled, duplicate detection, scoped diff checks, then repository readiness hooks before implementation completion."],"skills":["concise","playwright-e2e","typescript","verification"],"verification":["Inspected the target spec, Apex class QuickPick definition, shared helper implementation and export, Apex Log package scripts, required Playwright references, verification guidance, and readiness-hook adapter.","Updated and reread `.claude/plans/W-23711825.md`; synchronized tracked evidence to revision `2026-08-04T17:46:55.000+0000`.","Plan specifies focused web and desktop commands using `WIREIT_CACHE=none`, `--retries 0`, and `test/playwright/specs/apexGenerateClass.headless.spec.ts`; it also requires `npm run check:dupes`, scoped diff checks, and passing readiness hooks after implementation.","No commit, push, pull-request mutation, or Salesforce mutation performed."]}} -->
