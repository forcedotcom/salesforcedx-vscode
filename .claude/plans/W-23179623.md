# W-23179623 — Migrate apex-testing to shared buildAllServicesLayer

## Context

- Goal: drop inline `buildAllServicesLayer` factory in `packages/salesforcedx-vscode-apex-testing/src/services/extensionProvider.ts`; use shared `buildAllServicesLayer` from `@salesforce/effect-ext-utils`.
- Shared helper `(context, fallbackDisplayName)` already provides: pjson.displayName fallback, ChannelServiceLayer, ErrorHandlerService, ExtensionContextServiceLayer, SdkLayerFor, ExtensionProviderServiceLive, prebuiltServicesDependencies. Source: `packages/effect-ext-utils/src/allServicesLayer.ts`.
- WI detail says add `ApexTestDiscoveryService.Default` via `Layer.merge`. **No Effect service** — only plain class `ApexTestDiscoveryStore` (`src/discoveryVfs/apexTestDiscoveryStore.ts`, not a Layer). Current local factory adds **zero** extra layers vs shared. So migration = pure drop-in; no per-extension merge layer.
- Pattern ref: apex-replay-debugger (`src/services/extensionProvider.ts` + `src/index.ts:182`) — shared helper called at `activate()` w/ `nls.localize('channel_name')`; local file keeps only `setAllServicesLayer`/`AllServicesLayer`. apex-testing also keeps runtime singleton (`getApexTestingRuntime`).
- `channel_name` = "Apex Testing" (`src/messages/i18n.ts:39`). `CHANNEL_NAME` const currently feeds fallback.

## Phases

### Phase 1 — migrate to shared helper (one commit)

`packages/salesforcedx-vscode-apex-testing/src/services/extensionProvider.ts`:
- import `buildAllServicesLayer` from `@salesforce/effect-ext-utils` (replace `ExtensionProviderService`/`getServicesApi`/Schema/pjson imports no longer needed).
- delete local `ExtensionProviderServiceLive`, local `buildAllServicesLayer` factory, `CHANNEL_NAME` const, now-unused imports. Remove (only consumed by deleted code): `ExtensionPackageJsonSchema`, `ExtensionProviderService`, `type ExtensionPackageJson`, `getServicesApi` (all from `@salesforce/effect-ext-utils`, lines 8-12), `import * as Effect from 'effect/Effect'` (line 13), `import * as Schema from 'effect/Schema'` (line 16), `import type { ExtensionContext } from 'vscode'` (line 17 — local `buildAllServicesLayer` param was its only consumer; shared import now backs `ReturnType<typeof buildAllServicesLayer>`), `import { nls } from '../messages'` (line 18 — only fed `CHANNEL_NAME`).
- keep imports still used: `import * as Layer from 'effect/Layer'`, `import * as ManagedRuntime from 'effect/ManagedRuntime'` (runtime singleton + `Layer.Layer.Success/Error` type plumbing).
- keep `AllServicesLayer` mutable, `setAllServicesLayer`, runtime singleton (`ApexTestingRuntime` type, `_apexTestingRuntime`, `getApexTestingRuntime`). `ReturnType<typeof buildAllServicesLayer>` now refs shared import — type plumbing unchanged.

`packages/salesforcedx-vscode-apex-testing/src/index.ts`:
- line 32: import `buildAllServicesLayer` from `@salesforce/effect-ext-utils`; keep `getApexTestingRuntime`, `setAllServicesLayer` from local `./services/extensionProvider`.
- line 109: `setAllServicesLayer(buildAllServicesLayer(context, nls.localize('channel_name')))`. Add `nls` import (from `./messages`).

Verify no other live import of removed local `buildAllServicesLayer`/`ExtensionProviderServiceLive` (grep). Test mock `test/jest/views/testController.test.ts` mocks whole local module incl `buildAllServicesLayer: jest.fn()` — harmless extra key, leave.

Commit: `refactor(apex-testing): use shared buildAllServicesLayer - W-23179623`

## Skills to apply

- effect-best-practices — Layer/runtime correctness
- typescript — type plumbing on `ReturnType<typeof buildAllServicesLayer>`
- ts4023-effect-errors — exported runtime type referencing shared layer
- verification — compile + jest gates below
- concise — plan/docs style

## Verification

- `npm run compile` (or package tsc) apex-testing clean — load-bearing (type of `AllServicesLayer`/runtime now from shared).
- jest apex-testing: `testController.test.ts`, `testDiscovery.test.ts`, `orgApexClassProvider.test.ts`, `testReportGenerator.test.ts`, `colorizer.test.ts` pass (these mock local extensionProvider).
- eslint apex-testing clean (no unused imports after deletions; no new eslint-disable).
- grep: no remaining ref to local `ExtensionProviderServiceLive` or local-defined factory.
- Not e2e-covered: no apex-testing e2e on this branch; activation path validated via compile + existing jest mocks only.
