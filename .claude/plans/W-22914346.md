# W-22914346: e2e Create Project With Manifest

## Context

- `createProject.desktop.spec.ts` covers standard only
- `emptyWorkspaceSfdxCommands.desktop.spec.ts` verifies with-manifest command exists but never executes it
- Same flow: template prompt -> project name -> parent folder; `manifest: true` passed to TemplateService
- Generated project contains `manifest/package.xml` in addition to `sfdx-project.json` + `force-app`

## Phases

### Phase 1: Add createProjectWithManifest.desktop.spec.ts

- New file `packages/salesforcedx-vscode-metadata/test/playwright/specs/createProjectWithManifest.desktop.spec.ts`
- Mirror createProject.desktop.spec.ts
  - Use `emptyWorkspaceDesktopTest` fixture (already has `files.simpleDialog.enable`)
  - `prepareNoFolderOpenForPaletteTests`
  - Execute `packageNls.project_generate_with_manifest_text`
  - Select "Standard" template
  - Enter project name
  - Select parent folder
  - Assert `sfdx-project.json`, `force-app`, AND `manifest/package.xml` exist

**Commit:** `test(metadata): add e2e for Create Project With Manifest - W-22914346`

## Skills

- playwright-e2e
- verification
- feature-branch
- pr-draft
- typescript

## Verification

- Compile: stop hook covers
- Lint: stop hook covers
- Dupes: `npm run check:dupes` (new file similar to existing; confirm no flags)
- E2E-covered: the new test itself validates the feature (run `npm run test:desktop -w salesforcedx-vscode-metadata -- --retries 0 --grep "Create Project With Manifest"`)
