# W-24194352 — Remove YourKit profiler support from Apex LS

## Context

- Remove YourKit-specific Apex Language Server startup support.
- Remove local debug configuration that forwards `YOURKIT_PROFILER_AGENT`.
- Remove the bundled YourKit distribution and license because Apex LS no longer uses them.
- Keep unrelated profiler references and Apex LS behavior unchanged.

## Phases

1. Remove YourKit support
   - Delete `YOURKIT_PROFILER_AGENT` handling, including its suspend-startup conflict and `-agentpath` JVM argument, from `packages/salesforcedx-vscode-apex/src/languageServer.ts`.
   - Remove `YOURKIT_PROFILER_AGENT` entries from `.vscode/launch.json`.
   - Delete `yourkit-distro/`.
   - Commit message: `feat(apex): remove YourKit profiler support`

## Skills to apply

- `typescript` — TypeScript changes in `languageServer.ts`.
- `verification` — repository checks after changes.

## Verification

- Run formatting and lint checks covering `.vscode/launch.json` and `packages/salesforcedx-vscode-apex/src/languageServer.ts`.
- Run the existing Apex extension unit tests and TypeScript compilation.
- Before PR, run the Apex desktop spec `packages/salesforcedx-vscode-apex/test/playwright/specs/apexLsp.desktop.spec.ts`.
- Search tracked files for `YourKit`, `YOURKIT_PROFILER_AGENT`, and `yourkit-distro`; expect no YourKit-specific Apex LS or launch-configuration references.
- Confirm the diff contains only the planned source, launch configuration, and bundled-distribution removals.
