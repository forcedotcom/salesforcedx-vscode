# W-23031858 — soql-model real internal package (ADR + conversion)

## Context

`src/soql-model/` in `salesforcedx-vscode-soql` = quasi-package: imported as `@salesforce/soql-model/*`, no `package.json`/`exports`/boundary. `@salesforce/` specifier faked by 4 alias mechanisms (tsconfig paths, 2 jest mappers, rollup alias). Abandoned tail of soql-tooling→vscode move (PR #6821).

RESET: prior PR #7486 closed (reviewer rejected sub-dir barrels). Branch fresh — no soql-model package, no barrels exist yet. NEW REQUIREMENTS supersede original barrel approach.

Closure verified: soql-model imports only `@salesforce/soql-common` (subpaths `/soql-parser.lib`, `/soqlComments`) + `antlr4ts`. No vscode/effect/core. Web-safe.

Consumers (src): 8 files, 28 imports — soql-builder-ui (7 files), queryDataView/queryDataHelper.ts (1). Tests: 36 files in `test/jest/soql-model/**`, deep relative imports (zero alias). 3 divergent dup pairs: deserializer (782 vs 721 lines), serializer (36/36), selectAnalyzer (142/142).

Curated export set (33 names, only in-repo src consumers): `AndOr, ColumnData, ConditionOperator, deserialize, ErrorType, FieldCompareConditionImpl, FieldRefImpl, FieldSelectionImpl, FromImpl, getFieldInputValidator, getFieldMultipleInputValidator, getOperatorValidator, HeaderCommentsImpl, IncludesConditionImpl, InListConditionImpl, LimitImpl, LiteralImpl, LiteralType, ModelSerializer, OrderByExpressionImpl, OrderByImpl, Query, QueryImpl, Select, SelectAnalyzer, SelectCountImpl, SelectExprs, SelectExprsImpl, SObjectFieldType, SoqlModelUtils, splitMultiInputValues, UiOperatorValue, WhereImpl`. (Tests need more internal names — see Phase 6.)

Reference internal package: soql-common. Note soql-common is `versionedIndependently`+published; soql-model is `private`+consumed via `*` (NOT published).

ESLint state (`eslint.config.mjs`):
- base TS block: `files: ['**/*.ts']` (line 101), rules from 128. Has `local/no-self-barrel-import`. Missing `barrel-files/avoid-barrel-files`+`avoid-re-export-all`.
- `barrel-files/avoid-*` currently ONLY in Effect-override block (628-629, files 616-625).
- exemption `packages/**/src/index.ts` → `avoid-barrel-files: off` (703-706) already present.
- test-files relaxed block: 717-749 (files 719-735, rules 736-748).
- `avoid-barrel-files` default threshold = 5 re-exports. Most `messages/index.ts` = `createNls` (not re-export) → safe. Risk = sub-dir barrels w/ ≥5 re-exports in non-exempt packages → measure via lint (Phase 7).

## Phases

### Phase 1 — ADR
`docs/adr/0020-soql-model-internal-package.md` (grill-me format). Decision: real INTERNAL workspace package, consumed via `*`, NOT published. Invariant: soql-common must never import soql-model (would force publish). Rejected alt: publish like soql-common (no external consumer → pure overhead).
- files: `docs/adr/0020-soql-model-internal-package.md`
- commit: `docs: ADR-0020 soql-model internal package - W-23031858`

### Phase 2 — Create package + move source
- `git mv packages/salesforcedx-vscode-soql/src/soql-model packages/soql-model/src` (preserve history).
- `packages/soql-model/package.json`: name `@salesforce/soql-model`, `private: true`, `main: out/src/index.js`, `types: out/src/index.d.ts`, single `exports` `.` entry. deps: `@salesforce/soql-common` (`*`), `antlr4ts`. wireit compile/lint/test (model Phase 4).
- `packages/soql-model/tsconfig.json`: extend `../../tsconfig.common.json`, `rootDir: .`, `outDir: out`, `include src/**/*.ts`.
- `model/model.ts` L291 `export * from './unmodeled'` → named re-export (`UnmodeledSyntax, UnmodeledSyntaxReason` + REASON_* consts consumed internally/in tests).
- Compile in isolation to confirm closure.
- files: `packages/soql-model/{package.json,tsconfig.json}`, moved `src/**`, `model/model.ts`
- commit: `feat(soql-model): create internal package, move source - W-23031858`

### Phase 3 — Top-level barrel + rewrite consumers + tear down fakes
- `packages/soql-model/src/index.ts`: 33 curated named exports (NO `export *`). Collapses 15 `model/impl/*` deep imports + analyzers/serialization/validators.
- Rewrite 8 src consumers (28 imports) → bare `@salesforce/soql-model`.
- Tear down fakes:
  - `salesforcedx-vscode-soql/tsconfig.json`: delete `paths` alias.
  - `salesforcedx-vscode-soql/jest.config.js`: delete soql-model moduleNameMapper.
  - `src/soql-builder-ui/jest.config.js`: delete soql-model mapper.
  - `src/soql-builder-ui/rollup.config.mjs`: delete `@salesforce/soql-model` alias entry; simplify Babel/plugin-typescript comments re aliased `../soql-model` sibling (real pkg resolves normally). Keep nodeShims (antlr4ts assert/util) + node-resolve.
- esbuild: no change — bundles from compiled `out/src/index.js`; tsc resolves real pkg at compile. No soql-model alias in esbuild.config.mjs.
- Add `@salesforce/soql-model` (`*`) to `salesforcedx-vscode-soql` package.json deps.
- files: `packages/soql-model/src/index.ts`, 8 consumer .ts, soql `tsconfig.json`+`jest.config.js`+`package.json`, `soql-builder-ui/{jest.config.js,rollup.config.mjs}`
- commit: `refactor(soql-model): curated barrel, rewrite consumers, drop alias fakes - W-23031858`

### Phase 4 — Wireit graph
- `packages/soql-model` wireit: `compile` (dep `../soql-common:compile`), `lint`, `test`, `check:circular-deps` w/ files+output caching.
- `salesforcedx-vscode-soql:compile` deps += `../soql-model:compile` (keep `../soql-common:compile`).
- `build:soql-builder-ui` deps += `../soql-model:compile`; update `files` globs (no longer `src/soql-model/**`).
- root package.json `compile`/`lint`/`test` deps += `./packages/soql-model:*`.
- No cycle: vscode-soql→soql-model→soql-common.
- files: `packages/soql-model/package.json`, `salesforcedx-vscode-soql/package.json`, root `package.json`
- commit: `build(soql-model): wireit compile/lint/test wiring - W-23031858`

### Phase 5 — Test reorg
- `git mv` 36 `test/jest/soql-model/**` → `packages/soql-model/test/`.
- De-dup 3 divergent pairs (keep mirror-dir variant: analyzers/serialization; verify deserializer 782-line is superset before dropping 721).
- Rewrite deep relative imports → new package layout / curated exports + internal deep paths as needed.
- `packages/soql-model/jest.config.js` (base config, like soql-common) wired to `soql-model:test`.
- `salesforcedx-vscode-soql/jest.config.js`: drop `testPathIgnorePatterns` soql-model entry if now stale; root host stops owning soql-model tests.
- files: `packages/soql-model/test/**`, `packages/soql-model/jest.config.js`, `salesforcedx-vscode-soql/jest.config.js`
- commit: `test(soql-model): co-locate tests, de-dup, base jest config - W-23031858`

### Phase 6 — ESLint barrel enforcement
- Add `barrel-files/avoid-barrel-files: 'error'` + `barrel-files/avoid-re-export-all: 'error'` to BASE block (rules ~128, files `['**/*.ts']`).
- Keep existing `packages/**/src/index.ts` exemption (703-706).
- Add both rules `'off'` in test-files relaxed block (736-748).
- Package-level exempt `avoid-barrel-files` (legacy, out of scope): new override block for `salesforcedx-apex-debugger`, `salesforcedx-apex-replay-debugger`, `salesforcedx-vscode-apex-testing`, `salesforcedx-vscode-org`, `salesforcedx-vscode-core` (`**/*.ts` each). `avoid-re-export-all` stays ON for these.
- Fix stray `export *`: `salesforcedx-apex-debugger/src/commands/index.ts` L `export * from './protocol'` → named (`DebuggerResponse, Reference, Value, Tuple, LocalValue, DebuggerRequest, ReferenceRequest`).
- files: `eslint.config.mjs`, `salesforcedx-apex-debugger/src/commands/index.ts`
- commit: `chore(eslint): enforce no-barrel-files repo-wide, fix stray re-export - W-23031858`

### Phase 7 — Lint verification + hard-stop scope guard (CONTINGENT)
WI scope = exactly 6 items, all handled in Phase 6: 5 package-level `avoid-barrel-files` exemptions + 1 stray `export *` fix in `salesforcedx-apex-debugger/src/commands/index.ts`. Phase 7 does NOT expand on this.
- Run `npm run lint`. Expect ZERO `avoid-barrel-files`/`avoid-re-export-all` flags beyond the 6 scoped items (Phase 6 exemptions + apex-debugger fix already neutralize them).
- If any additional barrel/`export *` surfaces (e.g. sub-dir barrels ≥5 re-exports in non-exempt packages — candidates per Plan context L21: lightning, apex-log, services, lwc-language-server, aura, lightning-lsp-common, visualforce, effect-ext-utils, metadata, lwc, apex, org-browser, soql, services-types; today ≥6 such barrels exist e.g. apex-replay-debugger/states, lightning-lsp-common/decorators, vscode-org/commands, apex-debugger/commands, vscode-apex/languageUtils, vscode-lwc/testSupport/types): **log the file paths, STOP, escalate to human.** Do NOT silently convert, do NOT eslint-disable, do NOT add to exempt list. Out-of-scope barrel cleanup is a separate WI.
- Rationale: repo-wide barrel conversion is unbounded scope creep; the 5-package exemption was deliberately chosen so the new rule lands without touching legacy code outside the WI.
- files: none expected (verification-only). If flags surface → escalation note, no code change in this branch.
- commit: none (no code change expected). If escalation needed, surface paths in PR description.

## Skills to apply
- concise — plan + any md
- wireit — Phase 4 graph edits
- grill-me — Phase 1 ADR format
- packageJson — Phase 2/4 package.json
- ts1261-filename-casing — moved files
- typescript — exports/imports
- verification — final checks

## Verification
NOT e2e-covered (run locally):
- `npm run compile` (top-level) — full graph, web-safe closure.
- `npm run lint` (top-level) — barrel rules pass; expect zero flags beyond the 6 scoped items. Any extra flag → escalate per Phase 7 (do NOT fix in this WI).
- `npm run test` (top-level) jest incl new `soql-model:test`.
- `soql-builder-ui` jest: `npm run test:soql-builder-ui -w salesforcedx-vscode-soql`.
- `soql-builder-ui` rollup: `npm run build:soql-builder-ui -w salesforcedx-vscode-soql` (antlr4ts shims + real pkg resolve).
- esbuild bundles web+desktop: `npm run vscode:bundle -w salesforcedx-vscode-soql`.
- `git mv` preserved history (`git log --follow` spot-check).
- Grep: zero remaining `@salesforce/soql-model/` deep specifiers; zero `src/soql-model` refs.

e2e-covered (branch Playwright web+desktop SOQL specs): SOQL builder runtime behavior, query data view.
