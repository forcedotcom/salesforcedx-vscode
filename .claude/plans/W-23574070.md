# W-23574070

## Context

Replace production TypeScript array-to-`Set`-to-array deduplication with Effect `Arr.dedupe`. The affected values are strings, so `Arr.dedupeWith` is unnecessary. Preserve first-occurrence ordering and existing return shapes while removing hand-rolled collection conversion.

Files:

- `packages/salesforcedx-vscode-apex/src/settings.ts`
- `packages/salesforcedx-lightning-lsp-common/src/baseContext.ts`
- `packages/salesforcedx-vscode-services/src/orgCatalog/orgCatalogRemoteRetrieve.ts`
- `packages/salesforcedx-vscode-services/src/orgCatalog/orgCatalogWorkspace.ts`
- `packages/salesforcedx-vscode-services/src/orgCatalog/orgCatalogTreeProjection.ts`
- `packages/salesforcedx-vscode-services/src/orgCatalog/orgMetadataCatalogRecorder.ts`
- `packages/salesforcedx-lwc-language-server/src/context/lwcContext.ts`
- `packages/salesforcedx-apex-debugger/src/adapter/apexDebug.ts`
- `packages/salesforcedx-vscode-apex-log/src/logs/logAutoCollect.ts`

## Phases

### Phase 1: Replace `Set` deduplication

- Import `effect/Array` as `Arr` in each affected file.
- Replace array-to-`Set`-to-array expressions with `Arr.dedupe` over the same input sequence.
- Keep `Set` instances used for membership, counting, or set-valued APIs unchanged.
- Commit message: `refactor: use Arr.dedupe for array deduplication`

## Skills to apply

- `typescript`: preserve types, comments, immutability, and direct collection transformations.
- `effect-best-practices`: use the Effect Array module and repository import style.
- `verification`: run affected-package checks after implementation.

## Verification

- Confirm no replaced expression changes input order or output element equality.
- Run the repository stop-hook verification: compile, lint, Effect Language Service diagnostics, unit tests, bundles, and knip.
- Run `npm run test:desktop -w salesforcedx-vscode-apex -- --retries 0`.
- Run `npm run test:desktop -w salesforcedx-vscode-services -- --retries 0`.
- Run `npm run test:desktop -w salesforcedx-vscode-apex-log -- --retries 0`.
- Review the final diff to confirm only deduplication expressions and required imports changed.
