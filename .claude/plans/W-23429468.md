# W-23429468 — Fix + enforce globalErrorInEffectFailure

## Context

Effect LS rule `globalErrorInEffectFailure`: warns when global `Error` used in Effect failure channel; use tagged error. 7 findings across 5 packages. After fixing all sites, append `globalErrorInEffectFailure` to `config/effect-diagnostics.json` `enforcedRules` (ratchet gate in `scripts/effect-diagnostics.mjs`; CI `check:effect-diagnostics`). Mirrors prior sibling WIs (`lazyPromiseInEffectSync`, `runEffectInsideEffect`).

Sites:
- `salesforcedx-lightning-lsp-common/src/workspaceReadFileHandler.ts:92` — `Effect.fail(new Error('Invalid stat result'))`
- `salesforcedx-vscode-apex-log/src/traceFlags/traceFlagJsonSync.ts:76` — `catch: () => new Error('search failed')` (swallowed by `catchAll`)
- `salesforcedx-vscode-apex-testing/src/testDiscovery/testDiscovery.ts:57` — `Either<ToolingTestsPage, Error>` + `catch: (error): Error => new Error(...)`
- `salesforcedx-vscode-services/src/virtualFsProvider/indexedDbStorage.ts:70` — `Effect.async<IDBDatabase, Error>` + `Effect.fail(new Error(...))`
- `salesforcedx-vscode-services/src/virtualFsProvider/memfsWatcher.ts:62` — `Stream.fromAsyncIterable(..., e => new Error(String(e)))`
- `salesforcedx-vscode-soql/src/commands/queryPlan.ts:109,118` — `Effect.fail(new Error('No Salesforce project found'))` ×2 (fix = swap to `getSfProject()` precondition, no new error; see Phase 5)

Note: `Schema/Data.TaggedError` extend `globalThis.Error` (`effect/internal/core.js` YieldableError) → `.message` preserved; `isError(e)` guards + `errorMessage(e)` in catchAll still work; `toThrow('...message')` tests still match.

## Phases

### Phase 1 — lightning-lsp-common
- New tagged error `InvalidStatResultError` (`Schema.TaggedError`, `message` field). Colocate in `workspaceReadFileHandler.ts` (module-local, not exported) or reuse if existing pattern.
- `:92` → `Effect.fail(new InvalidStatResultError(...))`.
- catchAll paths unaffected (`errorMessage` reads `.message`).
- Files: `packages/salesforcedx-lightning-lsp-common/src/workspaceReadFileHandler.ts`
- Commit: `fix(lightning): tagged error for invalid stat result per globalErrorInEffectFailure - W-23429468`

### Phase 2 — apex-log
- Search failure swallowed by `catchAll` → still needs typed failure channel. Add `UserSearchError` tagged error (local to `traceFlagJsonSync.ts`, not exported).
- `:76` `catch` → `new UserSearchError(...)`.
- REQUIRED: swap the blanket `Effect.catchAll(() => Effect.sync(() => { picker.busy = false; }))` at `:82-89` → `Effect.catchTag('UserSearchError', () => Effect.sync(() => { picker.busy = false; }))`. SKILL.md Quick Reference bans `catchAll`/swallow; anti-patterns.md:37-53 marks `catchAll` losing type info FORBIDDEN, fix = `catchTag`/`catchTags`. Same reset-busy behavior, but a future fallible `Effect.sync` in the gen block won't be silently absorbed.
- Files: `packages/salesforcedx-vscode-apex-log/src/traceFlags/traceFlagJsonSync.ts`
- Commit: `fix(apex-log): tagged error + catchTag for SOSL user search per globalErrorInEffectFailure - W-23429468`

### Phase 3 — apex-testing
- Add `TestDiscoveryFetchError` tagged error (`message` field). Location: `testDiscovery/schemas.ts` or local.
- MUST export `TestDiscoveryFetchError`: `discoverTests()` return type crosses the package `.d.ts` boundary (consumed by `commands/apexTestSuite.ts`, `commands/apexTestRun.ts`, `views/apexTestTreeService.ts`), so the error type leaks into emitted declarations. A module-local (unexported) tagged error → TS4023 on build. Apply `ts4023-effect-errors` skill.
- `:68-69` `catch` → `new TestDiscoveryFetchError(...)` w/ same message text (`Failed to fetch test discovery page: ...`) to keep `testDiscovery.test.ts:126` `toThrow` green.
- Update `Either.Either<ToolingTestsPage, Error>` annotation → tagged error type. `:82` re-fail unchanged. `431` substring check (`:77`) unchanged (reads `.message`).
- Verify: run wireit `compile` (emits `out/*.d.ts`) and confirm NO TS4023 before commit.
- Files: `packages/salesforcedx-vscode-apex-testing/src/testDiscovery/testDiscovery.ts` (+ error def, exported)
- Commit: `fix(apex-testing): tagged error for test discovery fetch per globalErrorInEffectFailure - W-23429468`

### Phase 4 — services (2 sites, same package)
- Reuse existing `VirtualFsProviderError` (`virtualFsProvider/virtualFsProviderError.ts`).
- `indexedDbStorage.ts:70`: `Effect.async<IDBDatabase, Error>` → `<IDBDatabase, VirtualFsProviderError>`; fail w/ `VirtualFsProviderError` (`message` + `unknownToErrorCause`).
- `memfsWatcher.ts:62`: `Stream.fromAsyncIterable(..., e => new VirtualFsProviderError(...))`; `tapError` types `Error` → `VirtualFsProviderError` (has `message`).
- No new exported tagged error (respects `noExportTaggedErrorInServices` eslint-local-rule).
- Files: `packages/salesforcedx-vscode-services/src/virtualFsProvider/indexedDbStorage.ts`, `.../memfsWatcher.ts`
- Commit: `fix(services): VirtualFsProviderError for idb/memfs per globalErrorInEffectFailure - W-23429468`

### Phase 5 — soql (2 sites)
- Do NOT invent `NotInSalesforceProjectError`. Precondition already exists as typed-failure service call: `ProjectService.getSfProject()` fails w/ exported `FailedToResolveSfProjectError` (projectService.ts:165-173; re-exported services `index.ts:178`). Same pattern as `orgDisplayDefaultCommand`/`orgDisplayUsernameCommand` (orgDisplay.ts:69-98).
- Replace `:108-110` and `:117-119` `isSalesforceProject().pipe(flatMap(... Effect.fail(new Error(...))))` blocks with `yield* servicesApi.services.ProjectService.getSfProject();` (discard returned SfProject). No new error type. Drop now-unused `formatErrorMessage`/other imports if any.
- No fallback loss: `getSfProject()` never calls `isSalesforceProject()`'s vscode-fs fallback (`workspaceRootSalesforceManifestExistsViaVscodeFs`, projectService.ts:155-161) — that branch is wired only inside `isSalesforceProject()`'s own `catchTag`, structurally unreachable from `getSfProject()`.
- soql web e2e (`test/playwright/web/headlessServer.ts` → `createHeadlessServer`, no `folderPath`/`folderUri`) never mounts `vscode-test-web://`; `@vscode/test-web` `workbench.js:169-174` falls to `tmp:` workspaceUri, so `fileSystemSetup.ts:63-68` injects a `memfs:` workspace folder. Under `memfs`, `@salesforce/core/lib/fs/fs.js` `isWeb()` and `virtualFsProvider/fileSystemProvider.ts:13` share one `memfs` store — Node `SfProject.resolve` sees every editor write; vscode-fs fallback has no scenario to cover.
- Files: `packages/salesforcedx-vscode-soql/src/commands/queryPlan.ts`
- Commit: `fix(soql): use ProjectService.getSfProject precondition per globalErrorInEffectFailure - W-23429468`
- Status: implemented — commit `fff58c986` on this branch already does exactly this swap.

### Phase 6 — enforce + skill dedupe
- Append `"globalErrorInEffectFailure"` to `enforcedRules` in `config/effect-diagnostics.json`.
- Dedupe skill/ESLint coverage: SKILL.md line 20 + Quick Reference Errors row already name `globalErrorInEffectFailure`/plain-Error. Collapse to single pointer at rule (config-enforced), matching prior `runEffectInsideEffect` anti-patterns.md treatment. Trim redundant prose in `effect-best-practices/references/anti-patterns.md` + SKILL.md.
- Files: `config/effect-diagnostics.json`, `.claude/skills/effect-best-practices/SKILL.md`, `.claude/skills/effect-best-practices/references/anti-patterns.md`
- Commit: `chore(effect): enforce globalErrorInEffectFailure + dedupe skill - W-23429468`

## Skills to apply
- effect-best-practices — tagged error patterns, `Schema.TaggedError`, no plain Error in failure channel
- ts4023-effect-errors — exported tagged error `.d.ts` visibility if any error crosses package boundary
- typescript — annotation changes (Either/async type params)
- concise — plan + any md edits
- verification — drive affected flows before commit

## Verification
- `npm run check:effect-diagnostics` — 0 `globalErrorInEffectFailure` findings; gate passes w/ rule enforced. Primary DoD.
- `npx effect-language-service diagnostics --file <each fixed file>` — confirm site cleared (PostToolUse hook auto-runs on edit).
- compile all 5 packages (wireit `compile`; ratchet needs `out/*.d.ts`). Phase 3: explicitly confirm NO TS4023 (exported `TestDiscoveryFetchError` crosses `.d.ts` boundary).
- jest — only `testDiscovery.test.ts:126` genuinely exercises a changed site (`toThrow('Failed to fetch test discovery page: Boom')` → `TestDiscoveryFetchError.message` preserved). No other changed site has unit coverage:
  - Phase 2 `searchUsersEffect` is module-local (unexported), driven via a `Stream` inside `pickOrgUser` — no jest imports it, and exporting it solely to test the failure branch is a smell. Existing `indexedDbStorage.test.ts` covers `parseMyDomain` only; `index.test.ts`'s `mockIndexedDB` drives the open success path, never fires `onerror`. So the Phase 4 `onerror → VirtualFsProviderError` branch and Phase 2 `catchTag` reset-busy-on-failure branch have zero runtime tests.
  - `queryPlan.test.ts` covers pure note-dedup/message helpers only — it does NOT exercise the Phase 5 `getSfProject()` precondition; no `FailedToResolveSfProjectError` assertion exists.
- Guarantee is compile-time: type-channel swaps (plain `Error` → tagged error). `tsc`/wireit `compile` proves failure types line up; `catchTag` (vs `catchAll`) is compiler-narrowed so non-`UserSearchError` escapes rather than being swallowed. DoD = that + `check:effect-diagnostics` gate (0 findings, rule enforced).
- e2e — happy-path only, failure branches not driven:
  - Phase 2: `traceFlagsForOtherUser.headless.spec.ts` drives the SOSL picker (`fill('Integration')` → `waitForQuickInputFirstOption`), exercising `searchUsersEffect`'s success path + `picker.busy` toggle. The `catchTag` failure branch is NOT exercised (no forced search error).
  - Phase 5: `soql-query-plan.spec.ts` (web + desktop) exercises `sf.data.query.explain[.document]` with a real project open — confirms the `getSfProject()` swap works on the happy path. vscode-fs-fallback loss is proven inert in Phase 5 (see that phase), not re-checked here.
  - Phases 1, 4 (`memfsWatcher`, `indexedDbStorage`) failure paths: no e2e drives them. Coverage is compile + diagnostics gate only.
