# W-23371053

## Context

Enforce `@typescript-eslint/consistent-type-imports` on `salesforcedx-vscode-apex-debugger`. Type-only imports stay explicit, same as packages already scoped in `eslint.config.mjs` (`prefer: 'type-imports'`, `fixStyle: 'inline-type-imports'` so `no-duplicate-imports` stays satisfied).

Files:

- `eslint.config.mjs`
- `packages/salesforcedx-vscode-apex-debugger/**/*.ts` that the new rule reports (`src/`, `test/`)

## Phases

1. Add a package-scoped `@typescript-eslint/consistent-type-imports` block for `packages/salesforcedx-vscode-apex-debugger/**/*.ts` (`error`, `prefer: 'type-imports'`, `fixStyle: 'inline-type-imports'`). Apply ESLint fixes to every reported import. Hand-fix any leftover reports. Import syntax only. Commit: `refactor: enforce type imports in apex debugger`

## Skills to apply

- `typescript`: keep import semantics and repo style
- `verification`: check the package after the config and import edits

## Verification

- `WIREIT_CACHE=none npm run lint -w salesforcedx-vscode-apex-debugger` from repo root
- `WIREIT_CACHE=none npm run compile -w salesforcedx-vscode-apex-debugger` from repo root
- `WIREIT_CACHE=none npm test -w salesforcedx-vscode-apex-debugger` from repo root
- Diff limited to the ESLint override and type-only import syntax
