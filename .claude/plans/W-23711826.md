# W-23711826

## Context

`lwcGenerateComponent` headless E2E component type pick: `JavaScript` quick pick (`createLwc.ts` label `JavaScript`, value `default`).

File:

- `packages/salesforcedx-vscode-lwc/test/playwright/specs/lwcGenerateComponent.headless.spec.ts`

## Phases

### 1. Select JavaScript

- Import `selectQuickInputOption` from `@salesforce/playwright-vscode-ext`.
- Step 1 (component type): replace `waitForQuickInputFirstOption` plus `getByRole('option').first().click` with `selectQuickInputOption(page, 'JavaScript')`.
- Leave the name prompt and output-directory steps.
- Drop unused imports. `waitForQuickInputFirstOption` stays for the directory step.

Commit: `refactor: use quick input helper for lwc javascript choice`

## Skills to apply

- `playwright-e2e`: E2E helpers and test execution.
- `typescript`: imports and formatting.
- `verification`: plan and post-change checks.

## Verification

1. From repo root: `npm run test:web -w salesforcedx-vscode-lwc -- --retries 0`.
2. Stop hook: compile, lint, effect LS, test, vscode:bundle, knip.
