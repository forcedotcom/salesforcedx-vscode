# W-23429470 — fix + enforce effectSucceedWithVoid

## Context

- Effect LS rule `effectSucceedWithVoid`: `Effect.succeed(undefined)` → `Effect.void`. Mechanical, quick-fixable.
- Ratchet: `config/effect-diagnostics.json` `enforcedRules` array; `scripts/effect-diagnostics.mjs` fails build on listed rules (any severity). Fix all sites, then append rule name.
- Ground truth (LS `diagnostics --project`, not WI list — WI stale):
  - apex-oas/src/oas/externalServiceRegistrationManager.ts:356
  - apex-testing/src/commands/apexTestSuite.ts:239, :256 (not in WI; real)
  - apex-testing/src/utils/testUtils.ts:184 (WI said :297)
  - core/src/context/workspaceContext.ts:85
  - org-browser/src/services/orgBrowserMetadataRetrieveService.ts:45 (WI said :44)
  - services/src/core/connectionService.ts:489, :492 (WI said :391/:394)
  - services/src/observability/cliTelemetry.ts:52
  - services/src/virtualFsProvider/fileSystemProvider.ts:144 (WI said :133)
  - services/src/virtualFsProvider/indexedDbStorage.ts:175, :176 (WI said :168/:169)
  - services/src/vscode/contextKeyWatchers.ts:44, :49
  - services/src/vscode/fsService.ts:230 (WI said :229)
  - soql/src/editor/soqlEditorInstance.ts:40 (WI said :39)
  - soql/src/lspClient/codeCompletion.ts:203
- WI apex-log traceflagsCommands:184 already refactored away (commit 3f6d47c1b) — no site. Skip.
- Line drift vs WI: use LS output, not WI numbers.
- Rule fires on literal `undefined`/`void 0` arg regardless of type-arg annotation (`isVoidExpression` in transform.js:81536 checks only the identifier). So `Effect.succeed<T | undefined>(undefined)` fires, but the naive `Effect.void` swap narrows the success channel to `void` — safe only when the value is discarded downstream. 3 swap categories (verified via `tsc -p packages/*/tsconfig.json --noEmit`):
  - **Cat A — plain `Effect.void` OK** (value discarded / channel is genuinely void): cliTelemetry:52 (verified clean), plus untyped sites listed below.
  - **Cat B — coupled multi-line, still `Effect.void`** (result feeds a `catchAll(() => …)` that also returns void, both must swap together): soql soqlEditorInstance:40 (`.pipe(Effect.catchAll(() => Effect.succeed<DescribeSObjectResult|undefined>(undefined)))`) and codeCompletion:203 — the `undefined` is only ever the catch-fallback for a discarded describe; `Effect.void` compiles (soql pkg can't standalone-`tsc` — missing built cross-pkg deps; verify via `wireit compile` or the ratchet's compiled tree, not raw `tsc -p`).
  - **Cat C — `Effect.void` is WRONG, needs a real fix** (the `undefined` is a live `T | undefined` DATA value the caller reads, not a discarded result): see below. 2 sites.
- Cat C sites (both break `tsc`; naive `Effect.void` narrows away the declared channel):
  - **indexedDbStorage.ts:175+176** — `IndexedDBStorageServicesNoop` fields typed `() => Effect<undefined, VirtualFsProviderError, never>` (deleteFile) and `() => Effect<SerializedEntryWithPath | undefined, VirtualFsProviderError, never>` (loadFile) from the real service methods. Naive `Effect.void` → `TS2322` (verified). FIX: make the *real* `deleteFile`/`loadFile` methods (indexedDbStorage.ts:139-140, :142-155) end their pipe with `Effect.asVoid` so their declared success type becomes `void`; THEN noop `Effect.void` is type-correct + idiomatic. Verified: `tsc -p packages/salesforcedx-vscode-services/tsconfig.json --noEmit` clean. (loadFile's return is used only for its side effect — `writeFileWithOrWithoutDir` — never read; deleteFile likewise, memfsWatcher.ts:39 just `yield*`s it. Also update the jest mock at test/jest/index.test.ts:95 — already `Effect.succeed(undefined)`, keep or align to `Effect.void`.)
  - **connectionService.ts:489+492** — `buildDevHubId` returns `string | undefined` (devHubOrgId, consumed at :407 → written into DefaultOrgInfo). :489 (ternary else) and :492 (`catchAll` fallback) BOTH carry a real `undefined` value; naive `Effect.void` → `TS2345` at :492 (verified). Swapping only one is worse. FIX: rewrite as `Effect.fn('getDevHubId')` returning `undefined` on the no-username branch and `Effect.orElseSucceed(() => undefined)` for the failed-lookup fallback — preserves the `string | undefined` channel, removes the literal `Effect.succeed(undefined)` so the rule clears, keeps the span. Verified: `tsc` clean + LS diagnostics `0 findings` on the file.
- If any swap fails to compile in an unanticipated way, stop and replan — do not suppress/disable the rule.
- OVERLAP: skill already lists `effectSucceedWithVoid` (SKILL.md:20). No dedupe needed — already single mention. No skill edit.

## Phases

### Phase 1 — fix all sites
- Cat A (direct `Effect.void`): externalServiceRegistrationManager:356, apexTestSuite:239+256, testUtils:184, workspaceContext:85, orgBrowserMetadataRetrieveService:45, cliTelemetry:52, fileSystemProvider:144, contextKeyWatchers:44+49, fsService:230.
- Cat B (coupled, `Effect.void` on both value + catchAll fallback): soqlEditorInstance:40, codeCompletion:203.
- Cat C (real fix, NOT plain `Effect.void` — see Context):
  - indexedDbStorage: add `Effect.asVoid` to real deleteFile:139-140 + loadFile:142-155 pipes, then swap noop :175+176 to `Effect.void`; align jest mock test/jest/index.test.ts:95.
  - connectionService: rewrite `buildDevHubId` (:486-494) to `Effect.fn` + `Effect.orElseSucceed(() => undefined)`; removes both :489 + :492 literals.
- Files: apex-oas/.../externalServiceRegistrationManager.ts, apex-testing/.../apexTestSuite.ts, apex-testing/.../testUtils.ts, core/.../workspaceContext.ts, org-browser/.../orgBrowserMetadataRetrieveService.ts, services/.../connectionService.ts, services/.../cliTelemetry.ts, services/.../fileSystemProvider.ts, services/.../indexedDbStorage.ts, services/test/jest/index.test.ts, services/.../contextKeyWatchers.ts, services/.../fsService.ts, soql/.../soqlEditorInstance.ts, soql/.../codeCompletion.ts
- commit: `chore(effect): fix effectSucceedWithVoid sites - W-23429470`

### Phase 2 — enforce
- Append `"effectSucceedWithVoid"` to `enforcedRules` in config/effect-diagnostics.json.
- Files: config/effect-diagnostics.json
- commit: `chore(effect): enforce effectSucceedWithVoid - W-23429470`

## Skills to apply
- effect-best-practices (Effect.void idiom)
- concise (plan + any comments)
- typescript (compile after swaps)
- verification

## Verification
- `node node_modules/@effect/language-service/cli.js diagnostics --project packages/<pkg>/tsconfig.json --format json` → 0 `effectSucceedWithVoid` for each touched pkg (confirm Cat C `connectionService`/`indexedDbStorage` clear too).
- Compile via `wireit compile` (or the ratchet's compiled tree), NOT raw `tsc -p packages/salesforcedx-vscode-soql/tsconfig.json` — soql can't standalone-compile (cross-pkg imports resolve to `unknown`, ~62 phantom errors). services pkg does standalone-`tsc` fine; used above to verify Cat C.
- `node scripts/effect-diagnostics.mjs` exit 0 after Phase 2 (requires compiled `out/*.d.ts` — wireit `dependencies: ["compile"]` guarantees; run compile first).
- Unit tests for touched pkgs (services, soql, core, apex-testing, apex-oas, org-browser). Cat A/B `Effect.void` = runtime-equivalent to `Effect.succeed(undefined)`. Cat C changes value flow at the type level only — indexedDbStorage `asVoid` discards an already-unused result; connectionService still yields `string | undefined`. Watch the services jest suite (index.test mock + any devHubId assertions).
- Not e2e-covered: pure lint/compile ratchet; no e2e exercises these branches. Cat C connectionService feeds `devHubOrgId` into DefaultOrgInfo/telemetry spans — behavior unchanged (still `string | undefined`), so no new e2e needed; if devHubId resolution regresses it surfaces in existing services unit tests, not e2e.
