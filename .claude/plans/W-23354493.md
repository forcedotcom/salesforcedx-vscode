# W-23354493 — enable functional/no-throw-statements for apex-testing

## Context
- Enable `functional/no-throw-statements` as error for `salesforcedx-vscode-apex-testing/src/**`.
- Blockers 5/6/7 landed (commits W-23354492, W-23354479, etc).
- Rule already `error` for Effect pkgs (eslint.config.mjs:703), `off` for tests (:873).
- Add rule to apex-testing src block (eslint.config.mjs:817-832).
- 17 src throw sites, 2 files:
  - `src/discoveryVfs/apexTestingDiscoveryFsProvider.ts` — 15 throws. Implements vscode `FileSystemProvider`; interface contract = throw `FileSystemError` synchronously. Precedent: services `virtualFsProvider/fileSystemProvider.ts:10` uses file-level `/* eslint-disable functional/no-throw-statements */`.
  - `src/testDiscovery/packageResolution.ts` — 2 throws (`throw new TrySubjectIdError()` :268 sentinel; `throw error` :273 re-throw). Plain async/await + try/catch, not Effect. Throw-as-control-flow across nested `runMemberQuery`.

## Phases

### Phase 1 — FileSystemProvider: file-level disable + enable rule
- eslint.config.mjs: add `'functional/no-throw-statements': 'error'` to apex-testing src block (:817).
- `apexTestingDiscoveryFsProvider.ts`: file-level `/* eslint-disable functional/no-throw-statements */` w/ comment (vscode `FileSystemProvider` contract requires sync `FileSystemError` throw).
- Commit: `refactor(apex-testing): enable functional/no-throw-statements - W-23354493`

### Phase 2 — packageResolution: eliminate throw-as-control-flow
- Refactor `TrySubjectIdError` sentinel → discriminated return (signal "try SubjectId" via result value, not throw).
- Re-throw `throw error` (:273): only wrapper is side-effect (`packageResolutionUnavailableOrgs.add`) before propagate. Refactor catch to record side-effect, then let error propagate w/o explicit re-throw (e.g. move side-effect to a `finally`, or restructure so the throw site isn't inside a catch). No line-disable fallback — if genuinely infeasible, stop and escalate rather than disable.
- Keep existing comments.
- Verify callers (apexTestTreeService.ts:461,890,1124 boundary comments) still accurate; update if refactor changes error surface.
- Commit: `refactor(apex-testing): remove throw-as-control-flow in packageResolution - W-23354493`

Note: could collapse to 1 commit (small WI); 2 phases for review clarity (disable-justified vs refactored).

## Skills
- effect-best-practices — typed-error patterns (though these files are non-Effect async)
- ts4023-effect-errors — if any tagged error export touched
- typescript — compile discipline
- verification — drive lint/tests before commit
- concise — plan + comment style

## Verification
- `npm run lint` in apex-testing pkg → 0 `functional/no-throw-statements` violations.
- `npm run compile` (tsc) clean.
- jest: `packages/salesforcedx-vscode-apex-testing/test/jest/{discoveryVfs,testDiscovery}/**` pass (fs provider throw behavior, package resolution fallback paths).
- Not e2e-covered: VFS error semantics + package2 fallback are unit-tested; no branch e2e touches these.
