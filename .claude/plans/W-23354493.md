# W-23354493 — enable functional/no-throw-statements for apex-testing

## Context
- Enable `functional/no-throw-statements` as error for `salesforcedx-vscode-apex-testing/src/**`.
- Blockers 5/6/7 landed (commits W-23354492, W-23354479, etc).
- Rule already `error` for Effect pkgs (eslint.config.mjs:703), `off` for tests (:873).
- Add rule to apex-testing src block (eslint.config.mjs:817-832).
- 17 src throw sites, 2 files:
  - `src/discoveryVfs/apexTestingDiscoveryFsProvider.ts` — 15 throws. vscode `FileSystemProvider` contract: throw `FileSystemError` sync. Precedent: services `virtualFsProvider/fileSystemProvider.ts:10` file-level disable.
  - `src/testDiscovery/packageResolution.ts` — eliminated. Refactored query to use SubjectId + SubscriberPackageId (documented columns); removed undocumented MetadataComponentId/Package2Id fields and control-flow sentinel (TrySubjectIdError).

## Phases

### Phase 1 — FileSystemProvider: file-level disable + enable rule
- eslint.config.mjs: add `'functional/no-throw-statements': 'error'` to apex-testing src block (:817).
- `apexTestingDiscoveryFsProvider.ts`: file-level `/* eslint-disable functional/no-throw-statements */` w/ comment (vscode `FileSystemProvider` contract requires sync `FileSystemError` throw).
- Commit: `refactor(apex-testing): enable functional/no-throw-statements - W-23354493`

### Phase 2 — packageResolution: eliminate throw-as-control-flow
✅ **Completed**: Refactored to query only documented columns (SubjectId, SubjectKeyPrefix, SubscriberPackageId). Removed:
- `TrySubjectIdError` sentinel & control-flow throw
- MetadataComponentId column + retry logic (`isNoSuchColumnForField` helper)
- Package2Id + Package2 join on Package2.Id (now SubscriberPackageId + Package2.SubscriberPackageId)
- `subjectIdOnly` fallback path tracking
- Simplified `runMemberQuery` (no field parameter; 1 outcome path instead of 2)

Commit: `fix(apex-testing): query Package2Member by documented fields - W-23354493`

## Skills
- effect-best-practices — typed-error patterns (though these files are non-Effect async)
- ts4023-effect-errors — if any tagged error export touched
- typescript — compile discipline
- verification — drive lint/tests before commit
- concise — plan + comment style

## Verification
- Package resolution query now uses only documented Salesforce Tooling API columns (SubjectId, SubscriberPackageId); no MetadataComponentId/Package2Id.
- `npm run lint` → 0 `functional/no-throw-statements` violations in apex-testing src.
- `npm run compile` (tsc) clean.
- jest: package resolution tests pass (direct SubjectId query, Package2 join via SubscriberPackageId, fallback enumerate-by-package).
- Callers (apexTestDiscoveryService) unaffected — error surface unchanged.
