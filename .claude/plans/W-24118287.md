# W-24118287 — Convert ARD + adapter path handling to vscode-uri

## Context

- Replace remaining `node:path` basename handling in Apex Replay Debugger (ARD) and the Apex debug adapter with `vscode-uri`.
- Keep URI parsing and path-component extraction URI-aware and consistent with the repository path guidance.
- Scope:
  - `packages/salesforcedx-vscode-apex-replay-debugger/src/commands/launchApexReplayDebuggerWithCurrentFile.ts`
  - `packages/salesforcedx-apex-replay-debugger/src/states/frameEntryState.ts`
  - `packages/salesforcedx-apex-debugger/src/adapter/apexDebug.ts`
  - Existing tests that assert the affected source/class names, including `packages/salesforcedx-apex-debugger/test/unit/adapter/apexDebug.test.ts`

## Phases

### 1. Convert ARD and adapter basename handling

- Use `Utils.basename` with existing or parsed `URI` values.
- In the current-file ARD command, derive the Apex class name from `document.uri` without `node:path`.
- In replay and interactive debugger stack frames and event source links, parse each URI once, then use that URI for both basename and filesystem-path conversion where needed.
- Remove the replaced `node:path` imports.
- Update affected existing assertions for URI-derived source names; do not add unrelated path behavior.
- Commit: `refactor: use vscode-uri for debugger path handling`

## Skills to apply

- `paths`: use `URI` and `Utils` for source URI parsing and basename extraction.
- `typescript`: preserve repository TypeScript import, typing, and naming conventions.
- `verification`: verify the changed packages after implementation.

## Verification

- Run the targeted Apex debugger and Apex Replay Debugger tests covering the changed source-name handling.
- Let the repository stop hook run compile, lint, Effect language service checks, tests, `vscode:bundle`, and `knip`.
- Run `npm run check:dupes`; inspect `jscpd-report` if it flags changed code.
- Confirm the 3 changed source files no longer import basename from `node:path`.
