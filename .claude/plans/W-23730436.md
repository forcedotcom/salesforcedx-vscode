# W-23730436 — Remove 8 validated dead-code exports

## Context

- Validated set:
  - `packages/effect-ext-utils/src/utils.ts` → `stripAnsiInJson`
  - `packages/salesforcedx-vscode-apex/src/languageUtils/index.ts` → `indexerDoneHandler`
  - `packages/salesforcedx-vscode-apex-testing/src/utils/orgApexClassProvider.ts` → `openOrgApexClass`
  - `packages/salesforcedx-vscode-apex-testing/src/utils/orgApexClassProvider.ts` → `createOrgApexClassUri`
  - `packages/salesforcedx-utils-vscode/src/context/orgExtensionUtils.ts` → `getSalesforceVSCodeOrgExtension`
  - `packages/salesforcedx-vscode-org/src/util/orgAlias.ts` → `isAlphaNumSpaceString`
  - `packages/salesforcedx-vscode-services/src/virtualFsProvider/indexedDbStorage.ts` → `parseMyDomain`
  - `packages/salesforcedx-vscode-metadata/src/conflict/conflictDetectionSettings.ts` → `isConflictDetectionEnabledSync`
- This tree's base already removed `openOrgApexClass` and `createOrgApexClassUri` in `2326d1eff`, and `isAlphaNumSpaceString` in `69fa2bdd6`.
- Remove the 5 remaining exports; do not recreate changes already present in the base.
- Reduce unsupported package API surface without changing runtime behavior.
- Do not widen the cleanup to other findings.

## Phases

### Phase 1 — remove the 5 exports present in this tree's base

- Remove each of the 5 remaining symbols from its module's export surface.
- Retain a declaration without `export` when it still has an in-module consumer; otherwise remove the unused declaration and imports made unused by that removal.
- Update only directly affected imports or explicit export lists.

Commit: `refactor: remove remaining validated dead-code exports - W-23730436`

## Skills to apply

- `concise` — compact tracked-plan prose.
- `typescript` — explicit exports, inferred types, and cleanup of newly unused imports.
- `verification` — repository checks after implementation.

## Verification

- Confirm all 8 validated exports are absent and the final diff removes exactly the 5 present in this tree's base.
- Run `npm run check:dupes`; inspect findings affecting changed code.
- Require the repository stop hook to pass compile, lint, Effect diagnostics, tests, `vscode:bundle`, and knip.
