# W-24162918: Adopt `Predicate.isString` for string type-guard filters

## Context

`packages/salesforcedx-vscode-services/scripts/spanFileServer.ts` has 2 inline string type guards used to filter parsed span lines. Replace both with Effect's shared `Predicate.isString` predicate, preserving filtering and JSONL output behavior while removing duplicate guard definitions.

## Phases

### 1. Replace inline string predicates

- Import `Predicate` from `effect`.
- Pass `Predicate.isString` directly to both `Array.filter` calls.
- Commit message: `refactor: use Predicate.isString in span file server`

## Skills to apply

- `concise`: keep this plan compact.
- `typescript`: preserve inferred string narrowing; avoid new abstractions.
- `span-file-export`: preserve the span server's JSONL behavior.
- `verification`: verify the affected package after the change.

## Verification

1. Run `npm run lint -w salesforcedx-vscode-services`.
2. Run `npm run test:web -w salesforcedx-vscode-services -- --retries 0`.
3. Confirm the diff only changes `packages/salesforcedx-vscode-services/scripts/spanFileServer.ts` as planned.
