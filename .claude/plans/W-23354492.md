# W-23354492 — enable avoid-barrel-files for apex-testing

## Context
- WI: drop `salesforcedx-vscode-apex-testing` from `barrel-files/avoid-barrel-files` exemption (eslint.config.mjs ~L806).
- Legacy-barrel exemption block: `eslint.config.mjs:803-813` (avoid-barrel-files off; avoid-re-export-all stays on globally).
- Verified w/ temp exemption removal + `npx eslint`: only 1 error →
  - `src/commands/index.ts:7` — flagged barrel (pure re-export of apexTestRun / apexTestRunCodeAction / apexTestSuite).
  - `src/messages/index.ts` — NOT flagged; runtime NLS factory (`createNls`), not a re-export barrel. WI premise re: messages is wrong; leave it.
  - `src/index.ts` — separately exempt via `packages/**/src/index.ts` block (L793); public API, stays.
- `commands/index.ts` sole importer = `src/index.ts:33`. No test/other pkg imports it (grepped repo).

## Phases

### Phase 1 — inline commands barrel, enable rule
- Rewrite `src/index.ts:20-33` import: pull the 11 command symbols direct from source files instead of `./commands`:
  - `apexTestRun` ← `./commands/apexTestRun`
  - 8 codeAction delegates/actions ← `./commands/apexTestRunCodeAction`
  - `apexTestSuiteCreate/Edit/Run` ← `./commands/apexTestSuite`
- Delete `src/commands/index.ts`.
- Remove `'packages/salesforcedx-vscode-apex-testing/**/*.ts',` line from exemption block (`eslint.config.mjs:806`).
- commit: `refactor(apex-testing): enable avoid-barrel-files, inline commands barrel - W-23354492`

## Skills
- concise (plan/docs)
- typescript (import edits)
- verification

## Verification
- `npx eslint packages/salesforcedx-vscode-apex-testing/src/**/*.ts` → 0 errors (pre-build `eslint-local-rules`: `npm run compile` in packages/eslint-local-rules if `out/` missing).
- `npm run compile` (tsc) apex-testing pkg — direct imports resolve.
- jest apex-testing suite — no import breakage.
- NOT e2e-covered (lint/build-only change; no runtime behavior delta).
