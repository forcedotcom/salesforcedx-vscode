# W-23602244 — Extract shared E2E setup action

## Context

E2E workflows repeat the same setup: Node.js 22.22.3 with npm caching, Wireit GitHub Actions caching, and dependency installation with retries. Extract those steps into one local composite action to remove duplication and keep E2E setup consistent. Checkout remains in each workflow because the repository must exist before a local action can run.

Files:

- Add `.github/actions/setup-e2e/action.yml`.
- Update `.github/workflows/playwrightVscodeExtE2E.yml`.
- Update `.github/workflows/servicesE2E.yml`.
- Update `.github/workflows/metadataE2E.yml`.
- Update `.github/workflows/apexLogE2E.yml`.
- Update `.github/workflows/apexTestingE2E.yml`.
- Update `.github/workflows/apexOasE2E.yml`.
- Update `.github/workflows/orgE2E.yml`.
- Update `.github/workflows/orgBrowserE2E.yml`.
- Update `.github/workflows/soqlE2E.yml`.
- Update `.github/workflows/coreE2E.yml`.
- Update `.github/workflows/apexReplayDebuggerE2E.yml`.
- Update `.github/workflows/apexDebuggerE2E.yml`.
- Update `.github/workflows/visualforceE2E.yml`.
- Update `.github/workflows/auraE2E.yml`.
- Update `.github/workflows/lwcPlaywrightE2E.yml`.
- Update `.github/workflows/apexLspE2E.yml`.

## Phases

### 1. Extract and adopt shared E2E setup

- Create the composite action with the existing Node setup, Wireit cache setup, and retrying npm install steps.
- Replace each matching inline sequence with `uses: ./.github/actions/setup-e2e` after checkout.
- Preserve workflow-specific environment, conditions, matrices, and test commands.
- Commit: `refactor: extract shared E2E workflow setup`

## Skills to apply

- `concise` — keep action metadata and workflow edits minimal.
- `verification` — apply repository checks for GitHub Actions changes.

## Verification

- Run `npm run check:actions` from the repository root.
- Review the diff to confirm every composite-action call follows checkout and only the duplicated setup steps changed.
