# W-22865161 — Enable jest/no-standalone-expect

## Context
- Goal: add `'jest/no-standalone-expect': 'error'` to jest rules block in `eslint.config.mjs` (~line 562, after `jest/no-focused-tests`).
- Blocker W-11678119 landed: `jest/no-focused-tests` already present at line 562; `jest/unbound-method` at 561.
- `eslint-plugin-jest` 28.14.0 installed, wired (`jest: eslintPluginJest` plugin) for all test globs in block @ lines 517-532.
- Pre-checked: temp-added rule, built local plugin (`npm run compile -w packages/eslint-local-rules`), linted all jest globs (salesforcedx test/unit/__tests__/*.spec/*.test/web/playwright, automation-tests, playwright-vscode-ext, aura/lwc/lightning-lsp language servers). **0 violations.** Single-line change only.

## Phases

### Phase 1 — add rule (one commit)
- `eslint.config.mjs`: after line 562 (`'jest/no-focused-tests': 'error',`) add:
  - `'jest/no-standalone-expect': 'error',`
- No violations to fix (verified).
- commit: `test(lint): enable jest/no-standalone-expect ESLint rule - W-22865161`

## Skills
- typescript
- verification — stop hook covers lint/compile
- concise

## Verification
- `npm run lint` passes (stop hook covers).
- No test/source code changes; no unit/e2e impact.
- Not e2e-covered (lint-only change).
