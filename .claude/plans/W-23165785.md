# W-23165785 — 5 Thin ApexTestController shell; wire services + SettingsService

## Context

- Last WI in group 4. Siblings CLOSED: #3 CodeCoverage svc (W-23165782), #4.1 discovery/tree svc (W-23165783), #4.2 execution svc (W-23165784), discovery-store svc (W-23165788). Follow-on #6 (W-23165786, Triaged) deletes `coreExtensionUtils.ts` outright + inlines org access — NOT this WI.
- Branch currently empty (0 diff vs develop). Baseline = post-4.2 shell: `src/views/testController.ts` (931 lines) + `apexTestTreeService.ts` (688) + `apexTestExecutionService.ts` (656).
- Goal: controller = glue only (registration, run-profile callbacks, disposal). Remaining tree/discovery logic → tree svc; connection/org → `ConnectionService`/`TargetOrgRef`; raw `settings.*` → `SettingsService`.

### Controller logic still to move (not glue)

- Incremental tree mutation: `incrementalUpdate`, `applyIncrementalDiff`, `addClassToTree`, `diffClassMethods`, `removeClassFromTree`, `removeEmptyAncestors` (testController.ts:308-576) → tree svc.
- Discovery persistence: `persistDiscoveredClasses`, `fetchClassBodiesByFullName`, `getMethodIdsFromResultFile` (221-302) → tree svc (currently passed as DiscoveryContext callbacks).
- Suite children: `resolveSuiteChildren` (727-779) → tree svc. Its writer of `suiteToClasses` is why that field stayed on shell (4.1/4.2 comments testController.ts:87-89) — now in scope.
- Connection glue: `ensureInitialized`/`getConnection`/`getTestService`/`invalidateConnection` (184-214, 593-597) → replace with `ConnectionService.getConnection()` (already cached in services `connectionCache`) + on-demand `new TestService(conn)` inside svcs. The three throw idioms at 191/201/211 (connection-failed / service-not-initialized / connection-not-initialized) are NOT silently dropped by deleting the methods: `ConnectionService.getConnection()` already returns a tagged failure (no-org/connection error) that propagates to callers, so the explicit failure path is preserved as a tagged error, not lost. Sequencing constraint below (see Phase 2 vs 3): these methods have direct on-shell callers until Phase 3, so their deletion is deferred to Phase 3.

### Nine Refs (AC "nine shared-state fields in Ref/SubscriptionRef inside services")

- tree svc has 7: suiteItems, classItems, methodItems, classToParentItem, hasRestoredResults, isRestoringResults, inFlightDiscovery.
- exec svc has 1: lastProcessedResultFile.
- +1 this WI: `suiteToClasses` (shell `Map` field, testController.ts:89) → tree-svc Ref = 9.
- connection/testService NOT Ref-ified — routed to cached `ConnectionService` per WI, so eliminated (not counted).

### settings.* migration (delete src/settings.ts)

Raw `settings.*` callers to migrate → `SettingsService` (section `salesforcedx-vscode-apex-testing`):
- `commands/apexTestRun.ts` (123/124/155/164), `commands/apexTestRunCodeAction.ts` (46/73), `commands/apexTestRunUtils.ts` (80/81) — Effect ctx already; `yield* SettingsService.getValue(...)`.
- `views/apexTestExecutionService.ts` (142/143/249/250/437), `views/apexTestTreeService.ts` (444/561), `utils/testReportGenerator.ts` (retrieveCoverageThreshold/retrievePerformanceThreshold).
- Keys/defaults: `retrieve-test-code-coverage`/false, `test-run-concise`/false, `outputFormat`/'markdown', `testSortOrder`/'runtime', `testPerformanceThresholdMs`/5000, `testCoverageThresholdPercent`/75, `restore-previous-results`/true. codeCoverageService already uses `getValue` this way (codeCoverageService.ts:187/274) — mirror.
- WRITE: `disableRestorePreviousResults` writes `restore-previous-results=false` to **Workspace** target (settings.ts:40); `SettingsService.setValue` hardcodes **Global** (settingsService.ts:71). DECISION: add optional `target?: vscode.ConfigurationTarget` param to `SettingsService.setValue` (default Global preserves existing callers) in `salesforcedx-vscode-services/src/vscode/settingsService.ts`; call with `Workspace` to preserve behavior. Small cross-package change; note in PR.

### coreExtensionUtils (controller only this WI)

- Controller imports `getConnection, getDefaultOrgInfo` (testController.ts:18). Remove: connection → `ConnectionService.getConnection()`; org id → `SubscriptionRef.get(TargetOrgRef())` (pattern already in coreExtensionUtils.ts:19-27, pathHelpers.ts:30, watchers/testDiscovery.ts:20). Tree svc's `getDefaultOrgInfo` import stays until #6 (file not deleted here).

### Already-satisfied ACs (verify no regression; no work)

- in-flight dedup via Deferred (inFlightDiscovery, tree svc) — NOT Promise guard. ✅
- `Effect.yieldNow()` not `setImmediate` — 0 `setImmediate` in src. ✅

### catchAll / console (AC: catchTags, no catchAll/console swallow)

- catchAll in touched svcs: exec 241/256 (write/report failure → logError, non-fatal), tree 315/582 (→ logWarning/RestoreResultsError). These LOG, not silent-swallow. Audit: convert to `catchTags` where E is a tagged union; keep + document where genuinely broad IO logging. Not a blanket rewrite.
- console.* to remove in run/results paths touched: `utils/testResultProcessor.ts:168`, `utils/testUtils.ts:174/243`, `watchers/testDiscovery.ts:62` → `Effect.log*`. LEAVE `index.ts:122/137` (activation; owned by closed #2).

## Phases

### Phase 1 — settings.* → SettingsService; delete settings.ts

- Add optional `target` param to `SettingsService.setValue` (services pkg).
- Migrate all raw `settings.*` callers (list above) to `SettingsService.getValue`/`setValue`. Sync command bodies already Effect — `yield*`. `testReportGenerator.ts` reads thresholds inside its Effect.
- Delete `src/settings.ts` + `test/jest/settings.test.ts`; drop `jest.mock('../settings')` from execution/tree/testController/codecoverage/testReportGenerator test files (replace w/ SettingsService test-layer stub or real getValue).
- Verify: 0 hits `from '../settings'` / `src/settings` in src+test.
- Commit: `refactor(apex-testing): route settings reads through SettingsService, drop settings.ts - W-23165785`

### Phase 2 — Service-context connection/org → ConnectionService/TargetOrgRef

- Svcs call `api.services.ConnectionService.getConnection()` + `new TestService(conn)` in-body instead of `ctx.getConnection`/`ctx.getTestService`/`ctx.ensureInitialized` callbacks. Drop those 3 from `DiscoveryContext` (apexTestTreeService.ts:87-92) + `ExecutionContext` (apexTestExecutionService.ts:48-49).
- Org id via `SubscriptionRef.get(yield* api.services.TargetOrgRef())`; drop tree svc `getDefaultOrgInfo` callback usage where controller-sourced (tree svc keeps its own import until #6).
- SEQUENCING: Do NOT delete shell `ensureInitialized`/`getConnection`/`getTestService` (184-214) or the `connection`/`testService` fields in Phase 2. Their remaining on-shell callers — `resolveSuiteChildren` (740/743/753), `fetchClassBodiesByFullName` (251), `addClassToTree` (424) — do not move to the tree svc until Phase 3. Deleting the methods here while those callers stay on the shell is a TS2339 compile break for the inter-commit span. These methods + fields + the `getConnection`/`getDefaultOrgInfo` import (testController.ts:18) are deleted in Phase 3, in the same commit that moves their last callers off the shell.
- `invalidateConnection` (593) stays in Phase 2: it nulls the still-present `connection`/`testService` fields AND calls `getOrgApexClassProvider().clearAllCache()`. Since the fields survive until Phase 3, the method must too (its sole caller is `resetState` at 130). To keep the services' cached connection consistent on reset, add `yield* api.services.ConnectionService.invalidateCachedConnections()` to `resetState`'s runtime block in Phase 2. In Phase 3, once the `connection`/`testService` fields are deleted, `invalidateConnection` collapses to just the `clearAllCache()` call → fold it inline into `resetState` and delete the method.
- Commit: `refactor(apex-testing): route service-context org access via ConnectionService/TargetOrgRef - W-23165785`

### Phase 3 — Move tree logic into tree svc; suiteToClasses → Ref (9th)

- Move to `apexTestTreeService.ts`: `incrementalUpdate`, `applyIncrementalDiff`, `addClassToTree`, `diffClassMethods`, `removeClassFromTree`, `removeEmptyAncestors`, `resolveSuiteChildren`, `persistDiscoveredClasses`, `fetchClassBodiesByFullName`, `getMethodIdsFromResultFile`. `Effect.fn` per method (span names).
- `suiteToClasses` → `yield* Ref.make(new Map<string, Set<string>>())` in tree svc; add accessors (get + set-per-suite + clear). Remove shell field + `getSuiteToClasses` callbacks from both contexts; exec svc reads via accessor (apexTestExecutionService.ts:261/468).
- DELETE (this phase, same commit as the callers' move): shell `ensureInitialized`/`getConnection`/`getTestService` (184-214) + `connection`/`testService` fields + `getConnection`/`getDefaultOrgInfo` import (testController.ts:18) + collapse `invalidateConnection` into `resetState` (Phase 2 deferral). Every direct caller (`resolveSuiteChildren`, `fetchClassBodiesByFullName`, `addClassToTree`) is rewritten to `ConnectionService.getConnection()` in this same commit, so no TS2339 span exists.
- Errors: reuse/extend tagged errors (no bare `throw new Error`, no `throw` inside `Effect.gen` — use `Effect.fail(new TaggedError(...))`). `resolveSuiteChildren`'s try/catch throw at testController.ts:777 → in the moved Effect version, the connection failure already arrives as a `ConnectionService` tagged error; the suite-resolution wrapping (`apex_test_resolve_suite_children_failed_message`) becomes an `Effect.fail(new ResolveSuiteChildrenError(...))` (new/extended Schema.TaggedError carrying suiteName + friendly message), surfaced at the vscode boundary. The 191/201/211 messages (connection-failed / not-initialized) have no analog in the moved code — connection acquisition is now `ConnectionService.getConnection()`'s tagged failure, so those nls keys' notification path is preserved via a `catchTags` on the ConnectionService errors at the boundary (not dropped). `catchTags` not `catchAll`.
- Controller shrinks to: constructor (createTestController + tags + setup* handlers), `getController`, run-profile callbacks (`setupRunProfiles`→`runTests`), `refresh`/`clearAllTestItems`/`resetState`/`clearResults`, resolve/refresh handler wiring, `openOrgOnlyTest`/`retrieveOrgOnlyClass*`, `dispose`, `buildDiscoveryContext`/`buildExecutionContext` (now vscode objects + tags only), module helpers. Each public method = single `runPromise` at the vscode boundary delegating to a svc.
- Module exports `getTestClassName`/`clearAllSuiteChildren` read tree-svc accessors (already do via runSync).
- Verify: controller has NO `catchAll`, NO `console.*`, NO `settings.*`, NO `coreExtensionUtils`; `runPromise`/`runSync` only in vscode-boundary methods/module helpers.
- Commit: `refactor(apex-testing): thin ApexTestController to glue; move tree logic + suiteToClasses Ref to tree service - W-23165785`

### Phase 4 — catchAll→catchTags audit + console removal (touched paths)

- Convert catchAll→catchTags in exec (241/256) + tree (315/582) where E is tagged; keep+comment where genuinely broad IO log.
- `testResultProcessor.ts:168`, `testUtils.ts:174/243`, `watchers/testDiscovery.ts:62` console.* → `Effect.log*` (or drop debug noise). Leave index.ts activation logs.
- Commit: `refactor(apex-testing): catchTags + structured logging in test run/results paths - W-23165785`

### Phase 5 — tests (owns testController.test.ts)

- Rewrite `test/jest/views/testController.test.ts` for glue: assert delegation to svc accessors/runtime (not moved logic bodies). Drop `coreExtensionUtils` + `settings` mocks; mock runtime provides ConnectionService/TargetOrgRef/SettingsService.
- Extend `apexTestTreeService.test.ts`: incremental diff (created/changed/deleted → add/diff/remove class), removeEmptyAncestors, resolveSuiteChildren + suiteToClasses Ref, persistDiscoveredClasses best-effort (no-org skip), getMethodIdsFromResultFile parse.
- Fresh layer per case; assert suiteToClasses Ref isolation.
- Commit: `test(apex-testing): glue-only controller tests; tree-service incremental/suite coverage - W-23165785`

## Skills to apply

- effect-best-practices — Effect.Service accessors, `Effect.fn` span names, Ref for shared state (suiteToClasses), catchTags not catchAll, no console swallow, Schema.TaggedError per failure, params-vs-deps (drop connection callbacks; ConnectionService in-body), no runPromise/runSync inside fiber (boundary only), cachedFunction/Deferred dedup (verify unchanged)
- services-extension-consumption — ConnectionService.getConnection (cached), TargetOrgRef SubscriptionRef, SettingsService getValue/setValue
- i18n-messages — tagged-error messages from existing nls keys
- typescript / ts4023-effect-errors — type not interface, no null, effect error inference
- unit-test-critic — Phase 5 fresh-layer isolation, no over-mock
- concise — this plan + any doc edits
- verification — pre-PR checks

## Verification

- `npx effect-language-service diagnostics --file` on testController.ts + apexTestTreeService.ts + apexTestExecutionService.ts — clean (warnings+messages).
- tsc compile + eslint (no disable for autofixable).
- jest: rewritten testController suite + extended tree suite + exec/codeCoverage/testReportGenerator suites green after settings-mock removal.
- Layer builds: `getApexTestingRuntime()` resolves all svcs; SettingsService available via prebuilt api.services (no double-provision).
- grep guards (0 hits): `from '../settings'`/`src/settings` (src+test); `coreExtensionUtils` in testController.ts; `settings\.` / `catchAll` / `console\.` / `this\.connection` / `this\.testService` in testController.ts; `runPromise|runSync` in testController.ts only in vscode-boundary methods.
- suiteToClasses: 0 shell-field refs; tree-svc Ref + accessors present → 9 total Refs across svcs.
- setValue Workspace-target preserved for restore-previous-results disable (unit-assert target arg).

### e2e-covered (branch specs — exclude from manual)

- Run/suite/refresh/tree-item actions: `testExplorer.headless.spec.ts`, `apexTestSuite.headless.spec.ts`.
- Org-only retrieve + resolveSuiteChildren move: `orgOnlyClassRetrieve.headless.spec.ts`.
- Result restoration + stale + suiteToClasses tagging: `staleTestResultsRestoration.headless.spec.ts`.
- Settings read (code coverage/concise/format): `runApexTestsCodeLens.headless.spec.ts`, `runApexTestsCommandPalette.headless.spec.ts`, `codeCoverageColorizer.headless.spec.ts`.
- Incremental deploy-triggered tree update (edit+redeploy diff): `runApexTestsFailAndFix.headless.spec.ts`.
- No-org / logout / no-project / in-workspace filter: `noOrgVisibility`, `clearOnLogout`, `noProjectVisibility`, `inWorkspaceFilter`, `clearApexTestResults` specs.

### Manual (not e2e-covered)

- restore-previous-results disable writes **Workspace** target (unit + confirm setValue target).
- incrementalUpdate delete-only + removeEmptyAncestors pruning (unit).
