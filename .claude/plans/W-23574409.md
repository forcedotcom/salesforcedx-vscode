# W-23574409: Adopt `HashSet.union` in Apex settings

## Context

`packages/salesforcedx-vscode-apex/src/settings.ts` currently combines built-in and user-configured eligibility values with array spreading plus `Arr.dedupe`. Replace that combination with Effect `HashSet.union`, while preserving each exported setting reader's returned values and duplicate removal.

## Phases

### 1. Replace array deduplication with `HashSet.union`

- In `packages/salesforcedx-vscode-apex/src/settings.ts`, replace the Effect Array dependency and `Arr.dedupe` calls with Effect HashSet construction, union, and conversion back to `string[]`.
- Use `HashSet.union` where defaults and configured values are combined; convert fixed annotation arrays through HashSet without an artificial second input.
- Commit: `refactor(apex): adopt HashSet.union for settings`

## Skills to apply

- `typescript`: follow repository TypeScript import, typing, and naming conventions.
- `verification`: run focused tests and package checks after implementation.

## Verification

- `npm test -w salesforcedx-vscode-apex -- --runInBand settings.test.ts`
- `npm run compile -w salesforcedx-vscode-apex`
- `npm run lint -w salesforcedx-vscode-apex`
