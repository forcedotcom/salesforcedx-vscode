# W-23354497 — 8.12 functional/no-let for apex-testing

## Context

- Enable `functional/no-let: error` for `packages/salesforcedx-vscode-apex-testing/src/**`.
- 37 `let` in src (WI est ~41). Tests exempt (config line 876 already `no-let: off`).
- Blockers 5/6/7 landed: apex-testing src block exists (eslint.config.mjs:817-831).
- Plugin `functional` already registered (eslint.config.mjs:125).
- Strategy: land code fixes first (lint stays green, rule not yet on), enable rule + singleton disables last commit.

### `let` inventory (src only)

Function-local reassign → const/functional:
- utils/testReportGenerator.ts:45 `offset` (Uint8Array fill accumulator)
- utils/testResultProcessor.ts:32 `classItem`, 108-111 totals, 216-219 suite totals, 223 `childResult`
- utils/testUtils.ts:60 `documentSymbols`, 63 `document`
- utils/newlineUtils.ts:17 `text`, 18 `prefix`
- utils/orgApexClassProvider.ts:84 `className`
- testDiscovery/testDiscovery.ts:53 `nextUrl`, 54 `partialResult` (while-loop pagination)
- testDiscovery/packageResolution.ts:180/255/329 `for(let i...)`, 278 `subjectIdOnly`, 408 `package2List`
- views/orgTestItems.ts:55 `pkMap`, 70 `list` (get-or-create map)
- views/apexTestExecutionService.ts:483 `testsToRun`
- views/apexTestTreeService.ts:534 `for(let start...)`, 913 `namespaceItem`, 932 `packageItem`
- discoveryVfs/apexTestingDiscoveryFsProvider.ts:170 `current` (getOrCreateDirectory), 190 `current` (getEntry) — path-walk loop accumulators (37 total = 35 above + these 2)

Module-level mutable singletons → `eslint-disable-next-line functional/no-let` + justification (not auto-fixable; reassigned via `??=`/`= undefined`; mirrors apex-log runtime.ts:19/26, apexLogState.ts:29):
- discoveryVfs/apexTestingDiscoveryFsProvider.ts:222 `providerInstance`
- utils/orgApexClassProvider.ts:61 `classBodyCache`, 123 `providerInstance`
- views/testController.ts:460 `testControllerInst`
- services/extensionProvider.ts:34 `AllServicesLayer`, 49 `_apexTestingRuntime`

## Phases

### Phase 1 — utils/* function-local lets → const/functional

Files: testReportGenerator.ts, testResultProcessor.ts, testUtils.ts, newlineUtils.ts, orgApexClassProvider.ts (line 84 only).

- testReportGenerator.ts:45 offset — `parts.reduce` tracking offset, or single write via `Uint8Array` + running offset from `reduce`.
- testResultProcessor.ts:32/223 find-with-fallback — const via `?? ` fallback lookup.
- testResultProcessor.ts:108-111 totals — derive from `classResults` via reduce after side-effect loop (loop keeps `for...of`; only no-let enforced).
- testResultProcessor.ts:216-219 suite totals — `reduce` over children.
- testUtils.ts:60/63 — const; open-document via `??`/await expression.
- newlineUtils.ts:17/18 — const computed (prefix + trimmed text).
- orgApexClassProvider.ts:84 className — const + ternary for `.cls` strip.

Commit: `refactor(apex-testing): utils let -> const/functional - W-23354497`

### Phase 2 — testDiscovery/* lets

Files: testDiscovery.ts, packageResolution.ts.

- packageResolution.ts:180/255/329 + apexTestTreeService handled P3 — `for(let i;i+=size)` → `Array.chunksOf(arr, size)` then `for (const batch of chunks)`. `Array.chunksOf` confirmed available (effect/Array).
- packageResolution.ts:408 package2List — extract query into helper returning `Package2[]`; const.
- packageResolution.ts:278 subjectIdOnly — restructure so value returned from try/catch expression path (const).
- testDiscovery.ts:53/54 pagination — refactor while-loop to `Effect.iterate` (precedent: `promptService.ts:103-118` BFS, same accumulate-then-continue shape). State `{ classes: ToolingTestClass[]; nextUrl: string | undefined; partialResult: boolean }`; `while: s => s.nextUrl !== undefined`; `body` fetches one page, returns next state (431 → `{ ...s, nextUrl: undefined, partialResult: true }`; decode failure path unchanged, appends via spread not `push`). No disable-comment fallback — single approach.

Commit: `refactor(apex-testing): testDiscovery let -> const/functional - W-23354497`

### Phase 3 — views/* lets

Files: orgTestItems.ts, apexTestExecutionService.ts, apexTestTreeService.ts.

- orgTestItems.ts:55/70 get-or-create — const via `structure.get(k) ?? new Map()` then set (idempotent).
- apexTestExecutionService.ts:483 testsToRun — const + ternary on `runScope`/implicit-full-run filter.
- apexTestTreeService.ts:534 `for(let start...)` — `Array.chunksOf`.
- apexTestTreeService.ts:913/932 find-or-create TestItem — extract find-or-create helper returning const.

Commit: `refactor(apex-testing): views let -> const/functional - W-23354497`

### Phase 3b — discoveryVfs path-walk lets → functional

File: discoveryVfs/apexTestingDiscoveryFsProvider.ts.

- :170 getOrCreateDirectory `let current = this.root` — `parts.reduce<DirectoryEntry>((dir, part) => { ... }, this.root)`; each step reads/creates child dir, sets mtime side-effect, returns child. Throws FileNotADirectory when a part collides with a file entry (behavior unchanged).
- :190 getEntry `let current: Entry = this.root` — `parts.reduce<Entry | undefined>((entry, part) => ...)` starting `this.root`; returns undefined on non-directory/missing (or creates dir when `createDirectories`), else child. Preserve early-exit semantics via short-circuit on undefined in the reducer.
- Jest-covered by test/jest/discoveryVfs/apexTestingDiscoveryFsProvider.test.ts (stat/readDirectory/readFile/createDirectoryInternal/writeFileInternal path-walk) — run before commit.

Commit: `refactor(apex-testing): discoveryVfs path-walk let -> const/functional - W-23354497`

### Phase 4 — enable rule + singleton disables

- eslint.config.mjs:817 block: add `'functional/no-let': 'error'`.
- Add `eslint-disable-next-line functional/no-let` + accurate reason to the 6 module-level singletons (list above). Note: apexTestingDiscoveryFsProvider.ts:222 `providerInstance` is the ONLY disable in that file — its function-local lets (170/190) are fixed in Phase 3b, not disabled.
- Run `eslint --fix` to confirm zero residual; `npm run lint`.

Commit: `refactor(apex-testing): enable functional/no-let - W-23354497`

## Skills to apply

- typescript
- verification
- concise (plan/docs)

## Verification

- `npm run lint -w packages/salesforcedx-vscode-apex-testing` — zero no-let violations.
- `npm run compile -w packages/salesforcedx-vscode-apex-testing` — type-check clean.
- `npm run test -w packages/salesforcedx-vscode-apex-testing` — jest unit (26 files). Direct coverage of each refactored file:
  - test/jest/utils/{testResultProcessor,newlineUtils,testReportGenerator,testUtils,orgApexClassProvider}.test.ts
  - test/jest/testDiscovery/{testDiscovery,packageResolution}.test.ts (pagination + Package2/namespace resolution logic)
  - test/jest/views/{orgTestItems,apexTestExecutionService,apexTestTreeService}.test.ts
  - test/jest/discoveryVfs/apexTestingDiscoveryFsProvider.test.ts (Phase 3b path-walk)
- e2e (run locally before PR — playwright headless specs that exercise the runtime paths):
  - test/playwright/specs/testExplorer.headless.spec.ts — `openTestExplorerAndDiscover` drives testDiscovery.ts pagination + apexTestTreeService.ts tree building.
  - test/playwright/specs/apexTestSuite.headless.spec.ts — suite discovery/build over deployed classes.
- Coverage gap: NO existing playwright spec exercises packageResolution.ts 2GP/1GP/namespace batching (grep for Package2/2GP/1GP/namespace across specs returns zero hits). packageResolution.ts is a pure refactor; behavior pinned by test/jest/testDiscovery/packageResolution.test.ts. No e2e coverage for package resolution.
