# W-23558161

## Context

Migrate `sf.open.documentation` from direct VS Code registration to the core extension's Effect runtime registration. Preserve URL selection while expressing file-type dispatch with `Match` and emitting telemetry for the selected documentation type.

Files:

- `packages/salesforcedx-vscode-core/src/commands/openDocumentation.ts`
- `packages/salesforcedx-vscode-core/src/commands/index.ts`
- `packages/salesforcedx-vscode-core/src/index.ts`
- `packages/salesforcedx-vscode-core/test/jest/commands/openDocumentation.test.ts`
- `packages/salesforcedx-vscode-core/test/playwright/fixtures/desktopFixtures.ts`
- `packages/salesforcedx-vscode-core/test/playwright/specs/openDocumentation.desktop.spec.ts`

## Phases

### 1. Migrate command implementation and registration

- Convert `openDocumentation` to a named `Effect.fn` command.
- Replace the file-path/extension conditional chain with exhaustive `Match` dispatch, retaining Aura, Apex, SOQL, LWC, Functions, and default URL selection.
- Emit command telemetry through the existing telemetry service for the selected documentation type.
- Open the selected URL inside the Effect workflow.
- Register `sf.open.documentation` with `registerCommandWithRuntime(getRuntime())`; remove its direct `vscode.commands.registerCommand` registration.
- Keep command exports aligned with the Effect command name.

Commit: `refactor(core): migrate open documentation command to Effect`

### 2. Add command coverage

- Add focused tests for each supported editor path and the default path.
- Assert the selected URL, telemetry payload, and external-open side effect.

Commit: `test(core): cover open documentation Effect command`

## Skills to apply

- `command-ui`: Effect runtime command registration.
- `effect-best-practices`: named `Effect.fn`, exhaustive `Match`, typed service use.
- `typescript`: repository TypeScript conventions.
- `verification`: package checks and test selection.

## Verification

- Run the new open-documentation Jest test from the repository root.
- Run the core package desktop Playwright suite: `npm run test:desktop -w salesforcedx-vscode-core -- --retries 0`.
- Let the repository stop hook run compile, lint, Effect Language Service diagnostics, tests, bundle, and knip checks.
- Review the final diff to confirm only `sf.open.documentation` registration and implementation changed.
