# W-23127989 — E2E flake: Apex Test Suite (palette timeout)

## Context

Two flakes observed; only one is fixable in code.

### Flake 1 — command-palette 30s timeout (FIXABLE, this PR)

- `executeCommandWithCommandPalette` self-retries `toPass({timeout: 30_000})` (`pages/commands.ts:148`). Run-suite cmd (`apexTestSuite.headless.spec.ts:163`) races extension-command registration → `executeCommand` throws "not found" until registered.
- Fix: gate on registration with `verifyCommandExists` before the call.
- `verifyCommandExists` opens its own palette and closes it (`commands.ts:209` via `retryCommandPaletteSearch`). Both helpers open fresh palettes — gate value is "command registered," not "palette open." Sequence: gate → `executeCommandWithCommandPalette` (fresh palette; its 30s `toPass` then succeeds immediately).

### Flake 2 — trace ENOENT (NOT fixable, retry-mitigated)

- `ENOENT open '.../test-results/.playwright-artifacts-N/<hash>.zip'` at trace-zip write on worker teardown.
- Root cause = worker/Electron death during teardown, NOT trace volume. Evidence:
  - Zip path `outputDir/.playwright-artifacts-${workerIndex}` (`playwright/lib/runner/index.js:5012`); dir removed at worker `onExit` (`:5025`). Write at `workerProcessEntry.js:2092` races dir-removal when worker is killed.
  - CI logs show `-0` then `-1` suffix = worker restarted = crashed (run 28025922405).
  - Upstream identical report microsoft/playwright#31358 — closed **not planned**, no fix.
  - #31437's fix (existsSync filter) is ALREADY in 1.60 at `workerProcessEntry.js:2186` — so it is not the cause; prior plan's "31437 recurs on 1.60" was wrong.
- Independent of Flake 1: run 28011873444 logged ENOENT, no timeout, passed on retry (marked flaky).
- No code fix exists. `retries: 2` (`createDesktopConfig.ts:47`) already absorbs it. `trace: 'on-first-retry'` would only hide the symptom log AND lose success traces (rejected — traces wanted on success). `use.tracesDir` does NOT relocate the failing write (zip path bound to `outputDir/.playwright-artifacts-N`; `_tracesDir` at `workerProcessEntry.js:1942` feeds only the live trace).

## Phase — readiness gate before run-suite command

- Insert before run-suite `executeCommandWithCommandPalette` (`:163`): `verifyCommandExists(page, packageNls.apex_test_suite_run_text, 30_000)`.
- Files: `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/apexTestSuite.headless.spec.ts`
- No shared-config change. No trace-mode change.
- Commit: `test(apex-testing): readiness wait before run-suite palette cmd - W-23127989`

## Verification

- lint + compile changed package [not e2e-covered]
- run-suite stability over CI runs — **e2e-covered**: `apexTestSuite.headless.spec.ts` (desktop via `apexTestingE2E.yml`)
- ENOENT will still appear in logs on worker crash; that is expected and retry-absorbed, not a regression
