# W-23450773 — apex-log composition-style + services-consumption refactor

## Context

- Align `salesforcedx-vscode-apex-log` w/ `effect-best-practices` composition-style + `services-extension-consumption`.
- Mechanical; 12 playwright specs cover behavior (`test/playwright/specs/*.headless.spec.ts`).
- Base shared factory: `@salesforce/effect-ext-utils` `buildAllServicesLayer(context, fallbackName)` (`packages/effect-ext-utils/src/allServicesLayer.ts`).
- `SettingsService` already in `prebuiltServicesDependencies` (services `index.ts:101,386`); static accessor `SettingsService.getValue(section, key, default)`.
- `UserCancellationError` reachable via `promptService.considerUndefinedAsCancellation` (`promptService.ts:85`) — no direct constructor import needed.
- Sibling migration precedent: apex-oas `services/extensionProvider.ts` (base layer + `Layer.merge` extras).

### Scope correction
- WI item "traceFlagJsonSync.ts:76 bare Error → TaggedError" ALREADY DONE in commit 09a8d3420 (now `UserSearchError`). No-op; skip.

### Out of scope (separate WIs)
- `traceFlagActive.ts:14` `Date.now()` → Clock (ripples to sync `.filter` callers).
- `apexLogState.ts` EXISTING module-singleton + `Effect.runSync` for trace-flag/collector refs (needs runtime redesign post-factory). NOTE: new `KnownLogIdsRef` tag added this WI is additive + singleton-free (Phase 2) — does not touch the existing getOrCreate singletons.

## Phases

### Phase 1 — composition-style flips
Files:
- `src/traceFlags/traceFlagsCodeLensProvider.ts` (~170): flip `getRuntime().runPromise(effect.pipe(...))` → `effect.pipe(..., getRuntime().runPromise)` (terminal runner; `ManagedRuntime.runPromise` closure-based, point-free safe).
- `src/traceFlags/traceFlagsContentProvider.ts` (~122): same terminal-runPromise flip.
- `src/commands/executeAnonymous.ts` (~49): replace `Effect.sync(() => void showInformationMessage(...).then(...))` tail w/ `Effect.tap` on success value; open-log branch as `Effect.flatMap`/tap using `api.services.FsService.showTextDocument`, keep in pipe (no `.then` after run).

commit: `refactor(apex-log): terminal-runPromise + tap composition style - W-23450773`

### Phase 2 — services consumption
Files:
- `src/services/apexLogState.ts`: add new Context tag `KnownLogIdsRef = Context.GenericTag<Ref.Ref<HashSet.HashSet<string>>>('KnownLogIdsRef')` (co-located w/ `CurrentTraceFlags`/`LogCollectorStateRef`). No module-singleton getOrCreate — knownIds is only touched by the activation-run stream (commands never read/write it), so cross-run sharing isn't needed (unlike the trace-flag/collector refs). `import * as Ref from 'effect/Ref'`, `import * as HashSet from 'effect/HashSet'`.
- `src/services/extensionProvider.ts`: drop inline `Layer.unwrapEffect` factory + local `ExtensionProviderServiceLive`; import base `buildAllServicesLayer` from `@salesforce/effect-ext-utils`; re-export apex-log `buildAllServicesLayer(context, fallbackName)` = `Layer.merge(base(context, name), apexLogServicesLayer)` (apex-oas pattern). apexLogServicesLayer = `Layer.mergeAll(TraceFlagsContentProviderService.Default, Layer.sync(CurrentTraceFlags, getOrCreateTraceFlagRefreshSubscriptionRef), Layer.sync(LogCollectorStateRef, getOrCreateLogCollectorStateRef), Layer.sync(KnownLogIdsRef, () => Ref.unsafeMake(HashSet.empty<string>())))`. (`Ref.unsafeMake` verified in `node_modules/effect/dist/dts/Ref.d.ts:249`; plain build — no `Effect.runSync` singleton, so no dependency on the out-of-scope apexLogState runtime redesign.)
- `src/index.ts` (~49): call `buildAllServicesLayer(context, 'Salesforce Apex Log')` (2nd arg = current hardcoded default; package.json displayName matches).
- `src/services/runtime.ts`: type import unchanged (still `typeof buildAllServicesLayer` from `./extensionProvider`).
- `src/traceFlags/traceFlagJsonSync.ts` (~23): `readDefaultDurationMinutes` → `SettingsService.getValue('salesforcedx-vscode-apex-log', 'traceFlagsDefaultDurationMinutes', 30)`.
- `src/logs/logAutoCollect.ts` (~104): replace sync `getPollIntervalSeconds()` w/ effectful read `SettingsService.getValue('salesforcedx-vscode-apex-log', 'logPollIntervalSeconds', 30)`. Ripple: 3 call sites (SubscriptionRef.make ~115, runForEach set ~121, debounce ~158). Read once into `const` for build-time debounce+initial interval; re-read effectfully in the settings-change `runForEach`. `calculateDebounce` stays pure (takes number).

commit: `refactor(apex-log): shared buildAllServicesLayer + SettingsService reads - W-23450773`

### Phase 3 — errors + types
Files:
- `src/errors/commandErrors.ts` (14): `OpenLogsFolderError` `cause: Schema.instanceOf(Error)` → `cause: Schema.optional(Schema.Unknown)` (sibling convention, e.g. `services/src/errors/*`).
- `src/commands/openLogsFolder.ts` (~24): simplify — pass raw `e` as `cause` (drop `isError(e) ? e : new Error(...)` + `isError` import), matches optional-unknown.
- `src/logs/logAutoCollect.ts` — `knownIdsRef` params-as-deps fix + `Set`→`HashSet`:
  - **Params vs deps** (SKILL.md 'Params vs Dependencies'): `collectNewLogs` currently receives `knownIdsRef` + `collectorRef` as params (shared infra threaded from `createLogAutoCollect` call sites, line 38/159) — WRONG. Drop both params; yield from context inside `collectNewLogs`: `const collectorRef = yield* LogCollectorStateRef` (tag already exists), `const knownIdsRef = yield* KnownLogIdsRef` (new tag from apexLogState.ts). `collectNewLogs` becomes a no-arg `Effect.fn`; call site (line 159) → `collectNewLogs()`.
  - `createLogAutoCollect`: drop `const knownIdsRef = yield* Ref.make(new Set<string>())` (line 111) — no longer built locally; the org-change reset fork (line 126-133) instead yields `const knownIdsRef = yield* KnownLogIdsRef` and resets via `Ref.set(knownIdsRef, HashSet.empty<string>())`. (Reset still needs the ref in scope for the fork; yielding the tag is the correct source.)
  - `Set<string>` → `HashSet<string>` throughout: `HashSet.has`, `HashSet.add` (immutable, drops the `new Set(set).add()` copy at line 96), `HashSet.empty<string>()` reset. Leave `execAnonIds` (external `Set` from `getExecAnonLogIds`); the `.filter` at line 77 stays `execAnonIds.has(...)` for the JS Set + `HashSet.has(knownIds, l.Id)` for the HashSet.
- `src/commands/traceflags/traceflagsCommands.ts` (~178): replace `Effect.fail(undefined)` sentinel + `catchAll(() => succeed(undefined))` w/ `promptService.considerUndefinedAsCancellation(p)` + `Effect.catchTag('UserCancellationError', () => Effect.succeed(undefined))`. Requires `promptService` in scope (yield from `api.services.PromptService`).

commit: `refactor(apex-log): optional-unknown cause, KnownLogIdsRef dep + HashSet knownIds, UserCancellationError sentinel - W-23450773`

## Skills to apply
- `effect-best-practices` (composition-style ref, errors, HashSet)
- `services-extension-consumption` (shared layer, prebuilt SettingsService)
- `typescript` (naming/casing, coding standards for touched .ts files)
- `concise` (this plan)
- `verification`

## Verification
- Not e2e-covered (run locally):
  - `npx effect-language-service diagnostics --project tsconfig.json` (apex-log) — address warnings + messages.
  - eslint + `tsc` for apex-log (no `functional/*`, `local/*` regressions; confirm no new eslint-disable).
  - jest: `test/jest/traceFlags/traceFlagsContentProvider.test.ts`, `test/unit/**` (statusBar, pickTraceFlag, pickDebugLevelToRemove, traceFlagActive).
- e2e-covered (branch playwright, don't re-verify manually):
  - executeAnonymous tap flip → `executeAnonymous.headless.spec.ts`
  - codelens/content-provider runPromise flips → `traceFlagsCrud.headless.spec.ts`, `traceFlagsForOtherUser.headless.spec.ts`, `traceFlagExpiry.headless.spec.ts`
  - logAutoCollect settings read + HashSet + KnownLogIdsRef dep wiring (collectNewLogs yields tags from ctx; org-change reset) → `autoCollection.headless.spec.ts`, `logRetrieval.headless.spec.ts` (dedup across polls + org-switch clear both exercise knownIds through the new tag)
  - shared-layer migration (activation) → all specs (activation gate)
  - openLogsFolder cause → no dedicated spec; rely on tsc + jest
