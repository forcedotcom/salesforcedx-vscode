# W-24093888: Record pnpm cutover decisions

## Context

Record why the repository cut over from npm to pnpm and preserve the supported decisions in a repo-wide ADR. Supersede the existing npm-workspaces decision so the ADR set does not give conflicting guidance.

Files:

- `docs/adr/0022-pnpm-cutover.md`
- `docs/adr/0001-npm-workspaces-wireit.md`

## Phases

### 1. Record the pnpm cutover

- Add `docs/adr/0022-pnpm-cutover.md` using the repository's short ADR format.
- State only cutover decisions and rationale supported by repository configuration and history.
- Update `docs/adr/0001-npm-workspaces-wireit.md` to identify ADR 0022 as its superseding decision.
- Commit message: `docs: record pnpm cutover decisions`

## Skills to apply

- `domain-modeling` — ADR scope, numbering, and decision-focused content.
- `concise` — short, non-repetitive Markdown.
- `verification` — verify the documentation change after editing.

## Verification

- Confirm ADR 0022 follows `docs/adr/README.md`: sequential filename, short title, and concise decision rationale.
- Confirm ADR 0001 points to ADR 0022 and no longer reads as current npm guidance.
- Run `npm run check:links` from the repository root.
- Review `git diff --check` and the final Markdown diff.
