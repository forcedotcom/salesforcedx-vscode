# W-23573459

## Context

Adopt `effect/Order` for remaining inline, hand-written multi-key and mechanical sort comparators. Replace comparator arithmetic and branching with named orders while preserving each sort key, key priority, direction, and existing array mutation behavior.

## Phases

### 1. Compose Apex report orders

- Change `packages/salesforcedx-apex/src/reporters/markdownTextFormatTransformer.ts`.
- Define `Order.mapInput`, `Order.reverse`, and `Order.combine` orders for runtime, coverage, and severity.
- Use the composed orders for runtime-first and coverage-first test sorting; use the same primitive orders for warning and coverage-table sorting.
- Preserve current defaults, tie-break priority, and ascending/descending direction.
- Commit: `refactor(apex): compose report sorting with Effect Order`

### 2. Replace mechanical comparators

- Replace single-key numeric, string, and date comparator callbacks with named `effect/Order` values in:
  - `scripts/analyzeE2E.ts`
  - `scripts/xsd/fixXsdErrors.ts`
  - `packages/salesforcedx-vscode-lwc/src/testSupport/testExplorer/lwcTestController.ts`
  - `packages/salesforcedx-vscode-apex/src/languageServerScanConfig.ts`
  - `packages/salesforcedx-lwc-language-server/src/javascript/typeMapping.ts`
  - `packages/salesforcedx-aura-language-server/src/aura-indexer/indexer.ts`
- Add `effect` to `packages/salesforcedx-aura-language-server/package.json`; regenerate `package-lock.json` with the root npm install.
- Preserve each sort key, direction, and in-place versus copied-array behavior.
- Commit: `refactor: replace mechanical sort comparators with Effect Order`

## Skills to apply

- `effect-best-practices`: idiomatic `Order` construction and composition.
- `typescript`: imports, types, and naming.
- `packageJson`: Aura dependency and lockfile conventions.
- `verification`: repository checks and package-specific test selection.

## Verification

- Root stop-hook checks: compile, lint, Effect diagnostics, unit tests, VS Code bundles, and knip.
- `npm run test:scripts`
- `npm run test -w @salesforce/apex-node`
- `npm run test -w @salesforce/salesforcedx-lwc-language-server`
- `npm run test -w @salesforce/salesforcedx-aura-language-server`
- `npm run test:web -w salesforcedx-vscode-lwc -- --retries 0`
- `npm run test:desktop -w salesforcedx-vscode-apex -- --retries 0`
- `npm run check:dupes`
