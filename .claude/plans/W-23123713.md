# W-23123713 — Remove unused files (5 files)

## Context
- Dead-code scan: 268 unused; 5 confirmed real dead code (6th = PR 7527).
- Each deletion target shares a basename with an ACTIVE sibling file in another package. The active siblings are package-local copies imported via relative path (`./x`), NOT the deletion targets. Verified monorepo-wide (Phase 0).
- Goal: delete 5 files, no other code change.

## Phase 0 — cross-package verification (evidence, no code change)
Per target: grep every exported symbol across `packages/**/*.ts` (excluding `/out/`, target itself). Any hit must resolve to same-named sibling on different module path, not target. Evidence below.

### 1. `effect-ext-utils/src/extensionContext.ts`
Exports: `getExtensionContext`, `setExtensionContext`, `ExtensionContextNotAvailableError`
- All importers resolve to services' `salesforcedx-vscode-services/src/vscode/extensionContext.ts` via `./extensionContext` / `../vscode/extensionContext` (index.ts:55, appInsights.ts:10, extensionContextService.ts:12)
- `ExtensionContextNotAvailableError` importers resolve to services' `vscode/extensionContextErrors.ts`
- Target not in effect-ext-utils barrel (`index.ts`); no relative import `./extensionContext` within effect-ext-utils
- 0 importers of target → safe. (Refutes advocate finding 1: services imports own copy, not this)

### 2. `apex-replay-debugger/src/commands/range.ts`
Exports: `getZeroBasedRange`
- Only importer `apex-testing/src/commands/apexTestRunCodeAction.ts:30` imports `./range` = `apex-testing/src/commands/range.ts` (identical content, package-local)
- 0 importers within apex-replay-debugger; 0 cross-package imports of target path
- Safe. (Refutes advocate finding 2)

### 3. `apex-replay-debugger/src/utils/coreExtensionUtils.ts`
Exports: `getVscodeCoreExtension`
- Importers `apex/src/languageServer.ts:21`, `apex/src/index.ts:20` import `./coreExtensionUtils` = `apex/src/coreExtensionUtils.ts` (package-local, memoized variant)
- 0 importers within apex-replay-debugger; 0 cross-package imports of target path
- Safe. (Refutes advocate finding 3)

### 4. `metadata/src/commands/retrieveStart/retrieveErrors.ts`
Exports: `SourceTrackingFailedError`. 0 refs anywhere in repo. Safe to delete.

### 5. `org-browser/src/tree/metadataTypeTreeProviderTypes.ts`
Exports: `type MetadataTypeTreeProvider` (a `{ fireChangeEvent }` type alias for circular-dep avoidance)
- All `MetadataTypeTreeProvider` refs (index.ts:25, retrieveMetadata.ts:7) import CLASS from `./tree/metadataTypeTreeProvider`, not this type alias from `metadataTypeTreeProviderTypes`
- 0 imports of `metadataTypeTreeProviderTypes` module path. Safe.

Scan command (run per target before deleting):
`grep -rn "\b<symbol>\b" packages --include="*.ts" | grep -v /out/ | grep -v <target>` → every hit must be a different module path.

## Phase 1 — delete files (1 commit)
Delete (all 5 confirmed orphaned in Phase 0):
- `packages/effect-ext-utils/src/extensionContext.ts`
- `packages/salesforcedx-vscode-apex-replay-debugger/src/commands/range.ts`
- `packages/salesforcedx-vscode-apex-replay-debugger/src/utils/coreExtensionUtils.ts`
- `packages/salesforcedx-vscode-metadata/src/commands/retrieveStart/retrieveErrors.ts`
- `packages/salesforcedx-vscode-org-browser/src/tree/metadataTypeTreeProviderTypes.ts`

Commit msg:
`chore: remove 5 unused dead-code files - W-23123713`

## Skills
- typescript — confirm builds still pass post-delete
- verification — test that affected packages compile

## Verification
- `git rm` 5 files.
- Re-run Phase 0 scan post-delete: 0 unresolved hits to deleted module paths.
- Build affected packages (tsc) — no unresolved-import errors:
  effect-ext-utils, services, apex-replay-debugger, apex-testing, apex, metadata, org-browser
  (include the sibling packages that own the active same-named copies, to prove no accidental breakage).
- No e2e coverage (pure deletions; no runtime behavior change).
