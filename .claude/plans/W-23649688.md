# W-23649688 — Extend `no-explicit-effect-return-type` to `Layer.Layer`

## Context

- Extend the local ESLint rule beyond explicit `Effect.Effect` return annotations to explicit `Layer.Layer` return annotations.
- Preserve the rule's purpose: rely on TypeScript inference for Effect return types.
- Files: the rule, its test, and existing `salesforcedx-vscode-services` functions with explicit `Layer.Layer` return annotations.

## Phases

1. Update `noExplicitEffectReturnType.ts` to identify and remove explicit `Layer.Layer` return annotations through the rule's existing detection and fixer path.
   - Commit: `feat(eslint): check explicit Layer return types - W-23649688`
2. Add a focused rule case covering an explicit `Layer.Layer` return and fixer output.
   - Commit: `test(eslint): cover explicit Layer return types - W-23649688`
3. Remove explicit `Layer.Layer` return annotations from existing functions linted by the expanded rule.

## Skills to apply

- `typescript`: follow repository TypeScript and naming conventions.
- `verification`: use targeted tests, then repository checks required by the stop hook.

## Verification

1. Run the focused `no-explicit-effect-return-type` rule test.
2. Run the `eslint-local-rules` package test suite.
3. Run lint for `salesforcedx-vscode-services`.
4. Confirm compile, lint, Effect LS, tests, bundle, and knip pass through the repository stop hook.
