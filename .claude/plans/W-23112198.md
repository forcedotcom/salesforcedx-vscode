# W-23112198 — Remove unused SalesforcePackageDirectories

## Context

- Dead code removal
- `packages/salesforcedx-vscode-core/src/salesforceProject/salesforcePackageDirectories.ts` — zero callers
- Not imported anywhere, not in `index.ts`, no external consumers
- No test file exists (WI mentions tests; none present)
- Dependency `SalesforceProjectConfig` retained — many other consumers

## Phases

### Phase 1 — delete dead file
- Delete `packages/salesforcedx-vscode-core/src/salesforceProject/salesforcePackageDirectories.ts`
- Only file referencing class; no other edits needed
- commit: `chore(core): delete unused SalesforcePackageDirectories - W-23112198`

## Skills

- typescript

## Verification

- `grep -rn "SalesforcePackageDirectories" packages --include='*.ts'` → no hits
- core compiles: `npm run compile` in salesforcedx-vscode-core (or wireit build)
- not e2e-covered (dead code, no runtime path)
