# W-23112198 — Remove unused SalesforcePackageDirectories

## Context

- `salesforcePackageDirectories.ts` — no callers
- No imports, no external consumers
- No test file (WI mentions tests)
- `SalesforceProjectConfig` retained (has other consumers)

## Phases

### Phase 1 — delete dead file
- Delete `salesforcePackageDirectories.ts`
- Only file referencing class; no other edits needed
- commit: `chore(core): delete unused SalesforcePackageDirectories - W-23112198`

## Skills

- typescript

## Verification

- `grep -rn "SalesforcePackageDirectories" packages --include='*.ts'` → no hits
- core compiles: `npm run compile` (or wireit build)
- not e2e-covered (dead code, no runtime path)
