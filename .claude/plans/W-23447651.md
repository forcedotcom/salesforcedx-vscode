# W-23447651 — Remove dead code from vscode-org

## Context

2 test-only symbols in `salesforcedx-vscode-org`, zero prod callers. Both artifacts of Effect migration (W-23069593).

1. **`runGatherer`** — `src/parameterGatherers/runGatherer.ts`. Adapter wrapping gatherer Effect → legacy `ParametersGatherer` shape (`Promise<CancelResponse | ContinueResponse<T>>`). Prod `yield*`s gatherers directly (orgLoginWeb.ts:31, orgLoginAccessToken.ts:17); CONTINUE/CANCEL wrapper unused. Not in index.ts. Only caller: `authParamsGatherer.test.ts` (10 call sites).
2. **`extractPortConflictCliMessage`** — `src/util/authErrorParser.ts:20`. Parses `PortInUseError` CLI string block; nothing consumes. Only caller: `authErrorParser.test.ts` (2 asserts, lines 30-46). Siblings `isAuthPortConflictError` + `getPortKillInstructions` stay live (orgLoginWebExec.ts:14).

## Phases

### Phase 1 — delete runGatherer, rework its test
- delete `src/parameterGatherers/runGatherer.ts`
- `test/jest/commands/auth/authParamsGatherer.test.ts`:
  - drop `runGatherer` import (line 23)
  - run gatherer Effect directly via `getOrgRuntime().runPromiseExit(...)` (already imports `resetOrgRuntimeForTesting`/`setAllServicesLayer`; add `getOrgRuntime` from extensionProvider, `Exit` from effect)
  - CONTINUE cases → assert `Exit.isSuccess` + value = former `data` (drop `{type:'CONTINUE',data}` wrapper)
  - CANCEL cases (lines 123-137) → assert `Exit.isFailure` w/ `UserCancellationError` tag (replaces removed shim)
  - `validateInput` test (139-158) unchanged except drop wrapper
- verify parameterGatherers dir not orphaned (other files remain) — else no dir cleanup needed
- commit: `refactor(org): remove dead runGatherer adapter - W-23447651`

### Phase 2 — delete extractPortConflictCliMessage + its tests
- `src/util/authErrorParser.ts`: delete `extractPortConflictCliMessage` (lines 20-26). Keep `isAuthPortConflictError`, `getPortKillInstructions`, `PORT_CONFLICT_PATTERNS`, `nls` import (still used by getPortKillInstructions)
- `test/jest/util/authErrorParser.test.ts`: drop import (line 9) + 2 `it` blocks (lines 30-46)
- commit: `refactor(org): remove dead extractPortConflictCliMessage - W-23447651`

## Skills
- effect-best-practices (Exit/runPromiseExit assertion pattern in reworked test)
- typescript (unused-import hygiene post-deletion)
- verification

## Verification
- `npm run compile` (or tsc) in salesforcedx-vscode-org — no dangling refs
- jest: `authParamsGatherer.test.ts` + `authErrorParser.test.ts` pass
- `grep -rn "runGatherer\|extractPortConflictCliMessage" packages/salesforcedx-vscode-org` → 0 hits
- lint (no unused imports/no-floating)
- not e2e-covered; unit-only WI
