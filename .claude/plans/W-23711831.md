# W-23711831

## Context

Refactor the Standard project-template selection in the 3 metadata project-creation desktop E2E tests. Replace direct quick-input row location, waiting, and clicking with the shared `selectQuickInputOption` helper. This keeps quick-input interaction in the Playwright helper used by other E2E tests.

Files:

- `packages/salesforcedx-vscode-metadata/test/playwright/specs/createProject.desktop.spec.ts`
- `packages/salesforcedx-vscode-metadata/test/playwright/specs/createProjectEmptyWindow.desktop.spec.ts`
- `packages/salesforcedx-vscode-metadata/test/playwright/specs/createProjectWithManifest.desktop.spec.ts`

## Phases

### 1. Use `selectQuickInputOption` for Standard template selection

- Import `selectQuickInputOption` from `@salesforce/playwright-vscode-ext` in all 3 specs.
- Replace each `waitForQuickInputFirstOption` plus `QUICK_INPUT_LIST_ROW` locator/click sequence with one `selectQuickInputOption` call for the Standard option, retaining the existing timeouts.
- Remove imports made unused by the refactor.

Commit: `refactor: use quick input helper for standard project template`

## Skills to apply

- `playwright-e2e`: E2E interaction and test execution conventions.
- `typescript`: TypeScript imports and formatting.
- `verification`: repository verification requirements.

## Verification

1. Run the metadata desktop Playwright suite from the repository root: `npm run test:desktop -w salesforcedx-vscode-metadata -- --retries 0`.
2. Confirm compile, lint, tests, bundle, and knip pass through the repository stop hook.
