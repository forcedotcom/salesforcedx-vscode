# W-22865165 — enable jest/prefer-to-be

## Context
- Add `'jest/prefer-to-be': 'error'` to jest rules block in `eslint.config.mjs`.
- Blocker W-11678119 satisfied: `jest/no-focused-tests` present at line 562.
- `eslint-plugin-jest` 28.14.0 installed + wired for all test globs. No plugin add.
- Rule is `fixable: code` — all violations autofixable (verified `--fix` → 0 errors).
- Verified violation count via per-package eslint (each package lints from own dir for tsconfig type-info): 136 violations, 13 packages.

## Phases (1 commit)

### Phase 1 — enable rule + autofix violations
- Edit `eslint.config.mjs`: add `'jest/prefer-to-be': 'error',` after line 562 (`jest/no-focused-tests`).
- Autofix all packages. From repo root, per package dir with lint script:
  `eslint --no-cache --fix .` (each package; root-level `eslint packages` fails — package.json files need per-package tsconfig).
- Affected packages (count): apex-debugger(9), apex-replay-debugger(8), aura-language-server(7), lightning-lsp-common(1), lwc-language-server(51), utils-vscode(7), utils(1), vscode-apex-oas(2), vscode-apex-replay-debugger(1), vscode-core(8), vscode-i18n(9), vscode-lwc(1), vscode-soql(31).
- Confirm 0 remaining `jest/prefer-to-be`.
- commit msg: `style(eslint): enable jest/prefer-to-be rule and autofix violations`

## Skills
- verification
- packageJson (none expected; rule is config-only)
- typescript

## Verification
- `npm run lint` passes (full wireit lint). Not e2e-covered.
- `git grep "jest/prefer-to-be" eslint.config.mjs` → 1 hit, `error`.
- No `jest/prefer-to-be` in lint output across packages.
- Spot-check diff: only `.toBe(...)` rewrites of `.toEqual`/`.toStrictEqual` on primitive literals; no logic change.
