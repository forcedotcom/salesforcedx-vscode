# W-22973351 — Migrate auraLsp e2e WDIO → Playwright

## Context
- Port `packages/salesforcedx-vscode-automation-tests/test/specs/auraLsp.e2e.ts` (WDIO, 3 tests: indexing status bar, Go to Definition, Autocompletion) → Playwright desktop specs in `packages/salesforcedx-vscode-lightning/test/playwright/specs/`.
- Lightning pkg: desktop config + fixtures + auraRename/auraTemplates specs exist; no LSP coverage. Desktop-only (no `browser` entry).
- Precedent: apexLsp `salesforcedx-vscode-apex/test/playwright/{specs/apexLsp.desktop.spec.ts,utils/apexLspUtils.ts,fixtures/desktopFixtures.ts}`; lwcLsp `salesforcedx-vscode-lwc/test/playwright/specs/lwcLsp*.headless.spec.ts`.
- Aura status item: `lightning/src/auraLspStatusBarItem.ts` → `createLanguageStatusItem('auraLanguageServerStatus')`, ready text `Indexing complete $(check)` (i18n `aura_language_server_loaded`). Same surface as LWC/Apex → reuse `getByRole('button', {name:/Indexing complete/})`.
- aura1.cmp template (from test-tools `createAura`, `lwcUtils.ts:141`):
  - L1 `<aura:component>`
  - L3 `\t<aura:attribute name="simpleNewContact" type="Object"/>` (def site, col ~27 = inside `simpleNewContact`)
  - L8 `\t\t{!v.simpleNewContact}` (ref site, WDIO cursor 8:15)
- WDIO go-to-def = within-file (ref L8 → def L3). reloadWindow workaround dropped: pre-seed onto disk pre-launch (apex precedent), Aura LS indexes on startup scan.
- Lightning fixtures currently DON'T seed files / don't extend `workspaceDir` (`fixtures/desktopFixtures.ts` = bare `createDesktopTest`). Must add seed + gotoLocation:'goto' settings (apex `desktopFixtures.ts:54-65,70-87` pattern).
- CI auto-covered: `.github/workflows/auraE2E.yml` runs `npm run test:desktop -w salesforcedx-vscode-lightning` over `./specs` glob → new specs auto-included. No workflow edit.
- WDIO spec not referenced in any suite/wdio.conf (glob-discovered) → safe delete.
- Aura folder layout: `force-app/main/default/aura/aura1/aura1.cmp` (+ `aura1.cmp-meta.xml`).

## Phases

### Phase 1 — fixture: seed aura1 bundle + gotoLocation settings
- Files: `packages/salesforcedx-vscode-lightning/test/playwright/fixtures/desktopFixtures.ts`
- Add `AURA_CMP` const (10-line `createAura` component template verbatim; layout load-bearing: L3 def, L8 ref). Add `AURA_CMP_META` (`<AuraDefinitionBundle>` xml).
- Convert `noOrgDesktopTest` `createDesktopTest` call: add `userSettings` w/ `editor.gotoLocation.multiple*: 'goto'` + `alternativeDefinitionCommand: ''` (peek→goto, apex precedent).
- `.extend<{workspaceDir:string}>` overriding `workspaceDir`: mkdir `force-app/main/default/aura/aura1`, write `aura1.cmp` + `aura1.cmp-meta.xml` pre-launch.
- Keep `noOrgDesktopTest` export name (fixtures/index.ts re-exports it as `test`).
- Commit: `test(lightning): pre-seed aura1 bundle + goto settings for aura LSP playwright specs`

### Phase 2 — auraLspUtils (new utils dir for lightning)
- Files: `packages/salesforcedx-vscode-lightning/test/playwright/utils/auraLspUtils.ts` (new — lightning has NO `utils/` dir yet, confirmed: only `fixtures/` + `specs/`; create it).
- Export `waitForAuraLspReady(page, timeout=120_000)`: `expect(page.locator(WORKBENCH).getByRole('button',{name:/Indexing complete/})).toBeVisible({timeout})` (+ legacy `STATUS_BAR_ITEM_LABEL` fallback, mirror `lwcUtils.waitForLwcLspReady`). No on-disk dir check (Aura LS has no StandardApexLibrary analog).
- Export `goToLineCol(page,line,col)`: copy verbatim from `lwcUtils.ts:291-298` (lightning can't cross-import lwc pkg utils; no shared util path). Body: `executeCommandWithCommandPalette(page,'Go to Line/Column...')`; wait `QUICK_INPUT_WIDGET` visible; `keyboard.type(`${line}:${col}`)`; `Enter`; wait widget hidden. Used by Phase 3 (position cursor on ref token) + Phase 4 (position at L2 C1). Resolves the "helper doesn't exist" gap — single owned copy, no inline duplication.
- Import `WORKBENCH`/`QUICK_INPUT_WIDGET`/`STATUS_BAR_ITEM_LABEL`/`executeCommandWithCommandPalette` from `playwright-vscode-ext` (same source lwcUtils uses; locators at `playwright-vscode-ext/src/utils/locators.ts`).
- Commit: `test(lightning): add auraLspUtils (waitForAuraLspReady + goToLineCol)`

### Phase 3 — Go to Definition spec
- Files: `packages/salesforcedx-vscode-lightning/test/playwright/specs/auraLspGoToDefinition.desktop.spec.ts` (new)
- `setupConsoleMonitoring`/`setupNetworkMonitoring`.
- step setup: `waitForVSCodeWorkbench`/`closeWelcomeTabs`/`ensureSecondarySideBarHidden`/`waitForWorkspaceReady` (auraRename precedent).
- step open+index: `openFileByName(page,'aura1.cmp')` → `waitForAuraLspReady`.
- step position cursor on ref site: `goToLineCol(page,8,15)` (mirrors WDIO `moveCursorWithFallback(textEditor,8,15)` — cursor inside `simpleNewContact` on ref L8). Using the command-palette cursor placement (not token hover/click) guarantees the editor has focus and the exact 8:15 position WDIO used, avoiding token-locator fragility on the templated bundle.
- step goto: `executeCommandWithCommandPalette(page,'Go to Definition')` (matches WDIO `executeQuickPick('Go to Definition')` / F12; do NOT use Ctrl+Click — apex used Ctrl+Click only because its nav is CROSS-file with a tab to assert; aura is WITHIN-file so no tab event). Belt-and-suspenders LSP-ready sync already covered by `waitForAuraLspReady`; optionally hover the L8 token first and wait `.monaco-hover` `contains('simpleNewContact')` to confirm symbol resolution before nav (cheap, prevents no-op navigation race).
- step assert cursor moved to def site (within-file, no new tab — this is the load-bearing fix):
  - PRIMARY: assert VS Code status-bar position item. `page.locator(WORKBENCH).getByRole('button',{name:'Go to Line/Column'})` (command title) has text `Ln 3, Col 27` after nav. `await expect(positionItem).toContainText(/Ln 3, Col 27/)`. This is the direct Playwright analog of WDIO `getCoordinates()` → `[3,27]` (def L3 col 27, inside `simpleNewContact`), is version-stable (status-bar selection item is long-standing), and does NOT depend on active-line CSS class behavior (which the adversary flagged as unreliable across VS Code versions for within-file nav).
  - Col exact-27 caveat: WDIO asserted `[3,27]`. If the status-bar text proves col-brittle on the seeded template (LS may land at token start vs WDIO's mid-token col), relax column assertion to `/Ln 3, Col (1?[0-9]|2[0-9])/` (line 3 is the load-bearing claim; column proves landing inside the attribute). Decide exact-vs-range only after local run; default = exact `Ln 3, Col 27` to match WDIO, fall back to range if a real run shows drift.
  - SECONDARY (defense): assert `aura1.cmp` is still the active tab (`getByRole('tab',{name:/aura1\.cmp/}).getAttribute('aria-selected','true')`) confirming within-file nav (no spurious new tab) — guards against the LS opening a peek/new editor.
  - DO NOT rely on `.cursor-line`/active-line text containing `simpleNewContact` (adversary finding: within-file nav may not flip active-line styling reliably; and a text-contains check can't distinguish def L3 from ref L8 since both contain the token).
- Verify locally before PR (adversary directive): run this spec via `npm run test:desktop -w salesforcedx-vscode-lightning` (or scoped grep) at least once to confirm the status-bar position assertion fires and the exact column; record actual `Ln/Col` in the spec comment.
- `saveScreenshot`; `validateNoCriticalErrors`.
- Commit: `test(lightning): add aura LSP go-to-definition playwright spec`

### Phase 4 — Autocompletion spec
- Files: `packages/salesforcedx-vscode-lightning/test/playwright/specs/auraLspAutocompletion.desktop.spec.ts` (new)
- setup + open + `waitForAuraLspReady` (specs independent; re-index per spec).
- step: `goToLineCol(page,2,1)` (imported from Phase 2 `auraLspUtils.ts` — owned copy, NOT inline; resolves adversary "helper doesn't exist" gap); type `<aura:appl`.
- completion rows `div.monaco-list-row.show-file-icons`; assert first row `aria-label` `/aura:application/`; click; type `>`; `File: Save`.
- assert L2 editor text contains `aura:application`. `saveScreenshot`.
- `validateNoCriticalErrors`.
- Commit: `test(lightning): add aura LSP autocompletion playwright spec`

### Phase 5 — delete WDIO spec
- Files: delete `packages/salesforcedx-vscode-automation-tests/test/specs/auraLsp.e2e.ts`
- No suite/conf ref to remove (glob-discovered; verified).
- Commit: `test(automation): delete WDIO auraLsp e2e (migrated to playwright) — W-22973351`

## Skills to apply
- playwright-e2e (writing/running/debugging; scratch-org not needed — isOrgRequired false)
- concise (plan + any .claude md)
- typescript
- verification
- changelog (test-only — likely no changelog entry; confirm)

## Verification
- e2e-covered (CI auraE2E.yml desktop matrix, Linux+Mac): indexing-complete status, go-to-definition nav, autocompletion insert. Primary validation = CI run of new specs.
- Local non-e2e:
  - `npm run compile -w salesforcedx-vscode-lightning` (tsc on new specs/utils/fixtures).
  - lint: `npm run lint -w salesforcedx-vscode-lightning`.
  - confirm `auraLsp.e2e.ts` gone; `grep -rn auraLsp packages/salesforcedx-vscode-automation-tests` empty.
  - confirm automation-tests still compiles (`npm run compile -w salesforcedx-vscode-automation-tests`).
- REQUIRED local run before PR (NOT optional — adversary directive on go-to-def): `npm run test:desktop -w salesforcedx-vscode-lightning` (grep to the go-to-def spec). Confirm: (a) status-bar position assertion locates `Ln X, Col Y` button, (b) actual cursor lands on line 3 after within-file Go to Definition, (c) the real column value — lock spec to exact col if it matches WDIO's 27, else switch to the documented range matcher. Record findings in spec comment. Autocompletion spec also run to confirm `goToLineCol(2,1)` from the new owned util works.
