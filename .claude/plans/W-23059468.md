# W-23059468 — ADR: how far Effect/shared-infra goes into CLI-consumed apex lib

## Context

- Doc-only WI. No code.
- Write ADR in ROOT `docs/adr/` capturing decision + logic: how far Effect + shared vscode-named infra (services API, effect-ext-utils, shared i18n, OTEL) may go into CLI-consumed published libs (apex-node first). REPO-WIDE scope: governs all future CLI-lib migrations + citable by external `@salesforce/templates` (separate npm pkg, outside this monorepo) → root, not package-local. Per ADR-FORMAT.md:5 repo-wide → root `docs/adr/`; package-local → `packages/*/docs/adr/`.
- `@salesforce/apex-node` = npm-published (`packages/salesforcedx-apex`, not private, v8.x), consumed by CLI plugins (plugin-apex, plugin-flow).
- ADR-FORMAT triad: hard to reverse (transitive deps to every CLI consumer); surprising (reader wonders why a CLI lib carries Effect/OTEL/i18n); real trade-off (shared infra reuse vs CLI bundle/dep weight).
- Anchor WI: referenced by Effect-OTEL, HeapMonitor, streaming, residual-Effect, Effect-test, shared-i18n WIs.
- Generic (apex = first; templates likely next): citable by future lib migrations.
- Blocked by W-23051181 (verify done before merge; plan-authoring unblocked).

### Current state (verified)

- `packages/salesforcedx-apex` runtime deps: `@salesforce/core`, kit, ts-types, fast-glob, faye, istanbul-*, json-stream-stringify. No Effect, no effect-ext-utils, no services.
- Root `docs/adr/` highest = `0020-soql-model-internal-package.md` → next ADR = `0021` (ADR-FORMAT.md:25 highest existing + 1).
- Root ADR precedent: `docs/adr/0009-reduce-cli-deps-web-no-cli.md` etc. — repo-wide boundary decisions live at root.

### Cite-able existing ADRs

- root 0008 — services = sole host of heavy Salesforce deps (vscode-side boundary).
- root 0009 — fewer CLI shell-outs; web must avoid CLI.
- root 0011 — Effect-TS runtime (umbrella).
- root 0012 — spans-only observability (OTEL).
- root 0007 — reuse `prebuiltServicesDependencies` (services consumption — vscode-only, N/A to CLI).
- format: `.claude/skills/grill-me/ADR-FORMAT.md`; conventions `docs/adr/README.md`.

### Key distinction ADR must draw

- vscode-side: route heavy deps through services API (root 0008). NOT available to CLI — no services extension/runtime in `sf`.
- CLI-side: apex-node is a plain published npm lib. Anything in its runtime `dependencies` ships transitively to plugin-apex/plugin-flow.
- Boundary: which of {Effect core, OTEL, shared i18n, effect-ext-utils, vscode-services-types} may be runtime deps vs must stay vscode-only / peer / dev.

## Phases

### Phase 1 — author ADR (single commit)

- Create `docs/adr/0021-effect-shared-infra-boundary-in-cli-libs.md` (ROOT, not package-local — repo-wide scope).
- Generic title (not apex-specific) so future libs cite it. e.g. "How far Effect/shared infra goes into CLI-consumed libraries".
- Follow ADR-FORMAT: short title + tight prose; `## Considered Options` + `## Consequences` earned here (real rejected alts + non-obvious transitive effects).
- Content:
  - decision: allowed depth of Effect/OTEL/i18n/effect-ext-utils/services into a published, CLI-consumed lib; state the boundary (what MAY be runtime dep vs MUST stay vscode-only/peer/dev).
  - logic/trade-off: transitive dep flow to every CLI consumer (plugin-apex, plugin-flow); bundle/install weight; can't use vscode services API from CLI (contrast root 0008); CLI has no browser-target constraint but no services host either.
  - considered options: (a) full Effect+services like vscode extensions — rejected: no services runtime in CLI, heavy transitive deps; (b) zero shared infra, keep pre-Effect — rejected: blocks Effect-OTEL/streaming/i18n WIs; (c) chosen middle — defined Effect/observability surface as runtime dep, vscode-named infra stays out / peer.
  - consequences: deps flow transitively to CLI; sets precedent cited by `@salesforce/templates` (external repo — cite by ADR number/title + repo path, not relative link) + future monorepo migrations; note relationship to the 6 referencing WIs.
  - generic framing + explicit note: apex = first mover, templates likely next (TBD).
  - cross-link root ADRs 0008/0009/0011/0012 as SIBLINGS now that this ADR is at root: relative paths `./0008-...md` (same dir, matches existing root ADR style e.g. 0011 links `./0004-...md`). ADR-FORMAT path = `../../.claude/skills/grill-me/ADR-FORMAT.md` (root ADR dir is `docs/adr/` → two `..` to repo root, verified vs 0011); README = `./README.md`.
- concise skill applies to ADR body.
- commit msg: `docs(adr): boundary for Effect/shared-infra in CLI-consumed libs - W-23059468` (repo-wide scope, not apex-package-scoped).

## Skills to apply

- concise — ADR body + this plan.
- effect-best-practices — correct Effect/services/OTEL terminology in ADR.
- external-consumers — frame CLI plugin consumers (plugin-apex, plugin-flow) as public-contract transitive consumers.
- packageJson — accurate runtime vs peer vs devDependency framing.
- work-item-sequencing — confirm this lands before the 6 referencing WIs; W-23051181 blocker.
- typescript — TS type/dep terminology (runtime dep vs devDependency vs peer; type imports in ADR context).
- grill-me (ADR-FORMAT) — triad + template compliance.

## Verification

- not e2e-covered (doc-only; no e2e on branch).
- ADR at ROOT `docs/adr/` (repo-wide scope per ADR-FORMAT.md:5), NOT package-local.
- sibling links to root ADRs resolve (`./0008-...md` same dir); ADR-FORMAT link `../../.claude/skills/grill-me/ADR-FORMAT.md` resolves (verified depth vs existing 0011); README `./README.md`; match existing root ADR link style.
- external `@salesforce/templates` citability: ADR is at root + cited by number/title (works cross-repo); confirm no package-local-only assumption remains.
- markdownlint / repo md lint passes on new file.
- ADR meets FORMAT triad (hard-to-reverse, surprising, real trade-off) — all hold.
- title generic, citable by future libs (no apex-only wording).
- numbering = `0021` (root highest 0020 + 1; re-check no concurrent 0021 landed before merge).
- confirm W-23051181 closed before merge (gus-cli).
