# W-23527536

## Context

`packages/salesforcedx-lwc-language-server/test/javascript/typeMapping.test.ts` uses `@lwc/old-compiler` only to produce expected metadata during tests. That dev dependency brings the vulnerable `js-yaml` paths identified by GHSA-8j8c-7jfh-h6hx and GHSA-mh29-5h37-fv8m. Replace the runtime comparison with committed Jest snapshots, preserving coverage of the existing `metadata.js` and `nogetter.js` fixtures while removing `@lwc/old-compiler` from the dependency tree.

## Phases

### 1. Replace the old compiler oracle with snapshots

Commit: `fix: remove old LWC compiler test dependency`

- Update `packages/salesforcedx-lwc-language-server/test/javascript/typeMapping.test.ts` to snapshot `mapLwcMetadataToInternal` results for both existing fixtures; remove old-compiler imports, options, transforms, and comparisons.
- Add `packages/salesforcedx-lwc-language-server/test/javascript/__snapshots__/typeMapping.test.ts.snap` with the expected mapped metadata for both tests.
- Remove `@lwc/old-compiler` from `packages/salesforcedx-lwc-language-server/package.json`.
- Regenerate `package-lock.json` so `@lwc/old-compiler` and its now-unused transitive packages are removed.

## Skills to apply

- `concise`: keep changes and descriptions minimal.
- `typescript`: preserve repository TypeScript conventions while simplifying the tests.
- `packageJson`: update the workspace dependency manifest correctly.
- `verification`: run targeted compile, lint, test, dependency, and audit checks.

## Verification

1. Run `npm install` to update the checkout and lockfile.
2. Run `npm run compile --workspace @salesforce/salesforcedx-lwc-language-server`.
3. Run `npm run lint --workspace @salesforce/salesforcedx-lwc-language-server`.
4. Run `npm test --workspace @salesforce/salesforcedx-lwc-language-server` and confirm both type-mapping snapshots pass.
5. Run `npm ls @lwc/old-compiler --all` and confirm the package is absent.
6. Run `npm audit` and confirm GHSA-8j8c-7jfh-h6hx and GHSA-mh29-5h37-fv8m are absent from the report.
