# W-23574073

## Context

- Target: `packages/salesforcedx-vscode-apex-testing/src/views/apexTestTreeService.ts`.
- Replace `scanned.filter(Option.isSome).map(o => o.value)` with Effect Array's `getSomes` helper.
- Rename the `effect/Array` namespace import from `Array` to `Arr`, matching the requested `Arr.getSomes` form and the package's existing convention.

## Phases

### 1. Replace manual `Option` extraction

- Rename the `effect/Array` import and its existing use to `Arr`.
- Replace the manual `filter(...).map(...)` chain with `Arr.getSomes(scanned)`.
- Commit: `refactor(apex-testing): use Arr.getSomes`

## Skills to apply

- `typescript`: preserve repository TypeScript naming and import conventions.
- `verification`: run package compile, lint, and tests after the change.
- `concise`: keep the change and documentation minimal.

## Verification

1. `npm install`
2. `npm run compile --workspace salesforcedx-vscode-apex-testing`
3. `npm run lint --workspace salesforcedx-vscode-apex-testing`
4. `npm test --workspace salesforcedx-vscode-apex-testing`
