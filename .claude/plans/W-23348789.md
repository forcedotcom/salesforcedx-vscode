# W-23348789 — Remove now-dead core exports

## Context

- Epic cleanup; Apex stopped consuming core exports
- Check 3 candidates; remove only those dead in-repo AND externally

Findings (verified via monorepo grep + `gh` search forcedotcom/salesforcecli/service-provider):

| Candidate | In-repo | External | Verdict |
|-----------|---------|----------|---------|
| top-level `getAuthFields` | dead (org pkg uses own `getAuthFieldsFor`) | none (agents/code-analyzer/metadata-visualizer/service-provider clean) | REMOVE |
| top-level `WorkspaceContext` | dead (no cross-ext `exports.WorkspaceContext`; replay migrated) | consumers use `services.WorkspaceContext` only | REMOVE field |
| `services.TelemetryService` | n/a | LIVE — vscode-agents + metadata-visualizer | KEEP |

Surface: `packages/salesforcedx-vscode-core/src/index.ts` (`SalesforceVSCodeCoreApi` type + `api` object), `src/util/orgAuthInfoExtensions.ts`.

Keep: `services.WorkspaceContext`, internal `WorkspaceContext.getInstance().initialize()`, top-level `telemetryService`, `getUserId`.

## Phases

### 1. Remove dead `getAuthFields` export + func
- `index.ts`: drop `getAuthFields` from import, `api` obj, `SalesforceVSCodeCoreApi` type
- `orgAuthInfoExtensions.ts`: delete `getAuthFields` func + now-unused `AuthFields` import
- keep `getUserId` (shares file, still exported)
- commit: `refactor(core): remove dead getAuthFields public API export - W-23348789`

### 2. Remove dead top-level `WorkspaceContext` API field
- `index.ts`: drop top-level `WorkspaceContext` from `api` obj + `SalesforceVSCodeCoreApi` type
- keep `services.WorkspaceContext` (external contract), keep `WorkspaceContext` import (used by `services` + `getInstance().initialize()`)
- commit: `refactor(core): remove dead top-level WorkspaceContext API export - W-23348789`

## Skills
- core-extension-api — API surface + breaking-change rules
- external-consumers — external repo verification
- concise — plan/doc style
- typescript, verification

## Verification
- `tsc` build core — no unused-symbol/type errors
- jest core (`index.test.ts` — doesn't assert removed shape; confirm still green)
- eslint core (no dangling imports)
- grep post-edit: no `exports.getAuthFields` / top-level `exports.WorkspaceContext` in monorepo
- not e2e-covered (pure export removal; no runtime flow)
