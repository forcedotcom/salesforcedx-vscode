# W-23451948 - Remove unused `metadataFolder` helper

## Context

- Inspected revision: `2026-07-30T19:45:11.000+0000`.
- Dependency WI 4 already deleted `packages/salesforcedx-utils-vscode/src/helpers/paths.ts`, `packages/salesforcedx-utils-vscode/test/jest/helpers/paths.test.ts`, and the utils-vscode root export of `projectPaths`.
- No `metadataFolder`, `projectPaths`, or utils-vscode `helpers/paths` reference remains under `packages/`.
- `packages/salesforcedx-utils-vscode/src/index.ts` retains `WorkspaceContextUtil`, which has independent runtime consumers in `salesforcedx-vscode-core`.
- Approach: accept WI 4's broader paths-helper deletion as the implementation, validate that it fully satisfies this WI, and make no replacement API or source edit unless verification exposes a deletion-attributable regression.

## Phases

### Phase 1 - Validate completed deletion

- Confirm the deleted source, dedicated Jest coverage, and root export remain absent.
- Recheck package and monorepo callers for `metadataFolder`, `projectPaths`, and utils-vscode `helpers/paths`.
- Validate the supplied external-consumer evidence where repository access permits: search `forcedotcom` and `salesforcecli` production code, inspect known shipping branches directly where default-branch code search is insufficient, and classify fork tests or unrelated same-name concepts as non-consumers.
- Preserve `WorkspaceContextUtil`. Do not add a cache, background fiber, async replacement, or other helper.
- If a production consumer is found, stop for scope clarification instead of restoring the API speculatively.

### Phase 2 - Verify readiness

- Run `npm run test -w @salesforce/salesforcedx-utils-vscode` to cover the package after the dedicated test deletion.
- Allow repository readiness hooks to run compile, lint, Effect diagnostics, Jest, VS Code bundle, and knip after the tracked-plan or implementation edit.
- Repair only deletion-attributable regressions, preserve unrelated worktree changes, and rerun the full failed readiness set.
- Report implementation ready only after all repository readiness hooks pass.

## Skills

- `concise`: tracked-plan structure and wording.
- `external-consumers`: exported-helper consumer validation beyond the monorepo.
- `typescript`: deleted API and unused-reference validation.
- `verification`: package tests, readiness hooks, and E2E applicability.

## Verification

- Zero `metadataFolder`, `projectPaths`, or utils-vscode `helpers/paths` references in package source and tests.
- Former source and Jest files remain absent; utils-vscode `src/index.ts` has no paths export.
- `WorkspaceContextUtil` remains exported and unchanged for its independent consumers.
- No production external consumer found in checked default and known shipping branches. Record each inaccessible repository or branch as a verification gap rather than proof of absence.
- Utils-vscode Jest suite passes. Not run during planning.
- Repository compile, lint, Effect diagnostics, Jest, VS Code bundle, and knip readiness hooks pass. Not run during planning.
- No Playwright run: utils-vscode is outside the repository's E2E package list, and this deletion has no runtime or UI path.

<!-- opencode-plan-evidence:{"planPath":".claude/plans/W-23451948.md","observedRevision":"2026-07-30T19:45:11.000+0000","summary":{"phases":["Validate WI 4's completed deletion across source, tests, exports, monorepo callers, and accessible external consumers; preserve independently used `WorkspaceContextUtil` and add no replacement helper.","Run the utils-vscode Jest suite and repository readiness hooks; repair only deletion-attributable regressions and rerun failed checks before implementation readiness."],"skills":["`concise` for tracked-plan wording.","`external-consumers` for exported-helper consumer validation beyond the monorepo.","`typescript` for deleted API and unused-reference checks.","`verification` for package tests, readiness hooks, and E2E applicability."],"verification":["Authored and reread `.claude/plans/W-23451948.md` against revision `2026-07-30T19:45:11.000+0000`.","Confirmed the former paths source and Jest files are absent and no `metadataFolder`, `projectPaths`, or utils-vscode `helpers/paths` references remain under `packages`.","Confirmed utils-vscode's root index has no paths export and retains independently consumed `WorkspaceContextUtil`.","Package tests, external repository searches, and repository readiness hooks were not run during planning; the tracked plan requires them before implementation readiness. Playwright is not applicable."]}} -->
