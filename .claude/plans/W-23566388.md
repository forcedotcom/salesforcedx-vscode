# W-23566388 Plan

## Context

Add the repository's Wireit-based `check:circular-deps` check to 7 extension packages:

- `packages/salesforcedx-vscode-apex/package.json`
- `packages/salesforcedx-vscode-apex-debugger/package.json`
- `packages/salesforcedx-vscode-apex-replay-debugger/package.json`
- `packages/salesforcedx-vscode-core/package.json`
- `packages/salesforcedx-vscode-lightning/package.json`
- `packages/salesforcedx-vscode-lwc/package.json`
- `packages/salesforcedx-vscode-visualforce/package.json`

This makes each package's lint workflow reject new circular TypeScript dependencies and produce the same `.circular-deps.json` artifact used by packages already enforcing the check.

The first replay-debugger check found an existing cycle: `src/index.ts` imported `src/breakpoints/checkpointService.ts`, which imported activation and channel helpers from `src/index.ts`. Remove that cycle as part of this work item.

## Phases

### 1. Add circular-dependency enforcement

For each listed `package.json`:

- expose `check:circular-deps` as a Wireit script
- add a `check:circular-deps` Wireit task using `dpdm` against `src`, with circular dependencies returning exit code 1
- track source/config inputs and `.circular-deps.json` output consistently with existing package tasks
- add `check:circular-deps` to the package's `lint` dependencies
- remove `.circular-deps.json` in `clean`

For `salesforcedx-vscode-apex-replay-debugger`, move the Apex extension and line-breakpoint integration from `src/index.ts` to `src/apexExtension.ts`, move debugger output-window handling to `src/channels/index.ts`, and import those modules directly from `src/breakpoints/checkpointService.ts`. Keep the existing exports from `src/index.ts` for consumers.

Commit: `ci: add circular dependency checks to 7 packages`

## Skills to apply

- `packageJson`: preserve repository `package.json` conventions
- `wireit`: declare the task, files, output, and lint dependency explicitly
- `verification`: run focused checks from the repository root with Wireit caching disabled

## Verification

1. Run `WIREIT_CACHE=none npm run check:circular-deps -w salesforcedx-vscode-apex -w salesforcedx-vscode-apex-debugger -w salesforcedx-vscode-apex-replay-debugger -w salesforcedx-vscode-core -w salesforcedx-vscode-lightning -w salesforcedx-vscode-lwc -w salesforcedx-vscode-visualforce`.
2. Run `WIREIT_CACHE=none npm run lint -w salesforcedx-vscode-apex -w salesforcedx-vscode-apex-debugger -w salesforcedx-vscode-apex-replay-debugger -w salesforcedx-vscode-core -w salesforcedx-vscode-lightning -w salesforcedx-vscode-lwc -w salesforcedx-vscode-visualforce` to confirm lint invokes each new dependency.
3. Run the replay-debugger `apexReplayDebugger.desktop.spec.ts` Playwright test to verify extension activation and replay launch.
4. Run the replay-debugger `checkpoints.desktop.spec.ts` Playwright test to verify line-breakpoint retrieval and checkpoint update paths.
5. Run the repository compile, lint, test, bundle, Effect diagnostics, and Knip stop-hook checks.
