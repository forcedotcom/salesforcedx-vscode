# W-23506240: Use `Effect.filterOrFail` for emptiness checks

## Scope

- Package: `salesforcedx-vscode-apex-testing`
- Source-only refactor; keep tests unchanged because existing Jest cases cover both transformed guards
- Preserve localized errors, successful values, ordering, last-write-wins coverage aggregation, and downstream behavior

## Skills

- `concise`: keep this tracked plan compact and implementation-oriented
- `effect-best-practices`: apply idiomatic `Effect.filterOrFail` composition and retain typed errors
- `typescript`: preserve inferred collection types and avoid mutation
- `verification`: rely on readiness hooks, then run required package Playwright checks
- `playwright-e2e`: run the package's web and desktop suites with retries disabled

## Phases

1. Validate resolved suite names in `packages/salesforcedx-vscode-apex-testing/src/utils/payloadBuilder.ts`.
   - Lift the resolved suite-name array into an Effect, then apply `Effect.filterOrFail(names => names.some(() => true), ...)` and yield the validated array.
   - Remove only the imperative zero-length branch.
   - Retain `SuiteNameUnresolvedError` and `apex_test_suite_name_not_determined_message`.
   - Continue deriving the single or comma-joined suite parameter from the validated array, preserving suite order and payload behavior.

2. Validate aggregated coverage values in `packages/salesforcedx-vscode-apex-testing/src/codecoverage/codeCoverageService.ts`.
   - Convert `coverageByName.values()` to the same ordered array currently returned, lift it into an Effect, then apply `Effect.filterOrFail(entries => entries.some(() => true), ...)`.
   - Remove only the imperative map-size branch.
   - Retain `StaleResultsError` and `colorizer_no_code_coverage_in_recent_results`.
   - Return the validated array unchanged so chronological processing, map insertion order, and last-write-wins aggregation remain intact.

## Verification

1. Keep tests unchanged: `payloadBuilder.test.ts` covers unresolved, single-suite, and ordered multi-suite behavior; `colorizer.test.ts` covers empty coverage, newest-file selection, and successful coverage processing.
2. Require repository readiness hooks to pass compile, lint, Effect LS diagnostics, Jest, bundle, and knip after implementation; address every Effect LS finding.
3. Run `npm run check:dupes` from the repository root and inspect any findings touching the changed code.
4. Run `npm run test:web -w salesforcedx-vscode-apex-testing -- --retries 0` from the repository root.
5. Run `npm run test:desktop -w salesforcedx-vscode-apex-testing -- --retries 0` from the repository root, stripping inherited Electron/VS Code variables if required by the agent shell.

<!-- opencode-plan-evidence:{"planPath":".claude/plans/W-23506240.md","observedRevision":"2026-07-31T05:05:47.000+0000","summary":{"phases":["Validate resolved suite names in `packages/salesforcedx-vscode-apex-testing/src/utils/payloadBuilder.ts` with `Effect.filterOrFail(names => names.some(() => true), ...)`, preserving `SuiteNameUnresolvedError`, localization, order, and payload behavior.","Validate ordered coverage map values in `packages/salesforcedx-vscode-apex-testing/src/codecoverage/codeCoverageService.ts` with `Effect.filterOrFail(entries => entries.some(() => true), ...)`, preserving `StaleResultsError`, insertion order, last-write-wins aggregation, and downstream behavior."],"skills":["concise","effect-best-practices","typescript","verification","playwright-e2e"],"verification":["Inspected both target source files, focused Jest coverage, package scripts, package context, required Playwright references, and safeguard-hook configuration.","Updated and reread `.claude/plans/W-23506240.md`; confirmed it names both intentional `.some(() => true)` predicates, retained errors, behavioral constraints, readiness gates, duplicate check, and web/desktop Playwright commands.","No implementation files were changed. Implementation verification is stated as future work and is not claimed as run during this plan-authoring phase; no commit, push, pull-request mutation, or Salesforce mutation was performed."]}} -->
