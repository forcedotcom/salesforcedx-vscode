# W-23094914: enable unicorn/no-useless-compound-assignment

## Context

- Enable `unicorn/no-useless-compound-assignment` (error). Flags identity compound assigns (`x += 0`, `x *= 1`, etc) = dead code.
- Single config file: `eslint.config.mjs`, main `**/*.ts` block (~line 130-234), alphabetical unicorn list.
- Dep W-23094911 merged (commit 5a8ec1d38).
- Plugin: eslint-plugin-unicorn v68 — rule present (`node_modules/eslint-plugin-unicorn/rules/no-useless-compound-assignment.js`).
- 0 violations repo-wide (grepped packages/**/*.ts — none).
- Pure config-enable. No code fixes.

## Phases

### Phase 1: enable rule

- Add `'unicorn/no-useless-compound-assignment': 'error',` after `'unicorn/no-useless-collection-argument': 'error',` (alphabetical) in `eslint.config.mjs`.
- File: `eslint.config.mjs`
- Commit: `chore(eslint): enable unicorn/no-useless-compound-assignment - W-23094914`

## Skills to apply

- `typescript` — TS lint config
- `wireit` — lint per-package; full-repo npx eslint . OOMs
- `verification` — lint-only
- `pr-draft` — PR

## Verification

- Build local rules first: `npm run compile --workspace=packages/eslint-local-rules` (else config import fails).
- `npm run lint` (wireit, per-package) passes.
- No e2e coverage relevant (lint-only WI).
