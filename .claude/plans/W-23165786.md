# W-23165786 — Delete coreExtensionUtils.ts; inline org access

## Context

- Target: `packages/salesforcedx-vscode-apex-testing/src/coreExtensionUtils.ts`
- Exports 2 `runPromise` wrappers over `getApexTestingRuntime()`:
  - `getConnection` → `ConnectionService.getConnection()`. **0 src consumers** (gate 4.3 already removed sync callers; grep confirms).
  - `getDefaultOrgInfo` → reads `TargetOrgRef` (`{orgId, username}`).
- `getDefaultOrgInfo` consumers: `views/apexTestTreeService.ts` only, 4 sites (~442, 562, 875, 1048), each already re-wrapped in `Effect.promise`/`Effect.tryPromise` (double-wrap around a runPromise).
- pathHelpers `getTestResultsFolder` already reads `TargetOrgRef` inline via `SubscriptionRef.get` — WI's pathHelpers step already done. It's the canonical shape to mirror:
  ```
  const api = yield* (yield* ExtensionProviderService).getServicesApi;
  const { orgId } = yield* SubscriptionRef.get(yield* api.services.TargetOrgRef());
  ```
- Stale jest.mocks of `coreExtensionUtils`:
  - `test/jest/views/apexTestTreeService.test.ts` (mocks `getDefaultOrgInfo`)
  - `test/jest/views/testController.test.ts` (mocks `getConnection`+`getDefaultOrgInfo`; testController.ts src no longer imports it — only a comment)

## Phases

### Phase 1 — inline org access, delete file, fix tests (1 commit)

- `apexTestTreeService.ts`:
  - add a module-level helper reading `TargetOrgRef` inline, mirroring pathHelpers'
    `getTestResultsFolder`. **Name it `getDefaultOrgInfo`** (verbNoun, matches file convention:
    `getSuiteItems`, `markStaleTags`, `persistDiscoveredClasses`). No `Effect` suffix — the skill's
    Naming rule (SKILL.md:36, 259-261) forbids it. Use `Effect.fn` with a span name to match
    pathHelpers + the file's other helpers:
    ```
    const getDefaultOrgInfo = Effect.fn('ApexTestTreeService.getDefaultOrgInfo')(function* () {
      const api = yield* (yield* ExtensionProviderService).getServicesApi;
      return yield* SubscriptionRef.get(yield* api.services.TargetOrgRef());
    });
    ```
  - add `SubscriptionRef` import (not currently imported); confirm `ExtensionProviderService` import present
  - replace 4 `getDefaultOrgInfo()` call sites: `yield* getDefaultOrgInfo()` (drop the
    `Effect.promise`/`Effect.tryPromise` wrappers). At line ~436 site it's inside `Effect.all`
    alongside `ConnectionService.getConnection()` — keep it in the `Effect.all` tuple to preserve concurrency.
  - remove `import { getDefaultOrgInfo } from '../coreExtensionUtils'`
- delete `src/coreExtensionUtils.ts`
- `apexTestTreeService.test.ts`:
  - drop `jest.mock('../../../src/coreExtensionUtils', ...)` (line 45-47) and the `mockOrgInfo`
    Promise seam
  - the src now reads org info through `api.services.TargetOrgRef()`, so this file's
    `mockServicesApi` (line 108-110) must gain a `TargetOrgRef` key. Back it with a
    `SubscriptionRef.make(mockOrgInfo)` and keep `mockOrgInfo` as the controllable var (tests at
    lines 476/579 set `mockOrgInfo = {}` for the no-org path). Mirror the watcher test's shape
    (`testDiscovery.test.ts:56` — `TargetOrgRef: () => Effect.succeed(targetOrgRef)`).
- `testController.test.ts`:
  - **first** add a `TargetOrgRef` entry to this file's `mockServicesApi.services` (line 44-63),
    SubscriptionRef-backed and controllable per test (a module-level `mockOrgInfo` var wrapped in
    `SubscriptionRef.make`, exposed via `TargetOrgRef: () => Effect.succeed(targetOrgRef)`, matching
    the watcher test). This is required because `mockServicesApi` backs the real
    `ApexTestTreeService.Default` (jest.requireActual at line 76) inside `mockRuntime`; the real
    `persistDiscoveredClasses`/`addClassToTree`/`applyIncrementalDiff` now call the inline helper's
    `yield* api.services.TargetOrgRef()`. Without it, tests like 'should discover tests and populate
    test items' (line 539-586, asserting `mockSaveDiscoveredClasses` called with `'org123'`) and the
    incrementalUpdate describe block (line 951+) throw `TargetOrgRef is not a function`.
  - **then** drop the stale `jest.mock('../../../src/coreExtensionUtils',...)` (line 118-121) +
    `coreExtensionUtils` import + the `getConnection`/`getDefaultOrgInfo` mock assignments (src
    doesn't import it; org info now flows through the services API seam above)

Commit: `refactor(apex-testing): inline org access, delete coreExtensionUtils.ts - W-23165786`

## Skills

- effect-best-practices — inline Effect vs runPromise wrapper; SubscriptionRef read; helper naming (no `Effect` suffix)
- typescript — build/lint conventions
- unit-test-critic — test mock removal + TargetOrgRef seam
- core-extension-api — ConnectionService/TargetOrgRef shape

## Verification

- `npm run compile` (or tsc) apex-testing — no unresolved `coreExtensionUtils` import
- `npx effect-language-service diagnostics --file .../views/apexTestTreeService.ts` — clean (no `effectFnOpportunity` etc.)
- eslint apex-testing changed files
- jest: `apexTestTreeService.test.ts`, `testController.test.ts` green (specifically the 'should discover
  tests and populate test items' and incrementalUpdate specs that assert on the org key)
- grep: `coreExtensionUtils` returns 0 hits under apex-testing
- e2e: run these specs locally before PR (they exercise org-key tree behavior through the real
  TargetOrgRef path this WI touches):
  - `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/testExplorer.headless.spec.ts`
  - `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/staleTestResultsRestoration.headless.spec.ts`
</content>
</invoke>
