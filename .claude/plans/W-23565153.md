# W-23565153 — Remove undeclared Wireit cross-package dependencies

## Context

Three package Wireit graphs reference workspace packages absent from the package's declared dependencies:

- `packages/effect-ext-utils/package.json` → `salesforcedx-vscode-services`
- `packages/salesforcedx-vscode-apex-testing/package.json` → `@salesforce/salesforcedx-utils`
- `packages/salesforcedx-vscode-services-types/package.json` → `salesforcedx-vscode-services`

Remove these stale Wireit edges so task ordering matches declared package relationships and does not drift independently.

## Phases

### Phase 1 — Remove undeclared Wireit edges

- Remove each listed cross-package dependency from its Wireit task configuration.
- Preserve commands, inputs, outputs, and declared-package Wireit dependencies.
- Commit: `chore: remove undeclared wireit dependencies - W-23565153`

## Skills to apply

- `wireit` — maintain explicit, acyclic task graphs.
- `packageJson` — preserve repository package metadata conventions.
- `concise` — keep the change and documentation minimal.
- `verification` — verify affected packages and repository checks.

## Verification

From the repository root:

1. Run `npm install`.
2. Run `WIREIT_CACHE=none npm run compile -w @salesforce/effect-ext-utils`.
3. Run `WIREIT_CACHE=none npm run compile -w salesforcedx-vscode-apex-testing`.
4. Run `WIREIT_CACHE=none npm run compile -w salesforcedx-vscode-services-types`.
5. Run the repository compile, lint, test, bundle, and Knip checks through the stop hook.
