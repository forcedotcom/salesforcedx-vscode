# W-23094902 — enable unicorn/no-duplicate-if-branches

## Context

- Rule: flags duplicate adjacent branches in if/else-if/else chains
- unicorn v68 (already installed via W-23094607); rule file present in node_modules
- **NOT auto-fixable** (no `fixable`/`hasSuggestions` in meta) — all fixes manual
- Dependency W-23094898 not in `develop` log; rebase onto develop before enabling, re-run lint to confirm count
- 9 violations / 4 files (whole-repo lint, `--max-old-space-size=8192`):
  - `packages/salesforcedx-aura-language-server/src/tern-server/stringUtil.ts` (51, 143) — 2 dup `break` in the FORWARD scan loops only (`findWord` 46-56, `findPreviousWord` 138-147). Rule flags by token-identical body; both forward branches are bare `break`. Backward scans (30-40, 112-126) are NOT flagged: newline branch body `break` differs from non-alphanumeric body `pos++; break` — do NOT touch them.
  - `packages/salesforcedx-vscode-metadata/src/sobjects/sobjectFilter.ts` (23, 25) — 3 branches all `return true`
  - `packages/salesforcedx-vscode-apex-testing/src/views/testController.ts` (1413) — 2 branches both `run.errored(...)`
  - `packages/soql-model/src/serialization/deserializer.ts` (690, 706, 708, 710) — `toCompareValue` 2 dup; `toLiteral` 4 dup `return new LiteralImpl('DATE', ...)`
- Sibling precedent: W-23094922 commit 38cbba745 — split refactor commit + enable commit; commit style `chore(eslint): enable unicorn/<rule> - W-...`

## Phases

### Phase 1 — manual refactors

Collapse duplicate branches by OR-ing conditions:

- stringUtil.ts: only the two FORWARD scan loops (`findWord` 46-56, `findPreviousWord` 138-147). Both branch bodies are bare `break` (no side effect), so collapse safely: `if (c===10||c===13) break; else if (!isAlphaNumberic(c)) break;` → `if (c===10||c===13||!isAlphaNumberic(c)) break;`; keep `// \n and \r` comment.
  - Do NOT touch backward scans (`findWord` 30-40, `findPreviousWord` 112-126): the non-alphanumeric branch runs `pos++` before `break` while the newline branch does not — bodies differ so the rule doesn't flag them, and OR-ing would drop the `pos++` and corrupt word-boundary `start`. Leave as-is.
- sobjectFilter.ts: combine 3 `return true` branches → single `if (A || B || C) return true`
- testController.ts: merge 2 `run.errored(...)` branches → `if ((isClass(test.id)&&getTestName(test)===className)||(isMethod(test.id)&&extractClassName(test.id)===className))`
- deserializer.ts `toCompareValue`: merge ColonExpr + ColonLike → 1 branch; `toLiteral`: merge 4 Date* ctx → 1 `if (Date||DateTime||Time||DateFormula) return new LiteralImpl('DATE', ctx.text)`

Verify: `npx eslint <4 files>` clean; existing jest passes per package

commit: `refactor: collapse duplicate if/else-if branches - W-23094902`

### Phase 2 — enable rule

- eslint.config.mjs: add `'unicorn/no-duplicate-if-branches': 'error',` (alpha order, before `no-empty-file` line ~172)
- Verify: full-repo lint clean for this rule

commit: `chore(eslint): enable unicorn/no-duplicate-if-branches - W-23094902`

## Skills

- concise — plan + any md
- typescript — refactor correctness
- verification — lint/build gate
- packageJson — n/a (no manifest change)

## Verification

- `NODE_OPTIONS=--max-old-space-size=8192 npx eslint .` → 0 `no-duplicate-if-branches` errors (run from worktree root)
- `npx eslint` on 4 touched files → clean
- jest per touched package (deserializer, sobjectFilter, testController, stringUtil) — behavior preserved
- stringUtil guard: `test/tern-server/stringUtil.test.ts` multiline cases (`findWord` ~31-39, `findPreviousWord` ~115-124) exercise newline/CR boundaries and word `start` — must stay green to prove forward-scan collapse + untouched backward scans preserve semantics
- Not e2e-covered: pure lint-enable + local refactor; no e2e on branch exercises these branch merges
- prettier/header unaffected
