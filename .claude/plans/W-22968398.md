# W-22968398 — Migrate debugApexTests e2e redhat → Playwright

## Context

- Source (delete): `packages/salesforcedx-vscode-automation-tests/test/specs/debugApexTests.e2e.ts` (redhat/vscode-extension-tester)
- Target (new): `packages/salesforcedx-vscode-apex-replay-debugger/test/playwright/specs/debugApexTests.desktop.spec.ts`
- Why replay-debugger pkg: codelens cmds `sf.test.view.debugTests` / `sf.test.view.debugSingleTest` registered in `packages/salesforcedx-vscode-apex-replay-debugger/src/index.ts:212-228` → `setupAndDebugTests` (`src/commands/quickLaunch.ts`)
- 4 entry points → `setupAndDebugTests`:
  1. "Debug All Tests" codelens (class)
  2. "Debug Test" codelens (single method)
  3. Test Explorer class-level "Debug Test"
  4. Test Explorer method-level "Debug Test"
- Pattern: mirror `apexReplayDebugger.desktop.spec.ts` — single `test()`, shared setup, one `test.step()` per entry point

### Regression-risk note (REQUIRED for reviewers)
4 entry points → `setupAndDebugTests` currently covered ONLY by WDIO `debugApexTests.e2e.ts:78-227`. Existing Playwright `apexReplayDebugger.desktop.spec.ts` covers replay-from-log, not live debug via codelens/Test Explorer. Migration REPLACES WDIO coverage; deleting WDIO (Phase 2) removes only existing coverage → new spec MUST be green before delete.
- Fixture `test/playwright/fixtures/desktopFixtures.ts`: `orgAlias: MINIMAL_ORG_ALIAS`; includes `salesforcedx-vscode-apex-testing` (Test Explorer + success notif) + apex/core/metadata exts
- Success notif NLS: `salesforcedx-vscode-apex-testing/package.nls.json` `apex_test_successful_execution_message` = `"%s successfully ran"`. `%s` = `Debug Test(s)` (replay-debugger i18n `debug_test_exec_name`, not in any package.nls.json). Match regex on `successfully ran` (per WI: NLS, not hardcoded full string)
- Helpers (all from `@salesforce/playwright-vscode-ext`): `setupMinimalOrgAndAuth`, `createApexClass`, `clickCodeLens`, `executeCommandWithCommandPalette`, `setupConsoleMonitoring`/`setupNetworkMonitoring`/`validateNoCriticalErrors`, `saveScreenshot`, `NOTIFICATION_LIST_ITEM`, `ensureSecondarySideBarHidden`, `ensureOutputPanelOpen`, `selectOutputChannel`, `waitForOutputChannelText`, `openFileByName`
- No Test Explorer helper in playwright-vscode-ext → inline locators (precedent `salesforcedx-vscode-lwc/.../lwcDebugTests.desktop.spec.ts:121-185`). MUST use retry/force-click pattern (mirrors WDIO StaleElementReferenceError comment debugApexTests.e2e.ts:159-162 — tree row click re-renders tree, invalidates element refs). Pattern (lwc precedent :166-183):
  ```
  const item = page.getByRole('treeitem', { name: /<targetName>/i });
  await item.waitFor({ state: 'visible', timeout: 30_000 });
  await item.scrollIntoViewIfNeeded();
  await expect(async () => {
    await item.click({ force: true });
    await item.hover({ force: true });
    const debugButton = item.getByRole('button', { name: /^Debug Test/ });
    await debugButton.waitFor({ state: 'visible', timeout: 3000 });
    await debugButton.click({ force: true });
  }).toPass({ timeout: 30_000 });
  ```
- `continueDebugSession` helper: copy from `apexReplayDebugger.desktop.spec.ts:38-53` (toolbar + Escape + F5)
- Deploy: metadata deploy cmd via `metadataNls.project_deploy_start_ignore_conflicts_default_org_text` (see ref spec :88-93)
- **Apex files — 4 `createApexClass(page, name, content)` calls** (playwright-ext `createApexClass` writes ONE file per call — apexReplayDebugger.desktop.spec.ts:84-85). Mirror WDIO `createApexClassWithTest` (apexUtils.js:79-102): emits main class + `@IsTest` class per name:
  - `ExampleApexClass1.cls` — `public with sharing class ExampleApexClass1` w/ `public static void SayHello(string name)` (System.debug body)
  - `ExampleApexClass1Test.cls` — `@IsTest public class ExampleApexClass1Test` w/ `@IsTest static void validateSayHello()` that calls `ExampleApexClass1.SayHello('Cody')` + `System.assertEquals`
  - `ExampleApexClass2.cls` — same shape as class1
  - `ExampleApexClass2Test.cls` — `@IsTest public class ExampleApexClass2Test` w/ `@IsTest static void validateSayHello()` calling `ExampleApexClass2.SayHello('Cody')`
  - Main class REQUIRED: each test method invokes `ExampleApexClassN.SayHello` (matches replay spec :74, WDIO :95). Do NOT collapse to single class w/ multiple methods — WDIO covers 4 tests across 2 test files, one entry point per file.
  - Deploy count = 4 components (WDIO :69 waits `Deploying 4 components`); Playwright waits `Deployed Source` output (no count assertion).
  - Test method name to target in Test Explorer = `validateSayHello` (the method `createApexClassWithTest` produces, WDIO :211).
- CI: runs under existing `test:desktop` glob (`playwright.config.desktop.ts` → `testDir:'./specs'`, matches `*.desktop.spec.ts`) in `apexReplayDebuggerE2E.yml` (:91). No new job.
- CI removals on redhat delete:
  - `.github/workflows/apexE2E.yml` — `debugApexTests` job (:59-66) + `debugApexTests` input (:11-15 dispatch, :33-37 call). Only active job → file becomes all-commented; comment out per repo convention (other migrated entries kept as comments)
  - `.github/workflows/triggerE2EForCommit.yml` — `apex-debug-tests` job (:41-47) → replace w/ migration comment (matches :39, :49)

## Phases

### Phase 1 — new Playwright spec
`commit: test(apex-replay-debugger): migrate debugApexTests e2e to Playwright - W-22968398`
- New `packages/salesforcedx-vscode-apex-replay-debugger/test/playwright/specs/debugApexTests.desktop.spec.ts`
- Single `test()`, `test.setTimeout(600000)`, console+network monitoring
- `continueDebugSession` helper copied from ref spec
- Imports: `test` from `../fixtures`; `apexTestingNls from 'salesforcedx-vscode-apex-testing/package.nls.json'`; `metadataNls from 'salesforcedx-vscode-metadata/package.nls.json'`
- Steps:
  - setup: `setupMinimalOrgAndAuth` → `ensureSecondarySideBarHidden` → 4× `createApexClass` (ExampleApexClass1, ExampleApexClass1Test, ExampleApexClass2, ExampleApexClass2Test — content per "Apex files" note above; main classes provide `SayHello`, test classes provide `validateSayHello`) → deploy via metadata cmd → wait `Deployed Source`
  - wait codelens: open `ExampleApexClass1Test.cls`, assert `Indexing complete` + codelens visible (ref spec :97-105)
  - step1 Debug All Tests codelens: open `ExampleApexClass1Test.cls`, `clickCodeLens(page,'Debug All Tests',{timeout:180000})` → assert notif `/successfully ran/` → `continueDebugSession`
  - step2 Debug Test codelens: open `ExampleApexClass2Test.cls`, `clickCodeLens(page,'Debug Test')` → notif → continue
  - step3 Test Explorer class-level: `Testing: Focus on Test Explorer View` → `Test: Refresh Tests` → use retry/force-click pattern (above) targeting treeitem `name:/ExampleApexClass1Test/i` → `Debug Test` button → notif → continue
  - step4 Test Explorer method-level: focus explorer → click+expand `ExampleApexClass2Test` treeitem to reveal method → use retry/force-click pattern targeting treeitem `name:/validateSayHello/i` → `Debug Test` button → notif → continue
  - `validateNoCriticalErrors(test, consoleErrors, networkErrors)`
- Success-notif assert: `page.locator(NOTIFICATION_LIST_ITEM).filter({hasText:/successfully ran/}).first()` visible (template from `apexTestingNls.apex_test_successful_execution_message`)
- `saveScreenshot` per step
- Verify locally: `npm run test:desktop -w salesforcedx-vscode-apex-replay-debugger -- --grep "Debug Apex Tests"` — iterate until green

### Phase 2 — delete redhat spec + CI cleanup
`commit: test(automation-tests): remove debugApexTests e2e migrated to Playwright - W-22968398`
- Delete `packages/salesforcedx-vscode-automation-tests/test/specs/debugApexTests.e2e.ts`
- Check helpers unused (`expandTestExplorerNamespaceAndPackage`/`findTestItemByName`/`verifyTestItems` in `test/utils/testsHelper.ts`, `apexTestExtensionConfigs`) — remove only if no other redhat spec imports (grep first)
- `.github/workflows/apexE2E.yml`: remove `debugApexTests` job + inputs (comment-out per convention)
- `.github/workflows/triggerE2EForCommit.yml`: replace `apex-debug-tests` job w/ migration comment

## Skills to apply

- `playwright-e2e` — read all references (coding/iterating/local-setup/analyze); running tests non-blocking, span files for debug
- `concise` — plan/doc style
- `typescript` — TS conventions
- `feature-branch` — branch/commit hygiene
- `verification` — pre-PR checks
- `paths` — file path conventions

## Verification

- e2e-covered (by new spec, run locally — required by WI): all 4 entry points fire `setupAndDebugTests` → `successfully ran` notif → session ends
- Local run (REQUIRED, not just typecheck): `npm run test:desktop -w salesforcedx-vscode-apex-replay-debugger` passes. Need minimal scratch org (`minimalTestOrg`) — see playwright-e2e local-setup
- NOT e2e-covered:
  - `npm run compile -w salesforcedx-vscode-apex-replay-debugger` (or tsc) clean
  - lint clean on new spec
  - grep confirms no remaining `debugApexTests.e2e` refs in `.github/workflows/`
  - grep confirms redhat helper removals leave no dangling imports
  - `apexE2E.yml` / `triggerE2EForCommit.yml` valid YAML (no orphaned `inputs`/`if` refs)
