# W-23942928 — Flatten LogContextUtil to functions

## Context

`packages/salesforcedx-apex-replay-debugger/src/core/logContextUtil.ts` mixes one exported function with a stateless `LogContextUtil` class. Replace the class with named functions, then update production and test callers. Preserve current behavior; this work item supplies no behavior changes.

## Phases

### 1. Replace `LogContextUtil` with named functions

- In `packages/salesforcedx-apex-replay-debugger/src/core/logContextUtil.ts`, convert every class method to a named exported function alongside `extractAnonApexSource`.
- In `packages/salesforcedx-apex-replay-debugger/src/core/logContext.ts`, import and call the functions directly; remove the `LogContextUtil` instance and its getter.
- Replace `LogContext.getUtil()` calls with direct function imports in:
  - `packages/salesforcedx-apex-replay-debugger/src/core/heapDumpService.ts`
  - `packages/salesforcedx-apex-replay-debugger/src/states/variableAssignmentState.ts`
  - `packages/salesforcedx-apex-replay-debugger/src/states/variableBeginState.ts`
- In `packages/salesforcedx-apex-replay-debugger/src/core/index.ts`, replace the class export with explicit function exports.
- Update function imports, calls, and Jest spies in:
  - `packages/salesforcedx-apex-replay-debugger/test/unit/core/logContextUtil.test.ts`
  - `packages/salesforcedx-apex-replay-debugger/test/unit/core/logContext.test.ts`
  - `packages/salesforcedx-apex-replay-debugger/test/unit/adapter/apexReplayDebug.test.ts`
  - `packages/salesforcedx-apex-replay-debugger/test/unit/states/logEntryState.test.ts`
- Commit: `refactor: flatten LogContextUtil to functions`

## Skills to apply

- `typescript`: named exports, direct function calls, no replacement class or namespace.
- `unit-test-critic`: keep tests focused on observable utility and caller behavior.
- `verification`: package-scoped compile, lint, and tests; duplicate-code check.

## Verification

From the repository root:

1. `npm install`
2. `npm run compile -w @salesforce/salesforcedx-apex-replay-debugger`
3. `npm run lint -w @salesforce/salesforcedx-apex-replay-debugger`
4. `npm test -w @salesforce/salesforcedx-apex-replay-debugger`
5. `npm run check:dupes`
6. Review the diff to confirm `LogContextUtil` construction, prototype spies, and exports are gone while each former method retains its existing implementation and coverage.
