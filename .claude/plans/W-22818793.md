# W-22818793 — Enable `unicorn/prefer-array-flat`

## Context

- Rule flags `[].concat(...arr)`, `Array.prototype.concat.apply([], arr)`, `_.flatten`, etc. → `.flat()`
- `eslint.config.mjs` already lists ~30 unicorn rules at error; `prefer-array-flat` absent
- Add line near other `prefer-array-*` (alphabetical), then autofix

## Phases

### Phase 1 — Enable rule + autofix

- Add `'unicorn/prefer-array-flat': 'error'` to unicorn rules block in `eslint.config.mjs` (after `prefer-array-find`)
- Run `npm run lint -- --fix` from repo root
- Manually resolve any non-autofixable violations (rare; check report)
- Files: `eslint.config.mjs` + any source files touched by autofix
- Commit: `chore: enable unicorn/prefer-array-flat`

## Skills

- concise (md edits)
- typescript (any source touch-ups)
- verification

## Verification

- `npm run lint` → 0 errors
- `npm run compile` → clean (autofix could alter types)
- Spot-check autofixed diffs: `.flat()` semantically equiv to original (depth=1)
- Unit tests: run unit suites for any package whose source files were modified by autofix (`npm test -w <package>`)
- e2e specs:
  - If autofix touches **zero runtime files** (lint-config-only change): no e2e specs required — precedent W-22816964 ("lint-only ESLint config change with zero runtime impact").
  - If autofix touches runtime files in a specific package, run the matching Playwright specs for that package, e.g.:
    - `salesforcedx-vscode-core` → `packages/salesforcedx-vscode-core/test/playwright/specs/*.spec.ts`
    - `salesforcedx-vscode-org-browser` → `packages/salesforcedx-vscode-org-browser/test/playwright/specs/*.spec.ts`
    - `salesforcedx-vscode-apex` → `packages/salesforcedx-vscode-apex/test/playwright/specs/*.spec.ts`
  - Final list of specs to be recorded in PR description after autofix runs and modified files are known.

