# W-22919091 — E2E: code coverage colorizer green/red highlighting

## Context

- Playwright spec: colorizer (`sf.apex.toggle.colorizer`) highlights covered lines green, uncovered red, clears on toggle-off.
- Runs desktop + web (`codeCoverageColorizer.headless.spec.ts`).
- No existing coverage-generating/asserting spec — new ground.

Code under test:
- Toggle command `sf.apex.toggle.colorizer` registered `packages/salesforcedx-vscode-apex-testing/src/index.ts:129` → `CodeCoverageHandler.toggleCoverage` (`src/codecoverage/colorizer.ts:192`).
- NOT in `contributes.commands` → NOT palette-accessible. Trigger via status-bar item click (`statusBarToggle.ts`, tooltip `Highlight Apex Code Coverage`, icons `$(three-bars)`/`$(tasklist)`).
- Decorations: bg `ThemeColor('testing.coveredBackground')` / `testing.uncoveredBackground` (`src/codecoverage/decorations.ts`).
- Colorizer SKIPS test classes — `IS_TEST_REG_EXP = /@isTest/i` guard (`colorizer.ts:233`). Must open NON-test class under test.
- `getCoverageData` reads `.sfdx` `test-result*.json` (`colorizer.ts:51`); requires `salesforcedx-vscode-apex-testing.retrieve-test-code-coverage=true` (defaults false → no JSON → throws).
- Lines value `1` = covered, else uncovered (`colorizer.ts:245`).

Key constraints (from coding-playwright ref):
- No Node fs/path or VS Code API in spec — UI only, web+desktop identical.
- Setting toggled cross-platform via `upsertSettings(page, {...})` (settings.ts UI helper).
- Run path: command-palette (`apex_test_run_text`), NOT code lens (desktop-gated). Existing `runApexTestsCommandPalette.headless.spec.ts` proves CP run works on web.
- Status-bar item: locate `getByRole('button', { name: /Highlight Apex Code Coverage/ })` (pattern per `sourceTrackingStatusBarPage.ts:25`).

Reuse helpers (`@salesforce/playwright-vscode-ext`): `setupNonTrackingOrgAndAuth`, `createAndDeployApexTestClass`, `createApexClass`+`deployCurrentSourceToOrg` (non-test), `upsertSettings`, `executeCommandWithCommandPalette`, `openFileByName`, `selectQuickInputOption`, `waitForRunApexTestsProgressNotificationGone`, `waitForOutputChannelText`, `saveScreenshot`, console/network monitors.

## Spec file (the NEW spec the WI demands)

`packages/salesforcedx-vscode-apex-testing/test/playwright/specs/codeCoverageColorizer.headless.spec.ts` — runs desktop + web. Asserts: ≥1 covered line, ≥1 uncovered line, zero of both after toggle-off.

## Phases

### Phase 1 — Spike + fixture + coverage-JSON gate (FRONT-LOADED, gates all; ALWAYS commits)
Builds the throwaway spike spec AND the fixture+coverage proof in one phase so there is always a commit recording what was tried — addresses [adversary:critical] (no conditional skip). Phase 2 (assertions) only proceeds if this phase's commit lands green.

DOM-renderability spike:
- Minimal spec: editor open + colorizer ON, dump `.view-overlays` DOM via `page.evaluate`.
- Confirm `TextEditorDecorationType` bg renders into `.view-overlays` child divs w/ queryable computed `background-color` (not canvas/`::before`). Overview-ruler is canvas → unusable.
- If DOM not queryable → STOP, reassess; pivot to Option B (see below) before Phase 2. Commit regardless, with a message recording the negative result (e.g. `test(apex-testing): colorizer DOM spike — view-overlays not queryable, pivot to API introspection`).

Theme determinism ([adversary:high] — ThemeColor is theme-dependent, RGB varies per theme):
- Do NOT assume `testing.coveredBackground`/`testing.uncoveredBackground` resolve to fixed RGB across themes.
- Spike must read+record the active theme in the harness: `page.evaluate` reading the rendered overlay computed bg AND confirm which theme the harness uses (default VS Code is `Default Dark Modern`). Check whether any test fixture/launch config sets `workbench.colorTheme`; if unset, harness default applies.
- Pin the theme in the spec via `upsertSettings(page, { 'workbench.colorTheme': '<recorded theme>' })` so CI and local resolve identically, THEN hard-code the RGB(A) values captured for that exact theme into a spec-local constant. Assertions match against those pinned values, not assumed defaults.
- Record the resolved RGB(A) for covered + uncovered for the pinned theme.

Fixture + coverage-JSON gate ([adversary:high] — `Test Summary` proves test ran, NOT that coverage JSON exists; `colorizer.ts:51` `getCoverageData` throws if no `test-result*.json` with `codecoverage`):
- Enable `salesforcedx-vscode-apex-testing.retrieve-test-code-coverage=true` via `upsertSettings`.
- Deploy NON-test class w/ `if/else` branch (one path covered, one not) — `createApexClass` + deploy.
- Deploy test class exercising ONLY covered path → ≥1 green + ≥1 red line.
- Run via CP (`apex_test_run_text` → select class), wait progress gone.
- GATE: prove coverage JSON exists before trusting Phase 2 assertions. Since spec is UI-only (no Node fs), gate by colorizer success itself: open the NON-test class, toggle colorizer ON, and assert NO error notification/output containing `colorizer_no_code_coverage_*` text appears AND ≥1 overlay div renders. `getCoverageData` throwing on missing/empty `test-result*.json` surfaces as a notification — its absence + presence of overlays proves coverage JSON was written with `codecoverage`. If a fs read is available in the harness throwaway step, additionally assert `.sfdx` `test-result*.json` (not `-codecoverage.json`) exists; otherwise the UI gate stands.
- Deliverables recorded in commit: confirmed DOM selector, pinned theme, RGB(A) values, coverage-gate result, screenshot.
- commit: `test(apex-testing): colorizer spike + fixture — pin theme, capture RGB, gate coverage JSON`
- files: `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/codeCoverageColorizer.headless.spec.ts`

### Phase 2 — Spec flow + assertions (Option A: pinned-theme computed color in DOM)
Only proceeds if Phase 1 confirmed DOM queryable + coverage JSON gate passed. If spike pivoted, use Option B.
- Setup non-tracking org+auth
- Pin theme via `upsertSettings` (recorded theme from Phase 1)
- Enable coverage setting
- Deploy both classes (branch class + covered-path test)
- Run test (coverage on); coverage-JSON gate (no `colorizer_no_code_coverage_*` notification)
- `openFileByName` NON-test class
- Click status-bar toggle ON (`getByRole('button', { name: /Highlight Apex Code Coverage/ })`)
- Query `.view-overlays` child divs by computed bg; assert ≥1 covered (pinned green RGB from Phase 1) AND ≥1 uncovered (pinned red RGB) present.
- `saveScreenshot` alongside for debug.
- Toggle OFF (click status-bar item) → assert zero covered AND zero uncovered overlays.
- `validateNoCriticalErrors`.
- Wrap DOM color query in small spec-local helper (count overlays matching target RGB).
- Option B fallback (if DOM not queryable): assert via extension-host/VS Code API introspection of the active decoration ranges rather than DOM color matching — count covered/uncovered decoration ranges through a harness-exposed API hook, removing theme-RGB dependency entirely.
- commit: `test(apex-testing): assert colorizer green/red highlighting + clear on toggle-off - W-22919091`
- files: `.../specs/codeCoverageColorizer.headless.spec.ts`

### Phase 3 — CI green (desktop + web)
- Local `test:desktop` + `test:web` (apex-testing pkg), iterate flakiness.
- If iterating in CI: temp `--grep` in `apexTestingE2E.yml` + branch in other workflows' `branches-ignore` (per playwright-e2e skill). Restore before done.
- Optional new constants in `contants.ts` (note typo'd filename) if timeouts/RGB shared.
- commit: `test(apex-testing): stabilize colorizer spec for CI desktop+web - W-22919091`
- files: `.../specs/codeCoverageColorizer.headless.spec.ts`, possibly `.../contants.ts`, `.github/workflows/apexTestingE2E.yml` (only if temp CI filter; revert)

## Skills to apply
- `/playwright-e2e` — spec authoring, shared helpers, run behavior, CI workflow disable/grep
- `/analyze-e2e` — CI triage on failures
- `/typescript`

## Verification
- e2e-covered by the NEW spec `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/codeCoverageColorizer.headless.spec.ts` (desktop + web): asserts ≥1 covered line (green), ≥1 uncovered line (red), zero of both after toggle-off.
- NOT e2e-covered (manual/local before push):
  - Phase 1 spike result: `.view-overlays` computed bg queryable (else pivot to Option B before Phase 2).
  - Theme pinned via `upsertSettings({ 'workbench.colorTheme': ... })`; RGB(A) constants in spec match the pinned theme (not assumed default) — re-run after any theme change.
  - Coverage-JSON gate passed: no `colorizer_no_code_coverage_*` notification + overlays render (proves `test-result*.json` with `codecoverage` written, not just that test ran).
  - Both classes deploy.
  - Status-bar item locator matches in web AND desktop (aria-label/tooltip).
  - Local `test:desktop` + `test:web` pass for apex-testing pkg pre-push.
  - CI workflow edits reverted (no leftover `--grep`/`branches-ignore`).
