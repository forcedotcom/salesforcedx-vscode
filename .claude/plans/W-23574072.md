# W-23574072

## Context

Replace unsafe first-element access and native array searches in the identified Effect-based production code with `Arr.head` and `Arr.findFirst`. Consume their `Option` results explicitly so absence remains visible in the types while preserving current behavior.

Files:

- `packages/salesforcedx-vscode-org-browser/src/services/orgBrowserMetadataRetrieveService.ts`
- `packages/salesforcedx-vscode-org/src/parameterGatherers/selectDeletableOrg.ts`
- `packages/salesforcedx-vscode-org/src/orgPicker/orgList.ts`
- `packages/salesforcedx-vscode-apex-debugger/src/commands/isvdebugging/bootstrapCmd.ts`
- `packages/salesforcedx-vscode-soql/src/commands/queryUtils.ts`

## Phases

### 1. Use Effect array operations

- Import `effect/Array` as `Arr`; import `effect/Option` where the file must consume the result.
- Replace unsafe `[0]` reads with `Arr.head`.
- Replace native `.find(...)` calls with `Arr.findFirst(...)`.
- Compose the returned `Option` with `Option.map`, `Option.flatMap`, `Option.match`, or `Option.getOrElse` according to each existing absent-value behavior.
- Leave type-level tuple indexing and accesses already guaranteed by surrounding control flow unchanged.

Commit message: `refactor: use Effect array operations for optional values`

## Skills to apply

- `effect-best-practices`: deep Effect imports and explicit `Option` handling.
- `typescript`: preserve inferred source types and avoid assertions or new nullable values.
- `verification`: run targeted diagnostics and package checks after implementation.

## Verification

- Run Effect Language Service diagnostics for each changed TypeScript file.
- Run compile and lint for `salesforcedx-vscode-org-browser`, `salesforcedx-vscode-org`, `salesforcedx-vscode-apex-debugger`, and `salesforcedx-vscode-soql` from the repository root.
- Run existing Jest tests for those 4 packages from the repository root.
- Run `WIREIT_CACHE=none npm run test:desktop -w salesforcedx-vscode-org -- --retries 0 test/playwright/specs/orgPicker.desktop.spec.ts test/playwright/specs/orgDeleteUsername.desktop.spec.ts`.
- Run `WIREIT_CACHE=none npm run test:web -w salesforcedx-vscode-org-browser -- --retries 0 test/playwright/specs/orgBrowser.customObject.headless.spec.ts`.
- Run `WIREIT_CACHE=none npm run test:desktop -w salesforcedx-vscode-apex-debugger -- --retries 0 test/playwright/specs/isvDebugBootstrap.desktop.spec.ts`; this covers only command availability and prompt cancellation, not the live folder-selection path.
- Run `WIREIT_CACHE=none npm run test:desktop -w salesforcedx-vscode-soql -- --retries 0 test/playwright/specs/soql-run-query.spec.ts`.
- Confirm the changed production files contain no targeted unsafe `[0]` or native `.find(...)` use and that absent-value behavior is unchanged.
