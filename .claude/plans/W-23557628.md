# W-23557628: Group ESLint Dependabot Updates

## Approach

Add one `eslint-tooling` group to the weekly root npm entry in `.github/dependabot.yml`. Group minor and patch updates for `@typescript-eslint/*`, `eslint`, and `eslint-plugin-*`; leave manifests, lockfiles, and other Dependabot entries unchanged.

## Evidence

- `.github/dependabot.yml:13-27`: weekly `/` npm entry without `target-branch`; contains the existing ignores, PR limit, rebase strategy, and versioning strategy.
- `package.json:5-7`: root npm workspace includes `packages/*`.
- `package.json:54-55,72-85`: root TypeScript ESLint and ESLint tooling dependencies.
- `packages/eslint-local-rules/package.json:120-128`: workspace TypeScript ESLint dependencies.
- Repository search finds only the root `package-lock.json`.

## Phases

1. Add `groups.eslint-tooling` to the first `.github/dependabot.yml` update item, alongside `schedule`, `ignore`, and related item keys. Set `patterns` to `['@typescript-eslint/*', 'eslint', 'eslint-plugin-*']` and `update-types` to `['minor', 'patch']`; preserve every existing key and value.
2. Review the scoped diff and parse the YAML. Assert the group is on the weekly root npm entry without `target-branch`, and that the daily `@salesforce/templates` and monthly `/docs` Bundler entries are unchanged.
3. Run repository readiness checks. Require compile, lint, tests, VS Code bundle, and knip to pass before reporting implementation complete. Dependabot grouping behavior is observable only on a later Dependabot run.

## Skills

- `concise`: compact tracked plan.
- `verification`: proportionate config validation and repository readiness checks.

## Verification

- Parse `.github/dependabot.yml` with the installed `yaml` package.
- Assert the first npm `/` entry has no `target-branch` and has the exact `eslint-tooling` group configuration.
- Inspect `git diff -- .github/dependabot.yml`; verify only the weekly root npm item changed and all retained settings are intact.
- Run the repository readiness hook; require compile, lint, tests, VS Code bundle, and knip to pass.

<!-- opencode-plan-evidence:{"planPath":".claude/plans/W-23557628.md","observedRevision":"2026-07-30T03:56:21.000+0000","summary":{"phases":["Add `groups.eslint-tooling` only to the first weekly root npm entry in `.github/dependabot.yml`, using the exact `@typescript-eslint/*`, `eslint`, and `eslint-plugin-*` patterns and minor/patch update types while preserving existing settings.","Review and parse the scoped YAML change, assert the group is attached to the intended entry, and confirm the templates and docs entries are unchanged.","Run repository readiness checks before implementation is reported complete; actual grouping behavior remains observable only on a later Dependabot run."],"skills":["concise","verification"],"verification":["Authored `.claude/plans/W-23557628.md` and passed `git diff --check`.","Repository readiness hook passed: compile, lint, tests, VS Code bundle, and knip.","Did not commit and left the pre-existing `package-lock.json` modification untouched."]}} -->
