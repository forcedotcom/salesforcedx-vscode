# W-22999463 — Delete all plans from .cursor/plans

## Context
- WI: delete all `*.plan.md` from `.cursor/plans/` (WI says 66; actual 64 on disk, all git-tracked)
- Stale AI plan artifacts; no code refs them
- Keep non-matching files: `e2e-plan.md`, `simplify-packaging-org-ext.md` (don't match `*.plan.md`)

## Phases

### Phase 1 — delete plan files
- `git rm .cursor/plans/*.plan.md`
- glob excludes the 2 keepers (no `.plan.md` suffix)
- files: all `.cursor/plans/*.plan.md` (64)
- commit: `chore: delete stale plan artifacts from .cursor/plans - W-22999463`

## Skills
- concise (plan file only)
- feature-branch
- pr-draft
- typescript

## Verification
- `ls .cursor/plans/*.plan.md` → none
- `ls .cursor/plans/` → only `e2e-plan.md`, `simplify-packaging-org-ext.md`
- `git status` → 64 deletions staged
- `grep -rl '.cursor/plans' packages/ scripts/ .github/ contributing/` → exactly 1 hit: `contributing/decisions/vsce-direct-use.md` (links to keeper `simplify-packaging-org-ext.md`, line 19). No refs to any deleted `*.plan.md`. Breaking the keeper link is out of scope.
- no e2e needed
