# W-24265021

## Context

- Remove unused `services.RegistryAccess` from `SalesforceVSCodeCoreApi`.
- Sole definition: `packages/salesforcedx-vscode-core/src/index.ts` — SDR import (L14), `services` value (L53), type field (L214).
- Monorepo has no `services.RegistryAccess` reads.
- In-repo registry use stays on `MetadataRegistryService.getRegistryAccess()` (`salesforcedx-vscode-services`). Do not touch that.
- Documented external core consumers (`external-consumers`) do not list `RegistryAccess`.

## Phases

### 1. Remove `services.RegistryAccess`

- `packages/salesforcedx-vscode-core/src/index.ts`: drop `@salesforce/source-deploy-retrieve` `RegistryAccess` import, the `services.RegistryAccess` value, and `RegistryAccess: typeof RegistryAccess`.
- `.claude/skills/core-extension-api/SKILL.md`: drop `RegistryAccess` from the `services` list.
- Same commit: if knip reports `@salesforce/source-deploy-retrieve` unused in core, drop that dependency.
- Commit message: `refactor(core): remove unused services.RegistryAccess`

## Skills to apply

- `core-extension-api`: field removal on `SalesforceVSCodeCoreApi.services` is breaking; this member is unused.
- `external-consumers`: confirm no external `services.RegistryAccess` / `exports.RegistryAccess` before the commit.
- `typescript`: deletion only; keep existing style in `index.ts`.
- `verification`: stop-hook plus checklist below.
- `concise`: skill-doc edit.

## Verification

- `gh` code search from `external-consumers` for `RegistryAccess` in `org:forcedotcom` and `org:salesforcecli`, plus `exports.RegistryAccess` in `org:forcedotcom`. Known consumer table has no `RegistryAccess` row.
- Stop hook: compile, lint, effect LS, test, `vscode:bundle`, knip.
- `npm run check:dupes`
- Core Playwright: `WIREIT_CACHE=none npm run test:desktop -w salesforcedx-vscode-core -- --retries 0`
- Doc-maintenance subagent after the code edit.
