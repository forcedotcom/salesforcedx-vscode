# W-22865166 — Enable jest/prefer-to-have-length

## Context

- Goal: enable `jest/prefer-to-have-length` = `error` in jest rules block, `eslint.config.mjs`.
- Branch in sync with `origin/develop` (0/0 ahead/behind, verified `git rev-list --left-right --count HEAD...origin/develop`). No rebase needed.
- Insertion point verified in working tree: line 561 = `'jest/unbound-method': 'error',`, line 562 = `'jest/no-focused-tests': 'error',` (blocker present). Insert after one of these in the jest block.
- Plugin `eslint-plugin-jest` 28.x already wired for all test globs; no plugin add.
- Rule autofixable (stylistic): `expect(x.length).toBe(n)` -> `expect(x).toHaveLength(n)`. Covers `.toBe/.toEqual/.toStrictEqual`.
- Scope = test globs only (jest block ~line 503-571). grep repo-wide found ~166 `.length).to*(` candidates; subset in test files = violations. Confirm via `eslint --fix` per package.
- Lint = per-package wireit, type-aware parser → can't run flat `eslint .` from root (needs per-pkg tsconfig). `eslint-local-rules` must be compiled before lint runs.
- Commit type = `test(lint):` per existing jest-rule precedent (f95931006, ab9f83688, 4a7a3ccf7) — stylistic test-file change, not feat.

## Phases

### Phase 1 — compile eslint-local-rules (infra pre-req)

- `npm run -w packages/eslint-local-rules compile` so lint can resolve local plugin.
- This is an infra prerequisite for lint, separate from WI scope.
- Commit msg: `chore(eslint-local-rules): compile before lint`

### Phase 2 — enable rule + autofix violations (WI scope)

- Add line in jest block, `eslint.config.mjs`, after `'jest/no-focused-tests': 'error',`:
  - `'jest/prefer-to-have-length': 'error',`
- Re-verify insertion point first: `grep -n "jest/no-focused-tests" eslint.config.mjs` (expect 562) before editing; insert relative to matched line, not hardcoded number.
- Autofix: per affected package run `npx eslint --fix .` (or root `npm run lint -- --fix` via wireit). Rule autofix handles `.toHaveLength` rewrite.
- Manually fix any non-autofixable residue (none expected — rule fully fixable).
- Verify `npm run lint` clean.
- Commit msg: `test(lint): enable jest/prefer-to-have-length`

## Skills to apply

- `concise` — this plan + any md.
- `verification` — lint gate.
- `wireit` — lint is wireit per-pkg; local-rules compiled in Phase 1.
- `typescript` — type-aware parser context.

## Verification

- `npm run lint` passes (NOT e2e-covered; primary DoD gate).
- Rule present + `error` in `eslint.config.mjs` jest block.
- spot-check diff: `.toHaveLength(` replaces `.length).toBe/.toEqual/.toStrictEqual(`.
- No behavior change (assertions equivalent). Existing unit suites (e2e-covered on branch) must still pass in CI; rerun affected package tests if any non-trivial residue manually edited.
