# W-23573139: Adopt Effect Encoding for base64/URI encode-decode

## Context

Replace direct Node `Buffer` base64 conversion and global `encodeURIComponent` calls in `salesforcedx-vscode-services` with `effect/Encoding`. This keeps encoding and decoding on the package's Effect APIs while preserving current binary serialization and URI path construction behavior.

Files:

- `packages/salesforcedx-vscode-services/src/virtualFsProvider/indexedDbStorage.ts`
- `packages/salesforcedx-vscode-services/scripts/o11yDebugServer.ts`
- `packages/salesforcedx-vscode-services/src/orgCatalog/orgMetadataShadowStore.ts`
- `packages/salesforcedx-vscode-services/src/orgCatalog/orgMetadataCatalogStore.ts`

## Phases

### 1. Replace direct encoding and decoding

- Import `effect/Encoding` directly.
- Use Effect Encoding base64 functions for IndexedDB binary file serialization and restoration.
- Use Effect Encoding base64 decoding for `base64Env`, composing its decode failure with the existing Effect error handling.
- Use Effect Encoding URI-component encoding for org IDs, metadata type names, full-name path segments, and revision names.
- Keep the existing data shapes, path segmentation, and error presentation.

Commit: `refactor: adopt Effect Encoding for base64 and URI handling`

## Skills to apply

- `effect-best-practices`: direct Effect module import, typed decode failures, Effect composition.
- `typescript`: repository TypeScript import and naming conventions.
- `verification`: package-focused compile, lint, tests, Effect diagnostics, and bundle checks.

## Verification

- Run `npm test -w salesforcedx-vscode-services -- --runInBand` to cover IndexedDB binary round trips and org-catalog encoded paths.
- Run `npm run compile -w salesforcedx-vscode-services`.
- Run `npm run lint -w salesforcedx-vscode-services`.
- Run `npx effect-language-service diagnostics --project packages/salesforcedx-vscode-services/tsconfig.json` and address all diagnostics for changed files.
- Run `npm run vscode:bundle -w salesforcedx-vscode-services` to verify desktop and web bundles resolve the direct Effect Encoding import.
