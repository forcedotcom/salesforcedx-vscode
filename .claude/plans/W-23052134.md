# W-23052134 — 3.4 ESLint conformance for salesforcedx-apex (single exemption block)

## Context

- Dep W-23051181 (monorepo migration) merged (52eec5b07). Unblocked.
- `packages/salesforcedx-apex` = history-preserving import of `@salesforce/apex-node`; predates monorepo style rules.
- Goal: 1 files-scoped override block for `packages/salesforcedx-apex/**/*.ts` in root `eslint.config.mjs`, exempting rules pkg can't yet satisfy. All apex exceptions in one place (like other per-pkg blocks). Surface shrinks as later refactors (methods→functions, Effect) land.
- Current state: apex exemptions scattered across shared blocks:
  - `eslint.config.mjs:632` — apex in shared `prefer-nullish-coalescing:off` list
  - `eslint.config.mjs:645` — apex-only block (consistent-type-assertions, explicit-member-accessibility, no-unsafe-enum-comparison, no-shadow, no-param-reassign, no-restricted-imports, unicorn/no-array-sort, unicorn/prefer-single-call)
  - `eslint.config.mjs:663` — apex in shared `header/header:off` list
  - `eslint.config.mjs:785` — apex in shared `barrel-files/avoid-barrel-files:off` list
- `eslint src/**/*.ts` on apex: 0 errors, 72 warnings. Rules warning: no-unsafe-member-access(15), no-unsafe-call(14), prefer-arrow/prefer-arrow-functions(13), no-unsafe-argument(7), no-restricted-types(4), no-redundant-type-constituents(4), restrict-template-expressions(3), require-await(3), no-unsafe-assignment(3), no-useless-escape(2), no-unsafe-return(2), no-misused-promises(2).
- test/** already relaxed separately (`eslint.config.mjs:563`); leave as-is.
- lint cmd = `eslint --cache` (no --max-warnings); warnings don't fail CI but count as unclean surface WI targets.

## Phases

### Phase 1 — consolidate apex exemptions into single block
- New block `files: ['packages/salesforcedx-apex/**/*.ts']` merging:
  - existing apex-only rules (from 645)
  - `@typescript-eslint/prefer-nullish-coalescing: off`
  - `header/header: off`
  - `barrel-files/avoid-barrel-files: off`
  - warn-level rules pkg can't satisfy → off: `no-unsafe-member-access`, `no-unsafe-call`, `no-unsafe-argument`, `no-unsafe-assignment`, `no-unsafe-return`, `no-redundant-type-constituents`, `no-restricted-types`, `restrict-template-expressions`, `require-await`, `no-misused-promises`, `no-useless-escape`, `prefer-arrow/prefer-arrow-functions`
- Remove `packages/salesforcedx-apex/**` from shared lists at 632, 663, 785 (keep those rules for other pkgs).
- Comment: single exemption block; shrinks as methods→functions + Effect land.
- commit msg: `chore(apex): consolidate eslint exemptions into single override block - W-23052134`
- files: `eslint.config.mjs`

## Skills to apply

- concise (plan/doc style)
- typescript (rules govern packages/salesforcedx-apex/**/*.ts)
- verification (run pkg lint + adjacent-pkg sanity)

## Verification

- `cd packages/eslint-local-rules && npm run compile` (config imports out/index.js) — prereq.
- `cd packages/salesforcedx-apex && npx eslint --no-cache 'src/**/*.ts'` → 0 problems.
- Adjacent-pkg sanity: shared rules still active elsewhere — spot-check `packages/salesforcedx-vscode-soql` + `salesforcedx-visualforce-language-server` lint unchanged (prefer-nullish-coalescing/header still off there via remaining shared blocks).
- `npx eslint eslint.config.mjs` (config self-valid / no parse err).
- No e2e coverage (config-only change; nothing runtime).
