# W-23165788 — ApexTestDiscoveryStore -> Effect service

## Context

- `discoveryVfs/apexTestDiscoveryStore.ts:40` — `ApexTestDiscoveryStore` class + module-singleton (`getApexTestDiscoveryStore`, line 97). Mutable class store.
- Writes through `ApexTestingDiscoveryFsProvider` (the real in-mem VFS, `apexTestingDiscoveryFsProvider.ts`). FsProvider class STAYS (vscode `FileSystemProvider` impl), module singleton at `apexTestingDiscoveryFsProvider.ts:222-226`.
- Store methods: `saveDiscoveredClasses(orgKey, classes, bodies)`, `readDiscoveredClassesIndex(orgKey)`, `clearOrg(orgKey)`. Plus free fn `resolveDiscoveryOrgKey` (pure; keep, re-export).
- Consumers:
  - `views/testController.ts:258` `saveDiscoveredClasses` (inside `persistDiscoveredClasses` try/catch lines 252-262; catch does `console.debug` swallow at 260); lines 255/652/887 `resolveDiscoveryOrgKey`.
  - `utils/pathHelpers.ts:23` `resolveDiscoveryOrgKey` only.
- Runtime wiring: `services/extensionProvider.ts` — `buildAllServicesLayer` `Layer.mergeAll` (line 47); single `getApexTestingRuntime()` ManagedRuntime (line 80). First package-local `Effect.Service` (none exist yet in apex-testing).
- Serialization boundary: index written `JSON.stringify` to VFS (`apexTestDiscoveryStore.ts:70`), read via `JSON.parse` + `isDiscoveredApexClassesIndex` guard (lines 79-80). This is a serialized boundary → Schema.
- FsProvider throws `vscode.FileSystemError.FileNotFound(uri)` on missing read (`apexTestingDiscoveryFsProvider.ts:68,88,122`).
- Cache pattern refs (in-repo): `utils/orgApexClassProvider.ts` (`Cache.make` + TTL, but module-level `let` cache — do NOT copy that singleton shape); canonical per-org registry: `salesforcedx-vscode-services/src/core/metadataDescribeService.ts` (`Cache.makeWith` registry keyed by orgId, `Exit.match` TTL, owned by `Effect.Service` effect field).
- Anti-pattern (do NOT replicate): `testDiscovery/packageResolution.ts:14-17` module Map cache + Set unavailable-orgs, no TTL, `resetPackageResolutionState`. Out of scope to refactor; just don't mirror.

## AC mapping (archived)

- ✅ no mutable class store → deleted `ApexTestDiscoveryStore` class + singleton
- ✅ no module-level Map/Set → none introduced
- ✅ FsProvider via Context.Tag layer dependency → `ApexTestingDiscoveryFsProviderTag` provided at layer-build
- ✅ service in mergeAll → `ApexTestDiscoveryService.Default` in `buildAllServicesLayer`
- ✅ old test → deleted; new service test, fresh layer per case

## Effect-idiom decisions (archived)

1. ✅ **FsProvider as a layer dependency (NOT bare getter).** `ApexTestingDiscoveryFsProviderTag` Context.Tag; `Layer.sync(ApexTestingDiscoveryFsProviderTag, getApexTestingDiscoveryFsProvider)` for prod; fresh per-case provider via `Layer.succeed` in tests.

2. ~~Index read/persist removed~~ — dead code (never wired to consumers). Service now writes only per-class `.cls` bodies to the VFS.

3. ✅ **No swallow at the consumer.** `clearOrg` maps `FileNotFound` to `Effect.void` (nothing to clear); surfaces other delete failures as `DiscoveryClearError`.

4. ✅ **Logging.** `Effect.log` (structured) for save/clear. No `console.*`.

## Status: Complete (index persistence removed in commit 27cb9cd94)

**Index read/persist half stripped:** `readDiscoveredClassesIndex`, `indexCache`, `DiscoveredApexClassesIndex` Schema, `DiscoveryReadError`, and `getOrgIndexUri`/`INDEX_FILE` removed — these were dead code (never wired to consumers). Service now writes only per-class `.cls` bodies to the VFS to back org-only TestItems and clears orgs; `DiscoveryClearError` + `resolveDiscoveryOrgKey` remain.

## Phases (archived)

### Phase 1 — DONE: ApexTestDiscoveryService (Effect.Service) + FsProvider Context.Tag

- ✅ New `discoveryVfs/apexTestDiscoveryFsProviderTag.ts`: `ApexTestingDiscoveryFsProviderTag` Context.Tag + `ApexTestingDiscoveryFsProviderLive` Layer.
- ✅ New `discoveryVfs/apexTestDiscoveryService.ts`:
  - ✅ `DiscoveryClearError` (Schema.TaggedError, fields `orgKey`/`message`/optional `cause`).
  - ✅ `export class ApexTestDiscoveryService` with `saveDiscoveredClasses(orgKey, classes, bodies)`, `clearOrg(orgKey)` methods.
  - ✅ `resolveDiscoveryOrgKey` pure fn (org key resolution).
  - ✅ Index read/persist removed (dead code; never had consumers).
- ✅ Registered in `services/extensionProvider.ts`: `ApexTestDiscoveryService.Default` in `Layer.mergeAll`.
- ✅ Commit: `refactor(apex-testing): ApexTestDiscoveryStore -> Effect.Service w/ FsProvider tag + cache - W-23165788`

### Phase 2 — DONE: consumers → runtime boundary + tests updated

- ✅ `views/testController.ts` `persistDiscoveredClasses`: replaced `getApexTestDiscoveryStore().saveDiscoveredClasses(...)` with runtime boundary call (no console.debug swallow).
- ✅ `resolveDiscoveryOrgKey` imports updated: `testController.ts` + `utils/pathHelpers.ts` import from `discoveryVfs/apexTestDiscoveryService`.
- ✅ Deleted `ApexTestDiscoveryStore` class + `getApexTestDiscoveryStore` singleton; `apexTestDiscoveryStore.ts` deleted.
- ✅ Deleted `test/jest/discoveryVfs/apexTestDiscoveryStore.test.ts` (class no longer exists).
- ✅ Updated `test/jest/views/testController.test.ts:94-101` with runtime mock (no more `getApexTestDiscoveryStore()`).
- ✅ Commit: `refactor(apex-testing): consumers use runtime; drop class singleton; update mocks - W-23165788`

### Phase 3 — DONE: service tests

- ✅ New `test/jest/discoveryVfs/apexTestDiscoveryService.test.ts`:
  - ✅ Fresh layer per case: `ApexTestingDiscoveryFsProviderTag` overridden per test.
  - ✅ Covered: org isolation, `clearOrg` (FileNotFound → void), per-class `.cls` body writes, `resolveDiscoveryOrgKey` cases.
  - ✅ Index read/persist tests removed (dead code).
- ✅ Commit: `test(apex-testing): fresh-layer tests for discovery service - W-23165788`

_Note: WI names old path; new file name follows service. Reviewer can rename — trivial._

## Skills to apply

- effect-best-practices — Effect.Service `accessors:true`; FsProvider via Context.Tag layer dependency (not bare getter inside effect); Schema.Struct + decodeUnknown for the serialized index (no hand-rolled guard); Schema.TaggedError for read/clear errors; FileNotFound→Option.none only, no catchAll/swallow; Effect.log not console.*; `Cache.makeWith` per-org + `Exit.match` TTL; `Effect.fn` not bare gen; no module globals
- typescript — type not interface, no null, const arrows
- concise — plan/doc style
- unit-test-critic — fresh-layer test quality, isolation
- verification — pre-PR checks

## Verification (archived)

- ✅ `npx effect-language-service diagnostics --file` — clean
- ✅ compile: `npm run compile` + tsc (no errors)
- ✅ lint: eslint (no module-level Map/Set, no `console.*`, no disable for autofixable)
- ✅ jest: `apexTestDiscoveryService.test.ts` green; `testController.test.ts` green (runtime mock updated)
- ✅ grep guards:
  - 0 hits `getApexTestDiscoveryStore` / `class ApexTestDiscoveryStore` (src + test)
  - 0 hits `ApexTestDiscoveryStore` in any test file
  - 0 hits `readDiscoveredClassesIndex` / `DiscoveredApexClassesIndex` / `isDiscoveredApexClassesIndex` (removed)
  - 0 hits `getOrgIndexUri` / `INDEX_FILE` (removed from FsProvider)
  - 0 hits `console.debug` in `views/testController.ts`
  - `ApexTestDiscoveryService.Default` in `buildAllServicesLayer` mergeAll
  - `getApexTestingDiscoveryFsProvider()` called only at layer-build (`Layer.sync`), 0 inside Effect bodies
