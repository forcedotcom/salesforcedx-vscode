# W-23358904 — Run visualforceLsp e2e on web

## Context

- WI text stale. Describes work already landed on develop:
  - `playwright.config.web.ts` (createWebConfig) — #7746
  - `fixtures/index.ts` webTest + VSCODE_DESKTOP switch — #7746
  - web-capable headless spec `specs/visualforceLsp.headless.spec.ts` (autocompletion + hover) — #7753, #7781
  - `.github/workflows/visualforceE2E.yml` e2e-web job — landed
  - `.vscodeignore` web packaging cleanup — WI 1 (0acc7e7d0)
  - referenced `visualforceLsp.desktop.spec.ts` already deleted → merged into `.headless.spec.ts`
- WI 3 dep (web-capable VF language server, #7789) landed.
- Ran `npm run test:web -w salesforcedx-vscode-visualforce`: suite runs, hover passes 1st try, **autocompletion flaky** — 1st attempt failed, retry passed.
- Root cause: `seedAndOpenPage` (spec L34-42) fires `New File...` then blind-types filename before VS Code inline Explorer input focused → keystrokes dropped → no `.page` editor opens → 30s `editor.waitFor` timeout. Retry hits warm workbench → passes.
- DoD: VF LSP web e2e reliably green (no retry dependence). Remaining work = harden seed step.

## Phases

### Phase 1 — de-flake seedAndOpenPage inline-input race

- File: `packages/salesforcedx-vscode-visualforce/test/playwright/specs/visualforceLsp.headless.spec.ts`
- After `executeExplorerContextMenuCommand(... /New File\.\.\./)`, before `keyboard.type`:
  - wait for inline Explorer input visible: `page.locator('.explorer-item-edited input.input, .monaco-inputbox input')` `.first().waitFor({state:'visible'})`
  - verify DOM selector against live run (page-snapshot / devtools on `run:web`) before finalizing
- keep existing comments
- optional: extract reusable `createFileInExplorer` helper into `packages/playwright-vscode-ext/src/utils/fileHelpers.ts` only if a 2nd consumer emerges — single consumer now, keep inline
- commit: `test(visualforce): de-flake VF LSP web autocompletion seed step - W-23358904`

## Skills to apply

- playwright-e2e — web fixture / headless-server model, selector conventions
- typescript — editing .spec.ts
- concise — plan + any doc/comment edits
- verification — prove de-flake, not just green-once

## Verification

- `npm run test:web -w salesforcedx-vscode-visualforce` — green (covered by e2e branch job)
- repeat autocompletion spec `--repeat-each=5` (or loop) → 0 failures on 1st attempt, no retry reliance — de-flake proof (NOT auto-covered; run manually)
- confirm inline-input selector matches live DOM before commit (page snapshot)
- lint/typecheck on edited spec
