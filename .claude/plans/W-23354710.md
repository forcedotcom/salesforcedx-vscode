# W-23354710 — Add ConfigService.getTargetOrg, migrate org-ext off OrgConfigProperties

## Context

- `ConfigService` (`packages/salesforcedx-vscode-services/src/core/configService.ts`) has `getTargetDevHub()` (lines 89-93): `agg.getPropertyValue<string>(TARGET_DEV_HUB)` → `value ? String(value) : undefined`. No `getTargetOrg` yet.
- org-ext reads `OrgConfigProperties.TARGET_ORG`/`TARGET_DEV_HUB` via raw aggregator (`getConfigAggregatorEffect`) in 2 spots:
  - `orgUtil.ts` `getDefaultOrgConfigurationEffect` (lines 334-347) — both props.
  - `orgDisplay.ts` `resolveUsername` (line 69) — TARGET_ORG.
- `util/configAggregatorEffect.ts` = sole indirection; only these 2 consumers (+ test mocks).
- Constraint: keep sync-local-config semantics. Do NOT use defaultOrgRef/TargetOrgRef (async, network, stale-if-auth-broken).
- `getTargetOrg`/`getTargetDevHub` = Effect.fn accessors:true → callable as `api.services.ConfigService.getTargetOrg()`.

## Phases

### Phase 1 — add ConfigService.getTargetOrg

- `configService.ts`: add `getTargetOrg` mirroring `getTargetDevHub` (`OrgConfigProperties.TARGET_ORG`); add to return object.
- `test/jest/core/configService.test.ts`: add getTargetOrg test (set/unset → value/undefined), mirror getTargetDevHub coverage.
- commit: `feat(services): add ConfigService.getTargetOrg - W-23354710`

### Phase 2 — migrate org-ext, delete configAggregatorEffect

- `orgUtil.ts` `getDefaultOrgConfigurationEffect`: replace `getConfigAggregatorEffect` + `getPropertyValue` with `api.services.ConfigService.getTargetDevHub()` + `.getTargetOrg()` (via `ExtensionProviderService`, already imported). Drop `getConfigAggregatorEffect` import + `OrgConfigProperties` from `@salesforce/core` import.
- `orgDisplay.ts` `resolveUsername`: replace `(yield* getConfigAggregatorEffect).getPropertyValue(TARGET_ORG)` with `api.services.ConfigService.getTargetOrg()`. Add `ExtensionProviderService` import; drop `getConfigAggregatorEffect` + `OrgConfigProperties`.
- delete `util/configAggregatorEffect.ts`.
- tests — 2 distinct patterns (do NOT apply the swap pattern to a `jest.mock(deletedModule)`):
  - `test/jest/commands/orgList.test.ts` (commands): **DELETE**, don't swap. Remove the `jest.mock('../../../src/util/configAggregatorEffect', ...)` block (lines 92-97) AND the `mockConfigAggregatorStore` scaffolding (lines 82-90) + its comment. This file mocks the whole deleted module by path; leaving it → jest.mock on nonexistent module → red suite. None of the tested fns (`removeExpiredAndDeletedOrgs`, `displayRemainingOrgs`, `findRemovableOrgs`, etc.) route through `getDefaultOrgConfigurationEffect`, so `buildServicesLayer` needs no `ConfigService.getTargetOrg`/`getTargetDevHub` stub. If post-delete typecheck/run shows a tested fn does hit ConfigService, add those Effect-returning stubs to `buildServicesLayer`'s `services` object.
  - `test/jest/orgPicker/orgList.test.ts` + `test/jest/util/orgDisplay.test.ts`: swap the `ConfigService.getConfigAggregator` stub → `getTargetOrg`/`getTargetDevHub` (Effect-returning; orgDisplay uses `opts.configTargetOrg`). These stub methods on a provided ConfigService (not a `jest.mock` of the deleted file), so the swap pattern applies.
- commit: `refactor(org): migrate org-ext off OrgConfigProperties to ConfigService.getTargetOrg - W-23354710`

## Skills to apply

- effect-best-practices (Effect.fn, accessors, Effect.all)
- services-extension-consumption (api.services access pattern)
- typescript / verification (jest + typecheck)
- concise (plan/docs)

## Verification

- jest `salesforcedx-vscode-services` configService.test.ts — getTargetOrg.
- jest `salesforcedx-vscode-org` orgUtil/orgDisplay/orgList tests green post-mock-update.
- typecheck both packages (dead import removal, deleted file has no refs).
- grep: no remaining `configAggregatorEffect` / `getConfigAggregatorEffect` refs.
- e2e-covered (skip manual): `orgDisplay.desktop.spec.ts` exercises resolveUsername → target-org config path.
