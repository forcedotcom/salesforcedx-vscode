# W-23506237: Use Effect.filterOrFail for guard-then-fail sites in salesforcedx-vscode-apex-oas

## Context

`packages/salesforcedx-vscode-apex-oas/src/services/apexMetadataService.ts` uses generator `if` guards and an `Effect.flatMap` branch only to reject invalid values. Replace those guard-then-fail sites with `Effect.filterOrFail`, preserving readiness checks, error types, messages, activation behavior, and returned values.

## Phases

1. Refactor Apex extension lookup, language-server readiness validation, and empty eligibility-response validation in `packages/salesforcedx-vscode-apex-oas/src/services/apexMetadataService.ts` to use `Effect.filterOrFail`. Keep the existing validation order and typed failures. Commit message: `refactor(apex-oas): use filterOrFail for guards`

## Skills to apply

- `effect-best-practices`: compose typed validation with `Effect.filterOrFail`.
- `typescript`: preserve narrowing and existing public types.
- `verification`: run focused and package-level checks after the refactor.

## Verification

- Run `npx effect-language-service diagnostics --file packages/salesforcedx-vscode-apex-oas/src/services/apexMetadataService.ts`.
- Run `node node_modules/.bin/jest packages/salesforcedx-vscode-apex-oas/test/jest/services/apexMetadataService.test.ts -c packages/salesforcedx-vscode-apex-oas/jest.config.js --runInBand`.
- Run `npm run compile --workspace salesforcedx-vscode-apex-oas`.
- Run `npm run lint --workspace salesforcedx-vscode-apex-oas`.
