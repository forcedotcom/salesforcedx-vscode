# W-22973849 — Remove WDIO/redhat e2e dir + deps

## Context

- WDIO migrations done. `packages/salesforcedx-vscode-automation-tests/test/specs/` already gone (no specs remain). Only test helpers/data left.
- Remove redhat/vscode-extension-tester harness entirely.
- `@salesforce/salesforcedx-vscode-test-tools`, `vscode-extension-tester`, `chai`, `mocha` are external npm pkgs declared in automation-tests/package.json + root. No other package references them.
- Verified unused outside automation-tests: `mocha`, `mocha-multi-reporters`, `@types/mocha` (only root pkg + lock). No `chai` imports anywhere.
- jest = unit runner; mocha not used by remaining tests.

### WI items found STALE (skip, note in PR — evidence required, see Phase 4)
- `.github/workflows/runE2ETest.yml` — does not exist. Greps (Phase 4) confirm no workflow refs `automation-test`/`gha-automation-test`/`extension-tester`/`salesforcedx-vscode-automation-tests`/`salesforcedx-vscode-test-tools`/`test-setup-and-runner`/`wdio`/`.e2e.js`. WI workflow section is moot. The existing `*E2E.yml` + `e2e.yml` workflows are Playwright (lwcPlaywrightE2E, apexOasE2E, playwright*) — KEEP. Phase 4 makes this evidence-backed, not assertion.
- eslint.config.mjs:53 `.mocharc-debug.ts` ignore — points to nonexistent file, unrelated to automation-tests. Out of scope.
- `@types/chai` root devDep — unused, but NOT in WI removal list. Leave; flag in PR.

### launch.json mocha refs — KEEP
- lines 372–492 = per-pkg unit-test debug (lightning/lwc/visualforce/apex-debugger) via each pkg's own `node_modules/mocha`. Unrelated to automation-tests.

## Phases

### Phase 1 — delete package + root configs
Commit: `chore: remove WDIO/redhat e2e automation-tests package - W-22973849`
- delete dir `packages/salesforcedx-vscode-automation-tests/` (whole pkg)
- delete `.mocharc.json` (root; only referenced automation-tests globalHooks + reporter)
- `.jscpd.json` — remove line `"**/salesforcedx-vscode-automation-tests/**"`
- `.prettierignore` — remove `packages/salesforcedx-vscode-automation-tests/lib/`
Files: packages/salesforcedx-vscode-automation-tests/**, .mocharc.json, .jscpd.json, .prettierignore

### Phase 2 — root package.json + lock
Commit: `chore: drop WDIO e2e deps + scripts from root - W-22973849`
- devDeps remove: `@salesforce/salesforcedx-vscode-test-tools`, `vscode-extension-tester`, `mocha`, `mocha-multi-reporters`, `@types/mocha`
- scripts remove: `automation-test`, `gha-automation-tests`, `setup`
- wireit `compile.dependencies` remove `./packages/salesforcedx-vscode-automation-tests:compile` (line 200)
- wireit `lint.dependencies` remove `./packages/salesforcedx-vscode-automation-tests:lint` (line 230)
- KEEP `ts-node` (12 other scripts use it)
- regen `package-lock.json` (`npm install`)
Files: package.json, package-lock.json

### Phase 3 — eslint + docs
Commit: `chore: remove automation-tests eslint globs + e2e docs - W-22973849`
- eslint.config.mjs: remove 2 `packages/salesforcedx-vscode-automation-tests/**/*` glob entries (~530, ~584)
- contributing/tests.md:
  - remove "Legacy End to End Tests - RedHat" list item (lines 14–15)
  - remove line 19 "Legacy RedHat/WDIO setup ... [e2e-instructions.md]" pointer (now dangling once legacy sections gone)
  - KEEP lines 12–13 (Playwright item) + line 18 (Playwright SKILL.md pointer) — canonical Playwright docs, untouched
- contributing/e2e-instructions.md — DO NOT delete the file (WI says "Other refs", not "delete"). Remove ONLY legacy WDIO/RedHat sections:
  - strip lines 1–85 + 88–106 (all legacy: title, RedHat note, prereqs, local-run via `automation-test`, GHA via deleted workflows, test-tools update flow, tips) and lines 16–20 of the old WDIO link in tests.md handled above
  - The file's only Playwright content (line 3 note, line 86 Apex-OAS-migration note) is duplicative — both merely point to `.claude/skills/playwright-e2e/SKILL.md`, which is the canonical Playwright doc and stays intact. Once legacy sections are stripped, nothing Playwright-original remains in this file.
  - Resolution: gutting all legacy content leaves an empty shell. Reduce the file to a short stub that redirects to the canonical Playwright SKILL.md (preserves the file path per WI "Other refs" wording, keeps any inbound links valid, no orphaned Playwright info since SKILL.md already owns it). Stub content: a one-line title + sentence linking to `../.claude/skills/playwright-e2e/SKILL.md` for Playwright e2e, noting the legacy RedHat/WDIO framework was removed in W-22973849.
  - Verify (Phase 4) the canonical `.claude/skills/playwright-e2e/SKILL.md` exists and is unchanged.
- docs/Testing.md (additional inbound refs found via grep):
  - remove the "### redhat/WDIO framework (legacy, being phased out)" section (lines 61–67) — describes the now-deleted framework
  - remove the See Also bullet "[contributing/e2e-instructions.md] - RedHat e2e framework details" (line 90)
  - KEEP all Playwright sections/links (Playwright is the surviving path)
- lwc/org playwright spec header comments referencing old WDIO paths: leave (historical "migrated from" notes, harmless) OR strip — low priority, strip the path refs
Files: eslint.config.mjs, contributing/tests.md, contributing/e2e-instructions.md, docs/Testing.md

### Phase 4 — workflow cleanup verification (evidence for STALE claim)
No commit expected (claim is workflows are already clean). Prove it before declaring moot:
- `grep -rnE 'automation-test|gha-automation-test|extension-tester|salesforcedx-vscode-automation-tests|salesforcedx-vscode-test-tools|test-setup-and-runner|wdio|\.e2e\.js' .github/workflows/` → expect ZERO hits.
- `ls .github/workflows/runE2ETest.yml` → expect "No such file".
- IF any hit exists: STOP, add a workflow-cleanup commit (`chore: remove WDIO e2e workflow refs - W-22973849`) removing `runE2ETest.yml` and/or the `gha-automation-tests` run step per WI; do NOT touch Playwright `*E2E.yml`/`e2e.yml` workflows.
- IF zero hits (verified): record grep output in PR description as evidence the WI workflow items are already satisfied.

## Skills to apply
- packageJson — root devDep/script edits
- wireit — compile/lint dependency removal
- concise — this plan + any md edits in .claude
- typescript
- verification — gating checks below

## Verification
- `npx eslint package.json eslint.config.mjs` clean (or repo lint task)
- `npm run compile` succeeds (wireit graph valid w/o automation-tests node)
- `npm run lint` succeeds
- `npm run check:knip` — no new unused-dep complaints from removed pkgs
- `npm run check:peer-deps` + `check:lock-sync` (precommit) pass after lock regen
- `grep -rn "automation-tests\|vscode-extension-tester\|mocha-multi-reporters" .` (excl node_modules/lock) → only stale plan md hits
- Phase 4 workflow greps clean (zero hits) — evidence captured for PR
- `.claude/skills/playwright-e2e/SKILL.md` exists + unchanged (canonical Playwright doc survives); tests.md still links to it (line 18); e2e-instructions.md stub links to it
- no dangling internal md links: `grep -rn "e2e-instructions.md" contributing/ .claude/` resolves to existing file (stub kept, not deleted)
- NOT e2e-covered (deletion-only); no Playwright run needed. Existing migrated Playwright specs (lwc/org/apexOas) already validate functional coverage on their own branches.
