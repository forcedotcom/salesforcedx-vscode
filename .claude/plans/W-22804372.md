# W-22804372 — enable unicorn/no-single-promise-in-promise-methods

## Context

- `unicorn/no-single-promise-in-promise-methods: error` in `eslint.config.mjs`
- catches single-arg `Promise.all([p])` / `race` / `any` / `allSettled`
- unicorn block ~line 178; alphabetical near other `no-*`
- ref PR: #7377

## Scope discipline

- ONLY: rule enable + mechanical unwraps
- NO `Effect.gen` → `Effect.fn` (out of scope)
- NO `Error` → `Schema.TaggedError` (out of scope)
- violations in `Effect.gen` / class-Error: fix unwrap only

## Phases

### Phase 1 — enable rule + fix violations

- `'unicorn/no-single-promise-in-promise-methods': 'error'` in unicorn block (alphabetical)
- compile if needed: `npm run compile`
- enumerate via `npm run lint`
- patterns to fix (mechanical):
  - `await Promise.all([p])` → `await p`
  - `const [x] = await Promise.all([p])` → `const x = await p`
  - `Promise.race([p])` / `Promise.any([p])` / `Promise.allSettled([p])` → `p` (or `await p`); preserve await/return semantics
  - empty `Promise.all([])` (if any): remove (resolves `[]` synchronously; out of rule scope)
  - intentional cases: disable-next-line + reason
- include `scripts/**/*.ts` if covered by lint config
- commit: `chore: enable unicorn/no-single-promise-in-promise-methods`

files:

- `eslint.config.mjs`
- any `.ts` / `.tsx` / `.mjs` files under `packages/` and `scripts/` flagged by lint

## Skills to apply

- concise (this plan)
- typescript (file naming, code style)
- pr-draft (PR title `chore: ... - W-22804372`, GUS ref)
- feature-branch (already on feature branch)
- verification

## Verification

- `npm run compile` passes
- `npm run lint` zero violations
- type-check via compile
- semantics: `await Promise.all([p])` ≡ `await p` (microtask-equivalent)
- spot-check rewrites: no lost error handling; destructure unwrap correct; return-type unchanged
- e2e: list actual `*.spec.ts` paths for modified packages (no generic "on branch" line)
  - orgBrowserPage.ts changes:
    - `packages/salesforcedx-vscode-org-browser/test/playwright/specs/orgBrowser.customObject.headless.spec.ts`
    - `packages/salesforcedx-vscode-org-browser/test/playwright/specs/orgBrowser.customTab.headless.spec.ts`
    - `packages/salesforcedx-vscode-org-browser/test/playwright/specs/orgBrowser.folderedReport.headless.spec.ts`
    - `packages/salesforcedx-vscode-org-browser/test/playwright/specs/orgBrowser.describe.scratch.spec.ts`
  - other packages: list matching specs in PR
- run locally: `npm run test:desktop -w salesforcedx-vscode-org-browser -- --retries 0` (also `npm run test:web` if web-touched)
