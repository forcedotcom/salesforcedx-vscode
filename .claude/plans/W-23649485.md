# W-23649485 — inferred AllServicesLayer type (apex-replay-debugger)

## Context

- only consumer of `OpaqueServicesLayer = Layer.Layer<any, never>`
- erases service context + forces error to `never`; eslint `any` + assertion suppressions
- 13 other extensions use `ReturnType<typeof buildAllServicesLayer>` (apex-debugger is the twin)

## Phases

### 1. Drop opaque layer type

**Commit:** `refactor(apex-replay-debugger): infer AllServicesLayer from buildAllServicesLayer - W-23649485`

File: `packages/salesforcedx-vscode-apex-replay-debugger/src/services/extensionProvider.ts`

Match apex-debugger / services-extension-consumption:

```ts
export let AllServicesLayer: ReturnType<typeof buildAllServicesLayer>;
export const setAllServicesLayer = (layer: ReturnType<typeof buildAllServicesLayer>) => {
  AllServicesLayer = layer;
};
```

- delete `OpaqueServicesLayer`
- delete `Layer` import if unused
- delete `no-explicit-any` + `consistent-type-assertions` suppressions on this type/cast
- keep `functional/no-let` on the mutable export

Do not change `runtime.ts` / `ensureTraceFlags.ts` — they consume `AllServicesLayer`; inferred type flows through.

Do not change tests unless compile/lint require it (jest already casts mock layer to setter param).

## Skills to apply

- `services-extension-consumption`
- `typescript`
- `verification`

## Verification

Existing compile + lint (no new tests):

- [ ] no `OpaqueServicesLayer` / `Layer.Layer<any` in replay-debugger
- [ ] `AllServicesLayer` typed as `ReturnType<typeof buildAllServicesLayer>`
- [ ] no explicit-any / type-assertion eslint disable for this layer
- [ ] compile + lint + apex-replay-debugger jest (stop hook)
