# W-23334955 — flakiness-review metrics: parse real artifacts, retryRate 0-1, thread runIds

## Context

Single file: `.claude/workflows/flakiness-review.js`. 3 bugs in metrics -> cluster -> hypothesize chain -> empty hypotheses on real-flakiness runs.

- **Bug1 — metrics parse is a python heredoc + wrong glob.** Lines 294-323. Violates repo jq-not-python rule. Globs `.e2e-artifacts/develop/**/results.json` — 0 matches; Playwright artifacts have no `results.json`. Real on-disk format (verified in main-dir `.e2e-artifacts/develop/`):
  - `playwright-report-<suite>/index.html` — embeds `<template id="playwrightReportBase64">data:application/zip;base64,...`; zip contains clean `report.json` (`stats{total,flaky,unexpected,...}`, `files[].tests[]{title,outcome,results[]}`; `outcome` ∈ expected|flaky|unexpected|skipped — flaky = retry-masked). Decode pure-bash: extract template payload, `base64 -d`, `unzip`, `jq report.json`.
  - `playwright-test-results-<suite>/<test-dir>[-retryN]/error-context.md` — per-test failure text (`# Error details` block) + `spans/`,`videos/`. `-retryN` suffix = retry signal.
- **Bug2 — retryRate can exceed 1** (obs 2.0). Line 325 `retryCount/totalRuns`, retryCount sums all retries (>1/run). Threshold checks (`RETRY_RATE_THRESHOLD` 0.5, lines 44/371/417) + report (line 783) assume 0-1. Fix: retried-runs / total-runs.
- **Bug3 — runIds never threaded.** Metrics drops runId per parsed result (avail as path segment `.e2e-artifacts/develop/<run-id>/`; strip non-numeric suffix e.g. `-extra`,`-End-to-End-Tests` -> numeric GH id for `gh run download`). testMetrics has no runIds field (schema lines 164-178). Cluster prompt (line 371) sets `runIds`=best-available -> `[]`. Artifact-download skips empty runIds (line 467) -> hypothesize gets 0 evidence -> `{hypotheses:[]}`.

Fix direction (from WI): jq not python; parse report.json; retryRate=retried/total; thread runIds parse->testMetrics->clusters.

Adjacent (out of scope, flag only): artifact-download span parse also python heredoc (lines 500-511). Not required for this WI; note for follow-up.

## Phases

### Phase 1: real artifact parse (jq) + retryRate 0-1

**Files**: `.claude/workflows/flakiness-review.js`

- Metrics prompt Part 2 (lines 279-327): drop python heredoc + `results.json` glob. Replace w/ bash+jq recipe over decoded `report.json`:
  - find `index.html` under `playwright-report*`; extract base64 template payload; `base64 -d`>zip; `unzip` report.json to temp; `jq` per-test.
  - per test per report: `totalRuns`+=1; `retried` if `results|length>1` OR `outcome=="flaky"`; `failed` if `outcome=="unexpected"`.
  - aggregate by testName across reports.
- retryRate = retriedRuns/totalRuns (0-1, 2dp). Update prose line 325 + inline comments (lines 259-261, 291-292).
- Keep workflowMetrics (Part 1) unchanged.

**Commit**: `fix(flakiness-review): parse playwright report.json via jq; retryRate as retried/total`

### Phase 2: thread runIds parse -> testMetrics -> clusters

**Files**: `.claude/workflows/flakiness-review.js`

- METRICS_SCHEMA testMetrics (lines 167-176): add `runIds: {type:'array',items:{type:'string'}}`; add to `required`.
- Metrics prompt: emit `runIds` per test = numeric run ids (path segment, suffix stripped) where that test retried/failed.
- Cluster prompt (line 368-371): set cluster `runIds` from matched testMetrics entry's `runIds` (not `[]`); keep testName match.
- Verify artifact-download (lines 462-469) now receives runId for retryRate-source clusters.

**Commit**: `fix(flakiness-review): thread runIds from parse through testMetrics to clusters`

## Skills to apply

- concise (plan + any prompt-string edits stay terse)
- verification (drive the parse against real on-disk artifacts)
- typescript (most rules apply to this `.js` diff too: avoid `let`/mutation, template literals not concatenation, `undefined` not `null`, no barrel-style imports, preserve comments; skip only the TS-specific bits — `.ts` filename casing, no enums, no `any` — since file is plain JS)
- No effect — workflow has no Effect usage (per `.claude/workflows/README.md`).

## Verification

No e2e/unit coverage — `.claude/workflows/*.js` are tooling, not test-covered. Manual:

- Run the Phase-1 bash+jq recipe against real artifacts in main dir `/Users/shane.mclaughlin/eng/forcedotcom/vscode-auto/.e2e-artifacts/develop/` (worktree has none). Confirm: nonzero testMetrics; every retryRate ∈ [0,1]; retried/failed counts match `-retryN` dirs + `outcome` fields.
- Confirm runId extraction strips `-extra`/`-End-to-End-Tests` -> numeric id (`gh run download` accepts it).
- Static: no `python` token remains in metrics phase; testMetrics carries `runIds`; cluster `runIds` non-empty when testMetrics matched.
- Optional full run: `/flakiness-review` w/ small `days`; confirm clusters carry runIds + hypothesize non-empty when flakiness present.
