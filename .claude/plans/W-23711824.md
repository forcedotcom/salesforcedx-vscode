# W-23711824

## Context

Create Apex Unit Test Class headless E2E selects the template by typing `ApexUnitTest` and pressing Enter. Replace that quick-pick choice with `selectQuickInputOption`. Template label is `ApexUnitTest` (`createApexClass.ts`).

File:

- `packages/salesforcedx-vscode-apex-log/test/playwright/specs/apexTestClassCreate.headless.spec.ts`

## Phases

### 1. Use `selectQuickInputOption` for ApexUnitTest

- Import `selectQuickInputOption` from `@salesforce/playwright-vscode-ext`.
- Step `select ApexUnitTest template in QuickPick`: replace `waitForQuickInputFirstOption`, `keyboard.type('ApexUnitTest')`, second wait, and Enter with `selectQuickInputOption(page, 'ApexUnitTest')`.
- Keep the step screenshot.
- Leave class-name and output-directory steps. `waitForQuickInputFirstOption` stays for the directory step.

Commit: `refactor: use quick input helper for apex unit test template`

## Skills to apply

- `playwright-e2e`: E2E helpers and test execution.
- `typescript`: imports and formatting.
- `verification`: plan and post-change checks.

## Verification

1. From repo root: `npm run test:web -w salesforcedx-vscode-apex-log -- --retries 0`.
2. Stop hook: compile, lint, effect LS, test, vscode:bundle, knip.
