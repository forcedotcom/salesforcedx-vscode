# W-23462557 — ProgressNotification: delete (not move)

## Context

WI (step 3, follow-up to W-23451863/#7782) assumes: after apex-debugger converts off SfCommandlet, `ProgressNotification` sole consumer = core `sfCommandletExecutor.ts` → move class + i18n key into core, colocated.

Premise stale. Verified on branch base:
- Step 2 (#7782, commit `3a03d87d6`) **deleted** `sfCommandletExecutor.ts` (77 lines, "delete dead SfCommandletExecutor + API export") — did not convert it. Was sole caller (`ProgressNotification.show(execution, cancellationTokenSource)`).
- `ProgressNotification` now has **0 real consumers** repo-wide. Only refs: own file + barrel export (`src/index.ts:10`).
- No `SfCommandletExecutor` in core (only stale comment in `salesforcedx-vscode-org/src/commands/orgCreate.ts:169`).
- No test file for the class.
- External-consumer sweep, all hits enumerated + disposed:
  - `gh` org:forcedotcom/salesforcecli: apex-language-support's own LSP `sendProgressNotification` helper; mobile-mcp-tools' MCP-SDK `ProgressNotification` type. Neither imports our symbol.
  - `gh api search/code?q=ProgressNotification+org:forcedotcom`: ui-preview + ci-testing have `packages/salesforcedx-utils-vscode/src/commands/{progressNotification,commandletExecutors,index}.ts` + jest test. Vendored/forked copies: their `package-lock.json` pins `@salesforce/salesforcedx-utils-vscode` to a **local** workspace (64.16.1 / 65.7.0, no `resolved` field), not this monorepo/npm (`npm view` → E404, never published). No live build/runtime coupling — deleting here has no effect on them.
  - Net: 0 real consumers.
- `i18n` key `progress_notification_text` ("Running %s" / "%s を実行しています。") sole consumer = the class.

Correct action: **delete** dead class + i18n keys + barrel export. Move-to-core is moot (no colocation target). Net effect matches WI intent (drop from utils-vscode).

Files:
- `packages/salesforcedx-utils-vscode/src/commands/progressNotification.ts` (delete)
- `packages/salesforcedx-utils-vscode/src/index.ts:10` (drop export)
- `packages/salesforcedx-utils-vscode/src/messages/i18n.ts:19` (drop key)
- `packages/salesforcedx-utils-vscode/src/messages/i18n.ja.ts:21` (drop key)

## Phases

### Phase 1 — delete dead ProgressNotification + i18n key

- `git rm` `progressNotification.ts`
- drop `export { ProgressNotification }` line, `index.ts`
- drop `progress_notification_text` key, `i18n.ts` + `i18n.ja.ts`

Commit: `refactor(utils-vscode): delete dead ProgressNotification + i18n key - W-23462557`

## Skills

- external-consumers — done (2 false positives, cleared)
- i18n-messages — key removal parity (en + ja both dropped)
- typescript — build/typecheck after removal

## Verification

- `npx wireit` / package build + `tsc` — no dangling import of removed export
- lint utils-vscode — no unused (rxjs still used by `notificationService.ts`, keep dep)
- grep confirms 0 `ProgressNotification` / `progress_notification_text` refs post-delete (excl playwright helper names `waitFor*ProgressNotificationGone`, unrelated)
- no e2e coverage relevant (class already unreachable; nothing to exercise)
