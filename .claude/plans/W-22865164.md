# W-22865164 — enable jest/prefer-to-contain

## Context
- `jest/prefer-to-contain: 'error'` to jest rules in `eslint.config.mjs` (after `jest/no-focused-tests`, ~line 562).
- Blocker `jest/no-focused-tests` already landed (line 562) — unblocked.
- Plugin `eslint-plugin-jest` 28.x already wired for test globs; no plugin add.
- `expect(x.includes(y)).toBe/toEqual/toStrictEqual(true|false)` → `.toContain`/`.not.toContain`; autofix.

## Violations found
Only bare-`.includes()`-as-sole-arg flagged. Confirmed:
- `packages/salesforcedx-vscode-apex-testing/test/jest/views/testController.test.ts:468-469`
  - `expect(orgMethod.tags?.includes(orgOnlyTag)).toBe(true)` → `expect(orgMethod.tags).toContain(orgOnlyTag)`
  - `.toBe(false)` → `.not.toContain(...)`
  - optional chain `tags?` — fix becomes `expect(orgMethod.tags)`; verify compiles (tags possibly undefined). Hand-adjust if needed.
- NOT flagged: `lwcCustomComponentsIndex.headless.spec.ts:44` — arg is `a.includes()||b.includes()` (logical OR), rule needs bare single `.includes()`. Leave as-is.

## Phases

### Phase 1 — enable rule + fix violations
- `eslint.config.mjs`: insert `'jest/prefer-to-contain': 'error',` after line 562.
- `npm run lint -- --fix` (builds local-rules plugin via wireit dep first) OR scoped autofix.
- `testController.test.ts` lines; confirm `tags?` optional-chain fix type-checks.
- commit: `test(lint): enable jest/prefer-to-contain ESLint rule - W-22865164`

## Skills to apply
- typescript, wireit, verification, packageJson, pr-draft, concise

## Verification
- `npm run lint` passes (DoD). Note: lint wireit task builds `packages/eslint-local-rules/out` first — required, `out/` absent in fresh worktree.
- `grep` confirms no remaining `expect(*.includes(*)).toBe/toEqual/toStrictEqual(...)` single-arg patterns.
- type-check touched test file (optional-chain fix).
- not e2e-covered (lint/config change).
