# W-23094607 — Bump eslint-plugin-unicorn to v68.0.0

## Context

- Current: `eslint-plugin-unicorn@64.0.0`, `eslint@^9.39.3` (installed 9.39.4).
- unicorn 68 peer: `eslint >=10.4`. unicorn 66+ all require eslint 10.4. **Forces ESLint 9→10 major upgrade.** Non-trivial bump.
- Flat config: `eslint.config.mjs` (root). Lint runs per-package via wireit (`lint` script → 30+ pkg `:lint`).
- unicorn rules listed explicitly in config (lines 165-209, off-overrides 542-543). Config NOT spread `unicorn/recommended` → v65-68 rules opt-in, no auto-error.
- Renamed/removed rules (v65-68) `prevent-abbreviations`→`name-replacements`, `no-array-for-each`→`no-for-each`, `prefer-dom-node-dataset`→`dom-node-dataset`, `no-hex-escape` removed, `better-regex` removed, `expiring-todo-comments`/`prefer-export-from` option renames. **None configured** → no rule-key edits needed.
- Node engines `>=22.15.1` satisfies unicorn `node>=22`. OK.

### Ecosystem ESLint-10 compat (verified via `npm view`, 2026-06)

Accept eslint 10 at **pinned versions** — no eslint-10 peer bump:
- `@typescript-eslint/eslint-plugin` + `@typescript-eslint/parser` + `typescript-eslint` @8.60.0 → peer `eslint: ^8.57.0 || ^9.0.0 || ^10.0.0`.
- `@html-eslint/eslint-plugin` + `@html-eslint/parser` @0.59.0 → peer `eslint: >=8.0.0 || ^10.0.0-0`.
- `eslint-config-prettier` @10.1.5 → peer `eslint: >=7.0.0`.
- `@stylistic/eslint-plugin-ts` @2.13.0 → peer `eslint: >=8.40.0` (open upper bound — eslint 10 satisfies). Earlier draft: peer `>=9` + 4.x bump; unneeded for peer compat. Only bump 2.13→4.x if eslint 10 trips a *runtime* failure in this plugin (verify in Phase 1; latest 4.4.1 peer is `>=9.0.0`).
- Loose-range / version-agnostic: eslint-plugin-functional, eslint-plugin-jsdoc, eslint-plugin-jest, @tony.ganchev/header, barrel-files, prefer-arrow, workspaces, jest-formatting.
- `@eslint/js` is **not** a direct devDependency (package.json has `@eslint/json`, not `@eslint/js`) — it's a transitive peer of `eslint` and upgrades automatically with the eslint bump. Nothing to manage in package.json.

- **Blocker**: `eslint-plugin-import@2.32.0` (latest) peer caps `^9`, no eslint 10. (a) accept via existing package.json `overrides` block (line 8) — runtime risk; (b) swap to `eslint-plugin-import-x@4.x` (peer incl `^10`), rename import in `eslint.config.mjs`. Prefer (a) if lint passes; fall back to (b).

## Phases

### Phase 1 — Bump ESLint 9→10 + (only) deps that need it
- `package.json`: only required edit for the eslint bump is `eslint ^9.39.3`→`^10.4.0` (or current). Other eslint plugins/configs declare eslint-10-compatible peers at their pinned versions (see Context) — do **not** bump @typescript-eslint/*, typescript-eslint, @html-eslint/*, eslint-config-prettier, @eslint/js (not a dep), or @stylistic for peer reasons.
- `eslint-plugin-import` peer: add to `overrides` accepting eslint 10, OR swap to `eslint-plugin-import-x` (update `eslint.config.mjs` import line 13 + plugins map).
- `npm install`; verify clean peer resolution (`check-peer-dependencies` runs via postinstall).
- Lint 2 packages, fix ESLint-10 flat-config API breaks (config-array, processors).
- **Verify @stylistic at runtime**: if eslint 10 throws inside `@stylistic/eslint-plugin-ts` 2.13.0 (despite open `>=8.40.0` peer), only then bump to `^4` and note the concrete error; otherwise leave at 2.13.0.
- Commit: `build(eslint): upgrade eslint 9→10 + plugins for unicorn v68 peer — W-23094607 (#XXX)`

### Phase 2 — Bump unicorn→68.0.0, fix lint errors
- `package.json` line 83: `eslint-plugin-unicorn 64.0.0`→`68.0.0`.
- `npm install`.
- `npm run lint` (full). Fix violations from bumped rules. Autofix where safe (`--fix`).
- No rule-key renames — confirm loads.
- Commit: `build(deps): bump eslint-plugin-unicorn to 68.0.0 — W-23094607`

### Phase 3 (if needed) — changelog
- Per changelog skill: dep bump = no customer-facing entry unless required.
- Commit: `chore(changelog): note eslint tooling upgrade — W-23094607`

## Skills to apply

- packageJson — version range conventions
- wireit — lint task graph
- concise — plan/doc style
- changelog — decide if entry needed
- typescript
- verification — pre-push checks

## Verification

- `npm install` clean (no unmet peer / no forced `--legacy-peer-deps`).
- `npm run lint` passes all packages.
- `npx eslint --print-config <sample.ts>` loads (no "rule not found") — catches renamed/removed rule refs.
- Spot-check unicorn rules: introduce temp `for...forEach`, confirm flagged, revert.
- `npm run compile`/typecheck unaffected.
- NOT e2e-covered: lint + dep resolution build-time; no branch e2e. Manual lint is gate.
