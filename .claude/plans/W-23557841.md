# W-23557841 — `@vscode/debugprotocol` → devDependencies

## Context

- `@vscode/debugprotocol` is type-only (`DebugProtocol` namespace = interfaces; erased at compile)
- still listed under `dependencies` in:
  - `packages/salesforcedx-vscode-apex-debugger` — 1 usage (`DebugProtocol.ExceptionBreakMode` in `src/index.ts`); esbuild-bundled
  - `packages/salesforcedx-apex-replay-debugger` — ~30 type usages (`adapter/types.ts`, `adapter/apexReplayDebug.ts`, `breakpoints/breakpointUtil.ts` + tests)
- move both to `devDependencies` so the type package is not a runtime/publish dep
- out of scope: `packages/salesforcedx-apex-debugger` also lists it under `dependencies` — WI does not include that package

## Phases

### 1. Move the dep in both package.json files

**Commit:** `chore: move @vscode/debugprotocol to devDependencies - W-23557841`

- `packages/salesforcedx-vscode-apex-debugger/package.json`: drop `@vscode/debugprotocol` from `dependencies`; add same version (`1.68.0`) to `devDependencies`
- `packages/salesforcedx-apex-replay-debugger/package.json`: same
- `npm install` at repo root so `package-lock.json` matches
- no source/import changes

## Skills to apply

- `packageJson` — dep vs devDep; don't duplicate a root-level devDep (this package is not at root)
- `verification` — compile + lint via `-w`; stop hook covers the rest
- `concise` — this plan

## Verification

WI: compile + lint both packages after the move.

- [x] `npm run compile -w salesforcedx-vscode-apex-debugger`
- [x] `npm run lint -w salesforcedx-vscode-apex-debugger`
- [x] `npm run compile -w @salesforce/salesforcedx-apex-replay-debugger`
- [x] `npm run lint -w @salesforce/salesforcedx-apex-replay-debugger`
- [x] both `package.json` files list `@vscode/debugprotocol` only under `devDependencies`
- no new tests — pjson-only; existing compile proves types still resolve
