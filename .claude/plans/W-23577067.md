# W-23577067: Remove stray LWC `@types/uuid` devDependency

## Evidence

- `packages/salesforcedx-vscode-lwc/package.json:43-48` declares `@types/uuid` only as a devDependency.
- No LWC `uuid` imports/requires or `@types/uuid` references.
- `package-lock.json:44759-44785` mirrors the LWC workspace devDependency.
- `package-lock.json:10486-10492` contains the resolved `@types/uuid` package entry; no other lockfile package declares it.
- Root `package.json:5-7` configures npm workspaces, so lockfile regeneration runs from the repository root.

## Phases

### Phase 1: Remove the dependency and refresh the lockfile

- Delete `@types/uuid` from `packages/salesforcedx-vscode-lwc/package.json`.
- From the repository root, run `npm install`.

## Applicable Skills

- `packageJson`: workspace dependency conventions and generated lockfile scope.
- `wireit`: uncached verification commands.
- `verification`: required checks.
- `concise`: terse plan.

## Verification

- `WIREIT_CACHE=none npm run prepush`
- `git diff --check`
- Search manifests and `package-lock.json`; confirm no remaining `@types/uuid` declaration or lock entry.

<!-- opencode-plan-evidence:{"planPath":".claude/plans/W-23577067.md","observedRevision":"2026-08-10T17:24:18.000+0000","summary":{"phases":["Removed the redundant Approach section.","Removed the duplicate Phase 1 confirmation step; Verification retains the dependency and lock-entry check."],"skills":["concise","verification","wi-build-pipeline","packageJson","wireit"],"verification":["Reread `.claude/plans/W-23577067.md`; both supplied revisions are resolved without scope expansion or fallback paths.","`git diff --check` passed.","Repository readiness hook `./.claude/hooks/verify-stop.sh` passed.","Plan remains uncommitted."]}} -->
