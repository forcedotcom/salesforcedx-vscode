# W-22865167 — Enable jest/valid-describe-callback

## Context
- Add `'jest/valid-describe-callback': 'error'` to jest rules block in `eslint.config.mjs` (~line 562, w/ other `jest/*`).
- Blocker W-11678119 landed: `jest/no-focused-tests` present line 562. Pattern, plugin baseline set.
- `eslint-plugin-jest` 28.14.0 installed, wired for test globs (`@salesforce/eslint-plugin-vscode-extensions` block, files list ~520-531). No plugin addition.
- Verified empirically: added rule, built `eslint-local-rules` (`npm run compile`), ran `eslint .` over 429 test files → **0 violations**. No code fixes needed.

## Phases

### Phase 1 — add rule
- Edit `eslint.config.mjs`: insert `'jest/valid-describe-callback': 'error',` after `'jest/no-focused-tests': 'error',` (~line 562).
- No violations to fix (confirmed).
- Files: `eslint.config.mjs`
- Commit: `chore: enable jest/valid-describe-callback eslint rule`

## Skills
- typescript (mandatory)
- concise (plan/docs)
- packageJson (only if dep change — none expected)
- feature-branch / pr-draft (PR stage)

## Verification
- `npm run lint` passes (gates on `eslint-local-rules:lint` + compile via wireit). Stop hook runs compile/lint/effect LS/test/vscode:bundle/knip — no manual runs needed.
- Grep `eslint.config.mjs` → rule present, `error`.
- Not e2e-covered: lint is build-gate, no runtime/e2e impact.
