# W-23450775 — composition-style + services-consumption refactor: org-browser

## Context

- Align `salesforcedx-vscode-org-browser` w/ `.claude/skills/effect-best-practices` composition-style + `services-extension-consumption`.
- In-package inconsistency; siblings (e.g. `getRetrieveMembers` already uses `Match.value`) do it right.
- 4 playwright specs cover retrieve/overwrite/tree: `customObject.headless`, `customTab.headless`, `folderedReport.headless`, `describe.scratch`.
- Files:
  - `packages/salesforcedx-vscode-org-browser/src/commands/retrieveMetadata.ts`
  - `.../src/tree/metadataTypeTreeProvider.ts`
  - `.../src/services/orgBrowserMetadataRetrieveService.ts`
  - `.../src/services/extensionProvider.ts`
  - `.../src/index.ts` (call site)
- Out of scope (separate WIs): Effect.repeat poll→.changes stream; globalState direct read→readGlobalStateKey.

## Phases

### Phase 1 — retrieveMetadata.ts: LS fix + composition + PromptService

commit: `refactor(org-browser): composition-style retrieveEffect + PromptService.confirmOrThrow - W-23450775`

- `:23` `return Effect.void` → `return yield* Effect.void` (avoid `Effect<Effect<void>>`).
- retrieveEffect post-retrieve UI (37-44): bind-then-branch-imperative → `Effect.tap` in pipe on `OrgBrowserRetrieveService.retrieve(...)`. Match `node.kind`: `component`/`customObject` → icon + fireChangeEvent; else `refreshType`. (literal effect-best-practices AVOID→PREFER).
- `confirmOverwrite` (84-101): replace hand-rolled `vscode.window.showWarningMessage` + `!== yesButton` + `api.services.UserCancellationError` w/ `(yield* api.services.PromptService).confirmOrThrow({ message: nls.localize('confirm_overwrite', String(count), typeName), confirmLabel: nls.localize('yes_button') })`. PromptService already in prebuiltServicesDependencies.
  - UI change (intended): confirmOrThrow renders `showWarningMessage(msg, { modal: true }, confirmLabel)` — a MODAL single-button dialog (`.monaco-dialog-box`), NOT the current non-modal 2-btn notification-list-item. `promptService.ts:76-81` confirms `{ modal: true }`. This changes the DOM the e2e specs target → specs + fixture MUST be updated in THIS phase (see below), or the 3 headless specs break on merge.
  - Drop now-unused `yes_button`/`no_button` locals + `vscode` import if unreferenced. Keep `yes_button` nls key (still passed as `confirmLabel`, still the button text 'Yes').

#### e2e adaptation (same commit — required so coverage survives the modal switch)

- **Fixture** `test/playwright/fixtures/desktopFixtures.ts`: currently `createDesktopTest({ fixturesDir, orgAlias })` with NO `userSettings`. Add `userSettings: { 'window.dialogStyle': 'custom' }` so the modal `showWarningMessage` routes through VS Code's DOM (`.monaco-dialog-box`) instead of a Playwright-inaccessible Electron native dialog. Mirrors every other package that clicks a `{modal:true}` warning (metadata `desktopFixtures.ts:60-66`, org, apex-oas, apex-testing). Ref `.claude/skills/playwright-e2e/references/coding-playwright-tests.md` 'Modal Dialog Buttons'.
- **Specs** — rewrite each overwrite-confirmation `test.step` in all 3 headless specs. The OLD pattern (`page.locator(NOTIFICATION_LIST_ITEM).filter({ hasText: /Overwrite.../i })` → `.getByRole('button', { name: /^Yes$/ }).click()`) targets the notification center and no longer renders. Replace with the shared modal helper `clickModalDialogButton(page, 'Yes')` (exported from `@salesforce/playwright-vscode-ext`, `src/pages/dialogs.ts` — waits for `.monaco-dialog-box`, clicks exact-name button). Do NOT blind-Escape (races the async render; see skill doc). The `confirm_overwrite` text is `'Overwrite local files for %s %s?'` — keep the per-type text assertion by asserting `.monaco-dialog-box` contains that regex before clicking (message still shown, just in the dialog element), or drop the text assert if awkward; the button label 'Yes' is deterministic.
    - `orgBrowser.customObject.headless.spec.ts` (85-91): 1 overwrite step.
    - `orgBrowser.customTab.headless.spec.ts` (103-110, 124-131): 2 overwrite steps.
    - `orgBrowser.folderedReport.headless.spec.ts` (122-132): 1 overwrite step.
  - Add `clickModalDialogButton` to the `@salesforce/playwright-vscode-ext` import; drop the now-unused `NOTIFICATION_LIST_ITEM` import from each spec IF no other step in that spec still uses it (customTab/folderedReport keep it for the `Retrieving …` toast assertion — verify per-spec before removing).

### Phase 2 — metadataTypeTreeProvider.ts: Match refactors

commit: `refactor(org-browser): if-chains to Match on tagged unions - W-23450775`

- `invalidateForNode` (115-128): 4-way if-chain on `node.kind` → `Match.value(node)` (`undefined`/`type`/`folderType`/`folder`/`customObject`), mirror sibling. `Match.orElse` no-op.
- `getChildrenOfTreeItem` (237-374): 6-way if-chain on `element.kind` (`customObject`/`folderType|type-folder`/`type`/`folder`/`component`/unsupported→`Effect.die`) → `Match.value`. Keep root (`!element`) branch as-is. Mirror `getRetrieveMembers` shape.

### Phase 3 — orgBrowserMetadataRetrieveService.ts: LS fixes + catchTag

commit: `refactor(org-browser): drop Effect.fail wrap, Effect.void, typed catchTag - W-23450775`

- `:39` `return yield* Effect.fail(new NoFilesRetrievedError(...))` → `return yield* new NoFilesRetrievedError(...)` (yieldable TaggedError).
- `:45` `onNone: () => Effect.succeed(undefined)` → `Effect.void`.
- `:51` `Effect.catchAll` → `Effect.catchTag('FsServiceError', ...)` — `showTextDocument` fails w/ `FsServiceError` (Data.TaggedError); preserve typed error.

### Phase 4 — extensionProvider.ts: shared buildAllServicesLayer

commit: `refactor(org-browser): shared buildAllServicesLayer from effect-ext-utils - W-23450775`

- Drop local `Layer.unwrapEffect` factory + `ExtensionProviderServiceLive`; import `buildAllServicesLayer` from `@salesforce/effect-ext-utils`.
- Shared helper omits `OrgBrowserRetrieveService.Default` → wrap: `export const buildAllServicesLayer = (context) => Layer.merge(sharedBuild(context, 'Salesforce Org Browser'), OrgBrowserRetrieveService.Default)` (OrgBrowserRetrieveService needs only ExtensionProviderService, present in shared).
- No `channel_name` nls key → pass literal fallback `'Salesforce Org Browser'`.
- Keep `AllServicesLayer`/`setAllServicesLayer`/`getOrgBrowserRuntime` + runtime types.
- `index.ts:236`: call site unchanged (`buildAllServicesLayer(context)` re-exported from extensionProvider).
- Remove now-unused imports (`ExtensionPackageJsonSchema`, `Schema`, etc.).

## Skills

- effect-best-practices (composition-style, Match, yieldable errors, Effect.void)
- services-extension-consumption (shared layer, PromptService, catchTag, prebuiltServicesDependencies)
- typescript
- verification
- playwright-e2e (specs already on branch)

## Verification

- `wireit` build + lint clean (org-browser + effect-ext-utils).
- effect-language-service diagnostics clear at cited lines (retrieveMetadata:23, orgBrowserMetadataRetrieveService:38/44/45).
- unit tests: org-browser jest suite.
- e2e (Phase 1 EDITS the overwrite specs — not passive coverage): re-run `customObject.headless`, `customTab.headless`, `folderedReport.headless` against the modal dialog after adding `window.dialogStyle: 'custom'` fixture + `clickModalDialogButton` swap. `describe.scratch` covers tree getChildren/refresh (unchanged shape).
- Playwright TS compile clean after import swap (`clickModalDialogButton` in, `NOTIFICATION_LIST_ITEM` out where unused).
