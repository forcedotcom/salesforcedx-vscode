# W-22966504 — Bump jsforce 3.10.15 → 3.10.16

## Context

- 3.10.16 ships [jsforce#1798](https://github.com/jsforce/jsforce/pull/1798) (drop XHR fallback in browser/worker realms) → fetch in VS Code extension host worker
- browser bundle affected (`dist/web/index.js` via `commonConfigBrowser`), not node bundle
- unblocks Web Console front-end proxy work
- four `package.json` files reference `jsforce` `^3.10.15`:
  - root `package.json`
  - `packages/salesforcedx-vscode-services/package.json`
  - `packages/salesforcedx-vscode-services-types/package.json`
  - `packages/salesforcedx-vscode-apex-log/package.json`
- related WI: W-22776037

## Phases

### Phase 1 — bump version + lockfile

- update all four `package.json` `jsforce` → `^3.10.16`
- single `npm install` from repo root → refresh `package-lock.json` (also normalizes a stale `@salesforce/templates` `66.10.0` → `^66.10.0` lockfile entry; source `package.json` already pinned `^`)
- commit: `chore(deps): bump jsforce to 3.10.16 - W-22966504`

files:

- `package.json`
- `packages/salesforcedx-vscode-services/package.json`
- `packages/salesforcedx-vscode-services-types/package.json`
- `packages/salesforcedx-vscode-apex-log/package.json`
- `package-lock.json`

### Phase 2 — open draft PR

- `pr-draft` → push + open draft PR against `develop`
- commit: none (skill action; no code change)
- done: PR URL recorded on WI

files:

- none

## Skills to apply

- `packageJson` — dependency hygiene
- `typescript` — type-level breakage check
- `concise` — plan/commit style
- `pr-draft` — draft PR

## Verification

Local checks before PR:

- `npm run compile` — clean (node-mode typecheck)
- `npm run lint` — clean
- `npm run check:dupes` — clean (jscpd; not covered by stop hook)
- `git diff package-lock.json` — expect:
  - (a) jsforce 3.10.15 → 3.10.16
  - (b) `@salesforce/templates` `66.10.0` → `^66.10.0` in core + services lockfile entries (source already pinned `^`)
  - anything else: red flag
- `npm run vscode:bundle -w packages/salesforcedx-vscode-apex-log` — verify `dist/web/index.js` bundles cleanly with 3.10.16
- `npm run vscode:bundle -w packages/salesforcedx-vscode-services` — same, for services consumer
- `npm run test:web -w packages/salesforcedx-vscode-apex-log` — web-mode Playwright against browser bundle
- `npm run test:web -w packages/salesforcedx-vscode-services` — same, for services

**Web Playwright specs covering the XHR→fetch path** (`playwright.config.web.ts`):

- `packages/salesforcedx-vscode-apex-log/test/playwright/specs/traceFlagsCrud.headless.spec.ts` — `traceflagsCommands.ts`: tooling API CRUD via `Connection`
- `packages/salesforcedx-vscode-apex-log/test/playwright/specs/executeAnonymous.headless.spec.ts` — `executeAnonymous.ts`: `conn.tooling.executeAnonymous` (XHR-fallback path pre-3.10.16)
- `packages/salesforcedx-vscode-apex-log/test/playwright/specs/logRetrieval.headless.spec.ts` — `logGet.ts`: ApexLog query + body download via jsforce
- `packages/salesforcedx-vscode-apex-log/test/playwright/specs/autoCollection.headless.spec.ts` — `logAutoCollect.ts`: ApexLog poll/query via jsforce
- `packages/salesforcedx-vscode-apex-log/test/playwright/specs/traceFlagsForOtherUser.headless.spec.ts` — same tooling-API path, non-current user
- `packages/salesforcedx-vscode-services/test/playwright/specs/retrieveOnLoadEmpty.headless.spec.ts` — `retrieveOnLoad.ts` → `metadataRetrieveService.ts`: metadata API via jsforce transport
- `packages/salesforcedx-vscode-services/test/playwright/specs/retrieveOnLoadMetadata.headless.spec.ts` — same, with metadata present
- `packages/salesforcedx-vscode-services/test/playwright/specs/retrieveOnLoadRetry.headless.spec.ts` — same, retry/error paths

`test:desktop` does **not** cover this path.
