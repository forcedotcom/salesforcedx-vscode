# W-23436145 — Extend doc-maintenance agent to shorten verbose new comments

## Context

- Recurring AI problem: verbose AI-authored `//` blocks narrating rationale/history/investigation. W-23424950/#7749: only manual /concise caught them pre-commit — nothing gates automatically.
- Fixture caveat: merged #7749 (84ff1f6) is post-/concise AFTER state — blocks already ≤3 lines. Don't assert reductions against it; pre-commit 4-7 line blocks never landed.
- Fix in doc-maintenance agent, not eslint.
- Current agent (`.claude/agents/doc-maintenance.md`, Responsibility 1): scoped to FIXING drift, "correct existing not add"; targets previously-X / no-longer-Y framing + per-item list narration — no rule for shortening newly-added over-long comments.
- Trigger already covers `**/*.ts`/`.tsx` via `.claude/rules/doc-maintenance.md` — no new trigger.
- Docs-only change; single file edited.

## Phases

### Phase 1 — extend agent comment responsibility to shorten verbose new comments

- Edit `.claude/agents/doc-maintenance.md` Responsibility 1 (Comments bullet):
  - Add: flag + shorten verbose NEW comments in diff. Trigger: `//` block ≥3 contiguous lines reading as prose/justification, not a what-it-does label.
  - Rule (relocation): move the shortened rationale to a JSDoc (`/** */`) on the symbol being explained, or to commit/PR body — not an inline narration block. Keep the inline comment to a 1-2 line what-it-does label.
  - Explicit reconciled rule text to write into the agent (do NOT defer wording): "Converting or relocating an over-long NEW inline block into a terse label plus (if the rationale is worth keeping) a JSDoc on the same symbol is shortening, not adding — the commentary already exists in the diff, you are only compressing and moving it. Do not introduce commentary that was absent from the diff entirely. Net comment volume must not increase."
  - Keep existing terse guidance (cut comments restating self-evident code).
- Apply concise skill to edited prose.
- File: `.claude/agents/doc-maintenance.md`
- Commit: `docs(agent): shorten verbose new code comments in doc-maintenance - W-23436145`

## Skills to apply

- concise — agent .md prose
- typescript — n/a (no .ts touched; agent .md only)
- verification — dry-run agent against synthetic fixture

## Verification

- Not e2e-covered (agent-instruction change; no branch e2e; no build/test wiring touched).
- Synthetic fixture (author it in the plan/PR body, do not rely on 84ff1f6): a small `.ts` diff hunk containing a fabricated 5-7 line `//` block narrating investigation history above a symbol — the pre-/concise shape #7749 originally had. Example shape:
  ```
  +  // We discovered during the macOS e2e investigation that the daemon
  +  // reveals the Output panel, which blurs any open quick input, which
  +  // VS Code then cancels, which surfaces as "User cancelled". The fix
  +  // is to only reveal when the user asks, mirroring the port-conflict
  +  // pattern in executeOrgLoginWeb. See W-23424950 for the full trace.
  +  const selection = yield* Effect.promise(() => vscode.window.showWarningMessage(...));
  ```
- Pass criterion: doc-maintenance, given that hunk, (a) flags the block, (b) reduces the inline block to a 1-2 line what-it-does label (e.g. `// Reveal only on user click; forkDaemon must not steal focus from an open picker.`), (c) if it preserves rationale, does so as a JSDoc on the symbol, and (d) adds no commentary absent from the input diff. Fail if it leaves the 5-7 lines, or if it invents new prose.
- Manual dry-run only (haiku agent, no automated harness). Record before/after line counts of the synthetic hunk in the PR body as the acceptance evidence.
- No src logic edits.
