# W-22936356 — Bump @playwright/test to 1.60.0 (Chromium 148)

## Context

- Playwright 1.58.2 (resolved in lock) bundles Chromium 145; VS Code 1.124 ships Chromium 148.
- Desktop E2E unpinned (latest VS Code) fails at `electronApp.firstWindow()` — "Target page... closed".
- CI masks via repo var `PLAYWRIGHT_VSCODE_VERSION=1.116.0`.
- 1.60.0 = latest on npm; bundles Chromium 148.0.7778.96.

### Findings (versions differ slightly from WI text)

- root `package.json:40` `@playwright/test` `^1.49.0`
- root `package.json:104` `playwright` `^1.49.0` (dependencies) — shares `playwright-core`; bump too for single-version resolution
- `packages/playwright-vscode-ext/package.json:36` `^1.57.0`
- `packages/salesforcedx-vscode-lwc/package.json:45` `^1.49.0`
- `packages/salesforcedx-vscode-apex-replay-debugger/package.json:42` `@playwright/test` `"*"` — floats to root; no edit needed (WI lists it but `*` already tracks bump)
- all resolve to single `playwright-core` 1.58.2 in lock

### Out of scope (manual follow-up, per WI)

- repo vars `PLAYWRIGHT_VSCODE_VERSION` / `PLAYWRIGHT_DESKTOP_VSCODE_VERSION` (gh/UI only)

## Phases

### Phase 1 — Bump playwright to 1.60.0 + relock

- edit `^1.49.0`/`^1.57.0` → `^1.60.0`:
  - root `@playwright/test` (40), root `playwright` (104)
  - `playwright-vscode-ext` (36)
  - `salesforcedx-vscode-lwc` (45)
- leave apex-replay-debugger `"*"` unchanged
- `npm install` → update `package-lock.json` (verify lock resolves `@playwright/test`, `playwright`, `playwright-core` all 1.60.0)
- `npx playwright install chromium` if needed for local run

commit msg: `chore: bump @playwright/test to 1.60.0 for Chromium 148 (W-22936356)`

files:
- `package.json`
- `packages/playwright-vscode-ext/package.json`
- `packages/salesforcedx-vscode-lwc/package.json`
- `package-lock.json`

## Skills

- typescript
- packageJson — devDep dup rule (note: pre-existing root/lwc/ext dup, not in scope)
- playwright-e2e — local desktop run, span/trace debugging
- wireit — build graph if relock triggers rebuild
- verification

## Verification

- `node -e` lock check: all three packages = 1.60.0 (above)
- no other `1.49`/`1.57` playwright refs left: `grep` package.json files
- typecheck/build unaffected (devDep version bump only) — **e2e/CI-covered**

### Desktop E2E (primary DoD) — run named specs locally, unpinned

All desktop specs go through `createDesktopTest` fixture which calls `electronApp.firstWindow()` (`packages/playwright-vscode-ext/src/fixtures/createDesktopTest.ts:384`) — the exact call that throws "Target page... closed" on the Chromium 145/148 mismatch. Any desktop spec exercises the fix.

Run with **no `PLAYWRIGHT_VSCODE_VERSION` / `PLAYWRIGHT_DESKTOP_VSCODE_VERSION` set** (so it pulls latest VS Code 1.124, Chromium 148) and `--retries 0` (a flaky retry must not mask a real `firstWindow()` failure):

- apex-replay-debugger (covers `apexReplayDebugger.desktop.spec.ts`, `checkpoints.desktop.spec.ts`):
  - `npm run test:desktop -w salesforcedx-vscode-apex-replay-debugger -- --retries 0`
- OR lwc (covers `lwcDebugTests.desktop.spec.ts`, `lwcRunTests.desktop.spec.ts`):
  - `npm run test:desktop -w salesforcedx-vscode-lwc -- --retries 0`

Pass = specs reach test body (browser context opens); `electronApp.firstWindow()` no longer throws "Target page closed". On failure inspect span/trace per playwright-e2e skill.
