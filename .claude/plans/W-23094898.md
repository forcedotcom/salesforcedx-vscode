# W-23094898 — Enable unicorn/no-double-comparison

## Context

- Enable `unicorn/no-double-comparison` (error). Disallows 2 comparisons of same operands combinable into 1.
- Plugin `eslint-plugin-unicorn@68.0.0`; rule file present at `node_modules/eslint-plugin-unicorn/rules/no-double-comparison.js`.
- Verified: 0 violations across 1447 linted files (added rule temporarily, ran `eslint . -f json`, filtered ruleId).
- Pure config change. No source edits needed.
- Depends W-23094607 (unicorn v68 bump) — merged at 0b4b10ce5; branch current.

## Phases

### Phase 1 — enable rule

- `eslint.config.mjs`: add `'unicorn/no-double-comparison': 'error',` in alpha order between `no-array-sort` and `no-empty-file` (after line 171).
- commit: `chore(lint): enable unicorn/no-double-comparison - W-23094898`

## Skills

- concise (plan/doc style)
- typescript
- verification

## Verification

- `npx eslint .` clean for the new rule (build `packages/eslint-local-rules` first via `npm run compile` if `out/index.js` missing). Use `NODE_OPTIONS=--max-old-space-size=8192` to avoid OOM on full-repo lint.
- 0 violations confirmed during planning; no follow-up fixes expected.
- No e2e coverage applicable (lint-config only).
