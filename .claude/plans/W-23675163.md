# W-23675163 — Remove superseded Dreamhouse scratch org setup

## Context

- Two Dreamhouse scratch-org setup modules in `playwright-vscode-ext`
- Canonical: `src/orgs/dreamhouseScratchOrgSetup.ts` — exported as `createDreamhouseOrg` / `DREAMHOUSE_ORG_ALIAS` from package index
- Superseded: `src/utils/dreamhouseScratchOrgSetup.ts` — older `create` + inline JSON-shape checks; **zero imports**
- WI: delete the unused copy. PR #8031 (closed) was the same change
- CI / `create-e2e-scratch-org.sh` / skill `local-setup.md` already match `orgs/` (clone + scratch + deploy + permset)

## Phases

### 1. Delete unused setup module

**Commit:** `chore: remove superseded Dreamhouse scratch org setup - W-23675163`

- Delete `packages/playwright-vscode-ext/src/utils/dreamhouseScratchOrgSetup.ts`
- Do not change `src/orgs/dreamhouseScratchOrgSetup.ts`, index export, workflows, or the org-browser shell script

## Skills to apply

- `concise` — this plan
- `playwright-e2e` — Dreamhouse setup lives in `orgs/`; CI + `create-e2e-scratch-org.sh` stay source of truth
- `verification` — compile/knip via stop hook; no workflow edits so skip `check:actions`

## Verification

- [ ] `rg dreamhouseScratchOrgSetup` — only `src/orgs/` + index export + comment in `create-e2e-scratch-org.sh`
- [ ] no remaining import of `utils/dreamhouseScratchOrgSetup`
- [ ] compile / knip (stop hook) — deleted file unused, should stay clean
- Existing E2E: not required to re-run for a dead-file delete; consumers already call `orgs/`
