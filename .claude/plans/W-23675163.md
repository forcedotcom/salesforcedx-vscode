# W-23675163 — Remove superseded Dreamhouse scratch org setup

## Context

- Two Dreamhouse scratch-org setups in `packages/playwright-vscode-ext`:
  - `src/orgs/dreamhouseScratchOrgSetup.ts` — exported (`createDreamhouseOrg`, `DREAMHOUSE_ORG_ALIAS`) via `src/index.ts`
  - `src/utils/dreamhouseScratchOrgSetup.ts` — unused (`create` + same alias); no imports
- WI: delete unused duplicate; keep required behavior on exported impl
- Same alias, repo URL, clone/create/deploy/permset
- `orgs` already has that via `tryUseExistingOrg`, `requireOrgInCI`, `extractAuthFields`
- utils-only bits (inline JSON guards, `create` name) unused — do not port
- bash `create-e2e-scratch-org.sh` independent; comment names the module conceptually

## Phases

### 1. Delete unused utils setup

**Commit:** `chore: remove unused utils/dreamhouseScratchOrgSetup - W-23675163`

- Delete `packages/playwright-vscode-ext/src/utils/dreamhouseScratchOrgSetup.ts`
- Leave `src/orgs/dreamhouseScratchOrgSetup.ts` and `src/index.ts` export as-is

## Skills to apply

- `playwright-e2e` — helper compile/lint/test scripts
- `concise` — this plan
- `typescript` — leftover import/type issues after delete

## Verification

- Repo grep: no remaining imports of `utils/dreamhouseScratchOrgSetup`
- `npm run compile -w @salesforce/playwright-vscode-ext`
- `npm run lint -w @salesforce/playwright-vscode-ext`
- `npm run test:web -w @salesforce/playwright-vscode-ext` and `npm run test:desktop -w @salesforce/playwright-vscode-ext` — helper package suite (workbench/helpers). Does **not** call `createDreamhouseOrg`. Proves delete does not break compile/lint/helper tests.
- No new tests for a deleted unused file
