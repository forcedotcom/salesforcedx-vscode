# W-23447405

## Context

`orgCreateCommand` already reads the target Dev Hub through `ConfigService`, but calls `getTargetDevHubOrAlias(true)` only to reproduce its missing-Dev-Hub prompt. Remove that duplicate config read and inline the existing localized message, button, and `sf.org.login.web.dev.hub` action in the org extension.

Files:

- `packages/salesforcedx-vscode-org/src/commands/orgCreate.ts`
- `packages/salesforcedx-vscode-org/src/messages/i18n.ts`
- `packages/salesforcedx-vscode-org/test/jest/commands/orgCreate.test.ts`

## Phases

### 1. Specify the prompt behavior

Commit: `test(org): specify missing Dev Hub prompt behavior`

- Remove the `getTargetDevHubOrAlias` test double.
- Assert that a missing Dev Hub displays the existing localized message and button.
- Assert that selecting the button executes `sf.org.login.web.dev.hub`; dismissal still cancels before input gathering or CLI execution.

### 2. Inline the prompt

Commit: `fix(org): inline missing Dev Hub prompt in org create`

- Move the existing missing-Dev-Hub message and button copy into the org extension runtime catalog.
- Remove the `getTargetDevHubOrAlias` import and call from `orgCreate.ts`.
- When `ConfigService.getTargetDevHub()` returns `undefined`, show the localized informational message with its button, execute `sf.org.login.web.dev.hub` only when selected, then return `UserCancellationError`.

## Skills to apply

- `concise`: keep plan and comments minimal.
- `typescript`: preserve repository TypeScript conventions.
- `i18n-messages`: add runtime message keys and placeholders correctly.
- `vscode-window-messages`: preserve message and button behavior.
- `verification`: verify the focused change and required repository checks.

## Verification

- Run the Jest tests in `packages/salesforcedx-vscode-org/test/jest/commands/orgCreate.test.ts`.
- Confirm both button selection and dismissal paths cancel before scratch-org prompts or `sf org create scratch` execution.
- Run the repository stop-hook checks: compile, lint, Effect LS, tests, bundle, and knip.
- Run `packages/salesforcedx-vscode-org/test/playwright/specs/orgCreateMissingDevHub.desktop.spec.ts` and the desktop Playwright suite with retries disabled. The package has no web Playwright configuration.
