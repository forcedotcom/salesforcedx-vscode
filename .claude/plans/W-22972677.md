# W-22972677 — remove unused jsforce & esbuild deps from apex-log

## Context
- `packages/salesforcedx-vscode-apex-log/package.json` declares 2 unused deps:
  - `jsforce` (dep `^3.10.15`): no import in src/test; only comment in `src/traceFlags/traceFlagJsonSync.ts:49`. Connection via services `ConnectionService`; code hand-rolls `ConnectionLike`/`UserRecord`.
  - `esbuild` (devDep `0.27.4`): `esbuild.config.mjs` imports `esbuild`, but root `package.json:69` declares same `0.27.4` → hoisted. 15 pkgs use `esbuild.config.mjs`; only apex-log declares local devDep; other 14 rely on hoisted root.
- No code change. Bundling/types unaffected.

## Phases
### Phase 1 — remove both deps
- Edit `packages/salesforcedx-vscode-apex-log/package.json`: drop `jsforce` line (deps), `esbuild` line (devDeps).
- Files: `packages/salesforcedx-vscode-apex-log/package.json`
- Commit: `chore(apex-log): remove unused jsforce and esbuild deps - W-22972677`

## Skills to apply
- packageJson
- typescript
- verification

## Verification
- `git diff` shows only 2 deleted lines.
- bundle resolves hoisted esbuild: apex-log `vscode:bundle` succeeds.
- typecheck/compile → passes (no jsforce imports).
- not e2e-covered (dep hygiene; no runtime behavior).
