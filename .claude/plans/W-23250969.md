# W-23250969 — migrate `sf.config.set` → `ConfigService.setTargetOrg`

## Context

- Replace `sf.config.set` vscode command w/ `ConfigService.setTargetOrg(usernameOrAlias)` Effect in `salesforcedx-vscode-services`.
- Sole runtime caller: org picker `orgPicker/orgList.ts:209` (`executeCommand('sf.config.set', usernameOrAlias)`). Not in palette. Pure `@salesforce/core` `Config`, no CLI.
- `ConfigService` owns `unsetTargetOrg`/`unsetTargetDevHub`, no setter — fix asymmetry.
- Status-bar refresh after set is **driven by the picker, not the file watcher**. `setTargetOrg` only writes config + invalidates the aggregator. The picker then calls `ConnectionService.invalidateCachedConnections()` + `ConnectionService.getConnection()`, which sets `defaultOrgRef` directly (`connectionService.ts:371` `SubscriptionRef.set(defaultOrgRef, updated)`). This is deterministic and identical on desktop + web — NO dependency on `createFileSystemWatcher` firing for the happy path (see Adversarial concerns: web watcher).
- `setTargetOrg` lives in `ConfigService`; it must NOT call `ConnectionService` (would create a ConfigService→ConnectionService cycle — ConnectionService already depends on ConfigService `connectionService.ts:205`). The connection refresh is orchestrated in the picker (`orgList.ts`), which already holds `api.services` for both.
- Picker filters expired orgs (`orgList.ts:162` `.filter(o => o.isExpired !== true)`) → drop old `Org.create` expiry/existence check. `Org.create` in old `setTargetOrgOrAlias` (`orgUtil.ts:146`) was an existence-validation side effect before write; picker only offers known, non-expired orgs so it is redundant. Accept seconds-wide TOCTOU. No `process.chdir` (legacy; core sets cwd at activation, `unsetTargetOrg` writes local config w/o chdir).
- **No project-precondition guard needed.** Old `configSet.ts` wrapped the write in `SfCommandlet(sfProjectPreconditionChecker, …)`. The org extension activates only via `workspaceContains:sfdx-project.json` (`package.json:197-198`) and `sf.set.default.org` is gated `"when": "sf:project_opened"` (`package.json:264-265`); the picker/status bar only exist inside an SF project. The precondition is structurally guaranteed by activation — dropping the explicit checker is deliberate and safe. No guard added to `setTargetOrg` or the picker.

### Findings (deviations from WI text)

- `updateConfigAndStateAggregators` / internal `refreshConnection` in `orgUtil.ts`: WI says drop "if no other caller" — they HAVE many callers (orgDelete, orgList, orgCreate, all auth commands). KEEP both.
- Removable from `orgUtil.ts`: only `setTargetOrgOrAlias` (export) + `setUsernameOrAlias` (internal) — sole consumer is `configSet.ts`.
- `config_set_org_text` nls key: KEEP — `sf.set.default.org` palette title uses `%config_set_org_text%` (`package.json:320`). Removable nls keys (only `configSet.ts` uses): `config_set_name`, `table_header_name`, `table_header_value`, `table_header_success`.
- e2e `orgPicker.desktop.spec.ts` asserts `SFDX: Set a Default Org successfully ran` toast (4x) + output-channel `target-org` table. Post-migration: no commandlet → no toast, no table. Spec MUST change to status-bar-only assertions. On-branch e2e work, part of this change.
- Two more desktop specs reference `config_set_org_text` and must be handled (Phase 4):
  - `orgCommands.desktop.spec.ts:33` `verifyCommandExists(page, packageNls.config_set_org_text, 60_000)` — checks the palette title `SFDX: Set a Default Org`. `sf.set.default.org` keeps title `%config_set_org_text%` (`package.json:319-320`), so the command still resolves. **No change needed; confirm it still passes** (do NOT delete).
  - `telemetryIdentitySeeding.desktop.spec.ts:72` `executeCommandWithCommandPalette(page, packageNls.config_set_org_text)` — runs the command by its palette title to trigger services activation/seeding. Post-migration the title still maps to `sf.set.default.org` (which opens the picker). The picker `showQuickPick` will block on a modal with no selection. Switch this to the surviving command in a way that doesn't hang: invoke a command that activates services without opening a modal picker (e.g. `executeCommandWithCommandPalette(page, packageNls.org_login_web_authorize_org_text)` opens an input flow, or use an existing non-modal activation command the spec already trusts). **Decision: switch to `sf.set.default.org` only if the picker can be dismissed deterministically; otherwise pick a non-blocking activation command.** Resolve concretely in Phase 4 — the current `config_set_org_text` palette entry remains valid but opening the picker mid-spec is a misfire.
- No web playwright harness exists in this package — only `playwright.config.desktop.ts` (`test/playwright/`). There is NO web/headless config, memfs fixture, or wireit script for `salesforcedx-vscode-org`. A new `orgPicker.web.spec.ts` would require standing up an entire web harness. The plan instead removes the web-watcher risk at the source (picker-driven ref refresh) so no web e2e is required to de-risk this change — see Adversarial concerns.

## Phases

### Phase 1 — add `ConfigService.setTargetOrg`

- `core/configService.ts`: add `setTargetOrg(usernameOrAlias: string)` Effect mirroring `unsetTargetOrg`:
  - `Config.create(Config.getDefaultOptions())` → `config.set(OrgConfigProperties.TARGET_ORG, value)` → `write()` → `invalidateConfigAggregator()`.
  - Do NOT touch `defaultOrgRef` here (no `clearDefaultOrgRef`, no `set`). `setTargetOrg` cannot reconstruct the full `DefaultOrgInfoSchema` (orgId/userId/isScratch require a live connection) and must not depend on `ConnectionService` (cycle). The ref is refreshed by the caller (picker) via `ConnectionService.getConnection()` — see Phase 2.
- Add `setTargetOrg` to the returned accessor object.
- Files: `packages/salesforcedx-vscode-services/src/core/configService.ts`
- Commit: `feat(services): add ConfigService.setTargetOrg Effect - W-23250969`

### Phase 2 — picker calls service; remove command

- `orgPicker/orgList.ts:208-209`: `usernameOrAlias` is already `selection.orgAlias ?? selection.orgUsername ?? ''` where `orgAlias = orgAuth.aliases?.[0]` (`orgList.ts:121`) — a single alias, never comma-separated. Replace `vscode.commands.executeCommand('sf.config.set', usernameOrAlias)` with:
  - `yield* api.services.ConfigService.setTargetOrg(usernameOrAlias)` (write config; `api` already yielded `:189`).
  - then `yield* api.services.ConnectionService.invalidateCachedConnections()` + `yield* api.services.ConnectionService.getConnection()` to refresh `defaultOrgRef` deterministically (sets ref at `connectionService.ts:371`), updating the status bar on both desktop and web without relying on the file watcher. Catch/log connection failure so a bad selection doesn't crash the picker effect (the watcher's `clearDefaultOrgRef` fallback no longer applies for the set path).
  - **Do NOT add `.split(',')[0]`.** The old `configSet.ts:32` split operated on a raw `executeCommand` argument; the picker already passes a single `aliases?.[0]`. A no-op split is misleading and could mangle an alias containing a comma. Pass the value unchanged.
- Remove `commands/configSet.ts`.
- `commands/index.ts:8`: drop `export { configSet }`.
- `index.ts`: drop `configSet` import (`:20`) + registration `registerCommand('sf.config.set', configSet)` (`:41`).
- `util/orgUtil.ts`: remove `setTargetOrgOrAlias` (export) + `setUsernameOrAlias` (internal). Drop now-unused imports (`Org`; `OrgConfigProperties`/`Config`/`workspaceUtils` only if no remaining use — verify).
- `messages/i18n.ts` + `messages/i18n.ja.ts`: remove `config_set_name`, `table_header_name`, `table_header_value`, `table_header_success`. KEEP `config_set_org_text`.
- Files: `orgPicker/orgList.ts`, `commands/configSet.ts` (del), `commands/index.ts`, `index.ts`, `util/orgUtil.ts`, `messages/i18n.ts`, `messages/i18n.ja.ts`
- Commit: `refactor(org): call ConfigService.setTargetOrg from picker, drop sf.config.set command - W-23250969`

### Phase 3 — jest tests

- `test/jest/orgPicker/orgList.test.ts`: stub `ConfigService.setTargetOrg` AND `ConnectionService.invalidateCachedConnections` + `ConnectionService.getConnection` in `buildLayer` mockServicesApi (each returns an Effect). Replace `executeCommandMock` assert `'sf.config.set','SeededOrg'` (`:246`) w/ assert `setTargetOrg` called w/ `'SeededOrg'` followed by `getConnection`. Update describe/it text mentioning `sf.config.set` (`:235,315,333,351,375,442`).
- `test/jest/util/orgUtil.test.ts`: remove `setTargetOrgOrAlias` import (`:31`) + `describe('testing setTargetOrgOrAlias')` block (`:333-`). Keep `updateConfigAndStateAggregators` tests.
- Files: `test/jest/orgPicker/orgList.test.ts`, `test/jest/util/orgUtil.test.ts`
- Commit: `test(org): assert ConfigService.setTargetOrg, drop setTargetOrgOrAlias tests - W-23250969`

### Phase 4 — e2e specs (3 desktop specs)

- `test/playwright/specs/orgPicker.desktop.spec.ts`: remove `SET_DEFAULT_ORG_RAN` toast waits (4x) + `output channel reports target-org` step (no commandlet/toast/table post-migration). Keep status-bar transitions (`expectOrgPickerStatusBar`) as the user-facing signal — now picker-driven via `ConnectionService.getConnection()` (deterministic, no toast to gate on). After a selection the status bar updates within the picker effect; assert with a short timeout. Drop `ORG_OUTPUT_CHANNEL`/`SET_DEFAULT_ORG_RAN` consts (now unused). Update header comment. **This spec is the blocking e2e gate for the set-default-org refresh** (desktop). Web is de-risked by design (picker-driven ref, not watcher) — no web spec needed.
- `test/playwright/specs/telemetryIdentitySeeding.desktop.spec.ts:72`: change `executeCommandWithCommandPalette(page, packageNls.config_set_org_text)`. Post-migration that title maps to `sf.set.default.org` which opens a modal `showQuickPick` and would hang the spec. Replace with a non-blocking command that still activates services + seeds identities (the spec only needs activation, not a default-org change). Confirm chosen command activates services without leaving a modal open; if the picker is used, dismiss it deterministically (`page.keyboard.press('Escape')`). Resolve the exact command during implementation.
- `test/playwright/specs/orgCommands.desktop.spec.ts:33`: `verifyCommandExists(page, packageNls.config_set_org_text, 60_000)` stays — `sf.set.default.org` keeps title `%config_set_org_text%`. No edit; re-run to confirm green.
- Files: `test/playwright/specs/orgPicker.desktop.spec.ts`, `test/playwright/specs/telemetryIdentitySeeding.desktop.spec.ts`, `test/playwright/specs/orgCommands.desktop.spec.ts` (verify-only)
- Commit: `test(playwright-e2e): org picker default-org via status bar, fix telemetry-seeding command - W-23250969`

## Skills to apply

- effect-best-practices — `Effect.fn`, accessors pattern matching `unsetTargetOrg`
- typescript — `const`/`type`, no loops
- services-extension-consumption — picker consumes `api.services.ConfigService`
- playwright-e2e — Phase 4 spec edits
- packageJson — confirm no `sf.config.set` cmd entry to remove (none exists; only `sf.set.default.org`)
- ts1261-filename-casing — if file deletions touch imports
- concise — this plan + comments
- verification — final gate

## Verification

- `npm run compile` (or package build) clean — vscode-services + vscode-org.
- lint clean (no unused imports after `orgUtil.ts` prune).
- jest: `salesforcedx-vscode-org` orgList.test + orgUtil.test green.
- grep: no remaining `sf.config.set` / `configSet` / `setTargetOrgOrAlias` / `setUsernameOrAlias` refs (src + test).
- grep: removed nls keys gone from both i18n files; `config_set_org_text` retained.
- e2e desktop (`orgPicker.desktop.spec.ts`): picker switches default org; status bar reflects new org via picker-driven `getConnection()` (no toast, no manual refresh); back-and-forth switching. Verifies the picker repopulates `defaultOrgRef` after `setTargetOrg` write.
- e2e desktop (`telemetryIdentitySeeding.desktop.spec.ts`): green after switching off the now-modal `config_set_org_text` command.
- e2e desktop (`orgCommands.desktop.spec.ts`): green unchanged (`config_set_org_text` palette title still resolves to `sf.set.default.org`).
- **Web — de-risked by design, not by watcher.** Status-bar refresh after set is driven by `ConnectionService.getConnection()` in the picker (`SubscriptionRef.set` at `connectionService.ts:371`), which runs identically on web and desktop and does NOT depend on `vscode.workspace.createFileSystemWatcher` firing for memfs writes (`fileWatcherService.ts:22`). No web e2e harness exists for this package and none is added; the watcher uncertainty no longer gates the user-visible flow. (Watcher remains a secondary/global-config path; not relied on for picker refresh.)
