# W-23559992

## Context

Adopt `PromptService.considerUndefinedAsCancellation` at the remaining direct VS Code prompt call sites so `undefined` prompt results use the shared user-cancellation path instead of local handling or unchecked values.

Source files identified from remaining direct prompt calls:

- `packages/salesforcedx-vscode-apex-debugger/src/index.ts`
- `packages/salesforcedx-vscode-apex-log/src/commands/createApexTrigger.ts`
- `packages/salesforcedx-vscode-apex-oas/src/oas/externalServiceRegistrationManager.ts`
- `packages/salesforcedx-vscode-apex-replay-debugger/src/index.ts`
- `packages/salesforcedx-vscode-apex-testing/src/commands/apexTestSuite.ts`
- `packages/salesforcedx-vscode-apex/src/languageUtils/languageClientManager.ts`
- `packages/salesforcedx-vscode-metadata/src/commands/refreshSObjects.ts`
- `packages/salesforcedx-vscode-org/src/commands/auth/orgLogout.ts`
- `packages/salesforcedx-vscode-org/src/parameterGatherers/selectDeletableOrg.ts`

## Phases

### 1. Adopt shared prompt cancellation

- Route each remaining single-value `showQuickPick`, `showInputBox`, or single-selection `showOpenDialog` result through `PromptService.considerUndefinedAsCancellation`.
- Use the existing services extension API and Effect execution pattern in each package.
- Preserve prompt options and successful-selection behavior.
- Add or update focused tests for cancellation and successful selection where affected behavior has test coverage.
- Commit: `feat: adopt shared cancellation for remaining prompts`

## Skills to apply

- `typescript`
- `effect-best-practices`
- `services-extension-consumption`
- `vscode-window-messages`
- `verification`

## Verification

- Run focused tests for each changed prompt command or manager.
- Run type-checking for every changed package.
- Run lint for every changed package.
- Confirm each changed prompt converts `undefined` into the shared user-cancellation error and preserves a selected value.
