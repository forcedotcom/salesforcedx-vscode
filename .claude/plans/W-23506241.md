# W-23506241: Use `Effect.filterOrFail` for the empty-logs guard

## Scope

- Package: `salesforcedx-vscode-apex-log`
- Files:
  - `packages/salesforcedx-vscode-apex-log/src/commands/logGet.ts`
  - `packages/salesforcedx-vscode-apex-log/test/unit/commands/logGet.test.ts`

## Skills

- `concise`
- `effect-best-practices`
- `verification`

## Phases

1. Pipe `logService.listLogs()` through `Effect.filterOrFail(logs => logs.some(() => true), () => new LogGetNoLogsError({ message: nls.localize('log_get_no_logs') }))`.
   - Preserve `LogGetNoLogsError`, localized `log_get_no_logs`, and downstream selection/body/save/open behavior.

2. Add a focused `logGetCommand` unit test following the `ExtensionProviderService` setup in `test/unit/traceFlags/pickTraceFlag.test.ts:32-40`.
   - Provide an `ApexLogService` whose `listLogs` returns `[]`; run the command to an `Exit`.
   - Assert failure with `_tag: 'LogGetNoLogsError'` and `message: 'No Apex debug logs found'` from `log_get_no_logs`.
   - Assert `PromptService`, `vscode.window.showQuickPick`, `getLogBody`, and mocked `saveAndOpenLog` are not invoked.

3. Verify.
   - Run the focused unit test with the package Jest config.
   - Retain existing non-empty Quick Pick/editor-open Playwright coverage at `packages/salesforcedx-vscode-apex-log/test/playwright/specs/logRetrieval.headless.spec.ts:97-114`; no Salesforce mutation.
   - `npm run check:dupes`; changed-code findings.
   - `effect-advocate` diff review; actionable findings.
   - Root readiness: `WIREIT_CACHE=none npm run compile`, `WIREIT_CACHE=none npm run lint`, `WIREIT_CACHE=none npm run check:effect-diagnostics`, `WIREIT_CACHE=none npm run test`, `WIREIT_CACHE=none npm run vscode:bundle`, `WIREIT_CACHE=none npm run check:knip`.
   - Diff scope: plan + the 2 scoped source/test files.

<!-- opencode-plan-evidence:{"planPath":".claude/plans/W-23506241.md","observedRevision":"2026-08-10T15:41:23.000+0000","summary":{"phases":["Replace the empty-list guard with the required `Effect.filterOrFail(logs => logs.some(() => true), ...)` while preserving the existing error, localization, and downstream success flow.","Add a focused `logGetCommand` unit test for `listLogs()` returning `[]`, including the typed/localized failure and absence of PromptService, Quick Pick, body retrieval, save, and open calls.","Verify the focused unit test, retain the existing non-empty Playwright coverage, review Effect usage, and run repository readiness checks."],"skills":["Applied `concise`, `effect-best-practices`, and `verification` guidance.","Removed the repetitive source-inspection phase and limited scope to `logGet.ts` plus one command unit test."],"verification":["`git diff --check -- .claude/plans/W-23506241.md` passed.","Repository readiness commands passed: compile, lint, Effect diagnostics, test, VS Code bundle, and knip.","Plan remains uncommitted at `.claude/plans/W-23506241.md`."]}} -->
