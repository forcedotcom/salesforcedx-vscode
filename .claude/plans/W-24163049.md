# W-24163049 — Drop utils-vscode `sendException` call sites

## Context

Remove the 4 production calls to `TelemetryService.sendException` from `salesforcedx-vscode-core` and `salesforcedx-utils-vscode`. Preserve each surrounding control flow; remove imports and helpers made unused by deleting those calls. This prepares consumers to stop using the utils-vscode exception telemetry API without changing that API in this work item.

Files:

- `packages/salesforcedx-vscode-core/src/index.ts`
- `packages/salesforcedx-vscode-core/src/commands/refreshSObjects.ts`
- `packages/salesforcedx-utils-vscode/src/config/configUtil.ts`
- `packages/salesforcedx-utils-vscode/src/util/authInfo.ts`

## Phases

### 1. Remove `sendException` consumers

- Delete all 4 `TelemetryService.sendException` calls.
- Keep existing return, throw, command execution, and exception-catcher console behavior.
- Remove imports, catch bindings, and error-formatting code used only by those calls.
- Confirm no production `.sendException(` call remains.

Commit: `refactor: drop utils-vscode sendException call sites`

## Skills to apply

- `typescript` — safe TypeScript cleanup and imports
- `verification` — package-scoped checks and required core desktop tests

## Verification

1. Search `packages/` for `.sendException(`; expect no production call sites.
2. Run compile, lint, and unit tests for `@salesforce/salesforcedx-utils-vscode` and `salesforcedx-vscode-core` from the repository root.
3. Run `npm run test -w salesforcedx-vscode-core -- --runTestsByPath test/jest/commands/refreshSObjects.test.ts`; its `propagates the refresh command rejection` test covers the changed rejection path. A desktop fixture cannot deterministically replace the metadata extension's `sf.internal.refreshsobjects` handler before core startup because core activation invokes the handler before a core-dependent test extension can activate.
4. Run `npm run test:desktop -w salesforcedx-vscode-core -- --retries 0`.
5. Run repository stop-hook checks; inspect every failure before completion.
