# W-23291527 — migrate apex langServer namespace read to ProjectService

## Context

- `packages/salesforcedx-vscode-apex/src/languageServer.ts` `provideCodeLenses` (~line 234) reads project namespace via core API: `vscodeCoreExtension.exports.services.SalesforceProjectConfig.getInstance().then(cfg => cfg.getContents().namespace)`.
- Migrate to Effect `ProjectService`: `api.services.ProjectService.getSfProject()` → `getSfProjectJson().getContents().namespace`.
- Pattern: `packages/salesforcedx-vscode-metadata/src/commands/projectInfo.ts:50,89`.
- `getServicesApi` available via `ExtensionProviderService` (index.ts:70). Runtime: `getRuntime().runPromise(...)` (services/runtime.ts). `provideCodeLenses` is plain async — run Effect inline, like scanConfig (languageServerScanConfig.ts:84).
- No project = reject (real problem). Do NOT catch-to-undefined.
- No new ProjectService method — inline `getSfProject()` for single caller.
- `nsFromOrg` still via `vscodeCoreExtension.exports.getAuthFields()` (core API, unchanged). `getVscodeCoreExtension()` still needed for that.
- After migration: apex is the last live in-repo `SalesforceProjectConfig` consumer. apex-oas SKILL.md row (line 83) still lists it but `grep` of `packages/salesforcedx-vscode-apex-oas/src` returns zero — stale, corrected in Phase 2. Core index.ts export + class def remain; `getValue` already dead. Deletion gated on Phase 2 rescan confirming einstein-gpt dropped core dep + apex-oas row corrected.

## Phases

### Phase 1 — migrate namespace read to ProjectService

- File: `packages/salesforcedx-vscode-apex/src/languageServer.ts`.
- In `provideCodeLenses`, replace the `SalesforceProjectConfig` promise in `Promise.all` with `getRuntime().runPromise` of Effect: `getServicesApi` → `api.services.ProjectService.getSfProject()` → `project.getSfProjectJson().getContents().namespace`.
- Keep `getVscodeCoreExtension()` + `getAuthFields()` for `nsFromOrg`.
- Add imports: `getRuntime` (services/runtime), `ExtensionProviderService`/`getServicesApi` + `Effect` per existing apex pattern (orphan handler / index.ts).
- No catch-to-undefined; project absence rejects.
- Commit: `refactor(apex): read project namespace via ProjectService not SalesforceProjectConfig - W-23291527`

### Phase 2 — docs: rescan external consumers + core API skills

- Skills: `.claude/skills/external-consumers/SKILL.md`, `.claude/skills/core-extension-api/SKILL.md`.
- FULL rescan forcedotcom + salesforcecli orgs (gh code search per external-consumers skill).
- Apply external-consumers + core-extension-api skills.
- Known stale to fix + reverify ALL rows:
  - einstein-gpt → Agentforce Vibes monorepo; dropped `salesforcedx-vscode-core` extensionDependency; depends only on `salesforcedx-vscode-services`. Its `services.{SalesforceProjectConfig,ChannelService,WorkspaceContext,CommandEventDispatcher}` + `getOrgShape` rows dead.
  - apex-oas (SKILL.md:83) row lists `.services.SalesforceProjectConfig` — STALE: `grep SalesforceProjectConfig packages/salesforcedx-vscode-apex-oas/src` returns zero. Drop that entry from the row. Phase 3 depends on this row being wrong (in-repo core consumer claim), so correct it explicitly here.
  - Note einstein-shared spinoff (no core dep).
- Reverify every table row (in-repo + external) still accurate post-migration (apex no longer consumes `services.SalesforceProjectConfig`).
- Commit: `docs(skills): rescan external + core-extension-api consumers - W-23291527`

### Phase 3 (conditional) — delete SalesforceProjectConfig class + core API export

- ONLY if Phase 2 rescan confirms zero remaining external consumers of `services.SalesforceProjectConfig`.
- Files: `packages/salesforcedx-vscode-core/src/index.ts` (remove import + `SalesforceProjectConfig` from api object + `SalesforceVSCodeCoreApi` type), `packages/salesforcedx-vscode-core/src/salesforceProject/salesforceProjectConfig.ts` (delete).
- Update core-extension-api skill `services` list.
- Breaking API removal — see core-extension-api skill.
- Commit: `refactor(core): remove dead SalesforceProjectConfig export + class - W-23291527`

## Skills to apply

- concise (plan + skill md edits)
- typescript (Phase 1 edits languageServer.ts)
- effect-best-practices (Phase 1 Effect usage)
- external-consumers (Phase 2 rescan)
- core-extension-api (Phase 2/3 API surface)
- services-extension-consumption (ProjectService usage)
- i18n-messages (only if any message touched — none expected)

## Verification

- e2e: behavior-preserving read-source swap. Phase 1 only changes where `nsFromProject` is read (`SalesforceProjectConfig.getInstance()` → `ProjectService.getSfProject()`); reject-on-no-project semantics and the `nsFromOrg`/`nsFromProject` values fed to `rewriteNamespaceLens` are unchanged. Regression backstop for the `provideCodeLenses` `Promise.all` plumbing touched here:
  - `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/runApexTestsCodeLens.headless.spec.ts` — clicks 'Run All Tests'/'Run Test' lenses; these don't render if the `Promise.all` rejects, so a broken project read fails the spec.
  - codeLens clicks in `packages/salesforcedx-vscode-apex-replay-debugger/test/playwright/specs/{debugApexTests,apexReplayDebugger}.desktop.spec.ts`.
  - No new/changed observable behavior → no new named spec required.
- Pre-existing gap (NOT introduced by this WI, non-blocking): the namespace-rewrite branch of `rewriteNamespaceLens` is untested e2e — every scratch-org fixture (`packages/playwright-vscode-ext/src/orgs/{minimalScratchOrgSetup,nonTrackingScratchOrgSetup}.ts`) sets `namespace: ''`, so the rewrite path never fires. Jest `packages/salesforcedx-vscode-apex/test/jest/namespaceLensRewriter.test.ts` covers the pure rewrite fn. This WI leaves that gap as-is.
- Non-e2e:
  - typecheck/lint apex + core packages.
  - grep confirms no remaining in-repo `services.SalesforceProjectConfig` / `SalesforceProjectConfig.getInstance` refs after Phase 1.
  - Phase 2: gh code search returns zero live external `SalesforceProjectConfig` consumers → confirms Phase 3 safe.
  - Phase 3 (if run): core package build passes; grep confirms no `SalesforceProjectConfig` refs remain.
