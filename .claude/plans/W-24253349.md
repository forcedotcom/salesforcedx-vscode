# W-24253349

## Context

- `.github/workflows/cleanupOldPlanFiles.yml` creates the cleanup PR with `peter-evans/create-pull-request@v8` and `token: ${{ secrets.GITHUB_TOKEN }}`
- that token's actor is `github-actions[bot]`
- `secrets.IDEE_GH_TOKEN` is the `svc-idee-bot` credential (`.github/workflows/aiAutoApprove.yml`)
- swap the create-pull-request token so the PR author is `svc-idee-bot`

## Phases

1. `fix: open plan-file cleanup PRs as svc-idee-bot`
   - `.github/workflows/cleanupOldPlanFiles.yml`: Create or update pull request step, `token: ${{ secrets.IDEE_GH_TOKEN }}`

## Skills to apply

- verification

## Verification

- `npm run check:actions` (workflow yaml change)
