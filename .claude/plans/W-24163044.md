# W-24163044 — Drop LWC telemetryService shim; emit Effect spans

## Context

- GUS subject: `1.1 Drop LWC telemetryService shim; emit Effect spans [repo:forcedotcom/salesforcedx-vscode] [ai-auto]`.
- `packages/salesforcedx-vscode-lwc/src/telemetry/index.ts` wraps each legacy-shaped `sendEventData` call in an Effect span and log. The wrapper preserves an event-service API after LWC already owns an Effect runtime.
- Delete that shim. Emit spans directly through the LWC runtime, preserving current span names and attributes. Do not add replacement telemetry abstractions or alternate paths.

## Phases

### Phase 1 — Replace the shim with direct Effect spans

- Delete `packages/salesforcedx-vscode-lwc/src/telemetry/index.ts`.
- Replace `telemetryService.sendEventData` with direct `Effect.withSpan` programs in:
  - `packages/salesforcedx-vscode-lwc/src/index.ts`
  - `packages/salesforcedx-vscode-lwc/src/testSupport/commands/lwcTestDebugAction.ts`
  - `packages/salesforcedx-vscode-lwc/src/testSupport/workspace/getTestWorkspaceFolder.ts`
  - `packages/salesforcedx-vscode-lwc/src/testSupport/workspace/getLwcTestRunnerExecutable.ts`
  - `packages/salesforcedx-vscode-lwc/src/testSupport/testRunner/testRunner.ts`
  - `packages/salesforcedx-vscode-lwc/src/testSupport/testExplorer/lwcTestController.ts`
- Yield spans directly inside existing Effect programs. From VS Code/task callbacks, run the span program through `getRuntime().runFork`.
- Preserve existing names: `extensionDeactivated`, `exception`, `LWC_TEST_DEBUG_LOG_NAME`, `LWC_TEST_RUN_LOG_NAME`, and each `TestRunner.logName`; preserve existing `workspaceType`, `executionTime`, `name`, and `message` attributes.
- Remove the shim-only telemetry mock from `packages/salesforcedx-vscode-lwc/test/jest/testSupport/testExplorer/lwcTestController.test.ts`; use its Effect runtime test layer for direct-span coverage.
- Commit: `refactor(lwc): drop telemetryService shim for Effect spans - W-24163044`

## Skills to apply

- `effect-best-practices` — direct Effect composition, named spans, runtime execution at imperative callbacks.
- `typescript` — deep imports, explicit types, no ignored promises.
- `verification` — package checks and LWC Playwright coverage.
- `concise` — keep code comments and plan updates minimal.

## Verification

1. Run `npm install` from the repository root.
2. Confirm no shim references remain: `rg "telemetryService|from ['\"](?:\.\./)*telemetry['\"]|sendEventData" packages/salesforcedx-vscode-lwc/src packages/salesforcedx-vscode-lwc/test`.
3. Run `npx effect-language-service diagnostics --project packages/salesforcedx-vscode-lwc/tsconfig.json` and address every diagnostic.
4. Run `WIREIT_CACHE=none npm run check:circular-deps -w salesforcedx-vscode-lwc`.
5. Run `WIREIT_CACHE=none npm run compile -w salesforcedx-vscode-lwc`.
6. Run `WIREIT_CACHE=none npm run lint -w salesforcedx-vscode-lwc`.
7. Run `WIREIT_CACHE=none npm test -w salesforcedx-vscode-lwc`.
8. Run `WIREIT_CACHE=none npm run vscode:bundle -w salesforcedx-vscode-lwc`.
9. Run `WIREIT_CACHE=none npm run test:web -w salesforcedx-vscode-lwc -- --retries 0`.
10. Run `WIREIT_CACHE=none npm run test:desktop -w salesforcedx-vscode-lwc -- --retries 0`.
