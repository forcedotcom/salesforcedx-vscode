# Context

- GUS subject: **LWC start language server: package directory URIs not strings [ai-auto] [repo:forcedotcom/salesforcedx-vscode]**.
- LWC activation currently reads package directory paths as strings and passes them through each language-client factory; `clientOptions.ts` reconstructs URIs before creating scoped file watchers.
- Pass package directory URIs from activation through client creation, then use those URIs directly as `RelativePattern` bases.

# Phases

## 1. Pass package directory URIs through LWC language-client startup

- Update `packages/salesforcedx-vscode-lwc/src/index.ts` to produce package directory URIs for initial startup and project-change restart.
- Change package-directory parameter types and forwarding in:
  - `packages/salesforcedx-vscode-lwc/src/languageClient/index.ts`
  - `packages/salesforcedx-vscode-lwc/src/languageClient/node.ts`
  - `packages/salesforcedx-vscode-lwc/src/languageClient/web.ts`
  - `packages/salesforcedx-vscode-lwc/src/languageClient/clientOptions.ts`
- Build watcher `RelativePattern` values directly from each supplied package directory URI.
- Commit: `fix: pass package directory URIs to LWC language client`

## 2. Cover URI-based package directory watchers

- Update `packages/salesforcedx-vscode-lwc/test/jest/languageClient/clientOptions.test.ts` to pass package directory URIs and assert those URIs are the watcher bases.
- Commit: `test: cover LWC package directory URI watchers`

# Skills to apply

- `lsp-custom-requests`: preserve URI data across language-client boundaries.
- `typescript`: derive accurate URI types; keep TypeScript changes consistent with repository rules.
- `verification`: run targeted and repository checks after source and test changes.

# Verification

- Install repository dependencies in this worktree.
- Run the `salesforcedx-vscode-lwc` Jest suite for `clientOptions.test.ts`.
- Run repository compile, lint, test, bundle, Effect language-service, and knip checks through the standard stop-hook verification.
- Confirm the URI watcher test verifies the exact URI supplied by activation is used as each `RelativePattern` base.
