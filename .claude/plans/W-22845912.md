# W-22845912 — Extract NON_CRITICAL_*_PATTERNS to standalone files

## Context

`helpers.ts` mixes ~100-line pattern arrays with runtime helpers. Extract each array to its own file for readability/maintainability. Pure move — no behavior change.

## Phases

### Phase 1: Extract patterns + update imports

- Create `packages/playwright-vscode-ext/src/utils/nonCriticalErrorPatterns.ts`
  - Move `NON_CRITICAL_ERROR_PATTERNS` verbatim, export `as const`
- Create `packages/playwright-vscode-ext/src/utils/nonCriticalNetworkPatterns.ts`
  - Move `NON_CRITICAL_NETWORK_PATTERNS` verbatim, export `as const`
- Update `helpers.ts`: remove arrays, import from new files
- Commit: `refactor: extract NON_CRITICAL_*_PATTERNS to standalone files - W-22845912`

### Phase 2: Add unit tests for filterErrors / filterNetworkErrors

- existing e2e spec covers: `waitForVSCodeWorkbench`, `closeWelcomeTabs`, `waitForWorkspaceReady` — not `filterErrors`/`filterNetworkErrors`
- pure functions (no Page dependency) — test via direct import, no browser fixture needed
- Create `packages/playwright-vscode-ext/test/playwright/specs/filterErrors.spec.ts`
  - import `filterErrors` and `filterNetworkErrors` from `../../../src/utils/helpers`
  - `ConsoleError` matching a known pattern (e.g. `'favicon.ico'`) → filtered out
  - `ConsoleError` not matching any pattern → passes through
  - `NetworkError` url matching a known network pattern → filtered out
  - `NetworkError` url not matching any pattern → passes through
  - `@playwright/test` `test`/`expect` — no new deps
- Commit: `test: add unit tests for filterErrors/filterNetworkErrors - W-22845912`

## Skills

- typescript
- verification

## Verification

- `tsc --noEmit` passes (static)
- `filterErrors` / `filterNetworkErrors` import from new files without error (static)
- grep extracted pattern files: array lengths match originals, no dropped entries (static)
- `filterErrors.spec.ts` — all four filter cases pass (e2e-covered)
