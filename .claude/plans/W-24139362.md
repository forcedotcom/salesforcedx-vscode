# W-24139362

## Context

`packages/salesforcedx-vscode-apex/src/languageServerScanConfig.ts` declares `SalesforceVSCodeServicesApiLike` and reads the services extension through `vscode.extensions`. Replace that local API shape and lookup with the typed `getServicesApi` Effect from `@salesforce/effect-ext-utils`, keeping the existing scan-config result and error behavior.

## Phases

### 1. Use the shared services API

- Update `packages/salesforcedx-vscode-apex/src/languageServerScanConfig.ts`:
  - remove `SalesforceVSCodeServicesApiLike`, its `Layer` dependency, and the direct services-extension lookup
  - compose `getServicesApi` with `MetadataRegistryService.getRegistryAccess()` and `prebuiltServicesLayer`
  - retain the existing metadata-folder derivation and `undefined` behavior
- Update `packages/salesforcedx-vscode-apex/test/jest/languageServerScanConfig.test.ts` to exercise `buildMetadataRegistryScanConfig` through a mocked `getServicesApi` Effect while retaining the pure folder-derivation coverage.
- Commit message: `refactor(apex): use shared services API getter`

## Skills to apply

- `services-extension-consumption`: use `getServicesApi` and the shared `prebuiltServicesLayer`
- `typescript`: rely on the exported API type inference; avoid replacement local shapes
- `verification`: run package-specific unit, compile, lint, bundle, and desktop checks

## Verification

- `npm test -w salesforcedx-vscode-apex -- --runInBand languageServerScanConfig.test.ts`
- `npm run compile -w salesforcedx-vscode-apex`
- `npm run lint -w salesforcedx-vscode-apex`
- `npm run vscode:bundle -w salesforcedx-vscode-apex`
- `npm run test:desktop -w salesforcedx-vscode-apex -- --retries 0`
