# W-24207324

## Context

**GUS subject:** 1.4 Migrate remaining extension settings get/set onto SettingsService [ai-auto] [repo:forcedotcom/salesforcedx-vscode]

Remaining Salesforce extension setting reads/writes call `vscode.workspace.getConfiguration(...).get/update` directly. Route them through the shared `SettingsService` while preserving each setting section, key, default, and configuration target.

Source migration set found in:

- `packages/salesforcedx-vscode-apex/src/{languageServer.ts,requirements.ts,settings.ts}`
- `packages/salesforcedx-vscode-apex/src/languageUtils/languageClientManager.ts`
- `packages/salesforcedx-vscode-apex-debugger/src/adapter/debugConfigurationProvider.ts`
- `packages/salesforcedx-vscode-apex-oas/src/commands/apexActionController.ts`
- `packages/salesforcedx-vscode-apex-oas/src/oas/{promptGenerationOrchestrator.ts,generationStrategy/json/apexRest.ts}`
- `packages/salesforcedx-vscode-apex-replay-debugger/src/{services/ensureTraceFlags.ts,utils/settings.ts}`
- `packages/salesforcedx-vscode-apex-testing/src/views/apexTestTreeService.ts`
- `packages/salesforcedx-vscode-core/src/{index.ts,settings/salesforceCoreSettings.ts}`
- `packages/salesforcedx-vscode-lightning/src/index.ts`
- `packages/salesforcedx-vscode-lwc/src/index.ts`
- `packages/salesforcedx-vscode-metadata/src/{index.ts,commands/projectInfo.ts,conflict/conflictDetectionSettings.ts,settings/deployOnSaveSettings.ts,statusBar/sourceTrackingStatusBar.ts}`
- `packages/salesforcedx-vscode-soql/src/{commands/dataQuery.ts,commands/queryPlan.ts,editor/soqlEditorInstance.ts,lspClient/queryValidation.ts}`

Exclude `WorkspaceConfiguration.inspect`, document-scoped editor configuration, language-server protocol configuration, and the `SettingsService` implementation: they are not extension setting get/set call sites addressable by the current service API.

## Phases

### 1. Migrate remaining extension setting access

- Replace each listed direct `get/update` with `SettingsService.getValue/setValue` from the extension's existing shared services API.
- Convert synchronous helpers and callers to Effect only where required to yield `SettingsService` operations.
- Update affected existing tests and service mocks; preserve current values, defaults, targets, and observable behavior.
- Remove obsolete direct-configuration helpers/imports after their callers migrate.

**Commit:** `refactor: migrate remaining extension settings to SettingsService`

## Skills to apply

- `services-extension-consumption`: consume the shared `SettingsService` through each extension's existing services API/runtime; do not construct duplicate service layers.
- `effect-best-practices`: compose newly Effectful reads/writes and typed failures.
- `typescript`: preserve inferred types; avoid new classes, barrels, mutation, and `any`.
- `verification`: use package-scoped checks plus repository stop-hook checks.

## Verification

1. Confirm no listed source file retains direct extension-setting `get/update` calls.
2. Run affected package unit tests.
3. Run Playwright web and desktop tests for affected packages that provide them, with `--retries 0`.
4. Run `npm run check:dupes`; inspect changed-code findings.
5. Let the repository stop hook run compile, lint, Effect LS, tests, VS Code bundle, and knip.
