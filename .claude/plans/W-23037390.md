# W-23037390 — Fix brace-expansion ReDoS

## Context
- Dependabot alerts 284, 285, 325: brace-expansion ReDoS (transitive, 1.x + 2.x).
- Fix: lockfile-only `npm update brace-expansion`. No `package.json` override.
- 1.x + 2.x copies re-resolve within range to patched versions.
- Verified in worktree: `--package-lock-only` bumps 1.1.12→1.1.15, 2.0.2→2.1.1, 5.0.4→5.0.6. Diff = 60 lines, lockfile-only.
- Out of scope: alert 283 (wireit→brace-expansion 4.0.1, existing override, untouched).

## Phases

### Phase 1 — update lockfile
- `npm update brace-expansion --package-lock-only` (or full `npm update brace-expansion`).
- Only `package-lock.json` changes.
- Confirm no 4.0.1 lines in diff (override intact).
- Files: `package-lock.json`
- Commit: `fix(deps): bump brace-expansion to patched versions via npm update - W-23037390`

## Skills
- packageJson — lockfile/dep change conventions
- typescript
- verification

## Verification
- `git diff package.json` empty (no override added).
- `git diff package-lock.json` brace-expansion 1.x→1.1.15, 2.x→2.1.1, 5.x→5.0.6; no 4.0.1 change.
- Dependabot alerts 284, 285, 325 clear after merge (post-merge, not local).
- `npm ls brace-expansion` resolves; no install errors.
- Not e2e-covered (deps-only).
