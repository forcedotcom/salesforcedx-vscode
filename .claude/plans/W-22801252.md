# W-22801252 — finish porting runApexTests.e2e to Playwright

## Context

- Predecessor #7375 ported 8/11 cases. 3 left in `packages/salesforcedx-vscode-automation-tests/test/specs/runApexTests.e2e.ts`:
  - Run All Tests via Apex Class (code lens at class-level)
  - Run Single Test via Apex Class (code lens at method-level)
  - Run a test that fails and fix it (deploy buggy AccountService, run, edit line 6, redeploy, re-run)
- Target dir: `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/`
- Helpers live in `packages/playwright-vscode-ext/src` and re-export from `index.ts`
- `CODELENS_ITEM = '.codelens-decoration a, .codelens-decoration button'` already defined in `utils/locators.ts`
- `waitForRunApexTestsProgressNotificationGone` already in `pages/notifications.ts`
- `editAndSaveOpenFile` exists but only appends comment after first line; need full line-replace for the bug-fix case
- Code lens titles confirmed: `Run All Tests`, `Run Test`, `Debug All Tests`, `Debug Test` (`packages/salesforcedx-vscode-apex/src/namespaceLensRewriter.ts:9-10`)
- `debugApexTests.e2e.ts` still uses helpers in `utils/testsHelper.ts` — leave intact
- AccountService bug: line 6 `TickerSymbol = accountNumber` → fix to `TickerSymbol = tickerSymbol`
- Notification format: `Apex test report is ready: test-result-<id>.md` with `Open Report` button

## Phases

### Phase 1 — add helpers to `@salesforce/playwright-vscode-ext`

`packages/playwright-vscode-ext/src/pages/codeLens.ts` (new):
- `clickCodeLens(page, text)`: locate `CODELENS_ITEM`, filter by exact text regex, wait visible, click

`packages/playwright-vscode-ext/src/pages/notifications.ts` (extend):
- `waitForNotification(page, pattern, opts?)`: assert a `NOTIFICATION_LIST_ITEM` matching regex appears within timeout; return locator
- `acceptNotification(page, pattern, buttonName, opts?)`: find notification by pattern, click named button (e.g. `Open Report`)

`packages/playwright-vscode-ext/src/utils/fileHelpers.ts` (extend):
- `replaceLineInOpenFile(page, lineNumber, newText)`: focus active editor; `Go to Line`/`workbench.action.gotoLine` via palette → set line → `Home`, `Shift+End`, type new text, `File: Save`. Reuses pattern in `editAndSaveOpenFile`

Re-export new helpers from `packages/playwright-vscode-ext/src/index.ts`.

**commit**: `chore(playwright-ext): add clickCodeLens, waitForNotification/acceptNotification, replaceLineInOpenFile helpers`

files:
- `packages/playwright-vscode-ext/src/pages/codeLens.ts`
- `packages/playwright-vscode-ext/src/pages/notifications.ts`
- `packages/playwright-vscode-ext/src/utils/fileHelpers.ts`
- `packages/playwright-vscode-ext/src/index.ts`

### Phase 2 — port `Run All Tests via Apex Class` + `Run Single Test via Apex Class`

New spec `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/runApexTestsCodeLens.headless.spec.ts`:
- Setup minimal org, deploy 1 Apex test class via `createAndDeployApexTestClass` (test method in same file with `validateSayHello` style)
- Run All: open class file, `clickCodeLens(page, 'Run All Tests')`, `waitForRunApexTestsProgressNotificationGone`, assert Apex Testing output channel has `=== Test Summary`, `Outcome              Passed`, `Tests Ran            1`, `Pass Rate            100%`, `Ended SFDX: Run Apex Tests`
- Run Single: clear output, `clickCodeLens(page, 'Run Test')` (first one — at method), same assertions

Use existing `selectOutputChannel('Apex Testing')` + `waitForOutputChannelText`.

**commit**: `test(apex-testing): port code-lens Run All Tests and Run Single Test to Playwright`

files:
- `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/runApexTestsCodeLens.headless.spec.ts`

### Phase 3 — port `Run a test that fails and fix it`

New spec `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/runApexTestsFailAndFix.headless.spec.ts`:
- Setup minimal org
- Inline the AccountService + AccountServiceTest content (mirror `createApexClassWithBugs` from test-tools); deploy both via `createAndDeployApexTestClass` per file (one creates+saves+deploys; second class deploys via `deployCurrentSourceToOrg`)
- Run via command palette (`SFDX: Run Apex Tests` → type `AccountServiceTest` → confirm)
- `waitForNotification(page, /Apex test report is ready: test-result-[a-zA-Z0-9]+\.md/)`
- Assert output channel contains `System.AssertException: Assertion Failed:` and `incorrect ticker symbol: Expected: CRM, Actual: SFDC`
- Open `AccountService.cls`, `replaceLineInOpenFile(page, 6, '\t\t\tTickerSymbol = tickerSymbol')`
- Redeploy current source, re-run tests, assert pass: `Outcome              Passed`, `AccountServiceTest.should_create_account  Pass`

**commit**: `test(apex-testing): port Run-test-then-fix scenario to Playwright`

files:
- `packages/salesforcedx-vscode-apex-testing/test/playwright/specs/runApexTestsFailAndFix.headless.spec.ts`

### Phase 4 — delete WDIO file + cleanup imports

- Delete `packages/salesforcedx-vscode-automation-tests/test/specs/runApexTests.e2e.ts`
- Verify `packages/salesforcedx-vscode-automation-tests/test/utils/testsHelper.ts` exports still used by `debugApexTests.e2e.ts` — leave untouched per WI
- Drop now-unused imports/symbols from `testData/constants.ts` only if no longer referenced; otherwise leave (debugApexTests uses `apexTestExtensionConfigs`)
- Update any wireit/spec includes if file is enumerated (check `packages/salesforcedx-vscode-automation-tests/package.json`)

**commit**: `chore(automation-tests): remove runApexTests.e2e.ts (ported to Playwright)`

files:
- `packages/salesforcedx-vscode-automation-tests/test/specs/runApexTests.e2e.ts` (delete)
- `packages/salesforcedx-vscode-automation-tests/package.json` (if file enumerated)

## Skills to apply

- playwright-e2e — spec authoring patterns, scratch-org setup
- typescript — file naming (`*.headless.spec.ts`), no barrel files, `type` not `interface`
- packageJson — only if wireit/spec list edits needed
- concise — for any new doc comments

## Verification

E2e-covered (run `npm run test:playwright:web` and `:desktop` for `salesforcedx-vscode-apex-testing`):
- Run All / Run Single via code lens
- Fail-then-fix scenario including deploy, edit, redeploy, re-run
- Notification + Open Report flow

Not e2e-covered:
- `npm run lint` clean for changed packages
- `npm run compile` (or `tsc --noEmit`) for `playwright-vscode-ext` and `salesforcedx-vscode-apex-testing`
- Confirm `runApexTests.e2e.ts` no longer referenced: `grep -rn "runApexTests" packages/salesforcedx-vscode-automation-tests`
- Confirm `debugApexTests.e2e.ts` still compiles (untouched helpers)
