# W-24163048

## Context

Migrate direct, core-internal `telemetryService` event and exception reporting to Effect spans. This removes internal coupling to the legacy telemetry singleton while preserving the frozen `telemetryService` export for existing consumers.

Files:

- `packages/salesforcedx-vscode-core/src/index.ts`
- `packages/salesforcedx-vscode-core/src/commands/refreshSObjects.ts`
- `packages/salesforcedx-vscode-core/test/jest/commands/refreshSObjects.test.ts`

## Phases

1. **Migrate core-internal telemetry calls**
   - Replace direct event/exception calls in `src/index.ts` and `src/commands/refreshSObjects.ts` with Effect spans and span attributes/errors.
   - Retain the exported `telemetryService` instance and its public API wiring; do not replace or remove the frozen export.
   - Keep telemetry-service initialization and disposal required by that export.
   - Commit: `refactor(core): migrate internal telemetry to Effect spans`

2. **Align focused tests**
   - Update `refreshSObjects.test.ts` to assert the migrated span behavior; do not add tests for unchanged frozen-export behavior.
   - Commit: `test(core): verify refresh SObject spans`

## Skills to apply

- `effect-best-practices` — Effect span composition and error handling
- `typescript` — repository TypeScript conventions
- `core-extension-api` and `external-consumers` — preserve the frozen `telemetryService` API
- `verification` — repository checks and focused Playwright requirement

## Verification

1. Run the focused core unit tests covering telemetry and `refreshSObjects`.
2. Confirm compile, lint, Effect LS, tests, bundle, and knip through the repository stop hook.
3. Run `npm run test:desktop -w salesforcedx-vscode-core -- --retries 0` (core is desktop-only).
4. Verify `telemetryService` remains exported from the core activation API with the same type and object wiring.
