# package.json

Use higher value for conflicts on the `version` property

User higher version for any conflicts in dependencies or devDependencies.

## package-lock.json

After all package.json conflicts are fixed, run `npm install` to fix the conflicts in the the lockfile. Never edit the lockfile directly.

## Misc other files

- CHANGELOG.md
- SHA256.md

Always take the incoming changes
