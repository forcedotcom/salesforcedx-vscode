---
description:
globs:
alwaysApply: true
---
when you change a file, run relevant compile/lint then unit tests so that you propose valid changes.

don't change both /src and /test unless it's to refactor imports or rename.  "real changes" should be one or the other.

lint whole project `npm run lint`.  use -w for a single workspace.
