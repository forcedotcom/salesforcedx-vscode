status: remediated
- `packages/salesforcedx-vscode-core/test/playwright/specs/openDocumentation.desktop.spec.ts:44-50` has no positive, durable execution signal. `verifyCommandExists` only verifies the contributed palette item, and `toHaveCount(0)` immediately after invoking the command can pass before VS Code asynchronously renders a missing-handler error. The test can therefore pass even when `sf.open.documentation` is not registered or never runs. Wait for an observable effect of the command (for example, a telemetry artifact) rather than asserting that an error toast is absent.
