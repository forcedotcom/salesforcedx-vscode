describe('PersistentStorageService', () => {
  describe('updateCacheAfterPushPull', () => {
    it('should update the properties in the cache for the files that were pushed or pulled', () => {});
  });

  describe('addMetaFiles', () => {
    it('should add metafiles for components that need them', () => {});
  });
});
