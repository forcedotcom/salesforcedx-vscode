import { FlagParameter } from '@salesforce/salesforcedx-utils-vscode/src';
import { forceSourcePull } from '../../../src/commands';

describe('ForceSourcePullExecutor', () => {
  describe('cacheRemoteChanges', () => {
    it('should get and store the remote changes from source tracking', () => {});
  });
  describe('forceSourcePull', () => {
    it('should cache the remote changes before being run', async () => {
      // const flagOverwrite: FlagParameter<string> = {
      //   flag: '--forceoverwrite'
      // };
      // await forceSourcePull();
    });
  });
});
