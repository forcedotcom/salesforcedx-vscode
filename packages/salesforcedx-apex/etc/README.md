# Next-major public API report

`apex-node.public.api.md` is the reviewed TypeScript baseline for the target
`@salesforce/apex-node` package root.

Run `npm run api:check --workspace @salesforce/apex-node` to verify the target.
Record an intentional, reviewed change with:

```sh
npm run api:update --workspace @salesforce/apex-node
```
