# Salesforce Shared Media

Shared fonts and icons for Salesforce VS Code extensions. This extension contributes product icons (e.g. `sf-org-leaf`, `sf-org-tree`) that other extensions can reference via `$(sf-org-leaf)` in labels and `new vscode.ThemeIcon("sf-org-leaf")` in code.
